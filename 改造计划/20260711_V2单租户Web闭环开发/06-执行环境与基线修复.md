# 执行环境与基线修复（B00）

> 协调者记录，2026-07-12。本文件回答两个问题：P00～P03 在 macOS arm64 工作机上完成并经 PR #1 合入 main 后，为什么当前共享基线不是绿的；以及在新的 Linux 工作站上把基线修复到绿、并让后续 17 项任务可持续执行的批次计划。本文件不改变 21 项任务范围与硬门顺序。

## 1. 执行环境切换事实

| 维度 | P00～P03 原环境 | 当前环境（自本文件起的执行基线） |
|---|---|---|
| 主机 | macOS Darwin arm64（OrbStack Docker） | Linux x86_64 工作站（6.8.0-124-generic，24C/62G，根盘剩余约 320G） |
| Node | v24.15.0 | v24.18.0（nvm，满足 `>=24.15.0 <25`；系统默认 v22 不得用于本仓库命令） |
| Docker | 29.4.0 / linux/arm64 | 本机 daemon / linux/amd64 |
| 供应链工具 | 按 P03 记录临时安装 | syft v1.44.0、grype v0.112.0、helm v3.21.0 已按官方 checksum 校验安装于 `~/.local/bin`（与 CI pinned 版本一致） |
| CI | 同左 | GitHub Actions：`scaffold` 作业 ubuntu-latest（amd64）现场重建镜像证据；`db-integration` 作业 ubuntu-24.04-arm（arm64） |

换机的两个直接后果：

1. `tests/integration/db/postgres-image.json` 只批准 `linux/arm64` exact digest，本机 amd64 上全部 DB 动态门按设计 fail-closed 为 BLOCKED——P02 开发记录早已预告该行为，需要按同一防伪标准补批准 amd64 digest（B00-4）。
2. `.reports/` 不入库，P03 exact-five 镜像证据是机器本地产物，必须在本机以 pinned 工具重建（B00-5）。

## 2. 合并事故与集成纪律教训

PR #1 使用 GitHub 默认 merge commit（`3babe5c6`，第一父为旧 main `801fb837`），导致 P02 证据链（codeEvidenceSha `352d0caa`、stageEvidenceSha `2f8e33e4`）整体落在 main 的第二父一侧。P02 trusted provenance 要求证据 SHA 位于 HEAD 的 first-parent 链上，因此 `tests/db/p03-merge-evidence.test.ts` 与 `tests/db/reviewer-round2-red.test.ts` 的 live 校验在合并后的 main 上确定性失败。此外，分支末尾的 `31d09818`/`1a53e396`（contracts 产品代码）落在证据冻结之后，即使不合并也已再次失效该链。

这不是校验器误报：它按设计把"证据冻结后又有代码变动/历史被 merge 改写"视为证据过期。教训固化为 [04-风险决策](04-风险决策与阻断项.md) 的 DEC-10。

## 3. 当前基线实测失败清单（2026-07-12，main `3babe5c6`）

官方 `npm test`：207 files 中 4 files / 41 tests 确定性失败，无其他功能回归；另有约 6 个文件在全量并发下偶发超时（5s 级 mutation 用例），单独运行稳定通过。

| 失败 | 根因 | 修复任务 |
|---|---|---|
| `tests/db/p03-merge-evidence.test.ts`、`tests/db/reviewer-round2-red.test.ts`（各 1 项） | §2 所述证据链断裂 | B00-6 在 main 上重冻结 |
| `tests/deploy-product-source-boundaries.test.ts`（2 项） | `check-supply-chain.mjs` 的 git `--name-status` 解析未防 `core.quotepath` 引号转义；原 mac 环境 git 配置掩盖了缺陷，本机默认配置下中文路径（`改造计划/**` 等 frozen 目录）被转义为 `"\346..."`，frozen 判定失效，docs 提交错误推进 product source | B00-3（P03 返工轮） |
| `tests/deploy-supply-chain-evidence-files.test.ts`（37 项） | 本机缺 `.reports/supply-chain/20260712_PR3部署供应链证据` | B00-5 重建（依赖 B00-3 先落码，因 `scripts/` 属镜像输入，落码会推进 product source） |

## 4. B00 修复批次

B00 不新增总表行：B00-3 记入 P03、B00-4/B00-6 记入 P02 的既有任务四件套，仍走 Developer→Reviewer→Tester；B00-1/B00-5 是环境供给（不产生产品提交）；B00-7 为协调者终审。

| 项 | 内容 | 角色归属 | 前置 |
|---|---|---|---|
| B00-1 | 安装 pinned syft/grype/helm（已完成，checksum 校验） | 协调者环境供给 | 无 |
| B00-2 | 本补篇与 DEC-10/11/12 决策记录落盘 | 协调者 | 无 |
| B00-3 | quotepath 安全的 git 路径解析（Red→Green，含 `check-docker-build.mjs` 排查与 P02 `-z` 免疫性验证） | P03 Developer 返工轮 + Reviewer + Tester | 无 |
| B00-4 | `postgres-image.json` 增补批准的 linux/amd64 digest，未批准平台/digest 仍 fail-closed；本机真实 PG17 Testcontainers 全绿 | P02 Developer 返工轮 + Reviewer + Tester | 无 |
| B00-5 | 本机以 pinned 工具重建 exact-five 证据（`npm run docker:build:check`） | 协调者环境供给（P03 门重放） | B00-1、B00-3 合入 |
| B00-6 | P02 证据链以 main 线性历史重冻结：`validate.mjs` 四份 raw 报告 → snapshot evidence-only 提交 → `prisma:validate` stage PASS → 官方 `npm test` 全绿 | P02 Developer 返工轮收尾 | B00-2/3/4/5 全部落定（冻结点之后只允许 allowlist 路径） |
| B00-7 | P03 Reviewer 按其 Round 5 完成要求在全绿共享 HEAD 终审；协调者逐项复核后勾选总表 P02、P03 两行 | P03 Reviewer + 协调者 | B00-6 |

## 5. 本机可用内网服务盘点（供 V1/V2 规划引用）

以下服务已在本机 Docker 运行，属于用户自有内网端点，不构成公网依赖：

- Authentik 2025.10（OIDC IdP）：`:9000`/`:9443` —— V201 内网 OIDC 的现实候选（BLK-03 的可解除路径）。
- Redis 7：`:16379`；另有 ChromaDB/Milvus/Mongo 等与本计划暂无关的服务。
- 缺口：K8s 集群（kind 未装）、gVisor runsc、MinIO/S3、内网 registry、内网模型网关——见 §6 与 BLK 表。

## 6. V1 阶段本地测试集群策略（DEC-12 草案）

V101～V107 需要真实集群证据。在用户提供云上集群之前，本工作站的可行路径：

- kind（Docker 内 K8s）作为 BLK-06 的测试集群载体；Calico 提供真实 NetworkPolicy 语义（V103）；本地 PVC/hostPath StorageClass + MinIO 容器（V104）；`ctr`/`docker` 导入 P03 镜像或起本地 registry 容器（BLK-05 测试侧）。
- gVisor：主机级安装 runsc 需要 sudo（本会话无免密 sudo）。优先尝试在 kind node 容器内安装 runsc 并注册 containerd runtime handler（无需主机 root）；若不可行，V101 兼容矩阵按 R3 记录 runc 基线 + gVisor BLOCKED，等待用户授权 sudo 或云集群。
- 模型/ASR：V102 真实 turn 需要内网模型端点。本机如有用户既有 vLLM/模型网关容器可作为批准内网端点；否则按 DEC-03 保持 BLOCKED，不得临时接公网 provider。
- 上述均为测试阶段基础设施（允许联网下载并固化）；生产断公网口径不变。Day-0 冻结文档由 V101 Planner 以本机实际值起草，用户对 DEC-12 有一票否决权；若用户提供真实云集群，V1 证据以云集群重跑为准。

## 7. 协调者集成纪律（自本文件起生效）

1. 主线线性：后续任务直接在 main 线性提交（或 rebase/fast-forward 合入）；禁止产生 merge commit，除非同批完成 P02 证据重冻结。
2. 每个任务落码后的集成收尾固定四步：全量 `npm test` → P02 证据重冻结（B00-6 同款流程，约 5 分钟）→ 复跑 P02/P03 live 校验 → 再更新总表/任务记录（总表精确修改与各任务纯记录提交属 provenance allowlist，不需要重冻结）。
3. 提交批次顺序：产品/测试代码 → 计划与任务文档 → P02 evidence-only 冻结提交 → 总表/角色记录。
4. 全仓命令一律使用 nvm Node 24（`PATH=~/.nvm/versions/node/v24.18.0/bin:...`），供应链命令追加 `~/.local/bin`。
