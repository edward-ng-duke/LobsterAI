# V2 阶段了断与剩余工作执行计划

> **后续执行要求：** 逐项使用 TDD 与既有 Developer → Reviewer → Tester → 总体协调者状态机；每个任务只在前一硬门真实通过后开始，不以历史成功报告、配置中的 `PASS` 声明或单次局部测试代替当前共享 `HEAD` 验收。

**目标：** 在不扩大 V2 范围的前提下，先冻结当前已完成成果，再把 P02/P03 的共享基线修复与其余 17 项未实施任务恢复为可顺序执行的唯一清单。

**执行结构：** 先完成 B00 基线修复和 P02/P03 重新签收，再依次推进 P04、V1、V2。P00/P01 作为已接受基线只做回归，不重新开发；任何会推进 P02 evidence source 或 P03 product source 的提交，都必须在同一批次重建相应证据。

**技术栈：** Node.js 24.18、Vitest、Prisma/PostgreSQL 17/Testcontainers、Docker/BuildKit、Syft、Grype、Helm、后续 kind/Calico/gVisor/MinIO 与 Playwright。

---

## 1. 审计快照

审计时间为 2026-07-12，基线为本地 `main` 的 `efa7f83d`。审计开始和测试结束后 tracked worktree 均为空，stash 为空；本地 `main` 比 `origin/main` 多 1 个已提交的 B00 计划补篇，没有可从旧工作树继续拆出的未提交产品改动。

当前 21 项任务按真实状态分为三组：

| 分类 | 数量 | 任务 | 当前结论 |
|---|---:|---|---|
| 已实现、已独立验收、当前回归仍通过 | 2 | P00、P01 | **完成并保留** |
| 主要实现已落库，但共享主线验收失效 | 2 | P02、P03 | **未完成，进入 B00 返工与重新签收** |
| 只有计划/占位脚手架，没有可验收实现 | 17 | P04、V101～V107、V201～V209 | **未开始实现，保持硬门阻断** |

不能把“主要代码已写完”与“任务完成”混为一谈。总表继续只勾 P00/P01 是正确状态；P02/P03 的历史 Reviewer/Tester 成功记录是返工输入，不是当前 `HEAD` 的有效完成证明。

本次在 Node `v24.18.0`、Linux x64 上的当前基线重放结果：

| 命令 | exit | 当前结果 | 状态归属 |
|---|---:|---|---|
| `npm run scaffold:check` | 0 | SCAF-1～SCAF-6 全部 passed | P00 当前仍绿 |
| `npm run contracts:check` | 0 | stage `PASS` | P01 当前仍绿 |
| `npm run prisma:validate` | 2 | `BLOCKED`：批准镜像为 arm64，本机 Docker 为 amd64 | P02 B00-4 |
| `npm run supply-chain:check` | 1 | 缺本机 exact-five `docker-build-check.json` | P03 B00-5 |
| `npm run poc:v1:check` | 78 | `NOT_APPLICABLE`，等待 P04 激活 | P04 未实施 |
| `npm run test:e2e` | 78 | `NOT_APPLICABLE`，没有 Playwright browser journeys | V208 未实施 |
| `npm test` | 1 | 207 files 中 203 pass / 4 fail；2260 tests 中 2218 pass / 41 fail / 1 skip | P02 2 项、P03 39 项 |

## 2. 已完成部分

### 2.1 P00 PR-0 SaaS workspace scaffold

- 协调者接受提交：`cde584c0`；总表记录 Developer `6a4db75c`、Reviewer `5b0196b6`、Tester `be856c2f`。
- 当前 `efa7f83d` 重跑 `npm run scaffold:check` 为 exit `0`，SCAF-1～SCAF-6 全部通过。
- 结论：已完成，不重开开发。后续只在全量回归中守住 workspace、stage gate、产物新鲜度和旧 Electron 构建兼容。

### 2.2 P01 PR-1 contracts/codegen

- 协调者接受提交：`a6066c31`；总表记录 Developer `dad75347`、Reviewer `5d574e98`、Tester `cea3edbf`/`fc9f619f`。
- 当前 `efa7f83d` 重跑 `npm run contracts:check` 为 exit `0`，生成、schema、inventory、bridge 与 consumer 校验仍为 `PASS`。
- 结论：已完成，不重开契约设计。P02～V209 必须继续消费 P01 已接受的机器事实源；若后续确需改契约，必须按 breaking gate 走独立变更链。

### 2.3 提交了断

- PR #1 的产品、测试、证据与角色记录已经通过 merge commit `3babe5c6` 进入 `main`；B00 补篇已经由 `efa7f83d` 提交。
- 本次审计前不存在未提交的已完成功能，因此不创建空提交，也不把 ignored 的 `.reports/`、build、cache 或生成目录强行入库。
- 本计划审计本身作为独立 docs 提交保存，和后续 B00 产品修复提交分离。

## 3. 已实现但尚未完成的部分

### 3.1 P02 PR-2 Prisma/RLS scaffold

已经具备 Prisma schema/migration、tenant scope、RLS、真实 PostgreSQL 17 Testcontainers、可信证据链和大量 mutation/corner 测试；历史上 Reviewer Round 4、Tester Round 2 曾通过，协调者也曾在 `703b20a8` 接受。

但该接受已在 `cde725ec` 明确撤回。当前主线有两个独立阻断：

1. PR #1 的 merge commit 使 `codeEvidenceSha`/`stageEvidenceSha` 不在当前 first-parent 链上；官方 `npm test` 中 P02 两项 current-evidence 校验稳定失败。
2. `tests/integration/db/postgres-image.json` 只批准 `linux/arm64` digest；当前 Linux/amd64 重跑 `npm run prisma:validate` 返回 `BLOCKED/2`，诊断为镜像平台不匹配。

因此 P02 不是“只差勾表”，而是需要 B00-4、B00-6 及新一轮 Reviewer/Tester/协调者签收。

### 3.2 P03 PR-3 Docker/Helm/supply-chain scaffold

Docker/Helm/supply-chain 产品代码、五镜像构建、SBOM/Grype 与插件状态核验已经大量实现；历史 Tester Round 2 为 `PASSED`。但最终共享基线 Reviewer Round 5 给出 `CHANGES_REQUESTED / BLOCKED_BY_P02_REBASELINE`，总表从未接受 P03。

当前 Linux 主线有两个独立阻断：

1. `check-supply-chain.mjs` 解析未关闭 Git `core.quotepath` 引号转义，中文冻结路径被误判为产品输入；`tests/deploy-product-source-boundaries.test.ts` 有 2 项稳定失败。
2. `.reports/supply-chain/20260712_PR3部署供应链证据` 没有随仓库迁移到新工作站；`npm run supply-chain:check` exit `1`，官方全量中相应 37 项测试因 exact-five 证据缺失失败。

因此 P03 需要 B00-3 修代码、B00-5 重建本机证据，并在 P02 重冻结后重新走 Reviewer/Tester/协调者签收。

## 4. 尚未实施的 17 项

### 4.1 P04 与 V1

| 批次 | 任务 | 当前状态 | 首个解锁条件 |
|---|---|---|---|
| B1 收尾 | P04 | 只有 PoC manifest/README 占位；`poc:v1:check` 为 `NOT_APPLICABLE/78` | P02、P03 均重新接受，Day-0 输入可机读 |
| B2 | V101～V106 | 只有详细计划和角色模板 | P04 接受；kind/Calico、runtime、内网模型、存储等各自硬门满足 |
| B3 | V107 | 只有决策门计划 | V101～V106 全部接受并有同一候选证据包 |

V1 当前仍受 BLK-01～BLK-06 约束。Authentik、pinned 扫描工具和拟议 kind 方案只代表存在解除路径，不代表真实 K8s、gVisor、模型、S3、registry 或签名信任根已经冻结。

### 4.2 V2

| 批次 | 任务 | 当前状态 | 首个解锁条件 |
|---|---|---|---|
| B4 | V201、V202 | 只有计划；认证/核心 schema 未实现 | V107=`GO`，P02 接受，真实内网 OIDC 等输入冻结 |
| B5 | V203～V205 | 只有计划；PG rewrite、Cowork Service、Config Sync 服务链未实现 | 各自上游 V202/V102/V105/V106 接受 |
| B6 | V206、V207 | 只有计划；Web bridge 与 Artifact 闭环未实现 | V201/V204/V105/V202 等上游接受 |
| B7 | V208、V209 | 只有质量门/验收计划；`test:e2e` 仍为 `NOT_APPLICABLE/78` | V201～V207 全部接受 |

仓库当前没有受管 Playwright 配置和真实浏览器 journey；不能把 P00 Web 壳、P01 bridge 契约或 Electron 现有能力记作 V2 实现。

## 5. B00 唯一剩余执行顺序

以下顺序替代“边修边勾表”的做法。B00 完成前不启动 P04 Developer。

### Task B00-3：修复 P03 Git 路径解析

**Files:**

- Modify: `scripts/check-supply-chain.mjs`
- Inspect/modify if needed: `scripts/check-docker-build.mjs`
- Test: `tests/deploy-product-source-boundaries.test.ts`
- Record: `任务/P03-PR3部署供应链/开发记录.md`、`审核意见.md`、`测试报告.md`

- [ ] 先新增/保留中文 docs、计划、角色记录在默认 `core.quotepath=true` 下的 Red。
- [ ] 改为 `-z` 字节安全解析或显式关闭 quoting；rename/copy 同时检查旧、新路径。
- [ ] 重跑 19 项 product-source boundary 与 P03 targeted；确认未知产品路径仍 fail closed。
- [ ] P03 Developer 提交实现和记录，Reviewer 只审本轮代码，不提前签收整个 P03。

### Task B00-4：批准并验证 Linux/amd64 PostgreSQL 17 digest

**Files:**

- Modify: `tests/integration/db/postgres-image.json`
- Test: `tests/db/*`、`tests/integration/db/*`
- Record: `任务/P02-PR2数据库脚手架/开发记录.md`、`审核意见.md`、`测试报告.md`

- [ ] 先写未批准 `linux/amd64` 仍 `BLOCKED/2`、错误 digest 仍 fail closed 的 Red。
- [ ] 只增加经本机 Docker 实际解析的 `postgres:17.10-bookworm` exact amd64 digest，不放宽 arm64 规则、不接受浮动 tag。
- [ ] 重跑 preflight、unit、真实 PG17 integration，要求非特权 RLS、`skipped=0`、容器清理完成。
- [ ] 保存本轮平台/digest/runId 记录；本步不复用 macOS arm64 报告。

### Task B00-5：重建 P03 exact-five 本机证据

**Files:**

- Runtime output only: `.reports/supply-chain/20260712_PR3部署供应链证据/`
- Record after validation: `任务/P03-PR3部署供应链/开发记录.md`

- [ ] 在 B00-3 产品提交稳定后，用 Node 24.18 与 pinned syft/grype/helm 执行 `npm run docker:build:check`。
- [ ] 验证五镜像均为 linux/amd64、non-root、Critical=0，SBOM/Grype hash 与插件安装状态可重算。
- [ ] 重跑 `npm run supply-chain:check`、`npm run helm:lint` 和 P03 证据 mutation。
- [ ] `.reports/` 保持 ignored；只提交可审计摘要、hash、runId 和角色记录，不提交本机镜像/缓存/大报告。

### Task B00-6：在最终产品 `HEAD` 重冻结 P02 证据

**Files:**

- Regenerate/modify: `docs/db/20260711_P02_Prisma与RLS脚手架证据/*`
- Record: `任务/P02-PR2数据库脚手架/开发记录.md`

- [ ] 确认 B00-3、B00-4 已落码且 B00-5 的 P03 门真实通过，冻结此时的最终产品 source。
- [ ] 执行 `scripts/db/validate.mjs` 生成四份 raw 报告并做 evidence-only 提交。
- [ ] 执行 `npm run prisma:validate` 生成 stage PASS，确认 source/stage SHA 在当前 first-parent 链上。
- [ ] 重跑 `npm test`，要求 207 files 全绿、P02 current-evidence 两项与 P03 39 项旧失败全部关闭。

### Task B00-7：重新签收 P02 与 P03

**Files:**

- Modify: P02/P03 各自 `审核意见.md`、`测试报告.md`
- Modify last: `00-总体完成表.md`

- [ ] P02 Reviewer 在 B00-6 clean `HEAD` 复核 trusted provenance、amd64 PG17/RLS、P03 联动和官方 full。
- [ ] P02 Tester 在 Reviewer 新批准后独立重放关键 mutation、真实 PG17、全量与构建门；协调者最后接受 P02。
- [ ] P03 Reviewer 在同一已接受 P02 基线上复核 quotepath、exact-five、Docker/Helm/supply-chain 和官方 full。
- [ ] P03 Tester 在 Reviewer 新批准后独立重放中文路径、证据篡改、镜像/插件状态与共享集成门；协调者最后接受 P03。
- [ ] 只有两项都完成新一轮四角色链后，协调者才勾总表 P02/P03；勾表后立即复跑 P02 provenance，避免再次由总表提交使证据失效。

## 6. B00 完成后的推进顺序

1. P04：激活 `poc:v1:check`，把 exit `78` 变成真实 `PASS/FAILED/BLOCKED` 三态。
2. V101：冻结本机/云上 Day-0 和 runtime 兼容矩阵；未解决 gVisor、内网模型、registry 时诚实 `BLOCKED`。
3. V102～V106：严格按依赖图执行真实 turn、egress、存储、bridge 契约冻结和 Config Sync spike。
4. V107：只在 V101～V106 全部接受后形成原子 `GO | NO_GO`。
5. V201～V207：按 B4～B6 实现浏览器内部 Alpha 主链路。
6. V208：激活 Playwright/真实 E2E 门；V209 最后做只读总验收与签署。

## 7. 每个后续提交的固定收尾门

- [ ] 使用 Node 24.18；记录命令、exit、source SHA、环境和 runId。
- [ ] changed-file ESLint 0 warning；相关 targeted、`npm test`、`npm run build`、`npm run compile:electron` 按变更风险执行。
- [ ] 若提交触及 P02 evidence source，随批重冻结 P02；若触及 P03 五镜像输入，随批重建 exact-five。
- [ ] Reviewer、Tester 使用独立运行，不复用 Developer 输出冒充签署。
- [ ] 提交前检查 secrets、ignored 产物、生成物手改、无关 churn 和中文/i18n 错误。
- [ ] 只在线性 main/rebase/FF 历史推进；不得再次用普通 merge commit打断 first-parent 证据链。
