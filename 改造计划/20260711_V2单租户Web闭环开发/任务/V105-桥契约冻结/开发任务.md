# V105 · Web bridge/WS 契约冻结验证

- 状态：TODO
- 依赖：P01；现有 preload/electron.d.ts/runtime forwarder。
- 权威输入：根 AGENTS、19、17 对应 V1/V2、16、附录 C；冲突以根 AGENTS 的生产断公网和附录 C 契约决策为准。

## 源码边界

contract fixtures/registry/bridge interface；V1 只冻结和验证，不实现完整 UI。

## TDD：先写的失败测试

先写附录A全覆盖、10 stream 事件、ticket scope、sinceSeq、activeSessionIds/resourceSubscriptions 的失败测试。 Red 必须因目标行为缺失而失败；环境缺失另记 BLOCKED，不算 Red。

## Corner cases

goal 遗漏、重复通道、未知帧、ticket 重放/过期、多 tab、resource 与 session 订阅混淆。

## 实施与验收

Developer 按最小垂直切片逐个 Green，避免无关重构。验收命令：npm run contracts:check；桥双跑 fixture；负向旧路径检查。

## 回滚

契约版本整体回滚并保留兼容说明，禁止前后端单边改。

## 完成证据

registry coverage=100%、10事件映射、ticket/sinceSeq schema 与评审签署。 证据须绑定 SHA/runId/环境；所有触碰 TS 文件 changed-file ESLint 0 warning，相关 Vitest 全绿。

## Developer → Reviewer → Tester 循环

Developer 记录每轮 Red/Green 与 diff → 独立 Reviewer 对照本文件和源码审核；若 CHANGES_REQUESTED，退同一 Developer 修复并进入下一轮 → Reviewer APPROVED_FOR_TEST 后独立 Tester 新增 corner-case/回归测试；Tester FAILED 必须退 Developer 且修复后重新 Reviewer → Tester PASSED 后由协调者核验证据并勾总体表。任何角色不得自行勾 Todo。

