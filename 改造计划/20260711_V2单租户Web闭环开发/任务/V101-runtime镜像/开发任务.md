# V101 · Linux runtime 镜像与兼容矩阵

- 状态：TODO
- 依赖：P00-P04、K8s/RuntimeClass/内部 registry 冻结。
- 权威输入：根 AGENTS、19、17 对应 V1/V2、16、附录 C；冲突以根 AGENTS 的生产断公网和附录 C 契约决策为准。

## 源码边界

docker/openclaw-runtime、runtime 构建脚本、PoC compatibility/resource 脚本；不改 Electron renderer。

## TDD：先写的失败测试

先写镜像内容、gateway health、spawn/node/shell/npm/stdio MCP/skill 兼容探针的失败用例。 Red 必须因目标行为缺失而失败；环境缺失另记 BLOCKED，不算 Red。

## Corner cases

linux x64/arm64、native addon、只读 root、SIGTERM、OOM、缺插件、无 HOME、Kata 回退。

## 实施与验收

Developer 按最小垂直切片逐个 Green，避免无关重构。验收命令：内部 registry 构建/扫描；npm run openclaw:patch；npm run poc:v1:check；兼容矩阵脚本。

## 回滚

按 digest 回退上一镜像；OpenClaw 源改动必须变版本 patch。

## 完成证据

digest/SBOM、无 UI/密钥/数据证明、gVisor/Kata 兼容和资源矩阵。 证据须绑定 SHA/runId/环境；所有触碰 TS 文件 changed-file ESLint 0 warning，相关 Vitest 全绿。

## Developer → Reviewer → Tester 循环

Developer 记录每轮 Red/Green 与 diff → 独立 Reviewer 对照本文件和源码审核；若 CHANGES_REQUESTED，退同一 Developer 修复并进入下一轮 → Reviewer APPROVED_FOR_TEST 后独立 Tester 新增 corner-case/回归测试；Tester FAILED 必须退 Developer 且修复后重新 Reviewer → Tester PASSED 后由协调者核验证据并勾总体表。任何角色不得自行勾 Todo。

