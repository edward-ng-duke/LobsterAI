# 测试报告

> 仅在独立 Reviewer 对精确 SHA 给出 `APPROVED_FOR_TEST` 后，由独立 Tester 填写。Tester 必须新增未由 Developer/Reviewer 覆盖的测试并形成交付 commit；失败退 Developer，修复后重新 Reviewer。

## 开始门与元数据

- Reviewer Agent / Round / `APPROVED_FOR_TEST` SHA：
- Tester Agent：
- 测试 SHA / 新增测试 commit：
- V105 / V201 / V204 accepted SHA/hash：
- 环境：Node/npm/OS、Chromium/WebKit/Firefox、app/API Origin、TLS/proxy、PG/Redis digest、K8s context/namespace/Pod UID/replica、OpenClaw image digest/version、NetworkPolicy：
- runId / trace root / 证据目录：
- 结论：`FAILED | PASSED | BLOCKED`

若 Reviewer 门、真实内网IdP、PG/Redis、V204 OpenClaw、两个replicas、三浏览器或proxy Upgrade环境缺失，只能`BLOCKED/2`；环境齐而断言失败是`FAILED/1`。fixture/loopback不能替代产品PASS。

## Round N · YYYY-MM-DD HH:mm · Tester/commit

## 独立新增角度（至少覆盖 14 类）

| 用例 ID | 独立角度 | 建议故障/边界 | 预期 |
|---|---|---|---|
| `TST-01` | registry property/fuzz | 随机删/换Bridge、Route、Channel target与optional | 全集合门立即失败；无硬编码count伪绿 |
| `TST-02` | REST proxy异常 | HTML 502、空204、坏gzip、错content-type、慢header/body | typed错误、timeout/abort、无成功误判 |
| `TST-03` | idempotent unknown outcome | API已接收turn但响应丢；同/异key重试 | receipt对账；不重复turn；异payload conflict |
| `TST-04` | auth风暴与撤销 | 50并发401、refresh迟到、logout/disabled同时 | single-flight；旧响应不复活；socket/ticket全失效 |
| `TST-05` | ticket安全 | TTL边界±1ms、双tab同ticket、换Origin/sid/jti/scope | 仅原绑定首次成功；拒绝不泄露存在性 |
| `TST-06` | ack crash window | reducer接纳/terminal finalize/IndexedDB transaction/ack各点杀tab | snapshot+连续seq恢复；无丢重UI终态/消息副作用 |
| `TST-07` | retention边界 | 刚好保留、刚trim、trim与live并发 | 窗口内补发；窗口外snapshot watermark恢复 |
| `TST-08` | 极端序列 | 1-10/1-2、3/1/2、缺2、重复、同seq异payload | 二元排序、有界reorder、gap/corruption不越checkpoint |
| `TST-09` | 四断点+terminal竞态 | 首帧前/中段/complete前/complete后未ack，error/abort同到 | 一个有效terminal；无假完成/重复toast |
| `TST-10` | 弱网/半开 | 2G延迟抖动、packet loss、offline、TLS reset、proxy idle close | 单reconnect loop；新ticket；sinceSeq恢复 |
| `TST-11` | 背压/大帧 | 慢tab、洪泛update夹terminal、队列/帧边界 | 有界内存；terminal不丢；overflow进入snapshot |
| `TST-12` | 多tab/多scope | 两tab两session、同session多callback、资源同名、A关闭 | ticket/socket/refcount独立；不串user/session/resource |
| `TST-13` | StrictMode/导航 | 双mount、后退/刷新/深链、active快切、pagehide | 单bridge/socket；旧generation不投递；清理完整 |
| `TST-14` | apiStream/cancel | server/client requestId不同，chunk/done/error/abort乱序 | canonical requestId；REST abort；单终态 |
| `TST-15` | ASR/全channel | 每个ChannelRegistry valid/invalid帧；ASR混主WS | 全通道有处置；ASR独立；未就绪明确错误 |
| `TST-16` | 降级矩阵 | 每个unsupported/deferred/no-op/browser-fallback叶调用 | 无undefined/假success/崩溃；code/messageKey/fallback正确 |
| `TST-17` | 三浏览器capability | Clipboard/Notification/Media权限允许/拒绝/缺API | 支持则工作；不支持有zh/en回退，不碰native |
| `TST-18` | schema/版本演进 | 当前/上minor、unknown optional、unknown critical、坏payload | 兼容窗口通过；critical/version fail closed+resync |
| `TST-19` | 隔离红队 | user A猜B session/request/resource；body/header伪tenant | 401/404/拒绝，无帧、无timing/metadata泄露 |
| `TST-20` | secret/观测 | token/ticket/code/prompt/PII嵌套canary | client/log/trace/report/URL/storage 0命中；指标可定位 |
| `TST-21` | 生产断公网 | 公共DNS/CDN/Sentry/font/EventSource fallback/运行期install | 全拒；批准内网OIDC/API/Redis/OTel仍可达 |
| `TST-22` | drain/rollback | active turn/replay/permission中滚动API、关flag、回旧Web | 新请求停止、有界drain、恢复不双发、cleanup=true |

Tester至少新增14类，其中必须包含`TST-03/04/05/06/07/08/09/10/11/12/16/19/20/21/22`中的12类；不得只复跑现有测试。所有故障注入用合成secret和独立prefix，不能污染共享账号/stream。

## 结果表

| 用例 ID / AC | 新测试文件/脚本 | 命令/exit code | 结果 | 证据/commit |
|---|---|---|---|---|

## 累计门

- [ ] 34 AC全PASS，`skipped=0`，strict report与实现/测试同SHA；V105/V201/V204 accepted hashes未漂移。
- [ ] 全Bridge/Route/Inventory/Channel动态集合相等；全部invoke→REST、on→WS/明确disposition；Cowork 10精确。
- [ ] ticket user/session/sid/jti/origin/scope/TTL/one-use，OIDC/CSRF/CORS/Origin与跨租户/跨会话安全全绿。
- [ ] seq/sinceSeq/ack/replay/live/retention/gap snapshot、duplicate/out-of-order/loss/corruption与terminal exactly-once全绿。
- [ ] idempotency/timeout/retry/cancel/abort、弱网/heartbeat/backpressure/multitab/concurrency/StrictMode全绿。
- [ ] 全降级矩阵、browser capability、zh/en、无native crash、无浏览器SSE私自fallback全绿。
- [ ] runtime schema、stable errors、trace/metrics、redaction、生产断公网与secret scan全绿。
- [ ] Chromium/WebKit/Firefox、真实proxy/两replicas/PG/Redis/V204 OpenClaw链路、targeted/full tests/build/compile/changed-file ESLint全绿。
- [ ] feature flag、contract minor兼容、SIGTERM drain、rollback、IndexedDB/socket/timer/Redis/ticket cleanup通过，`cleanup=true`。
- [ ] diff不含V204业务、V207 artifacts、V5 provider、Electron大重构、generated vendor、总表或无关文件；证据目录符合`YYYYMMDD_主题`。

## FAILED / BLOCKED 交接

| 严重级 | 用例 | 精确复现 | 实际/期望 | 证据 | 退回要求 |
|---|---|---|---|---|---|

- `FAILED`：退原Developer逐条修复；随后必须重新独立Reviewer，不能直接回Tester。
- `BLOCKED`：写清owner、缺失输入、非零exit与解除条件；不放公网、不用mock/loopback替代、不改期望。相同阻断第三次出现才按目标机制审计是否可标blocked。

## 回归、非功能与协调者交接

- 新增Tester commit：
- 三浏览器/弱网/多tab/性能与内存峰值：
- retention/replay/gap snapshot/terminal：
- 安全/断公网/redaction/Origin：
- migration/drain/rollback/cleanup：
- 最终 strict report：
- 结论说明：

Tester `PASSED` 只表示可交协调者审计，不自行勾总体表，也不能宣称V207 artifacts、V208质量门或V209总验收完成。
