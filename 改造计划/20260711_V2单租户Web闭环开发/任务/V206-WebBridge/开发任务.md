# V206 · 浏览器 Web bridge

- 状态：TODO
- 依赖：V105、V201、V204。
- 权威输入：根 AGENTS、19、17 对应 V1/V2、16、附录 C；冲突以根 AGENTS 的生产断公网和附录 C 契约决策为准。

## 源码边界

libs/client/bridge、apps/web bootstrap、REST/WS client；Electron bridge 保持。

## TDD：先写的失败测试

先对同一 ElectronBridge suite 写 Web 实现失败测试：invoke、local callbacks、subscribe、ticket、reconnect、sinceSeq、unsupported。 Red 必须因目标行为缺失而失败；环境缺失另记 BLOCKED，不算 Red。

## Corner cases

初始化竞态、多 tab、重复/乱序帧、断在complete、ticket过期、401刷新、慢消费者、activeSession切换。

## 实施与验收

Developer 按最小垂直切片逐个 Green，避免无关重构。验收命令：bridge unit/contract；Playwright 弱网/离线；三浏览器；eslint/typecheck。

## 回滚

Web feature flag/route 回退；Electron 仍用 preload；清理 WS/timer。

## 完成证据

双跑一致性、断线矩阵、Electron-only 降级表、无 native API 崩溃。 证据须绑定 SHA/runId/环境；所有触碰 TS 文件 changed-file ESLint 0 warning，相关 Vitest 全绿。

## Developer → Reviewer → Tester 循环

Developer 记录每轮 Red/Green 与 diff → 独立 Reviewer 对照本文件和源码审核；若 CHANGES_REQUESTED，退同一 Developer 修复并进入下一轮 → Reviewer APPROVED_FOR_TEST 后独立 Tester 新增 corner-case/回归测试；Tester FAILED 必须退 Developer 且修复后重新 Reviewer → Tester PASSED 后由协调者核验证据并勾总体表。任何角色不得自行勾 Todo。

