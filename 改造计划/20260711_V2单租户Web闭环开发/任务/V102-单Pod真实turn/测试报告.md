# 测试报告

> 初始为空白模板。独立 Tester 仅在 Reviewer `APPROVED_FOR_TEST` 后填写；必须使用新 runId 重放真实 gVisor turn，不得用 Developer 自测或旧证据替代。

## 元数据

- Tester Agent：
- Reviewer `APPROVED_FOR_TEST` round/commit：
- 测试 commit/SHA：
- context/cluster UID/namespace/RuntimeClass/node/image digest/model endpoint class：
- 独立 runId/证据路径：
- 结论：`BLOCKED | FAILED | PASSED`

## 独立新增测试

| 用例 ID | 角度/风险 | 文件/脚本 | 结果 | 证据 |
|---|---|---|---|---|

## 真实 turn、回归与非功能

- 独立 gVisor turn 命令、退出码、WS/RPC/event/model correlation、时延/资源、断公网、清理：
- 快环/累计门、changed-file ESLint、证据篡改负向：

## 失败交接 / 通过签署

- `BLOCKED`：缺独立集群/模型/审计权限或前置，不写 PASSED。
- `FAILED`：写明需求/用例 ID、实际/期望、复现命令、证据和回交 Developer；修复后必须重新 Reviewer。
- `PASSED`：逐项给出 AC01～AC24 独立定位和新 runId；仅协调者可更新总体完成表。
