# V102 · gVisor 单 Pod 真实 OpenClaw turn

- 状态：TODO
- 依赖：V101；内网模型 gateway；测试集群。
- 权威输入：根 AGENTS、19、17 对应 V1/V2、16、附录 C；冲突以根 AGENTS 的生产断公网和附录 C 契约决策为准。

## 源码边界

PoC turn client、sandbox manifest、gateway adapter fixture；不写 V2 产品 API。

## TDD：先写的失败测试

先断言 ready 后 chat.send 必须按 sessionKey 收到 message/messageUpdate/complete，当前未接通应失败。 Red 必须因目标行为缺失而失败；环境缺失另记 BLOCKED，不算 Red。

## Corner cases

ready 假阳性、多 sessionKey、首帧/complete 丢失、abort、SIGTERM、模型超时、token 错误。

## 实施与验收

Developer 按最小垂直切片逐个 Green，避免无关重构。验收命令：poc acquire/turn/abort；日志 schema 校验；时延采样；资源采样。

## 回滚

删除 PoC Pod/Secret/PVC 测试数据并回退镜像 digest。

## 完成证据

同 runId 的 manifest、事件日志、时延/资源、失败场景和清理记录。 证据须绑定 SHA/runId/环境；所有触碰 TS 文件 changed-file ESLint 0 warning，相关 Vitest 全绿。

## Developer → Reviewer → Tester 循环

Developer 记录每轮 Red/Green 与 diff → 独立 Reviewer 对照本文件和源码审核；若 CHANGES_REQUESTED，退同一 Developer 修复并进入下一轮 → Reviewer APPROVED_FOR_TEST 后独立 Tester 新增 corner-case/回归测试；Tester FAILED 必须退 Developer 且修复后重新 Reviewer → Tester PASSED 后由协调者核验证据并勾总体表。任何角色不得自行勾 Todo。

