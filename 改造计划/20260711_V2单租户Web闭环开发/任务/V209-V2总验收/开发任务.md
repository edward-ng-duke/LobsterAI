# V209 · V2 总体验收

- 状态：TODO
- 依赖：V208 PASSED；V201-V208 四角色门全过。
- 权威输入：根 AGENTS、19、17 对应 V1/V2、16、附录 C；冲突以根 AGENTS 的生产断公网和附录 C 契约决策为准。

## 源码边界

总体表、验收报告、风险清账；不补写业务代码。

## TDD：先写的失败测试

机器审计 V2.1-V2.5、前置门、报告 runId/SHA/digest，任一缺失即失败。 Red 必须因目标行为缺失而失败；环境缺失另记 BLOCKED，不算 Red。

## Corner cases

demo 使用 mock/公网、跨环境拼证据、测试 skipped、unsupported 静默、已知缺陷未清、工作树脏。

## 实施与验收

Developer 按最小垂直切片逐个 Green，避免无关重构。验收命令：完整命令栈 + 生产同构 smoke + 公网 deny 探针 + diff/secret scan。

## 回滚

不通过则退回对应任务并保持 Todo 未勾；禁止“条件通过”。

## 完成证据

V2.1-V2.5 逐条证据、内部 Alpha 限制、Reviewer/Tester/协调者签署。 证据须绑定 SHA/runId/环境；所有触碰 TS 文件 changed-file ESLint 0 warning，相关 Vitest 全绿。

## Developer → Reviewer → Tester 循环

Developer 记录每轮 Red/Green 与 diff → 独立 Reviewer 对照本文件和源码审核；若 CHANGES_REQUESTED，退同一 Developer 修复并进入下一轮 → Reviewer APPROVED_FOR_TEST 后独立 Tester 新增 corner-case/回归测试；Tester FAILED 必须退 Developer 且修复后重新 Reviewer → Tester PASSED 后由协调者核验证据并勾总体表。任何角色不得自行勾 Todo。

