# V106 · Config Sync 纯渲染 spike

- 状态：TODO
- 依赖：P00-P04；桌面 golden fixtures。
- 权威输入：根 AGENTS、19、17 对应 V1/V2、16、附录 C；冲突以根 AGENTS 的生产断公网和附录 C 契约决策为准。

## 源码边界

目标 config-sync 纯函数/fixture/设计文档；不得新建独立 deployable。

## TDD：先写的失败测试

先把桌面输入固定为 golden，写 Node 环境无 Electron、输出字段等价和 secret 不落 fixture 的失败测试。 Red 必须因目标行为缺失而失败；环境缺失另记 BLOCKED，不算 Red。

## Corner cases

app.getPath/live getter、平台路径、稳定排序、缺省值、插件缺失、并发写、workspace 文件所有权。

## 实施与验收

Developer 按最小垂直切片逐个 Green，避免无关重构。验收命令：相关 Vitest golden；npm run typecheck；Node-only import graph 检查。

## 回滚

保留桌面实现，移除 spike entrypoint；fixture 可保留作为风险证据。

## 完成证据

四层边界设计、golden diff=0/已批准差异、Electron 依赖清单。 证据须绑定 SHA/runId/环境；所有触碰 TS 文件 changed-file ESLint 0 warning，相关 Vitest 全绿。

## Developer → Reviewer → Tester 循环

Developer 记录每轮 Red/Green 与 diff → 独立 Reviewer 对照本文件和源码审核；若 CHANGES_REQUESTED，退同一 Developer 修复并进入下一轮 → Reviewer APPROVED_FOR_TEST 后独立 Tester 新增 corner-case/回归测试；Tester FAILED 必须退 Developer 且修复后重新 Reviewer → Tester PASSED 后由协调者核验证据并勾总体表。任何角色不得自行勾 Todo。

