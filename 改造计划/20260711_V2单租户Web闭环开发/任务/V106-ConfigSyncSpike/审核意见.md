# V106 审核意见

> 仅由独立 Reviewer 在 Developer 写明 `REVIEW_PENDING` 后追加。Reviewer 不代写大块实现、不修改 Tester 报告或总体表。

## Reviewer 开始前核对

- Reviewer Agent / Round：
- 独立 checkout / review SHA / merge-base：
- 计划版本 / 计划 SHA：
- Developer Red/Green/evidence runId：
- Reviewer 独立 runId / temp root：
- P00～P04 协调者接受与新鲜 gate：`VERIFIED | BLOCKED`
- worktree 无关改动隔离：

前置不完整、Developer 无稳定 SHA、无有效 Red 或 production 已偷切 pure renderer 时，不得批准。

## Round 1 · YYYY-MM-DD HH:mm · Agent/commit

- 结论：`CHANGES_REQUESTED | APPROVED_FOR_TEST | BLOCKED`

| 严重级 | AC/T | 文件:行 / fixture | 实际 / 期望 | 独立复现命令 | 要求完成证据 |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

### 独立审查清单

- [ ] 范围只含 V106 spike/test/evidence；无总表、其它任务、deployable、生产切流或 3502 行大搬运。
- [ ] effect/input inventory 与当前源码、`main.ts` getter/queue/restart、existing tests 双向完整。
- [ ] snapshot 一次读取/深复制；pure transitive import graph 无 Electron/app/userData、SQLite/fs/env/time/random/live getter/console。
- [ ] providers、agents、IM/bindings、MCP、plugins/skills、sandbox/session/browser/cron/memory 与 AGENTS 全分母 golden。
- [ ] renderer repeat/reorder/TZ/locale byte stable；Windows/Linux pathFlavor、LF/排序/碰撞规则 fail closed。
- [ ] secret 字段、placeholder、env plan 双向；fixture/diff/log 无明文或可逆前后缀；legacy 明文差异未被自批。
- [ ] approved differences 均 exact pointer + DEC/owner/expiry，无 wildcard/整 section ignore。
- [ ] effects prototype allowed root/CAS/symlink/temp/hash/partial/concurrency/cleanup 可验证且 production disabled。
- [ ] legacy-owned exec/session/workspace/heartbeat 副作用与 V205 owner 明示，无遗漏。
- [ ] T01 有真实前置状态证据，T02～T28 有有效 Red→Green；AC01～AC22、coverage、lint、typecheck、全量/build 证据同 SHA。

### Reviewer 必跑 mutation（隔离副本）

| mutation | 预期 | 实际/exit | 证据 |
|---|---|---|---|
| pure module import `electron` / `fs` / `coworkStore` | audit 非零 |  |  |
| Date/process.env/random/live getter | purity 非零 |  |  |
| input reverse + TZ/locale | bytes/hash 不变 |  |  |
| Windows UNC/device/`..`/NUL | fail closed，write=0 |  |  |
| IM 前 8 位 account collision | 不覆盖，FAILED |  |  |
| MCP CJK/hash/header collision | 不覆盖，FAILED |  |  |
| AGENTS 双 marker/marker injection/CRLF | managed 唯一、用户区安全 |  |  |
| provider/embedding/MCP/plugin/IM secret sentinel | scan 非零/placeholder |  |  |
| existing config duplicate key/corrupt type | adapter FAILED |  |  |
| snapshot 后 hash 变/symlink swap | CAS 阻断，rename=0 |  |  |
| rename partial/old revision late apply | 非 PASS、可恢复 |  |  |
| approved diff wildcard/过期/secret 自批 | policy 非零/BLOCKED |  |  |

### 独立命令与证据

记录 targeted Vitest、import audit、golden completeness/secret scan、changed-file ESLint、typecheck、`npm test`、`build:saas`、`build`、`compile:electron`、前置 gates、diff/scope audit 的命令、CST+UTC、exit、SHA、runId、report hash。不能只引用 Developer 输出。

### 交接 Tester

只有结论为 `APPROVED_FOR_TEST` 时填写：稳定 SHA、Reviewer 新 runId、剩余非阻断风险、Tester 必须新增的独立角度。`BLOCKED/CHANGES_REQUESTED` 时明确退同一 Developer；修复后新增 Round，保留本轮历史。
