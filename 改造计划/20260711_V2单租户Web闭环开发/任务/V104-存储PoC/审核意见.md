# 审核意见

> 独立 Reviewer 必须读计划、全部 diff、Developer Red/Green 和原始存储/故障证据；不得复用 Developer runId、自己的历史 runId 或代填 Tester 结论。

## Round 1 · YYYY-MM-DD HH:mm · Reviewer/commit

- Reviewer Agent：
- 审核基线/commits：
- Developer runId（只作输入）：
- Reviewer 独立新 runId：
- context/cluster UID/namespace/region/AZ：
- StorageClass UID/provisioner/CSI version/PV-PVC UIDs：
- object endpoint ID/bucket/run prefix/policy hash：
- probe image digest/node A-B UIDs/fault capability：
- 证据目录：
- 结论：`BLOCKED | CHANGES_REQUESTED | APPROVED_FOR_TEST`

### 独立重放与需求核对

- P00～P04 前置、Day-0、唯一交接目录、PASS/FAILED/BLOCKED 与 AC01～AC28：
- Reviewer 新 runId 的 preflight、双向可见、20 并发、单写者/fencing、rename/fsync crash、PVC 断连不删 S3：
- S3 timeout/retry/idempotency/partial+complete 重放/checksum/大文件、配额边界：
- 路径/symlink/state/prefix/生产断公网、Pod 重建、恢复到新 PVC/prefix、回收/精确 cleanup：
- 证据 schema/hash/secret scan/summary 重算与候选 go/no-go：

### 问题清单

| 严重级 | 用例/需求 | 文件:行 | 实际/期望 | 复现命令 | 要求证据 |
|---|---|---|---|---|---|

### 交接 Tester（仅 `APPROVED_FOR_TEST`）

- Reviewer 独立 runId 与证据路径：
- Tester 必须使用第三个全新 runId 重放的完整候选/P0/故障/安全/恢复矩阵：
- 建议新增反向角度：symlink swap TOCTOU | fsync 后 node drain | complete 响应丢失 | 旧 tombstone 重排 | Unicode 归一化 | 配额释放竞态 | watcher 重启 | Retain PV 清理逃逸。

### BLOCKED 说明

- 缺独立集群/RWX StorageClass/内网 S3/双节点/故障权限/预算时，写明缺口和非零证据，不得用 Developer 证据写 `APPROVED_FOR_TEST`。
