# 开发记录

> Developer 只按 Round 追加，不删除 Planner/Reviewer/Tester 引用的历史证据，不自行勾总体完成表。

## Round 0 · 2026-07-11 · `/root/v104_planner`

- 任务 ID：`V104`；计划版本：`V104-v2`；计划文件：`V104-存储PoC/开发任务.md`。
- 规划基线：`c7d3f5ac1f73693993f65a13c1676224bec99817`；计划 commit：`docs(storage): detail V104 PVC S3 PoC plan`（SHA 见 Planner 交付）。Developer 开工时重新记录干净 checkout 和已验收前置 SHA。
- 当前状态：`IMPLEMENTATION_BLOCKED_BY_PREREQUISITES_AND_DAY0`。P00～P04 的独立验收、BLK-01/04/05/06、RWX StorageClass 候选、内网 S3/MinIO、真实双节点集群、故障注入权限和预算必须在开工/动态执行前分别核验。
- 冻结边界：PVC 是 `/workspace/project` 和受控 `/workspace/state` 的 POSIX 权威；S3 只承担 artifact/预览/分享、上传暂存、镜像和备份。Pod `/state` 不归 workspace，不允许用完整 HOME 挂载绕过。
- 硬门：单 workspace 同时仅一 RW Pod；file+dir fsync 后重建必须保留 checksum；PVC 断连空 listing 不得删 S3；S3 retry/multipart/complete 必须有界且幂等；路径/symlink/配额/回收/恢复/cleanup 全部 fail closed。
- 证据要求：Developer/Reviewer/Tester 各用彼此不同的新 runId 在真实集群执行；fixture/local-path/本地 MinIO 不能写 V1 PASS；生产 runtime 完全断公网。
- 唯一权威交接目录是本 `V104-存储PoC/`；不得再建近义 V104 目录或分拆 Round。

## 元数据

- Developer Agent：
- 开工基线 SHA/分支/工作树：
- P00～P04 已验收 SHA：
- context/cluster UID/namespace/region/AZ：
- StorageClass UID/provisioner/CSI version/PV-PVC UIDs：
- object endpoint ID/bucket/run prefix/policy hash：
- probe image digest/node A-B UIDs：
- Day-0 budget/fault capability version：
- Developer runId/证据目录：

## Round 1 · YYYY-MM-DD HH:mm · Agent/commit

### Red

- 用例 ID、目标行为、失败原因、命令、退出码、CST/UTC 时间、环境、日志路径：

### Green / Refactor

- 最小实现、变更文件、设计取舍：
- commits：

### 自测与真实证据

- 命令/退出码/时间：
- 可见性/并发/租约/rename-fsync/10k/watch：
- PVC/S3 故障注入、timeout/retry/idempotency/multipart/checksum/大文件/配额：
- 路径/symlink/state/断公网/跨 Pod 重建/备份恢复/回收/cleanup：
- AC01～AC28 定位：

### 交接 Reviewer

- 已知限制/风险：
- diff 范围与精确回滚点：
- Developer runId 仅作输入；Reviewer 必须使用全新 runId：
