# V104 · 工作区 PVC/S3 存储 PoC

- 状态：TODO
- 依赖：P04；RWX StorageClass 与内网 S3/MinIO。
- 权威输入：根 AGENTS、19、17 对应 V1/V2、16、附录 C；冲突以根 AGENTS 的生产断公网和附录 C 契约决策为准。

## 源码边界

PoC storage scripts、workspace lease prototype、S3 mirror fixture；不产品化文件 API。

## TDD：先写的失败测试

先写 Pod/文件服务并发写、断连重挂、inotify/补偿、S3 prefix/lifecycle 的失败场景。 Red 必须因目标行为缺失而失败；环境缺失另记 BLOCKED，不算 Red。

## Corner cases

原子 rename、小文件风暴、symlink、部分写、租约过期、双写冲突、S3 eventual consistency。

## 实施与验收

Developer 按最小垂直切片逐个 Green，避免无关重构。验收命令：storage benchmark/chaos；checksum 对账；清理验证。

## 回滚

释放租约、卸载/删除测试 PVC 与 prefix；失败则触发 workspace-service 备选评审。

## 完成证据

一致性延迟、吞吐、断连恢复、checksum、候选选型/go-no-go。 证据须绑定 SHA/runId/环境；所有触碰 TS 文件 changed-file ESLint 0 warning，相关 Vitest 全绿。

## Developer → Reviewer → Tester 循环

Developer 记录每轮 Red/Green 与 diff → 独立 Reviewer 对照本文件和源码审核；若 CHANGES_REQUESTED，退同一 Developer 修复并进入下一轮 → Reviewer APPROVED_FOR_TEST 后独立 Tester 新增 corner-case/回归测试；Tester FAILED 必须退 Developer 且修复后重新 Reviewer → Tester PASSED 后由协调者核验证据并勾总体表。任何角色不得自行勾 Todo。

