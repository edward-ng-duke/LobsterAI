# 测试记录

## Round 1 · 2026-07-11 23:25 CST · `/root/p02_tester` / `14c99764`

- Tester Agent：`/root/p02_tester`（全新独立 Tester；未参与 P02 Planner、Developer 或 Reviewer）。
- 测试启动门：Reviewer Round 3 `d1465cc51dd773735f07967f580f00f8da3578b4` 明确为 `APPROVED_FOR_TEST`；Developer final 为 `1b7a96654a1ebb49b0ed5f349b4f2cbe00016989`。
- Tester Red commit：`8c4dbcf7653ccdef0600f4cc18e6a698fda51964` (`test(db): capture tester evidence boundary gaps`)。
- Tester PostgreSQL corner commit：`14c99764e4bf6ba8dcf94bc653384c9015220ba3` (`test(db): add independent PostgreSQL corner coverage`)。
- 环境：macOS arm64，Node `v24.15.0`，npm `11.12.1`，Docker `linux/arm64`，PostgreSQL `17.10 (Debian 17.10-1.pgdg12+1)`，镜像 `postgres:17.10-bookworm@sha256:17b6c778de50f4bb9a878c36e736110fbcd9b7020377d6fdfdf20f7c0347e40a`。
- 结论：`FAILED_FOR_DEVELOPMENT`。数据库隔离本体的新增真实 PG17 corner 为 Green，但 AC18 证据链有 4 个独立阻断 Red；其中两个是状态机误杀，两个是可产生假 PASS 的完整性绕过。Tester 未修改业务实现、gate 实现或总体完成表。

## 独立 Red 与阻断缺陷

| 严重级 | 用例/需求 | 文件:行 | 期望 | 实际 | 复现命令 | 需要的完成证据 |
|---|---|---|---|---|---|---|
| P1 | P02-AC-18；Reviewer/Tester 交接与并行任务不串证据 | `scripts/db/validate-evidence.mjs:90-112`；`tests/db/tester-evidence-boundary.test.ts:118-164` | 已经完成实现与 raw evidence 后，精确允许 P02 Reviewer/Tester 交接记录以及其他任务的纯文档并行提交；同时仍必须拒绝 `libs/server/db/src/client.ts` 等实现路径。 | 当前 allowlist 只允许 P02 证据目录与 `开发记录.md`。最小 fixture 中增加 P02 `审核记录.md` + `测试记录.md` 后 validator exit 1；单独增加 V102 开发记录也 exit 1。真实批准 HEAD 上，`d1465cc5` 审核记录自身已使 gate 不可复验，后续 V102/V103 文档又增加误杀。独立实现代码越界对照持续正确 exit 1，证明 Red 不是要求宽放代码。 | `npx vitest run tests/db/tester-evidence-boundary.test.ts --reporter=verbose` | 同一测试中 handoff/并行纯文档 Green，而 implementation add/modify/delete/rename/revert/merge/non-first-parent 仍全部 fail closed；在真实 Reviewer/Tester 提交链上 `validate-evidence` 为 0。 |
| P1 | P02-AC-18；验证器自篡改 | `scripts/db/validate-evidence.mjs`；`scripts/db/evidence-provenance.mjs`；`tests/db/tester-evidence-boundary.test.ts:167-177` | 修改证据验证器或 provenance 检查器本身必须由一个不受该文件控制的 trust root 阻断，不能自己打印 PASS。 | 有效 evidence 链上将 `validate-evidence.mjs` 替换为只打印 `{"status":"PASS","bypassed":true}` 并提交，命令 exit 0；manifest 没有绑定 validator/provenance 的不可自修改 digest。 | 同上，用例 `fails closed when the evidence validator tampers with its own provenance checks` | 新 Red Green；独立 stage/trust-root 对 validator、schema、provenance runner 的 path+SHA-256 或等价绑定；自篡改 commit 必须使 stage 非零。 |
| P1 | P02-AC-18；raw report/manifest 协同篡改 | `scripts/db/evidence-bundle.schema.json:130-151`；`scripts/db/validate-evidence.mjs:65-88`；`tests/db/tester-evidence-boundary.test.ts:179-204` | 提交的 `validation.json` 必须证明原生 runner 实际执行了规定的 8 个不同阻断命令；不能通过重算同一可写 manifest 的 digest 伪造。 | 将 `commands[]` 改为 8 个重复的 `node -e process.exit(0)`，把 8 个 output SHA 改为全 0，并同步重算 manifest 中 `validation.json.sha256`。修改作为 evidence-only commit 后，严格 schema、report digest、runner digest 和 first-parent provenance 仍全部接受，validator exit 0。 | 同上，用例 `rejects a coordinated raw validation report and manifest digest rewrite` | 新 Red Green；校验精确命令集/顺序/唯一性，并把 raw report 出处绑定到 manifest 之外的不受证据 commit 自行改写的信任根。 |

## 新增真实 PostgreSQL 17 corner

| 用例 ID | 独立角度 | 测试文件 | 结果 | 证据 |
|---|---|---|---|---|
| P02-T-ROLE-01 | 递归检查 `p02_app` 的 role membership，不只检查直接 `rolsuper/rolbypassrls`；不得继承 owner/superuser/BYPASSRLS | `tests/integration/db/tester-corners.test.ts` | PASS | 真实 app login 角色，返回的特权成员集为空。 |
| P02-T-CTX-01 | 嵌套 A→B→A 事务、8 轮 A/B 并发、callback throw 后实际写入回滚 | `tests/integration/db/tester-corners.test.ts` | PASS | 嵌套可见序列 A/B/A，16 个并发查询零泄漏，`tester-rollback` 管理员查询为 0 行。 |
| P02-T-EXT-01 | RLS 临时关闭后，真实 Prisma extension 对 composite unique spoof、`findUnique/createMany/upsert` create/update、`update/deleteMany` 的防御纵深 | `tests/integration/db/tester-corners.test.ts` | PASS | B `main` 未被读/更新/删除；spoofed `createMany` 与 upsert create 只落 tenant A；B unique upsert fail closed `P2002`，B id update fail closed `P2025`。`finally` 恢复 ENABLE+FORCE。 |

Tester 首次编写 unique-spoof 期望时，将“B unique 不命中”误写为应当成功，真实 Prisma 以 `P2002` fail closed。Tester 只修正测试期望并加了独立 upsert-create 分支，未改业务实现。最终 `npm run test:db:integration`：4 files / 24 tests PASS，`skipped=0`，runId `4afa45a5-7b57-4050-aa77-bedb23382ca5`，container `664be518f31eb664582378ab13597d22b59518d8effb74b77127ea6783db81c5` 已删除。后续 stage 内部再次运行也 PASS，最终 report runId `b7602a71-421a-4a5d-9dc8-d6c4343a635e`，container `f6193ce0974c92ef403098409434bafadd8c844191aeca5ec876266165b31665` 已删除。

## 命令、计数与质量门

- `npx vitest run tests/db/tester-evidence-boundary.test.ts --reporter=verbose`：exit 1；1 file，6 tests，2 PASS / 4 FAIL。fixture baseline 和 implementation-code 越界对照 PASS；上述 4 个目标 Red 失败。
- `npm run test:db:integration`：exit 0；4 files / 24 tests PASS，`skipped=0`；新增 Tester 3/3 PASS。
- `npm run typecheck`：exit 0。
- `npx eslint --ext ts,tsx --report-unused-disable-directives --max-warnings 0 tests/db/tester-evidence-boundary.test.ts tests/integration/db/tester-corners.test.ts vitest.db.config.ts`：exit 0，0 warnings。
- `node scripts/db/validate-evidence.mjs`：exit 1。在真实链上首先报 `d1465cc5` Reviewer 记录，又报 V102/V103 并行文档与 Tester 测试 commit 为 `non-evidence change`。
- `npm run prisma:validate`：exit 1。active validation 内部 8 commands 和 PG17 integration 完成为 PASS，runId `14d78fa3-a2a3-46da-8a63-f3374cd8686f`；随后 committed-evidence validator 因上述 provenance 问题失败，因此 stage 整体不是 PASS。
- `npm ci` 完成后 `npm ls --depth=0` exit 0。第一次编排时 Tester 误在 `npm ci` 尚运行时启动 contracts/Prisma/preflight，产生短暂 missing-module/prisma-not-found；该批是测试编排错误，已明确排除，不计为产品失败。`npm ci` 完成后重跑 evidence 仍稳定 4 Red，PG 仍 24/24。
- 在已确认 P1 阻断且协调者要求尽快退回 Developer 后，未再消耗时间运行全量 `npm test`/`build:saas`/`build`/`compile:electron`；不将未运行项写为 PASS。

## 资源、工作树与交接

- `docker ps -a` 对本 Tester 的 PG17/Testcontainers/Ryuk container IDs 与标签无匹配；三次启动过的 PG container 均有 `removed=true`。
- 临时 Git/evidence fixture 均在 `afterEach` 删除；无残留临时数据库、role、volume 或 network。
- 提交前工作树只有本 `测试记录.md`待提交；分支同时出现的 V102/V103 纯文档 commit 属其他并行任务，Tester 未修改或回滚。
- 退回同一 P02 Developer Round 4：先为上述 4 Red 做 Green，必须保留实现代码越界拒绝；修复后重建 code/stage raw evidence 信任链，然后回到独立 Reviewer Round 4。只有新的 `APPROVED_FOR_TEST` 才能重启 Tester。

## Round 2 · 2026-07-12 04:05 CST · `/root/p02_tester` / `6c7bf0b9`

- 启动门：Reviewer Round 4 commit `6c7bf0b9f23241f28fc1e508cf8f1ba5b2ee858c` 明确给出 `APPROVED / APPROVED_FOR_TEST`；本 Tester 在该 clean shared HEAD 独立重跑，未使用 Developer/Reviewer 运行代替。
- 最终结论：`PASSED`。Round 1 的 4 个 evidence Red 已全部 Green，真实 PostgreSQL 17/RLS/Prisma extension、P03 exact-five 联动、官方全量与全部构建门均通过，未发现新 P0/P1/P2。
- 本轮未新增测试：Tester 既有 corner 和 Round 4 trusted-boundary 套件已覆盖 handoff/并行 docs、validator/bootstrap/launcher 协同篡改、raw+manifest 重写、first-parent add/modify/delete/rename/revert/merge/non-first-parent，再加同类测试只会重复。本轮只追加本记录，未改业务代码、测试代码、gate 或总体完成表。

### Trusted evidence / provenance

- `npx vitest run tests/db/tester-evidence-boundary.test.ts tests/db/validator-mutations.test.ts tests/db/evidence-bootstrap.test.ts tests/db/p03-merge-evidence.test.ts --reporter=verbose`：exit 0，4 files / 32 tests PASS。Round 1 的 handoff、并行 docs、validator 自篡改和 raw+manifest 协同篡改 4 Red 均 Green；implementation 越界与 add/modify/delete/rename/revert/merge/non-first-parent 仍 fail closed。
- `node scripts/db/evidence-trust-launcher.mjs --expected-bootstrap-sha256 3a539b57aeae01f2ad0b6fd4b6d5adab1c1cb2362cb0ca03adf7723965032c23`：exit 0，PASS；`codeEvidenceSha=21e141d47240988306fec7f45a7b23ae54a5f992`，`stageEvidenceSha=c49b66cd479f1906586806c1cdd9043a11650656`，5 份报告全部可验。
- `npm run prisma:validate`：exit 0，invocationId `77df53d7-12ad-4666-b9e4-134f0266daa1`；source `6c7bf0b9...`；outer runner SHA-256 `af982ca6ed4f5e0c0573abf976e08ab1d2c33d4e46615ff575a2fa892a1c03e6`；manifest 与 externally expected digest 均为 `658fac5b562a4a99e64a9abd947b321099901d3572b05392106f811c82e712e1`。active validation 原生 8 commands PASS，runId `6d46d2d2-0191-4289-acca-6697739a8e3f`。

### PostgreSQL 17 / RLS / factory-extension corner

- `npm run test:db:preflight`：exit 0，runId `77029f7a-389a-492a-81d8-b13cfa503fc6`；PostgreSQL `17.10 (Debian 17.10-1.pgdg12+1)`，`citext=1.6`，真实 `gen_random_uuid()`；container `4420bd36aca83a954235acc5fa4f49c1f7835bd40316a5d402d352525884b179` removed=true。
- `npm run test:db:unit`：exit 0，8 files / 74 tests PASS。
- `npm run test:db:integration`：exit 0，4 files / 24 tests PASS，`skipped=0`，runId `503453a0-a64f-4de9-8bbf-5d48e8616d8a`；container `edebbe00be85df8537bb53294e4963b4fc9536a3991457c88ee761ed8eb77742` removed=true。
- 集成套件包含 Tester 3/3 corner：非特权 role 继承链为空；嵌套 A→B→A、8 轮 A/B 并发、callback throw 回滚零泄漏；临时关闭 RLS 后真实 factory/extension 的 composite-unique spoof、`findUnique/createMany/upsert/update/deleteMany` 仍无跨租户读写。
- 正式 stage 内再次的 integration 也为 24/24 PASS，runId `f7e6cd1d-bba4-4ce0-8ef7-c5229829e2eb`，container `ecf5556368e3d69bc29e444b2400dcd6bfcde8683448004f4d70166cd1f0aeea` removed=true；preflight container `199cb6a493a19a759981e8343616d97256622c693623a1a22b17c0facb13dd15` removed=true。

### P03 exact-five 联动

- `npx vitest run tests/deploy-supply-chain-evidence-files.test.ts tests/deploy-product-source-boundaries.test.ts tests/deploy-docker-policy.test.ts tests/deploy-helm-policy.test.ts --reporter=verbose`：exit 0；4 files / 74 tests PASS（`vitest list --json` 独立核对 74 项）。P02 角色记录不会推进 P03 product source，产品输入 modify/delete/rename/no-ff merge 仍会使旧证据失效。
- `node scripts/check-supply-chain.mjs`：exit 0，PASS，57 assets / exact 5 image evidence；publish 状态诚实保持 `BLOCKED`。`node scripts/check-docker-build.mjs --static`：exit 0，web/api/worker/runtime-orchestrator/openclaw-runtime 5 images PASS。
- exact-five 原生 report：invocationId `5673b88b-2151-4f73-9f7c-b87a245a0d89`，product source `f93fdd59d98b029cb82da5cc4f5873520472bb7f`，status `PASSED`，image count=5。

### 官方全量、质量与构建

- `npm ci`：exit 0；clean install 后 postinstall Prisma Client 6.19.3 生成成功。npm audit 仍报仓库既有 22 项（12 moderate / 10 high），本 Tester 未运行破坏性 `audit fix --force`。
- `npm run contracts:check`：exit 0，invocationId `1c9e7f96-42e3-4096-b865-66f5cb88526f`。`npm run test:contract`：exit 0，15/15 PASS。`npm run prisma:generate`：exit 0。
- `npm test`：exit 0，207 files PASS，2252 tests PASS / 1 existing skipped / 0 failed；未使用 exclude、`|| true` 或忽略失败。
- `npm run typecheck`：exit 0。`npx eslint --ext ts,tsx --report-unused-disable-directives --max-warnings 0 libs/server/db/src tests/db tests/integration/db`：exit 0，0 warnings。`npm run lint:saas`：exit 0。
- `npm run build:saas`：exit 0，9 workspaces / 18 fresh artifacts。`npm run build`：exit 0，renderer/main/preload 均完成；仅有仓库既有 Vite CJS、lottie `eval`、dynamic/static import 和 chunk-size warning。`npm run compile:electron`：exit 0。

### 清理与交接

- `docker ps -a` 对本轮 PG17/Testcontainers/Ryuk container IDs/labels 无匹配；所有 preflight/integration 报告均 `removed=true`。
- 记录提交前 `git status --porcelain` 只有本 `测试记录.md`；未发现非相关 churn、generated 入库、容器/卷/网络残留。
- Tester 最终 verdict 为 `PASSED`。交回总体协调者核对依赖、四角色结论与证据；Tester 不自行修改 `00-总体完成表.md`。

## B00-6 schema remediation · Independent Tester · 2026-07-13 01:15 CST · `/root/p02_schema_tester`

- 启动门：Reviewer Round 2 commit `31737b11` 明确给出 `APPROVED_SCHEMA_REMEDIATION_FOR_TEST`；测试对象为 Developer `5a16a5a0..d7648f59`，启动时工作树 clean。
- Tester 测试提交：`3f637296`（`test(db): add independent P02 evidence corners`）。未修改实现、生产脚本、stage digest、evidence 或总体完成表。
- 结论：`FAILED_SCHEMA_REMEDIATION`。正式 trusted launcher 没有在读取前拒绝 protected regular-file symlink；两个同字节 root 外目标被完整接受为 `PASS`，另一个被 Node 跟随到 root 外后只因相对 import 缺失而偶然失败。该结论阻断 P03 B00-5A 与新的 P02 B00-6 freeze，退回同一 Developer 修复后再走独立 Reviewer。

### 阻断缺陷

| 严重级 | 用例/需求 | 期望 | 实际 | 根因范围 |
|---|---|---|---|---|
| P1 | protected direct/transitive/manifest input 被 root 外、同历史字节的 regular-file symlink 替换；不得跟随外部 import/manifest | bootstrap 在读取或 import 前以独立文件类型错误 exit 非零，三类路径均不得进入 validator | `scripts/json-without-duplicate-keys.mjs` 与 `tests/integration/db/postgres-image.json` symlink 均 exit `0` 并输出 evidence `PASS`；`scripts/db/validate-evidence.mjs` symlink 被 Node 跟随到 root 外，仅因缺 `common.mjs` 得到 `ERR_MODULE_NOT_FOUND`，不是 bootstrap 文件类型拒绝 | `scripts/db/evidence-bootstrap.mjs:48-70` 只以 `existsSync`/`readFileSync` 读取后比较内容 hash；没有 `lstat` regular-file/no-symlink 检查，也没有读取前的 root/realpath 边界 |

### 新增矩阵与真实结果

- 命令：`npx vitest run tests/db/evidence-bootstrap.test.ts tests/db/postgres-evidence-ci-state.test.ts --reporter=verbose`。
- 结果：exit `1`；2 files，94 tests；91 PASS / 3 FAIL。三个 FAIL 均为上述 symlink Red；`spawnSync` timeout 为 5 秒，三个进程均未收到 `SIGTERM`；所有临时 Git fixture 与 root 外 symlink target 由 `afterEach` 删除，没有 FIFO 或阻塞读取。
- 本次新增且 Green：RepoDigests extra / duplicate / extra-before-approved order；rollback partial-table 残留、concurrent unsafe；`testResults.skipped` 与 `todo` 分别非零；bootstrap 导出的 required + optional + bootstrap 与 auditable set 完全同源，实际集合中每一项 mismatch 在 push/PR 均为 pre-freeze、dispatch `true` 均拒绝。
- 复用既有覆盖、未制造重复测试：amd64/arm64 canonical；coherent unknown digest；report/manifest 协同改 digest；manifest duplicate-key、duplicate/missing/extra/reordered platform 与 unknown field；root/runner/Docker/inspect alias；count string/fraction/zero/negative/failed；history missing/extra/duplicate/unfinished/rolledBack 与 catalog payload/hash 协同伪造。其中落在本次两文件命令内的既有 case 为 Green；其余只经 source coverage review 确认，因 symlink Red 触发停止规则而未重跑，不记为本轮 PASS。

### 停止边界与 Developer 交接

- 按 Tester 规则，在目标缺陷暴露后停止；未运行后续 targeted 六文件、`validate-static`、`scaffold:check`、pre-freeze full、typecheck 或 changed-file ESLint，不将未运行项写成 PASS。
- 修改已受 stage fixture 管理的测试文件后，stage manifest/package/scaffold pin 会预期失配；Tester 遵守职责边界没有更新任何 digest。Developer 修复 symlink fail-closed 时须保留 `3f637296` Red、同步完整 stage source boundary，并重新执行计划 §11 的全部命令。
