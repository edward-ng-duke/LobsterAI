# 审核意见

> 初始为空白；Reviewer 独立审核并按 Round 追加。

## Round 1 · 2026-07-11 22:13 +0800 · `/root/p02_developer/p02_reviewer` / `192537c8`

- Reviewer Agent：`/root/p02_developer/p02_reviewer`（全新独立 Reviewer；未参与 Planner/Developer/其他审查或测试角色）。
- 被审稳定基线：`192537c86e530ff085d434262cd9fa100546801f`；P01 接受链独立核对为 accepted `a6066c3138e4d6c0f36462b9ad3fb8e0877d3a28`、Tester 报告 `fc9f619fd46afe4622512c5d4168e220816f246a`、contract `1.0.0` / source hash `897bd39077d409e52a88e9efe89383380564812f494efb88fe09e2af6a1ed2c8`。
- AC 结论：AC01～AC11、AC13～AC17 的当前实现与独立运行证据通过；AC12、AC18 存在 P1，18 AC 未全部满足。
- 结论：`CHANGES_REQUESTED`。禁止启动 Tester。

| 严重级 | 用例/需求 | 文件:行 | 期望 | 实际 | 复现命令 | 需要的完成证据 |
|---|---|---|---|---|---|---|
| P1 | P02-AC-12 应用层纵深、全部 scope 操作 | `libs/server/db/src/client.ts:43-48,105-110`；`libs/server/db/src/tenant-scope.ts:50-96`；`tests/db/tenant-scope.test.ts:11-61`；`tests/integration/db/rls.test.ts:179-185` | 安全 factory 的真实应用路径必须实际使用 tenant-scoping extension；在临时库关闭 RLS 后，extension 仍须对 `create/createMany/upsert/find/count/aggregate/groupBy/update/delete` 的过滤和写入 tenant 都 fail closed，尤其不能让 update/upsert/updateMany 把行迁到伪造 tenant。 | `createDatabaseFactory` 直接持有裸 `PrismaClient`，全仓没有任何调用 `createTenantScopeExtension`/`extendTenantClient`；集成仅用手写 facade 的一次 `find` 证明 DTO 映射。删掉整个 extension 后 `validate-static` 仍 PASS、相关 30 个单测仍 PASS。纯 helper 对 `update`、`updateMany`、`upsert.update` 还原样保留伪造的 `data.tenantId=B`，因此也没有满足“关闭 RLS 后应用层仍隔离”的纵深断言。 | `rg -n "createTenantScopeExtension|extendTenantClient" apps libs tests`；在临时副本删除 `tenant-scope.ts:77-96` 后运行 `node scripts/db/validate-static.mjs`（exit 0）及 Node 24 `vitest run tests/db/tenant-scope.test.ts tests/db/p02-red.test.ts`（30/30）；当前树运行 `node --experimental-strip-types --input-type=module -e "import {scopeTenantOperation} from './libs/server/db/src/tenant-scope.ts'; console.log(scopeTenantOperation('Agent','update',{where:{id:'x'},data:{tenantId:'20000000-0000-4000-8000-000000000002'}},'10000000-0000-4000-8000-000000000001'))"`，输出的 `data.tenantId` 仍为 B。 | factory/facade 接入真实 extension；所有列出的读写/聚合分支覆盖 tenant filter 与 tenant write-data；新增真实 PostgreSQL 临时库测试，admin 暂时关闭 RLS 后只靠 extension 验证 A 不能读、创建、迁移、更新或删除 B；新增“整段 extension 删除”mutation，必须使 unit/static/stage gate 非零。 |
| P1 | P02-AC-18 同 SHA、机器报告可信证据 | `docs/db/20260711_P02_Prisma与RLS脚手架证据/{contracts-preflight,preflight,integration,validation}.json:1-21`；`docs/db/20260711_P02_Prisma与RLS脚手架证据/README.md:57-67`；`scripts/db/validate.mjs:12-30,70-79` | 证据包必须保存由当前 runner 原子产生、可按 schema 复验且与交付代码 SHA 对齐的原始机器报告；README 的“稳定 HEAD 验证”不能指向另一 SHA 或人工改写字段。 | 四份提交快照均写 `sourceSha=dc98a77d...`，而交审 stable HEAD 是 `192537c8...`。更关键的是提交的 `validation.json` 使用 runner 从未输出的 `commandExitCodes` 对象；真实 `validate.mjs` 自引入起一直输出 `commands[]`（含 command/exitCode/outputSha256）。本 Reviewer 在 stable HEAD 现跑得到的 `.reports/db/validation.json` 确为 `commands[]`，证明提交文件不是所称原生机器报告快照。动态门本身通过不能修复已提交证据的来源/结构失真。 | `jq -r .sourceSha docs/db/20260711_P02_Prisma与RLS脚手架证据/*.json` 与 `git rev-parse HEAD` 对比；`jq 'has("commands"),has("commandExitCodes")' docs/db/20260711_P02_Prisma与RLS脚手架证据/validation.json .reports/db/validation.json`；`git log -p -- scripts/db/validate.mjs`。 | 用未修改的 runner 在修复后的稳定代码 SHA 重跑全部门；提交原样机器报告（保留 `commands[]`、runId、sourceSha、exitCode、output hash、container/cleanup）；增加报告 JSON schema/来源校验和 stale-SHA mutation，使人工改字段、旧 SHA 或非 runner schema 均阻断；README 明确区分 code-evidence SHA 与后续 evidence-only commit，且不得再把摘要冒充原始报告。 |

### 独立动态证据与 mutation

- `npm ci` exit 0；Red commit `be95c975` 在隔离 worktree 上 `tests/db/p02-red.test.ts` 为 12/12 目标行为缺失失败，Red 有效。
- `contracts:check`、`test:contract`（15/15）、Prisma 6.19.3 validate/generate、DB unit（42/42）、typecheck、changed-file ESLint、`lint:saas` 均 exit 0。
- 真实 PG17 preflight PASS：runId `825eb4a5-aac9-4659-8da6-87368f5bcf21`，`postgres:17.10-bookworm@sha256:17b6c...40a`，Linux arm64，server 17.10，citext available，真实 `gen_random_uuid()`，container `ef5c...8327` 已删除。
- 真实 integration PASS：runId `08e31c97-ebba-4baf-bba2-c661642ce969`，16/16、skipped=0；`p02_app` 直接登录、owner=`p02_admin`、`rolsuper=false`、`rolbypassrls=false`；ENABLE+FORCE、USING=WITH CHECK、missing context、跨租户 CRUD、双 `main`、DTO logical ID、commit/rollback/throw/同连接、12 轮 A/B 并发和 false-scope 泄漏对照均通过；container `987b...2b77` 已删除。
- `prisma:validate` stage PASS：invocation `6b5320fc-b211-45f3-9ceb-8bce64b3fe1f`，source SHA `192537c8...`。伪造缺 Docker 返回 `BLOCKED/2`；临时 manifest 改 `linux/amd64` 返回 `BLOCKED/2`，均无 skip/PASS。
- 六项独立临时副本 mutation 均由 `node scripts/db/validate-static.mjs --root <tmp>` 捕获并 exit 1：删除 FORCE、WITH CHECK 放宽为 true、app role 改 SUPERUSER、`set_config(..., true)` 改 false、tenant 双键改全局 logical unique、从 RLS manifest 删除 agents。临时副本已删除，共享树未污染。
- 总回归：`npm test` 201 files / 2153 passed / 1 existing skipped；`build:saas` 9 workspace / 18 fresh artifacts；`npm run build`、`npm run compile:electron` 均 exit 0。仅有既有 install-script/Vite/lottie warning。
- 结束检查：共享 worktree 除本审核文件外无改动；本 Reviewer 创建的 PG17/Testcontainers/Ryuk 容器无残留。

### 交接 Tester

`CHANGES_REQUESTED`，不得启动 Tester。Developer 修复上述两个 P1、补有效 Red/Green 与同源机器报告后，必须回到独立 Reviewer 复审 AC01～AC18、动态 RLS、extension-disabled-RLS 场景及 mutation；只有新的 `APPROVED_FOR_TEST` 才能交 Tester。

## Round 2 · 2026-07-11 22:49 +0800 · `/root/p02_developer/p02_reviewer` / `b98cb47d`

- 复审链：Round 1 `ff92dcf5bceb5cbf52ac502f3b9071bf90637578` → implementation `ec2cc92379ac754c9625614be7cd0f2c49e74435` → raw evidence `c80b9c2aff4ae9599942450f3628f315a1b8e4fe` → stage/docs/final `b98cb47d9292af767a7692cdda4425247c925e09`。
- Round 1 AC12 P1：`FIXED`。factory 真接 extension、所有写分支覆盖 tenantId、RLS-disabled 真实 PG17 全操作均独立通过。
- Round 1 AC18 的 raw shape、严格 schema、report/runner hash、stale/unknown mutation 与当前 HEAD 文件白名单：已通过；但 first-parent evidence-only 校验仍可接受“插入代码再回滚”的历史，AC18 尚未完整满足。
- 结论：`CHANGES_REQUESTED`。仍禁止启动 Tester。

| 严重级 | 用例/需求 | 文件:行 | 期望 | 实际 | 复现命令 | 需要的完成证据 |
|---|---|---|---|---|---|---|
| P1 | P02-AC-18 first-parent evidence-only provenance | `scripts/db/evidence-provenance.mjs:13-30`；`tests/db/validator-mutations.test.ts:120-151` | `codeEvidenceSha` 到 HEAD 的 first-parent 上，每一个 descendant commit 都必须是 evidence-only；插入任意代码 commit 必须拒绝，即使后续 commit 把代码回滚、使最终树看起来与实现 SHA 无代码差异。 | validator 只用 `git diff --name-only sourceSha..HEAD` 检查最终聚合差异，没有逐个 first-parent commit 检查。现有 mutation 只覆盖“插入代码且代码仍留在 HEAD”。独立临时 Git 仓库构造 `implementation → evidence → feat: inserted src/arbitrary.ts → revert inserted code` 后，first-parent 明确含代码 commit，但最终 aggregate diff 只剩 `docs/db/evidence/report.json`，`validateEvidenceOnlyDescendant(...)` 返回空数组并错误接受。 | 临时仓库：`git log --oneline --first-parent` 显示 `Revert → feat: inserted code → docs: evidence → feat: implementation`；`git diff --name-only <implementation>..HEAD` 仅输出 evidence；随后用 Node import `validateEvidenceOnlyDescendant` 并传 `allowedEvidencePath = file => file.startsWith('docs/db/evidence/')`，实际输出 `[]`。 | 沿 first-parent 从 `sourceSha` 的子提交到 HEAD 逐 commit 审计 changed paths（需覆盖 add/modify/delete/rename），任一非 evidence path 即失败，不允许后续 revert 洗掉历史；新增临时 Git mutation `implementation → evidence → code → revert code`，必须稳定返回非空错误并使 evidence/stage gate 非零；修复后重新生成与校验 runner hash、stage evidence。 |

### Round 2 独立通过证据

- AC12 静态/单元：`client.ts:117-120` 使用 `extendTenantClient`；`tenant-scope.ts:69-79` 对 `upsert.update`、`update`、`updateMany` 强制当前 tenantId；DB unit 5 files / 55 tests PASS。
- AC12 真实动态：`npm run test:db:integration` 3 files / 21 tests PASS、skipped=0，runId `96454b67-7084-4778-a013-a1aa7d2b196e`。在隔离容器临时 `DISABLE ROW LEVEL SECURITY` 后，factory 及 extended client 的 find/list/count/aggregate/groupBy/create/createMany/upsert/update/updateMany/delete/deleteMany 均未泄漏 B；container `053c...d9d` removed=true，结束 catalog 恢复 ENABLE+FORCE。
- AC18 当前正向链：`node scripts/db/validate-evidence.mjs` PASS，codeEvidenceSha=`ec2cc923...`、stageEvidenceSha=`c80b9c2a...`；`git diff --name-only ec2cc923..HEAD` 当前确实只有证据目录与 P02 开发记录；raw validation 保留原生 `commands[]`，manifest report/runner SHA-256、unknown-field 与 stale-source mutation 通过。
- `npm run prisma:validate` PASS，invocation `911da654-c979-4f2b-b6b0-0cd742db1604`，source SHA `b98cb47d...`；typecheck、Round 2 changed-file ESLint 均 exit 0。
- 本轮临时 Git 仓库与 PG17/Testcontainers 资源已清理；提交前共享树仅本审核文件变更。

### Round 2 交接 Tester

`CHANGES_REQUESTED`，不得启动 Tester。Developer 需先修复 first-parent 逐 commit provenance 及 add→revert mutation；修复后由同一 Reviewer 进行 Round 3，确认 AC18 完整闭环后方可写 `APPROVED_FOR_TEST`。

## Round 4 · 2026-07-12 03:54 +0800 · `/root/p02_developer/p02_reviewer` / `1536557b`

- 最终集成 HEAD：`1536557b0b57a70b32d7be58fd901d73266c647d`。
- 最终证据链：codeEvidenceSha `21e141d47240988306fec7f45a7b23ae54a5f992` → raw/stageEvidenceSha `c49b66cd479f1906586806c1cdd9043a11650656` → stage/docs `668dd0eff6216c18a01d11085e0c8c93c1eaee1b` → final regression `1536557b...`。
- P03 联动基线：product source `f93fdd59d98b029cb82da5cc4f5873520472bb7f`；exact-five invocation `5673b88b-2151-4f73-9f7c-b87a245a0d89`。
- 结论：`APPROVED` / `APPROVED_FOR_TEST`。最终共享集成基线未发现 P0/P1 阻断；P02 AC01～AC18、P03 exact-five 联动与官方全量回归均独立通过，可以启动独立 Tester。

### Trusted boundary、manifest 与 provenance

- manifest 的四份 raw report 全部绑定 `21e141d4...`，stage report 绑定 `c49b66cd...`；report content SHA-256、runner SHA-256 与 Git blob anchor 一致。
- `21e141d4..HEAD` 仅包含 P02 证据目录与任务角色记录；没有 implementation、test、stage runner 或 P03 产品输入漂移。
- `npx vitest run tests/db/tester-evidence-boundary.test.ts tests/db/validator-mutations.test.ts tests/db/evidence-bootstrap.test.ts tests/db/p03-merge-evidence.test.ts --reporter=verbose`：4 files / 32 tests PASS。协同篡改、裸 validator、launcher/bootstrap digest、raw+manifest rewrite、add/modify/delete/rename/revert/merge/non-first-parent 全部 fail closed。
- 独立 trusted launcher 运行 PASS，返回 codeEvidenceSha=`21e141d4...`、stageEvidenceSha=`c49b66cd...`。正式 stage invocation `374c1487-398f-4daa-abad-1d8a3fa15167` PASS；runner `af982ca6...`，manifest 与 externally expected digest 均为 `658fac5b...`。

### PostgreSQL 17 / RLS / tenant extension

- `npm run test:db:integration`：4 files / 24 tests PASS、skipped=0；Reviewer runId `22091ab0-433e-41fe-8d19-7ca0236ce6e2`。
- 实际镜像为 `postgres:17.10-bookworm@sha256:17b6c...40a`，server `17.10 (Debian 17.10-1.pgdg12+1)`，container `45fc...2339` removed=true。
- 非特权 direct-login role、ENABLE+FORCE、USING=WITH CHECK、missing context、跨租户 CRUD、事务/池泄漏、嵌套并发，以及临时关闭 RLS 后真实 factory/extension 全操作隔离均通过。

### P03 exact-five 联动

- P03 Docker/supply-chain/product-source/evidence 四文件独立运行：4 files / 78 tests PASS；其中 product-source boundary 19/19 PASS。
- `.reports/supply-chain/20260712_PR3部署供应链证据/docker-build-check.json` 原生记录 invocation `5673b88b...`、source `f93fdd59...`、status `PASSED`，镜像集合精确为 web/api/worker/runtime-orchestrator/openclaw-runtime 五项。
- `node scripts/check-supply-chain.mjs`：PASS，57 assets / exact 5 image evidence，publish 状态诚实保持 `BLOCKED`；`node scripts/check-docker-build.mjs --static`：5 images PASS。P02 docs/review 提交不会推进 P03 product source。

### 官方全量与清理

- `npm test`：exit 0；207 files PASS，2252 passed / 1 existing skipped / 0 failed。未使用 exclude、`|| true` 或忽略失败。
- 本轮 `npm run prisma:validate`、trusted launcher、P02/P03 targeted 与官方 full 均为独立现跑；证据 README 中先前 P03 shared-evidence 失败明确标注为“历史，已解除”，最终 rebaseline 表与当前结果一致。
- 结束时共享 worktree 除本审核文件外无改动；Reviewer 启动的 PostgreSQL 17/Testcontainers/Ryuk 资源无残留。

### Round 4 交接 Tester

Reviewer 最终结论为 `APPROVED_FOR_TEST`。独立 Tester 可以启动；仍须自行复跑 trusted boundary、PG17/RLS/extension、P03 exact-five 与官方 full，不能用 Developer/Reviewer 本轮结果替代 Tester 证据。

## B00 Round 5 · 2026-07-12 18:55 +0800 · `/root/p02_b00_4_reviewer` / `62d546e6`

- 角色与范围：全新独立 B00-4 Reviewer；未参与本轮 Planner/Developer/Tester/协调。完整审阅 Planner `550b804f`、Developer `de47cfa7..62d546e6`、可执行 source `6c8db08f`、§9/§10、全部 diff、policy/consumer/migration/artifact/stage/CI/P03 交接。
- 结论：`CHANGES_REQUESTED_B00_4`。当前不允许交 Tester，也不得写 `LOCAL_QUALITY_APPROVED_WAITING_EXTERNAL_ARM64_B00_4`。本机 amd64 快乐路径为 Green，但发现 3 个 P1 与 1 个 P2；同 source 原生 arm64 run/artifact 仍缺失，所以即使修复本轮缺陷也不能将 B00-4/P02 写为 PASS。
- arm64 digest 口径：manifest 中 `sha256:17b6c...7e40a` 可追溯到旧 P02 原生 macOS/Docker arm64 Tester 记录，保留它符合 Planner §9.2；该历史事实不是本轮 `db-platform-arm64` Green，Developer 记录也未伪造 run URL/id/artifact digest。

| 严重级 | 要求 | 文件:行 | 实际/复现 | 要求修复 |
|---|---|---|---|---|
| P1 | `skipped=0` 必须从真实 Vitest 结果 fail closed，不得自报 | `scripts/db/run-integration.mjs:24-29,196-211,212-231`；`tests/integration/db/migration-lifecycle.test.ts:42-48` | `report.skipped` 初始硬编码为 0，完成后只用 `/Tests\s+(\d+) passed/` 取 passed 数。独立临时 worktree 将并发 migration 用例改为 `test.skip`，Vitest 明确输出 `26 passed | 1 skipped (27)`，runner 仍 exit 0 并生成 `status=PASS, skipped=0, checksPassed=26, checksTotal=26`；artifact validator 会接受这种伪 Green。 | 不得解析人类 reporter 文本自报计数。使用 Vitest JSON/结构化 reporter 或可验证的机器结果，精确对账 passed/failed/skipped/todo/total；任何 skip/todo/parse 失败均 `FAILED/1`。先保留上述 skip mutation Red，再转 Green，artifact validator 也要拒绝不一致计数。 |
| P1 | B00-4 必须有不自锁的 pre-freeze → B00-5A → freeze CI 状态机 | `.github/workflows/saas-scaffold.yml:103-128,130-179`；`tests/db/postgres-platform-workflow.test.ts:31-79`；P02 计划 §10.3/§10.8 | 拆出的 `db-platform-arm64` 自身不依赖 evidence，但同一 workflow 的 `db-evidence-arm64` 无条件立即运行 `prisma:validate`；更关键是 `scaffold` 无条件运行官方 `npm test`。当前冻结前 HEAD 实测 `p03-merge-evidence`/`reviewer-round2-red` 因旧 evidence 与 `package.json` 漂移稳定 2 FAIL，因此 P03 B00-5A 要求的 SaaS required jobs Green 无法在 P02 B00-6 前获得，而 B00-6 又被规定必须等 B00-5A，形成真实循环。当前 workflow test 只检查 job/command 存在与 `needs`，没有检查 pre-freeze 可达性。 | Planner 与 Developer 先固定唯一非循环状态机：冻结前 required checks 必须能在不消费旧 trusted evidence 的前提下整体 Green 并交付同 source artifact，冻结后才激活 evidence required check；不得用忽略官方测试、`continue-on-error` 或仅依赖失败 workflow 的 artifact 规避。增加 pre-freeze 与 post-freeze 两态 workflow mutation test，并与 P03 §9.4/§9.5 required-check 口径同步。 |
| P1 | 空库、repeat、已有 schema 必须是三个可区分的 migration 事实 | `scripts/db/run-integration.mjs:107-115`；`tests/integration/db/migration-lifecycle.test.ts:24-31` | 三次调用对同一 database、同一 schema、同一 migration 连续执行完全相同的 `prisma migrate deploy`，随后无条件写入 `first:true, repeat:true, existingSchema:true`。第三次与第二次没有任何前置差异，没有构造“已有 schema”路径；新 test 只匹配这三个布尔值，无法阻止 runner 伪造。 | 先明确“已有 schema”的可执行 fixture（例如上一脚手架/catalog 状态的独立 database），记录前后 catalog/history/checksum；与 empty/repeat 分库或分阶段对账。新增删除实际 existing-schema 准备步骤后必须 Red 的 mutation，不再传递无条件 boolean。 |
| P2 | 跨 job artifact 读取必须对 schema、regular file、canonical containment 和 size fail closed | `scripts/db/validate-platform-artifact.mjs:16-35,39-61,63-129`；`tests/db/postgres-platform-artifact.test.ts:32-142` | validator 使用无上限 `readFileSync`并跟随 symlink，只抽查部分字段，不拒绝 unknown root/nested field，不校验 cleanup `attempted`。独立 fixture 中：给 `integration.json` 增 unknown field、将两个 report 改成指向工作树 `.reports` 的 symlink、或给 JSON 追加空白使其超过 5 MB，三组均 exit 0/PASS。duplicate-key parser 本身已 fail closed，但不能覆盖这些边界。 | `lstat` 拒绝 symlink/非 regular file，`realpath` 校验两个文件仍在 canonical artifact root 下，读取前设定明确小型 byte limit，为 preflight/integration 定义严格 root/nested schema 与完整 required/unknown-field 拒绝；新增 symlink、FIFO/非 regular、oversize、unknown/duplicate/missing field 和 cleanup attempted/removed 不一致 mutations。 |

### 独立 Red → Green 与快乐路径证据

- 关键 Red 交付独立重放均为非零：`de47cfa7` policy 1 file collection FAIL；`f2a002e7` workflow 4/4 FAIL；`4baa4433` migration lifecycle 3/3 FAIL；`d5863371` coordinated fixture mutation 1 FAIL / 23 PASS；`edec4dd3` structured artifact 1 FAIL / 4 PASS。当前 Green targeted 为 7 files / 74 tests PASS。
- Node `v24.18.0`，Docker `linux/x86_64` / server `28.3.3`。`npm run test:db:preflight` PASS；runId `9c6926ab-6b11-4e73-97da-fe1a37935baa`，PG `17.10 (Debian 17.10-1.pgdg12+1)`，locale `en_US.utf8`，timezone `Etc/UTC`，SSL `off`，container removed。
- `npm run test:db:integration` PASS；5 files / 27 tests，runId `f8e6fd3d-8e44-4c0f-9578-3c1b2b8c4d36`，rollback/repair 与 concurrent `[0,0]` 当前快乐路径通过，container removed。`validate-platform-artifact --platform linux/amd64` 对该报告 PASS。
- `node scripts/db/validate-static.mjs`、全部本轮 `.mjs` `node --check`、workflow YAML parse、10 个 changed TS 的 CI 等价 ESLint、`git diff --check 550b804f..HEAD` 均 exit 0。
- 未重复昂贵 full `npm test`；已精确重放已知两项，结果 2 files / 2 FAIL / 7 PASS，均为 `P02 evidence bootstrap: trusted file mismatch package.json`，与 Developer 记录一致。
- 临时 worktree/artifact fixture 已删除；当前工作树除本审核记录外无实现/测试改动。

### 交接

`CHANGES_REQUESTED_B00_4`。退同一 P02 Developer：先以 test-only Red 修复上述 P1/P2，再重跑 amd64 policy/static/preflight/integration/artifact 与 CI 两态可达性；任何 policy/manifest/consumer/test/workflow/package/source-boundary 改动均使当前 candidate 作废。修复后必须回到新的独立 Reviewer；只有本地缺陷关闭且取得同 source 原生 `db-platform-arm64` Green/artifact 后，才可以使用限定结论 `LOCAL_QUALITY_APPROVED_WAITING_EXTERNAL_ARM64_B00_4` 或进入下一状态；本轮不授权 Tester。

## B00 Round 6 · 2026-07-12 19:25 +0800 · `/root/p02_b00_4_rereviewer` / `8ce73f4a`

- 角色与范围：新的独立 B00-4 Reviewer；未参与本轮 Developer、Tester 或协调。完整复核 Round 5 commit `57440260` 的四项 finding、返工 `a0d6ab5d..4c537820`、记录 `8ce73f4a`、P02 计划 §9/§10、workflow 与 GitHub `needs/result/inputs` 语义。
- 可执行 source：`4c537820d8355887cc708d925d1728d4307f24c4`；当前 HEAD 后只有 Developer 记录。结论：`CHANGES_REQUESTED_B00_4`。上一轮 P2 的 regular/non-symlink/FIFO/1 MiB/realpath/unknown/missing/duplicate 边界已闭环，skip/todo 快乐拒绝和独立 existing database 也有实质进展；但失败 artifact、冻结后 CI 可达性、existing-schema history/checksum 仍有 3 个 P1。
- 外部门保持诚实：没有同 source 原生 arm64 workflow run URL/id/artifact digest，未执行 B00-6，不得标记 B00-4/P02 PASS，也不得启动独立 Tester。

| 严重级 | 未闭环要求 | 文件:行 | 独立复现与影响 | 要求修复 |
|---|---|---|---|---|
| P1 | Vitest 失败也必须把机器计数写入失败 artifact | `scripts/db/run-integration.mjs:199-216,242-253`；`scripts/db/vitest-json-evidence.mjs:23-43`；`tests/db/vitest-json-evidence.test.ts:48-67` | `loadVitestJsonEvidence` 能给非全绿报告附加 `testResults`，但 runner 在 `tests.status !== 0` 时于第 215 行先抛错，永远不会到第 216 行解析 JSON。Reviewer 在隔离 `4c537820` 增加一个真实失败 integration test 后运行 `npm run test:db:integration`：Vitest 已写 JSON、runner exit `1`、cleanup removed=true，但最终 `integration.json` 为 `status=FAILED, skipped=null, checks={}`，没有 passed/failed/skipped/todo/total。提交 `4c537820` 的“preserve rejected counts”只测 helper，未覆盖 runner 的非零退出路径。失败 artifact 无法证明失败类别与完整计数，原 finding 未闭环。 | 无论 Vitest exit 0/1，只要 outputFile 存在，都先严格解析机器报告并保存计数，再按 process status 与计数共同判定；缺失/坏 JSON 仍 FAILED 且明确原因。先加 runner 级 Red（真实 failure，断言失败 artifact 含 `failed=1` 等）再 Green；保留 skip/todo Red，并证明临时 outputFile 在成功、skip、failure、parse-failure 后都清理。 |
| P1 | pre-freeze → B00-6 → post-freeze 的 required workflow 必须双向可达 | `.github/workflows/saas-scaffold.yml:124-126,167-209`；`.github/workflows/ci.yml:153-171`；`tests/db/p03-merge-evidence.test.ts:42-50`；`tests/db/reviewer-round2-red.test.ts:134-151` | 聚合脚本自身 truth table 正确：push/PR 空 input 或 dispatch false + platform success/evidence skipped 为 `WAITING_POST_FREEZE/0`；dispatch true + 两 job success 为 `POST_FREEZE_PASSED/0`；platform failure/cancelled、非法 input、ready/result 不一致均 exit 1。可是两个 workflow 的官方 `npm test` 永久写死 `P02_EVIDENCE_PHASE=pre-freeze`，而两个测试在该 phase 明确要求 trusted validator **失败**。Reviewer 在隔离树将 trusted validator 模拟为冻结后成功，pre-freeze targeted 立即变为 `1 failed / 8 passed`。因此 B00-6 真正生效后，`workflow_dispatch(p02_evidence_ready=true)` 即使 evidence job Green，`scaffold` 仍会因 pre-freeze 官方测试失败；主 CI push 也反向失败。当前测试只覆盖“旧 evidence 时 pre-freeze 可绿”，没有覆盖“新 evidence 时整套 required jobs 可绿”，状态机仍自锁。 | phase 必须从可核验的 workflow 状态/source 派生，至少让显式 post-freeze dispatch 的 scaffold 官方测试运行 post-freeze；同时定义 B00-6 后 push/PR 主 CI 的唯一口径，不得永久伪装 pre-freeze。新增两态端到端 mutation：旧 evidence + pre-freeze required 全绿；有效 evidence + post-freeze required 全绿；旧 evidence 冒充 post-freeze、有效 evidence 冒充 pre-freeze、false input 留在冻结后 source 都必须非零。不要用 `continue-on-error`、排除测试或只看聚合器掩盖 scaffold/main CI。 |
| P1 | existing-schema artifact 必须绑定前后 catalog、migration history 与 checksum | `scripts/db/postgres-migration-lifecycle.mjs:82-156`；`scripts/db/validate-platform-artifact.mjs:137-149,241-267`；`tests/db/postgres-platform-artifact.test.ts:80-113` | 独立 `p02_existing_*` DB、baseline→current 两阶段 deploy、marker 保留和两张业务表已真实通过；Reviewer 原生 amd64 run 为 27/27、cleanup removed。可是报告只保存表名数组及其 SHA-256和 `completedMigrations=2`，没有 `_prisma_migrations` 的 migration name/checksum/history 明细。更直接地，artifact happy fixture 给正确 before/after arrays 配置 `beforeCatalogSha256='1'×64`、`afterCatalogSha256='2'×64`，与实际可重算的 `8e916f...`/`f74e7b...` 不同，现有 validator 仍 PASS，因为只验证 64 位 hex。跨 job artifact 可伪造 checksum，也无法证明两条 finished history 正是批准 baseline/current migration。 | 报告记录有序 migration history（至少 migration_name、checksum、finished/rolled-back 状态）及批准 SQL checksum；validator 从 before/after catalog 重算 hash并精确比对，从 history 重算/对照批准 migration 名与 checksum，拒绝任意 64hex、同 count 异 history、额外/缺失/未完成/rolled-back row。增加 fake hash、换 migration name/checksum、同 count 不同 history mutations。 |

### 独立通过与清理证据

- Reviewer targeted：8 files / 86 tests PASS；覆盖 policy、artifact 文件系统边界、CI 聚合器、stage fixture、validator mutations 和 scaffold stage。
- 原生 linux/amd64：`npm run test:db:preflight` PASS，runId `6972d58d-48bc-4c92-b002-e6ca06a70b3f`；`npm run test:db:integration` 为 27/27、failed/skipped/todo=0，runId `cadad0c4-9db7-4b3b-82fc-88fc60fe376f`；strict artifact validator PASS；两容器 cleanup attempted=true/removed=true。
- 官方 pre-freeze：`P02_EVIDENCE_PHASE=pre-freeze npm test` exit 0，220 files / 2418 passed / 6 existing skipped。默认 phase targeted 仍如实 exit 1：2 files 中 2 failed / 7 passed，均为当前 B00-6 前 `trusted file mismatch package.json`。
- `node --check`、两份 workflow YAML parse、`validate-static.mjs`、`scaffold:check`、`docker:static`、changed-test ESLint、`git diff --check 57440260..HEAD` 均 exit 0。
- 成功、失败 mutation 后 `/tmp/lobsterai-p02-vitest-report-*`、existing/rollback/concurrent fixture 与本轮 PostgreSQL/Testcontainers/Ryuk 均无残留；隔离 worktree 已删除。现存两个 PostgreSQL 16/15 容器名称分别为 authentik/onyx，非本轮资源。

### Round 6 交接

`CHANGES_REQUESTED_B00_4`。退同一 P02 Developer 先以 runner/CI/history-checksum 三组 test-only Red 返工；修改会触及 executable source、workflow、tests 与 stage fixtures，因此当前 amd64/arm64/P03 live/B00-6 evidence 均须按 §10.4/§10.8 作废重跑。修复后交新的独立 Reviewer；本轮不授权 Tester。

## B00 Round 7 · 2026-07-12 20:00 +0800 · `/root/p02_b00_4_round8_reviewer` / `0c36da35`

- 角色与范围：新的独立 P02 B00-4 Reviewer；只复核 Round 6 `bb6149a1` 的三项 P1 返工、`bb6149a1..0c36da35` 全部 Red/Green 与 executable source `aa84353218b1fd37050b31450855f985a435f74c`，未参与 Developer、Tester 或协调者工作。
- 结论：`CHANGES_REQUESTED_B00_4`。失败 artifact 与 existing-schema history/checksum 已本地闭环，但 CI phase 状态机仍有两个 P1：真实 provenance drift 输出流分类错误，且主 CI 的 PR checkout 拓扑不满足 trusted evidence 的 first-parent 契约。不得写 `LOCAL_QUALITY_APPROVED_WAITING_EXTERNAL_ARM64_B00_4`，不得启动 Tester。
- 外部门仍保持诚实：没有 `aa843532` 同 source 原生 arm64 run URL/id/artifact digest，未执行 B00-5A/B00-6；本轮本地发现独立于 arm64 的代码缺陷，因此即使外部 artifact 到位也不能接受 B00-4/P02。

| 严重级 | 未闭环要求 | 文件:行 | 独立复现与影响 | 要求修复 |
|---|---|---|---|---|
| P1 | push/PR 的真实 provenance source drift 必须派生 `pre-freeze` | `scripts/db/resolve-evidence-phase.mjs:37-52`；`scripts/db/validate-evidence.mjs:226-228`；`tests/db/postgres-evidence-ci-state.test.ts:83-102` | resolver 只对 `stdout` 做 `JSON.parse`，但真实 validator 对 `FAILED` 使用 `console.error`，JSON 在 `stderr`。Reviewer 从历史有效冻结 HEAD `6c7bf0b9` 线性追加普通 `src/p02-phase-drift-probe.ts` 提交：trusted launcher exit 1，stderr 精确包含 code/stage 两条 `non-evidence change after source SHA`；把真实 `{status,stdout,stderr}` 交给当前 classifier 后不是 `false/pre-freeze`，而是抛出 `failed outside the auditable source-drift states`。现有单测把人工 JSON 放在 stdout，未覆盖真实 CLI，导致冻结后首个普通产品提交会让 push/PR required test job 失败，无法回到新一轮 pre-freeze。 | 先加真实 launcher 子进程 Red，使用 validator 实际 stdout/stderr；只接受精确、完整且唯一的 provenance drift JSON 为 stale，仍拒绝 digest/schema/bootstrap/manifest/混合错误。修复 classifier 后证明线性 push 的 valid→post、source drift→pre；不得合并 stdout/stderr 后做宽松子串匹配。 |
| P1 | PR valid/stale phase 必须在被验证的 PR head 上计算，不能在 GitHub 合成 merge commit 上破坏 first-parent | `.github/workflows/ci.yml:157-159`；`.github/workflows/saas-scaffold.yml:99-102`；`scripts/db/evidence-provenance.mjs:40-48`；`tests/deploy-ci-evidence.test.ts`；`tests/db/postgres-platform-workflow.test.ts:68-92` | SaaS checkout 已显式使用 `github.event.pull_request.head.sha || github.sha`，主 CI test job 只有 `fetch-depth: 0`，PR 默认 checkout 合成 merge SHA。Reviewer 以有效冻结 HEAD 构造标准双父 merge（第一父为前进后的 base，第二父为 PR head）：PR head 上 trusted launcher PASS；合成 merge 上 source 不在 first-parent，launcher exit 1，返回 code/stage 两条 `evidence SHA is not on the first-parent history of HEAD`，当前 resolver 随即失败。现有 workflow 测试只检查 full history/phase step 存在，不检查 checkout `ref` 或 PR 双父拓扑，所以 PR valid→post 不成立。 | 主 CI test job 与 SaaS 使用同一 source checkout 契约，显式 checkout PR head（push 仍用 `github.sha`），并把被解析 source SHA 传给/对账 resolver；新增临时 Git 双父 PR fixture，证明 PR head valid→post、head drift→pre，且 base merge commit 不能改变 phase 或被冒充为 evidence source。保持 full history、`npm ci`、无 `continue-on-error`。 |

### Round 7 已闭环项与独立证据

- P1-A failure artifact：`node --test tests/db/integration-failure-artifact.test.mjs` 2/2 PASS。真实第 28 项 Vitest failure 令 runner exit 1，最终 artifact 保留 `{passed:27,failed:1,skipped:0,todo:0,total:28}`、`checksPassed=27/checksTotal=28`、provider/migration/RLS/role/seed base facts、明确 error 与 cleanup `attempted=true/removed=true`；malformed JSON 同样 exit 1，保留 base facts、64hex output hash、明确 parse error、`testResults=null` 和 removed cleanup；两种隔离运行都没有 Vitest temp report 残留。
- structured FAIL gate：artifact targeted 证明 schema 可识别字段完整的 `FAILED` integration report 并给出 `structurally valid FAILED integration artifact cannot pass the required gate`；未知/legacy/root/nested/duplicate/missing/symlink/FIFO/oversize/cleanup/count 不一致仍全部拒绝。
- P1-C existing-schema：真实 linux/amd64 integration 为 5 files / 27 tests、failed/skipped/todo=0；canonical before/after payload hash 为 `9c770abd...dc24` / `1b35b00c...72c`，批准 baseline/current SQL checksum 为 `07df3c3d...431e` / `129f3ce0...1aca`，有序 history 两条均 finished=true/rolledBack=false，独立 DB marker 保留。fake before/after hash、换 name/checksum、same-count duplicate、missing、extra、unfinished、rolled-back 与 legacy shape 共 10 类均非零。
- 原生 amd64：preflight PASS，runId `be2e8c80-e28c-4ac8-9fa6-f673f79c4bff`；integration PASS，runId `1ee7f197-ca8e-4fc9-a6e5-a4e0da540a94`；PostgreSQL `17.10 (Debian 17.10-1.pgdg12+1)`、exact amd64 digest、locale/timezone/SSL 对账，两个 container 均 removed；strict platform artifact validator 对当前 HEAD PASS。
- targeted：9 files / 119 tests PASS。`P02_EVIDENCE_PHASE=pre-freeze npm test` 为 220 files / 2441 passed / 6 existing skipped / 0 failed；默认 post-freeze 如实为 218 files PASS / 2 FAIL、2439 passed / 2 failed / 6 existing skipped，失败仅是 B00-6 前 `trusted file mismatch package.json`。
- Node syntax、两份 workflow YAML、`validate-static.mjs`、`scaffold:check`、`docker:static`、本轮 changed TS ESLint、`git diff --check bb6149a1..0c36da35` 均 exit 0。无 P02/Testcontainers/Ryuk/PostgreSQL 17.10 container、volume、network 或 `/tmp/lobsterai-p02-*` 残留；`.reports` 与 workspace build 输出均为 ignored，tracked tree 在写本记录前 clean。

### Round 7 交接

`CHANGES_REQUESTED_B00_4`。退同一 P02 Developer：以真实 validator stderr 的线性 source-drift Red 和 GitHub PR 双父 checkout Red 修复 phase 状态机；修复会触及 resolver、workflow、tests/stage source boundary，因此 `aa843532` candidate 与此前 amd64/arm64/P03 live/B00-6 evidence 全部按 §10.4/§10.8 作废。返工后交新的独立 Reviewer；只有本地 P0-P3 全清且同 source 原生 arm64 artifact 到位后，才可写限定批准并启动后续流程。

## B00-4 Round 8 · 2026-07-12 20:15 +0800 · `/root/p02_b00_4_phase_reviewer` / `09154806`

- 角色与范围：新的独立 P02 B00-4 Reviewer；只复核 Round 7 `f09ea3b7` 的两项 phase P1 返工、`f09ea3b7..09154806` 全部 diff/tests 与 executable source `8447120bd5bab0ad1520efaf38051a14c2d69e56`，未参与 Developer、Tester 或协调者工作。
- Round 7 的两个精确症状已有实质修复：单路径线性 drift 的真实 launcher stderr 可派生 `pre-freeze`；主 CI 与 SaaS 均 full-history checkout PR head，并且 `P02_SOURCE_SHA` 与实际 `HEAD` 字节级对账。
- 结论：`CHANGES_REQUESTED_B00_4`。不允许写 `LOCAL_QUALITY_APPROVED_WAITING_EXTERNAL_ARM64_B00_4`，不允许启动 Tester；本轮 executable source 的 amd64/arm64/P03 live/B00-6 evidence 仍必须按 §10.4/§10.8 作废重跑。

| 严重级 | 未闭环要求 | 文件:行 | 独立复现与影响 | 要求修复 |
|---|---|---|---|---|
| P1 | pre-freeze 必须接受完整、可审计的真实 stale 集合，不能只接受“单 commit 单路径” | `scripts/db/resolve-evidence-phase.mjs:40-88`；`tests/db/postgres-evidence-ci-state.test.ts:101-209` | 当前 classifier 把 stale 硬编码为 `errors.length === 2`且 code/stage 各一条相同路径。独立从有效冻结 HEAD `6c7bf0b9` 线性追加一个同时新增两个 `src/*.ts` 的普通提交：trusted launcher 正确 exit `1`，stderr 是唯一 `FAILED` JSON，共四条纯 provenance drift（code/stage × 两路径）；classifier 却抛 `failed outside the auditable source-drift states`。更直接地，在当前 clean `09154806` 上真实执行 resolver，push、PR 与 `workflow_dispatch(false)` 全部 exit `1`；底层 launcher exit `86` 并精确报 `trusted file mismatch package.json`，而官方 pre-freeze 测试本身又明确要求该当前 candidate 进入 pre-freeze。因此 workflow 中的 phase step 会在 `npm test` 前失败，手工注入 `P02_EVIDENCE_PHASE=pre-freeze` 的全绿不能证明 required CI 可达。 | 先保留两类真实 CLI Red：当前 candidate 的精确 trusted-source drift 必须派生 pre-freeze，以及同 commit 多路径/多个线性 commit 的 code/stage provenance 集合必须派生 pre-freeze。协议需对两个 source label 的非空、成对、同构集合逐条严格校验，不得仅放宽数量或做子串匹配；同时继续拒绝 bootstrap/launcher 自身 digest 错误、manifest/schema/report digest 错误、非唯一 JSON、stdout/stderr 冲突、噪声和混合 provenance/非 provenance 错误。新增真实 resolver 级 PR/push/dispatch pre/post 矩阵，不能只单测纯函数或直接注入最终 phase。 |

### 独立通过项、对照与清理

- phase/PR 定向 `tests/db/postgres-evidence-ci-state.test.ts` + `tests/db/postgres-platform-workflow.test.ts` 连续两次均为 2 files / 33 tests PASS；已覆盖单路径真实 stderr、valid/stale/invalid/noise/multiple diagnostics/stdout-stderr conflict、source SHA 对账和 PR 双父拓扑。这些现有 Green 没有覆盖上述真实 candidate/multi-path Red。
- `P02_EVIDENCE_PHASE=pre-freeze npm test`：exit `0`，220 files / 2452 passed / 6 existing skipped / 0 failed。默认 post-freeze `npm test`：exit `1`，218 files 通过、2 files 失败，2450 passed / 2 failed / 6 existing skipped；仅 `p03-merge-evidence` 与 `reviewer-round2-red` 的旧 evidence `trusted file mismatch package.json`，没有第三类失败。
- structured artifact/history 回归 1 file / 24 tests PASS；真实 integration failure artifact 两条定向 2/2 PASS，覆盖第 28 项 Vitest 失败与 malformed JSON，均保留失败计数/base facts/cleanup。本轮未重跑无相关的整套真实 DB 快乐路径。
- `node --check`、两份 workflow YAML parse、`validate-static.mjs`、`scaffold:check`、两个改动 TS 的 CI 等价 ESLint、`git diff --check f09ea3b7..09154806` 全部 exit `0`。未发现 `continue-on-error`、`|| true`或 required-job 反向 `needs` 死锁。
- 本轮临时 Git worktree、Vitest report、PostgreSQL 17.10/Testcontainers/Ryuk 资源均无残留；追加本记录前 tracked worktree clean。

### Round 8 交接

`CHANGES_REQUESTED_B00_4`。退同一 P02 Developer，只修复上述单一 stale-classification P1，保留已通过的 PR-head/full-history/source-SHA 契约与所有反向 fail-closed 边界。修复必然修改 resolver/tests 并形成新 executable source；因此必须作废 `8447120b` candidate，重跑同 source amd64/arm64/P03 B00-5A 与 P02 B00-6，然后再交新的独立 Reviewer。
