# 开发记录

> Developer 按 Round 追加 Red→Green→Refactor、commit、命令/退出码/证据与 Reviewer 交接；不得覆盖历史。

## Planner handoff · 2026-07-11 · P02 Planner

- 任务 ID：P02
- Planner Agent：`/root/p02_planner`
- 只读规划基线：`eae7da66ebfb2fe474c5657abbb8fd767fcce643`
- 计划版本：`P02-PLAN-v1`
- 计划提交：用 `git log -1 --format=%H -- 改造计划/20260711_V2单租户Web闭环开发/任务/P02-PR2数据库脚手架/开发任务.md` 解析本次独立 Planner commit。
- 当前状态：`PLANNED_BLOCKED`。P01 仍在评审/修复链路中；只有总体协调者将 P01 标为 ACCEPTED 后，P02 Developer 才能开始 Red。
- 已核对输入：根 AGENTS；V2 `01`～`05`；`19` PR-2；`06` Prisma/tenant/RLS/双键；`14` PEN-ISO-3/4；`16` Testcontainers/CI；`17` V2.4/V2.5；附录 C D2/D3/D7/D10/§4/§5；P00 workspace/stage gate；P01 当前 schema/route/bridge 命名（只读，未视为已接受契约）。
- Developer 首项动作：记录 P01 accepted SHA/contract version/hash；若总表或 contract gate 不满足，写 `blocked` 后停止，不得修改实现。
- 关键防伪：缺 Docker/Testcontainers、使用 superuser/owner/BYPASSRLS、只做静态 SQL grep、把 `SET` 当 `SET LOCAL`、两个租户共用同一 `main` UUID，均不能作为 P02 PASS。

## 元数据（Developer 解锁后填写）

- Developer Agent：`/root/p02_developer`
- P01 accepted SHA / contract version / hash：`a6066c3138e4d6c0f36462b9ad3fb8e0877d3a28` / `1.0.0` / `897bd39077d409e52a88e9efe89383380564812f494efb88fe09e2af6a1ed2c8`
- P01 Tester 报告 SHA：`fc9f619fd46afe4622512c5d4168e220816f246a`
- Developer 基线 SHA：`a6066c3138e4d6c0f36462b9ad3fb8e0877d3a28`
- PostgreSQL image ref / digest / server version：`postgres:17.10-bookworm` / `sha256:17b6c778de50f4bb9a878c36e736110fbcd9b7020377d6fdfdf20f7c0347e40a` / `17.10 (Debian 17.10-1.pgdg12+1)`
- Testcontainers runId：preflight `07842da3-e55c-409d-89cb-0675973ccf08`；integration `23213c88-868c-4bc4-9db1-a81002d47c1e`

## Round 1 · 2026-07-11 19:24～20:05 CST · `/root/p02_developer`

### Red

- `be95c975 test(db): capture prisma and rls scaffold red baseline`。
- `npm test -- tests/db/p02-red.test.ts`：12 tests / 12 failed。每项分别因 P02 scripts、PG image manifest、Tenant/Agent schema、migration、ENABLE+FORCE policy、RLS inventory、preflight、safe transaction context、facade、非特权动态测试、池泄漏和 CI gate 缺失而失败；没有语法或 Docker 环境型假 Red。

### Green / Refactor

- `ef187e60`：锁定 Prisma/client `6.19.3`、Testcontainers `12.0.4`、pg `8.22.0`、PostgreSQL `17.10-bookworm` linux/arm64 exact RepoDigest。
- `df18fae2`：最小 `Tenant/Agent` 真 schema、内部 UUID + tenant-local logical ID、citext、首条 recorded migration、ENABLE+FORCE/USING+WITH CHECK、deterministic A/B/main seed。
- `6fc5a211`：受控 DB factory/facade、参数化 transaction-local tenant/user context、应用层 tenant scope；package root 不导出 raw Prisma client。
- `802360a7`：Testcontainers runner、空库/重复 migrate、直接 app role、RLS CRUD、missing context、连接池泄漏/并发和机器报告。
- `5dd0bba8`：激活 `prisma:validate` PASS gate、独立 arm64 CI job、静态双向/mutation checker。
- `4b20066e`：production seed guard 和真实 safe facade `AgentDto.id=logical_id` 往返。
- `3da264db`：保持 `npm test` 快环，容器测试只走显式阻断 job。
- `b7e3a6d3`：新测试并发后，单独把 P01 generated-consumer mutation timeout 从 5s 调到 15s；断言与实现均未放宽。
- `dc98a77d`：DB 报告绑定 `git HEAD`，Testcontainers PG cleanup 失败即整门失败。

### 自测与证据

- `npm ci` exit 0；postinstall 从 schema 生成 ignored Prisma runtime，无 engine binary 入库。
- contracts：`npm run contracts:check`、`npm run test:contract` exit 0；accepted SHA/version/hash preflight PASS。
- Prisma/DB：`npm run prisma:generate`、`npm run test:db:preflight`、`npm run test:db:unit`（42/42）、`npm run test:db:integration`（16/16，skipped=0）、`npm run prisma:validate` 全部 exit 0。
- 动态 catalog：`relrowsecurity=true`、`relforcerowsecurity=true`、policy USING=WITH CHECK；`p02_app` 为 `rolsuper=false`、`rolbypassrls=false` 且 table owner 为 `p02_admin`。
- 隔离：缺 context fail closed；跨租户 SELECT 0 行，INSERT 42501，UPDATE/DELETE 0 行；A/B 都可有 `main` 且内部 UUID 不同；同租户 duplicate 23505；Unicode 往返、空/NUL 拒绝。
- 池语义：commit/rollback/throw/same-connection 后空 context；12 轮 A/B interleaved 0 leak；第三参数 false mutation 明确复现泄漏并清理。
- 回归：changed-file ESLint 和 `npm run lint:saas` 0 warning；`npm run typecheck` exit 0；`npm test` 201 files / 2153 passed / 1 existing skipped；`build:saas` 9 workspace / 18 fresh artifacts；`build`、`compile:electron` exit 0。
- 实际报告：[证据 README](../../../../docs/db/20260711_P02_Prisma与RLS脚手架证据/README.md)、`contracts-preflight.json`、`preflight.json`、`integration.json`、`validation.json`。报告 source SHA 均为 `dc98a77d153fd24f8f8376f10909deaa302126fa`；PG containers 均 `removed=true`，`docker ps -a` 无残留 test PG/Ryuk。
- `npm audit --omit=dev` 的既有 axios/Electron/xlsx 等 root findings 未由 P02 伪装解决；新增 P02 依赖未出现在 production findings，破坏性升级留给 P03/专项任务。

### 交接 Reviewer

- 状态：`REVIEW_PENDING`。Developer 未修改 `审核意见.md`、`测试报告.md` 或总体表。
- 请按 AC01～18 独立复跑，并至少执行 Planner 要求的五项 mutation：删除 FORCE、删除 WITH CHECK、把 set_config 第三参数改 false、把 app role 改 BYPASSRLS、把 logical ID 改全局唯一；`tests/db/validator-mutations.test.ts` 已为这些 mutation 保留 Red 捕获，但不能替代 Reviewer 独立验证。
- 当前 manifest 只批准 `linux/arm64` exact digest；runner/CI 对 amd64 或缺镜像环境 fail-closed/BLOCKED，不使用浮动 tag。若 Reviewer 要求新增 amd64 证据，应补批准 digest 与对应真实 run，不得把本机 arm64 报告冒充多平台通过。

## Round 2 · 2026-07-11 22:10～22:40 CST · `/root/p02_developer`

### Reviewer Round 1 结论

- Reviewer commit `ff92dcf5` 给出 `CHANGES_REQUESTED`，两项均为 P1。
- AC12：tenant extension 虽已定义，但 factory 未实际使用；`update`、`updateMany`、`upsert.update` 仍可能接受伪造 tenantId，且没有关闭 RLS 后的动态证据。
- AC18：已提交 JSON 仍绑定旧 SHA，且 validation 使用手工摘要结构，没有严格 unknown-field/stale-source provenance 验证。

### Red

- `1e86103a test(db): capture reviewer round one P1 gaps`：新增 7 项 Reviewer Red；factory 未接 extension、写分支 tenantId 覆盖、RLS-disabled 动态证据、原生 evidence shape/provenance 均先失败。

### Green / Refactor

- `95ece01c`：factory 使用 `extendTenantClient` 的 scoped client；transaction runner 兼容 extended client；全部写分支强制当前 tenantId；新增真实 `tenant-extension.test.ts`。
- `4b05874d`：增加严格 evidence schema/validator、报告与 runner hash、unknown/stale mutation；active gate 接入 committed evidence 校验。
- `708346cb`：分离 `test:db:core` 与 evidence-dependent unit suite，消除 bootstrap 循环。
- `32a32c6c`：增加原生报告逐字节 snapshot 工具，禁止人工重构报告。
- `ec2cc923`：改为 first-parent ancestry；抽出 Git provenance 检查，并用临时仓库真实构造 evidence 后插入任意代码提交的失败 mutation。

### AC12 动态结果

- `tests/integration/db/tenant-extension.test.ts` 只在隔离 owner connection 中临时关闭 RLS；测试结束恢复 ENABLE+FORCE。
- 真实 factory facade 与直接 extended Prisma client 均覆盖 `findMany/count/aggregate/groupBy/create/createMany/upsert/update/updateMany/delete/deleteMany`。
- A 上下文不能读取、更新、删除 B 行；写操作中伪造 B tenantId 被覆盖为 A；测试结束 B-only 行仍存在。
- PostgreSQL 17 Testcontainers 集成 21/21 PASS，runId `2b10f3df-d7ee-470a-aea9-fb259baad214`，`skipped=0`，container `removed=true`。

### AC18 原生证据链

- 实现冻结 SHA：`ec2cc92379ac754c9625614be7cd0f2c49e74435`。
- 原生 code report runIds：contracts `321841b8-85e7-4491-8225-d207258d5a9c`；preflight `89b9bc85-535f-4209-94d3-17d44acde61c`；integration `2b10f3df-d7ee-470a-aea9-fb259baad214`；validation `3ba5ebfc-051f-4d34-b1c3-99fdffef0d88`。
- `c80b9c2aff4ae9599942450f3628f315a1b8e4fe` 是只含 raw code report/manifest 的 evidence-only descendant；报告 `sourceSha` 仍为实现 SHA，不伪造自引用。
- 正式 stage 在 `c80b9c2a...` 上 PASS，invocationId `5697445a-1bff-43e3-9b06-dbffb3f75cb3`，stage report `sourceSha` 为 `c80b9c2a...`。
- 最终 validator 要求 code/stage SHA 均在 HEAD first-parent 链，并要求从 codeEvidenceSha 到 HEAD 的 diff 只包含本证据目录与本开发记录。任何代码、测试或 gate 变化都会失败。
- 严格 schema 对嵌套对象拒绝未知字段；manifest 验证 raw report SHA-256、runner path/SHA-256；validation 保留原生 `commands[]`。

### Round 2 自测与 Reviewer 交接

- `node scripts/db/validate.mjs` exit 0；四份 code report 均绑定 `ec2cc923...`。
- `npm run test:db:unit` exit 0，55/55。
- `npm run prisma:validate` exit 0，stage invocationId `5697445a-...`。
- `node scripts/db/validate-evidence.mjs` exit 0；codeEvidenceSha=`ec2cc923...`，stageEvidenceSha=`c80b9c2a...`。
- clean regression：`npm ci`、contracts gate/15 contract checks、Prisma generate、DB unit 55/55、DB integration 21/21、typecheck、changed-file ESLint、`lint:saas` 全部 exit 0。
- `npm test`：202 files，2166 passed，1 existing skipped；`build:saas`：9 workspaces / 18 fresh artifacts；`build` 与 `compile:electron` exit 0，仅输出仓库既有 Vite/eval/chunk warning。
- `npm ci` 的 22 项 audit finding（12 moderate、10 high）属于根依赖树既有风险；P02 未运行破坏性 `audit fix --force`。
- 状态恢复为 `REVIEW_PENDING`。请同一 Reviewer 复核 Round 1 两项 P1；Developer 未修改 `审核意见.md`、`测试报告.md`、总体表或 P03 文件。

## Round 3 · 2026-07-11 22:50～23:00 CST · `/root/p02_developer`

### Reviewer Round 2 结论与 Red

- Reviewer commit `d4f9e046`：AC12 已 FIXED；AC18 仍有唯一 P1。聚合 `git diff sourceSha..HEAD` 可被“插入代码 → revert”洗白。
- `ea771a03 test(db): capture per-commit provenance bypasses` 为独立 Red commit。真实临时 Git 仓库 20 tests 中 3 项按预期失败：code→evidence rename 没有审计旧路径、code add→revert 返回空错误、merge commit 被接受。
- 同一矩阵也固定 add/modify/delete 直接拒绝与 source 仅在 non-first-parent 可达时拒绝的既有行为。

### Green / Refactor

- `38ef2df6`：从 source exclusive 到 HEAD inclusive，按 `--first-parent --reverse` 逐提交审计；每个提交必须恰有一个父提交，merge 一律拒绝。
- 每个提交用 `git diff --name-status -z -M <first-parent> <commit>` 读取变更；A/M/D 检查单路径，R/C 同时检查旧路径和新路径。任何路径不在证据目录/P02 Developer 记录 allowlist 即失败。
- 真实 add/modify/delete/rename/revert/merge/non-first-parent mutation 全部 Green，20/20。
- `3d8eb58b`：code snapshot 模式先删除上一轮 stage snapshot，避免 manifest 已重置但旧 stage 文件仍被误当当前证据；静态门固定该行为。

### Round 3 原生证据链

- 新实现冻结 SHA：`3d8eb58b3fcbe7efd4d84788658cf6b350862cd2`。
- code report runIds：contracts `cf242ecc-c3ff-4d2b-a53c-ceb8d2ff082d`；preflight `400e274b-de63-4ca7-bf7c-0a9f3efd2e15`；integration `2e80be88-6437-492f-bd75-0653bb1d9882`；validation `c0a7cccd-d312-4d2c-8094-6af2e256739c`。四份 raw report 的 sourceSha 均为 `3d8eb58b...`。
- `37c7a4819fafd9a62c52202c022604024c8a3c26` 是 raw code report/manifest 的 evidence-only commit。
- stage 在 `37c7a481...` 上 PASS，invocationId `fa89ea5b-d583-4827-92f6-0ed6e60cede8`；stage report sourceSha=`37c7a481...`。
- `node scripts/db/validate-evidence.mjs` 对 code/stage first-parent 逐提交链、strict schema、report/runner digest 全部 PASS。

### Reviewer Round 3 交接

- clean install / contracts：`npm ci`、`contracts:check`、15/15 contract checks、Prisma generate 均 exit 0。
- DB：unit 62/62；真实 PostgreSQL 17 integration 21/21，final runId `108f6c81-d56b-456c-be91-f9d417531353`、`skipped=0`、container removed；active stage final invocationId `3ecd9048-0ffa-4196-bf50-1932ad72f8a1` PASS。
- quality：typecheck、Round 3 changed-file ESLint、`lint:saas` 均 exit 0；`npm test` 202 files / 2173 passed / 1 existing skipped。
- build：`build:saas` 9 workspaces / 18 fresh artifacts；renderer/main/preload build 与 `compile:electron` exit 0，仅既有 Vite/eval/chunk warning。
- 状态：`REVIEW_PENDING`。请同一 Reviewer 重点复现 add→revert、code→evidence rename、merge 与 non-first-parent mutation；确认不是最终聚合 diff。
- Developer 未修改 `审核意见.md`、`测试报告.md`、总体表或 P03 文件；Reviewer ACCEPTED 前不得启动 Tester。

## Round 4 · 2026-07-11 23:25～2026-07-12 00:15 CST · `/root/p02_developer`

### Tester Round 1 退回与 Red

- Tester 记录 commit `c7d3f5ac`，原始 Red `8c4dbcf7`：6 tests 中 4 个目标 Red。P02 Reviewer/Tester handoff 与并行 task 纯记录被误杀；替换 validator 可打印假 PASS；raw validation 与 manifest digest 协同改写可假绿。
- Tester 真实 PG corners `14c99764` 已由 Tester 独立证明 Green，使集成套件增至 24 项；Developer 未改 Tester 期望。
- `6f52f789` 追加 bootstrap+validator 同提交协同替换 Red，证明仅分别保护两个文件仍会假绿。
- `d32bcaee` 追加真实 outer 协同 Red：恶意 launcher 写入后计算其真实新 SHA 并同步回 gate manifest，旧 runner 错误 exit 0；不是把期望值简单改成 0 的假 mutation。

### Green 与可信根边界

- `f574dca4` / `f35e5a02`：精确 handoff/parallel record allowlist；raw report 首次 first-parent Git blob anchor；validation 精确 8-command sequence；bootstrap 在 validator 加载前对 codeEvidence Git blob 校验 protected files；裸 validator 不合格。
- `92a86a8c` / `770828d5`：active package 走 digest 参数 launcher；P00 stage runner 在 command 前固定 bootstrap/launcher digest；增加 bootstrap+validator 协同、launcher 篡改和入口移除负向。
- `2f603580` / `c3b62524`：P02 stage 必须接收协调者/CI 固定的 gate manifest SHA-256；缺失/不匹配立即失败。stage report/schema 记录并校验 outer runner digest、current manifest digest 与 externally expected manifest digest。
- 信任根明确为协调者/CI 已接受 source SHA、P00 runner digest 与 gate manifest digest。仓内 `trustedFiles` 只是由该外层 runner 执行的内层检查，不自称终极信任。本方案不声称抵抗同时控制全部仓内验证代码和外部协调配置的攻击者。

### Round 4 原生证据链

- implementation：`c3b625243b76a34aef6e8936bc3f09c50131ae6f`。
- raw evidence commit：`f079413bfe27df3a9a07c236e7597f5ccc3c345e`。
- raw runIds：contracts `53c25a50-2092-4bcd-a759-989250af8b21`；preflight `0d45db68-4e20-42b6-943e-42d6c2933b82`；integration `3aaf2faa-f882-4b2f-b89b-2b29e12297f3`；validation `f74eb514-6263-4869-b0fc-45b15705b015`。
- stage invocationId `d15fb42f-0488-4128-9a48-ae703b525178`，source=`f079413b...`，runner=`af982ca6...`，manifest=expectedManifest=`658fac5b...`，status PASS。

### Round 4 当前验证与交接

- Tester 原 evidence boundary 6/6；bootstrap/outer targeted 10/10；static/scaffold/lint targeted PASS。
- 真实 PostgreSQL 17 integration 24/24、skipped=0、container removed；原生 integration report runId `3aaf2faa-...`。
- final current-head DB：Tester evidence 6/6；validator/provenance/static mutation 21/21；DB unit 7 files / 72 tests；真实 PG integration 4 files / 24 tests，final runId `a277ee47-e3c9-479c-9c2b-229123541a2b`、skipped=0、container removed。
- final current-head stage：`npm run prisma:validate` invocationId `6eba84f6-ddae-4579-aa7e-3ee62157d87c` PASS，runner=`af982ca6...`，manifest=expectedManifest=`658fac5b...`。
- regression：`npm ci`、contracts gate、15/15 contract checks、Prisma generate、typecheck、Round 4 changed-file ESLint、`lint:saas` 全部 exit 0；`npm test` 204 files / 2186 passed / 1 existing skipped。
- build：`build:saas` 9 workspaces / 18 fresh artifacts；renderer/main/preload build 与 `compile:electron` exit 0，仅既有 Vite/eval/chunk warning。
- 当前状态：`REVIEW_PENDING_INTEGRATION`。本轮代码与证据已完整，但协调者将先合并 P03（会触及共享 package/stage 边界）；合并后必须以最终集成 HEAD 重新冻结 raw/stage 并复跑集成门，再交同一 Reviewer Round 4。当前不启动 Reviewer 或 Tester。

## Round 4 集成重冻 · 2026-07-12 · P03 accepted merge 后

- 协调者将 P03 完整 accepted SHA 链合入共享分支，merge commit `43665ce1b7519ed5dbd47591d7efabd9d436f366`；package 同时保留 P02 external manifest digest 入口与 P03 Bee timeout 配置。
- 合并后旧 P02 trusted launcher 按预期 exit 86，原因是 current `package.json` 与旧 codeEvidence Git blob 不一致；没有把旧证据误写为继续有效。
- `20b95e3c test(db): capture P03 merge evidence invalidation` 为集成 Red：旧 manifest 的 codeEvidenceSha 不包含 P03 merge（merge-base exit 1），正式 launcher 因 package mismatch exit 86，2/2 失败。
- 以 `20b95e3c1324fd368e8345d2b362d972c43c745c` 为新的 post-merge implementation source 后，集成测试 2/2 Green。
- raw evidence commit `b07694d936097c0d91fd6e2b6aca48abe80cbfb1`；runIds：contracts `150f5ba5-1628-4d80-9efb-07c9ab826672`、preflight `f6c65487-b55a-4765-9344-6ea40abe2a3c`、integration `d524744f-44f2-4689-84c9-3ddbe2045e64`、validation `88a78d74-90d9-4e4c-9e9e-5887389c235c`。
- stage 在 `b07694d9...` 上 PASS，invocationId `bb9fab84-6c45-4b90-9972-dd58877906f4`；runner=`af982ca6...`，manifest=expectedManifest=`658fac5b...`。
- 当前状态仍为 `REVIEW_PENDING_INTEGRATION`：提交 stage/docs 后复跑 P02 全门与 P03 targeted static，最终 clean 后才交同一 Reviewer；不启动 Tester、不改总体表。

### Post-merge 集成回归与共享阻断

- P02：evidence/bootstrap/provenance/P03-merge 组合 4 files / 32 tests PASS；DB unit 8 files / 74 tests PASS；真实 PostgreSQL 17 integration 4 files / 24 tests PASS，runId `08053ddd-f866-434f-8073-662a35e675e6`、skipped=0、container removed。
- P02 stage：集成 HEAD 重跑 invocationId `00329847-84cd-433b-9351-3db8f5f528bd` PASS，runner=`af982ca6...`，manifest=expectedManifest=`658fac5b...`；contracts 15/15、typecheck、changed-file ESLint、`lint:saas` 均 PASS。
- P03 static：不依赖 exact-five raw 目录的 4 targeted files / 34 tests PASS；Docker/Helm static checker PASS；Bee timeout/package 合并未破坏这些路径。
- P03 evidence blocker：共享 worktree 缺 ignored `.reports/supply-chain/20260712_PR3部署供应链证据`，`tests/deploy-supply-chain-evidence-files.test.ts` 有 36 项 ENOENT；第 37 项识别 product source 从 accepted `187da9a...` 错误推进到 P02 docs commit `613bbe91...`。
- 官方 `npm test` 如实 exit 1：1 file / 37 failed，其他 206 files / 2199 passed / 1 existing skipped。明确排除该 P03 external-evidence 文件的代码回归为 206 files / 2197 passed / 1 skipped，exit 0；不能用排除结果冒充官方 full PASS。
- build：`build:saas` 9 workspaces / 18 fresh artifacts；renderer/main/preload build 与 `compile:electron` PASS，仅既有 Vite/eval/chunk warning。
- 当前状态：`BLOCKED_BY_P03_SHARED_EVIDENCE_SOURCE`。协调者将让同一 P03 Developer 把 product-source resolver 收敛为仅实际 Docker image input 路径并重跑 exact-five。修复合入后 P02 必须最后一次 rebaseline raw/stage/full gates；本轮不唤 Reviewer、不启动 Tester、不改总体表。

## Round 4 最终 rebaseline · 2026-07-12 · P03 resolver/exact-five 稳定后

- P03 最终产品 source 为 `f93fdd59d98b029cb82da5cc4f5873520472bb7f`；exact-five invocation `5673b88b-2151-4f73-9f7c-b87a245a0d89` 的 5 项报告已在共享 worktree 存在，formal checker PASS。P03 后续 docs-only 提交后 resolver 仍返回 `f93fdd59...`。
- 旧 P02 evidence 的 targeted 基线精确复现 `2 failed / 7 passed`：`reviewer-round2-red` 和 `p03-merge-evidence` 各 1 项因旧 source 之后存在 P03 resolver/边界测试变更而失败，证明旧 manifest 没有伪绿。
- P02 本身无新产品代码改动，因此直接选择 clean integrated HEAD `21e141d47240988306fec7f45a7b23ae54a5f992` 作为最终 `codeEvidenceSha`；不制造无意义 code snapshot commit。
- `node scripts/db/validate.mjs` PASS；raw runIds：contracts `4ef139d8-37f2-4c67-9d54-7a6f906fb7bc`、preflight `fcd664b8-9d87-40df-b093-12f45da8e2c3`、integration `26c451e3-9683-40a9-90fd-0e8e9e8bfca5`、validation `4d73b8f7-ba20-4b5b-add8-d66990b6f731`。真实 PostgreSQL 17 integration 24/24、`skipped=0`、container removed。
- raw evidence-only commit 为 `c49b66cd479f1906586806c1cdd9043a11650656`。`npm run prisma:validate` 在该 clean commit PASS；stage invocation `4b004aa6-706f-452e-a608-7f292ae45bf5`，source=`c49b66cd...`，runner=`af982ca6...`，manifest=expectedManifest=`658fac5b...`。
- 本次 stage/docs 提交后从 clean HEAD 执行最终 full regression；结果追加在后续记录，Reviewer 通过前不启动 Tester，不修改总体表。
