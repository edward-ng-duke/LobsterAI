# 开发记录

> Developer 按 Round 追加 Red→Green→Refactor、commit、命令/退出码/证据与 Reviewer 交接；不得覆盖历史。

## Planner handoff · 2026-07-11 · P02 Planner

- 任务 ID：P02
- Planner Agent：`/root/p02_planner`
- 只读规划基线：`eae7da66ebfb2fe474c5657abbb8fd767fcce643`
- 计划版本：`P02-PLAN-v1`
- 计划提交：用 `git log -1 --format=%H -- 改造计划/20260711_V2单租户Web闭环开发/任务/P02-PR2数据库脚手架/开发任务.md` 解析本次独立 Planner commit。
- 当前状态：`PLANNED_BLOCKED`。P01 仍在评审/修复链路中；只有总体协调者将 P01 标为 ACCEPTED 后，P02 Developer 才能开始 Red。
- 已核对输入：根 AGENTS；V2 `01`～`05`；`19` PR-2；`06` Prisma/tenant/RLS/双键；`14` PEN-ISO-3/4；`16` Testcontainers/CI；`17` V2.4/V2.5；附录 C D2/D3/D7/D10/§4/§5；P00 workspace/stage gate；P01 当前 schema/route/bridge 命名（只读，未视为已接受契约）。
- Developer 首项动作：记录 P01 accepted SHA/contract version/hash；若总表或 contract gate 不满足，写 `blocked` 后停止，不得修改实现。
- 关键防伪：缺 Docker/Testcontainers、使用 superuser/owner/BYPASSRLS、只做静态 SQL grep、把 `SET` 当 `SET LOCAL`、两个租户共用同一 `main` UUID，均不能作为 P02 PASS。

## 元数据（Developer 解锁后填写）

- Developer Agent：`/root/p02_developer`
- P01 accepted SHA / contract version / hash：`a6066c3138e4d6c0f36462b9ad3fb8e0877d3a28` / `1.0.0` / `897bd39077d409e52a88e9efe89383380564812f494efb88fe09e2af6a1ed2c8`
- P01 Tester 报告 SHA：`fc9f619fd46afe4622512c5d4168e220816f246a`
- Developer 基线 SHA：`a6066c3138e4d6c0f36462b9ad3fb8e0877d3a28`
- PostgreSQL image ref / digest / server version：`postgres:17.10-bookworm` / `sha256:17b6c778de50f4bb9a878c36e736110fbcd9b7020377d6fdfdf20f7c0347e40a` / `17.10 (Debian 17.10-1.pgdg12+1)`
- Testcontainers runId：preflight `07842da3-e55c-409d-89cb-0675973ccf08`；integration `23213c88-868c-4bc4-9db1-a81002d47c1e`

## Round 1 · 2026-07-11 19:24～20:05 CST · `/root/p02_developer`

### Red

- `be95c975 test(db): capture prisma and rls scaffold red baseline`。
- `npm test -- tests/db/p02-red.test.ts`：12 tests / 12 failed。每项分别因 P02 scripts、PG image manifest、Tenant/Agent schema、migration、ENABLE+FORCE policy、RLS inventory、preflight、safe transaction context、facade、非特权动态测试、池泄漏和 CI gate 缺失而失败；没有语法或 Docker 环境型假 Red。

### Green / Refactor

- `ef187e60`：锁定 Prisma/client `6.19.3`、Testcontainers `12.0.4`、pg `8.22.0`、PostgreSQL `17.10-bookworm` linux/arm64 exact RepoDigest。
- `df18fae2`：最小 `Tenant/Agent` 真 schema、内部 UUID + tenant-local logical ID、citext、首条 recorded migration、ENABLE+FORCE/USING+WITH CHECK、deterministic A/B/main seed。
- `6fc5a211`：受控 DB factory/facade、参数化 transaction-local tenant/user context、应用层 tenant scope；package root 不导出 raw Prisma client。
- `802360a7`：Testcontainers runner、空库/重复 migrate、直接 app role、RLS CRUD、missing context、连接池泄漏/并发和机器报告。
- `5dd0bba8`：激活 `prisma:validate` PASS gate、独立 arm64 CI job、静态双向/mutation checker。
- `4b20066e`：production seed guard 和真实 safe facade `AgentDto.id=logical_id` 往返。
- `3da264db`：保持 `npm test` 快环，容器测试只走显式阻断 job。
- `b7e3a6d3`：新测试并发后，单独把 P01 generated-consumer mutation timeout 从 5s 调到 15s；断言与实现均未放宽。
- `dc98a77d`：DB 报告绑定 `git HEAD`，Testcontainers PG cleanup 失败即整门失败。

### 自测与证据

- `npm ci` exit 0；postinstall 从 schema 生成 ignored Prisma runtime，无 engine binary 入库。
- contracts：`npm run contracts:check`、`npm run test:contract` exit 0；accepted SHA/version/hash preflight PASS。
- Prisma/DB：`npm run prisma:generate`、`npm run test:db:preflight`、`npm run test:db:unit`（42/42）、`npm run test:db:integration`（16/16，skipped=0）、`npm run prisma:validate` 全部 exit 0。
- 动态 catalog：`relrowsecurity=true`、`relforcerowsecurity=true`、policy USING=WITH CHECK；`p02_app` 为 `rolsuper=false`、`rolbypassrls=false` 且 table owner 为 `p02_admin`。
- 隔离：缺 context fail closed；跨租户 SELECT 0 行，INSERT 42501，UPDATE/DELETE 0 行；A/B 都可有 `main` 且内部 UUID 不同；同租户 duplicate 23505；Unicode 往返、空/NUL 拒绝。
- 池语义：commit/rollback/throw/same-connection 后空 context；12 轮 A/B interleaved 0 leak；第三参数 false mutation 明确复现泄漏并清理。
- 回归：changed-file ESLint 和 `npm run lint:saas` 0 warning；`npm run typecheck` exit 0；`npm test` 201 files / 2153 passed / 1 existing skipped；`build:saas` 9 workspace / 18 fresh artifacts；`build`、`compile:electron` exit 0。
- 实际报告：[证据 README](../../../../docs/db/20260711_P02_Prisma与RLS脚手架证据/README.md)、`contracts-preflight.json`、`preflight.json`、`integration.json`、`validation.json`。报告 source SHA 均为 `dc98a77d153fd24f8f8376f10909deaa302126fa`；PG containers 均 `removed=true`，`docker ps -a` 无残留 test PG/Ryuk。
- `npm audit --omit=dev` 的既有 axios/Electron/xlsx 等 root findings 未由 P02 伪装解决；新增 P02 依赖未出现在 production findings，破坏性升级留给 P03/专项任务。

### 交接 Reviewer

- 状态：`REVIEW_PENDING`。Developer 未修改 `审核意见.md`、`测试报告.md` 或总体表。
- 请按 AC01～18 独立复跑，并至少执行 Planner 要求的五项 mutation：删除 FORCE、删除 WITH CHECK、把 set_config 第三参数改 false、把 app role 改 BYPASSRLS、把 logical ID 改全局唯一；`tests/db/validator-mutations.test.ts` 已为这些 mutation 保留 Red 捕获，但不能替代 Reviewer 独立验证。
- 当前 manifest 只批准 `linux/arm64` exact digest；runner/CI 对 amd64 或缺镜像环境 fail-closed/BLOCKED，不使用浮动 tag。若 Reviewer 要求新增 amd64 证据，应补批准 digest 与对应真实 run，不得把本机 arm64 报告冒充多平台通过。

## Round 2 · 2026-07-11 22:10～22:40 CST · `/root/p02_developer`

### Reviewer Round 1 结论

- Reviewer commit `ff92dcf5` 给出 `CHANGES_REQUESTED`，两项均为 P1。
- AC12：tenant extension 虽已定义，但 factory 未实际使用；`update`、`updateMany`、`upsert.update` 仍可能接受伪造 tenantId，且没有关闭 RLS 后的动态证据。
- AC18：已提交 JSON 仍绑定旧 SHA，且 validation 使用手工摘要结构，没有严格 unknown-field/stale-source provenance 验证。

### Red

- `1e86103a test(db): capture reviewer round one P1 gaps`：新增 7 项 Reviewer Red；factory 未接 extension、写分支 tenantId 覆盖、RLS-disabled 动态证据、原生 evidence shape/provenance 均先失败。

### Green / Refactor

- `95ece01c`：factory 使用 `extendTenantClient` 的 scoped client；transaction runner 兼容 extended client；全部写分支强制当前 tenantId；新增真实 `tenant-extension.test.ts`。
- `4b05874d`：增加严格 evidence schema/validator、报告与 runner hash、unknown/stale mutation；active gate 接入 committed evidence 校验。
- `708346cb`：分离 `test:db:core` 与 evidence-dependent unit suite，消除 bootstrap 循环。
- `32a32c6c`：增加原生报告逐字节 snapshot 工具，禁止人工重构报告。
- `ec2cc923`：改为 first-parent ancestry；抽出 Git provenance 检查，并用临时仓库真实构造 evidence 后插入任意代码提交的失败 mutation。

### AC12 动态结果

- `tests/integration/db/tenant-extension.test.ts` 只在隔离 owner connection 中临时关闭 RLS；测试结束恢复 ENABLE+FORCE。
- 真实 factory facade 与直接 extended Prisma client 均覆盖 `findMany/count/aggregate/groupBy/create/createMany/upsert/update/updateMany/delete/deleteMany`。
- A 上下文不能读取、更新、删除 B 行；写操作中伪造 B tenantId 被覆盖为 A；测试结束 B-only 行仍存在。
- PostgreSQL 17 Testcontainers 集成 21/21 PASS，runId `2b10f3df-d7ee-470a-aea9-fb259baad214`，`skipped=0`，container `removed=true`。

### AC18 原生证据链

- 实现冻结 SHA：`ec2cc92379ac754c9625614be7cd0f2c49e74435`。
- 原生 code report runIds：contracts `321841b8-85e7-4491-8225-d207258d5a9c`；preflight `89b9bc85-535f-4209-94d3-17d44acde61c`；integration `2b10f3df-d7ee-470a-aea9-fb259baad214`；validation `3ba5ebfc-051f-4d34-b1c3-99fdffef0d88`。
- `c80b9c2aff4ae9599942450f3628f315a1b8e4fe` 是只含 raw code report/manifest 的 evidence-only descendant；报告 `sourceSha` 仍为实现 SHA，不伪造自引用。
- 正式 stage 在 `c80b9c2a...` 上 PASS，invocationId `5697445a-1bff-43e3-9b06-dbffb3f75cb3`，stage report `sourceSha` 为 `c80b9c2a...`。
- 最终 validator 要求 code/stage SHA 均在 HEAD first-parent 链，并要求从 codeEvidenceSha 到 HEAD 的 diff 只包含本证据目录与本开发记录。任何代码、测试或 gate 变化都会失败。
- 严格 schema 对嵌套对象拒绝未知字段；manifest 验证 raw report SHA-256、runner path/SHA-256；validation 保留原生 `commands[]`。

### Round 2 自测与 Reviewer 交接

- `node scripts/db/validate.mjs` exit 0；四份 code report 均绑定 `ec2cc923...`。
- `npm run test:db:unit` exit 0，55/55。
- `npm run prisma:validate` exit 0，stage invocationId `5697445a-...`。
- `node scripts/db/validate-evidence.mjs` exit 0；codeEvidenceSha=`ec2cc923...`，stageEvidenceSha=`c80b9c2a...`。
- clean regression：`npm ci`、contracts gate/15 contract checks、Prisma generate、DB unit 55/55、DB integration 21/21、typecheck、changed-file ESLint、`lint:saas` 全部 exit 0。
- `npm test`：202 files，2166 passed，1 existing skipped；`build:saas`：9 workspaces / 18 fresh artifacts；`build` 与 `compile:electron` exit 0，仅输出仓库既有 Vite/eval/chunk warning。
- `npm ci` 的 22 项 audit finding（12 moderate、10 high）属于根依赖树既有风险；P02 未运行破坏性 `audit fix --force`。
- 状态恢复为 `REVIEW_PENDING`。请同一 Reviewer 复核 Round 1 两项 P1；Developer 未修改 `审核意见.md`、`测试报告.md`、总体表或 P03 文件。

## Round 3 · 2026-07-11 22:50～23:00 CST · `/root/p02_developer`

### Reviewer Round 2 结论与 Red

- Reviewer commit `d4f9e046`：AC12 已 FIXED；AC18 仍有唯一 P1。聚合 `git diff sourceSha..HEAD` 可被“插入代码 → revert”洗白。
- `ea771a03 test(db): capture per-commit provenance bypasses` 为独立 Red commit。真实临时 Git 仓库 20 tests 中 3 项按预期失败：code→evidence rename 没有审计旧路径、code add→revert 返回空错误、merge commit 被接受。
- 同一矩阵也固定 add/modify/delete 直接拒绝与 source 仅在 non-first-parent 可达时拒绝的既有行为。

### Green / Refactor

- `38ef2df6`：从 source exclusive 到 HEAD inclusive，按 `--first-parent --reverse` 逐提交审计；每个提交必须恰有一个父提交，merge 一律拒绝。
- 每个提交用 `git diff --name-status -z -M <first-parent> <commit>` 读取变更；A/M/D 检查单路径，R/C 同时检查旧路径和新路径。任何路径不在证据目录/P02 Developer 记录 allowlist 即失败。
- 真实 add/modify/delete/rename/revert/merge/non-first-parent mutation 全部 Green，20/20。
- `3d8eb58b`：code snapshot 模式先删除上一轮 stage snapshot，避免 manifest 已重置但旧 stage 文件仍被误当当前证据；静态门固定该行为。

### Round 3 原生证据链

- 新实现冻结 SHA：`3d8eb58b3fcbe7efd4d84788658cf6b350862cd2`。
- code report runIds：contracts `cf242ecc-c3ff-4d2b-a53c-ceb8d2ff082d`；preflight `400e274b-de63-4ca7-bf7c-0a9f3efd2e15`；integration `2e80be88-6437-492f-bd75-0653bb1d9882`；validation `c0a7cccd-d312-4d2c-8094-6af2e256739c`。四份 raw report 的 sourceSha 均为 `3d8eb58b...`。
- `37c7a4819fafd9a62c52202c022604024c8a3c26` 是 raw code report/manifest 的 evidence-only commit。
- stage 在 `37c7a481...` 上 PASS，invocationId `fa89ea5b-d583-4827-92f6-0ed6e60cede8`；stage report sourceSha=`37c7a481...`。
- `node scripts/db/validate-evidence.mjs` 对 code/stage first-parent 逐提交链、strict schema、report/runner digest 全部 PASS。

### Reviewer Round 3 交接

- clean install / contracts：`npm ci`、`contracts:check`、15/15 contract checks、Prisma generate 均 exit 0。
- DB：unit 62/62；真实 PostgreSQL 17 integration 21/21，final runId `108f6c81-d56b-456c-be91-f9d417531353`、`skipped=0`、container removed；active stage final invocationId `3ecd9048-0ffa-4196-bf50-1932ad72f8a1` PASS。
- quality：typecheck、Round 3 changed-file ESLint、`lint:saas` 均 exit 0；`npm test` 202 files / 2173 passed / 1 existing skipped。
- build：`build:saas` 9 workspaces / 18 fresh artifacts；renderer/main/preload build 与 `compile:electron` exit 0，仅既有 Vite/eval/chunk warning。
- 状态：`REVIEW_PENDING`。请同一 Reviewer 重点复现 add→revert、code→evidence rename、merge 与 non-first-parent mutation；确认不是最终聚合 diff。
- Developer 未修改 `审核意见.md`、`测试报告.md`、总体表或 P03 文件；Reviewer ACCEPTED 前不得启动 Tester。

## Round 4 · 2026-07-11 23:25～2026-07-12 00:15 CST · `/root/p02_developer`

### Tester Round 1 退回与 Red

- Tester 记录 commit `c7d3f5ac`，原始 Red `8c4dbcf7`：6 tests 中 4 个目标 Red。P02 Reviewer/Tester handoff 与并行 task 纯记录被误杀；替换 validator 可打印假 PASS；raw validation 与 manifest digest 协同改写可假绿。
- Tester 真实 PG corners `14c99764` 已由 Tester 独立证明 Green，使集成套件增至 24 项；Developer 未改 Tester 期望。
- `6f52f789` 追加 bootstrap+validator 同提交协同替换 Red，证明仅分别保护两个文件仍会假绿。
- `d32bcaee` 追加真实 outer 协同 Red：恶意 launcher 写入后计算其真实新 SHA 并同步回 gate manifest，旧 runner 错误 exit 0；不是把期望值简单改成 0 的假 mutation。

### Green 与可信根边界

- `f574dca4` / `f35e5a02`：精确 handoff/parallel record allowlist；raw report 首次 first-parent Git blob anchor；validation 精确 8-command sequence；bootstrap 在 validator 加载前对 codeEvidence Git blob 校验 protected files；裸 validator 不合格。
- `92a86a8c` / `770828d5`：active package 走 digest 参数 launcher；P00 stage runner 在 command 前固定 bootstrap/launcher digest；增加 bootstrap+validator 协同、launcher 篡改和入口移除负向。
- `2f603580` / `c3b62524`：P02 stage 必须接收协调者/CI 固定的 gate manifest SHA-256；缺失/不匹配立即失败。stage report/schema 记录并校验 outer runner digest、current manifest digest 与 externally expected manifest digest。
- 信任根明确为协调者/CI 已接受 source SHA、P00 runner digest 与 gate manifest digest。仓内 `trustedFiles` 只是由该外层 runner 执行的内层检查，不自称终极信任。本方案不声称抵抗同时控制全部仓内验证代码和外部协调配置的攻击者。

### Round 4 原生证据链

- implementation：`c3b625243b76a34aef6e8936bc3f09c50131ae6f`。
- raw evidence commit：`f079413bfe27df3a9a07c236e7597f5ccc3c345e`。
- raw runIds：contracts `53c25a50-2092-4bcd-a759-989250af8b21`；preflight `0d45db68-4e20-42b6-943e-42d6c2933b82`；integration `3aaf2faa-f882-4b2f-b89b-2b29e12297f3`；validation `f74eb514-6263-4869-b0fc-45b15705b015`。
- stage invocationId `d15fb42f-0488-4128-9a48-ae703b525178`，source=`f079413b...`，runner=`af982ca6...`，manifest=expectedManifest=`658fac5b...`，status PASS。

### Round 4 当前验证与交接

- Tester 原 evidence boundary 6/6；bootstrap/outer targeted 10/10；static/scaffold/lint targeted PASS。
- 真实 PostgreSQL 17 integration 24/24、skipped=0、container removed；原生 integration report runId `3aaf2faa-...`。
- final current-head DB：Tester evidence 6/6；validator/provenance/static mutation 21/21；DB unit 7 files / 72 tests；真实 PG integration 4 files / 24 tests，final runId `a277ee47-e3c9-479c-9c2b-229123541a2b`、skipped=0、container removed。
- final current-head stage：`npm run prisma:validate` invocationId `6eba84f6-ddae-4579-aa7e-3ee62157d87c` PASS，runner=`af982ca6...`，manifest=expectedManifest=`658fac5b...`。
- regression：`npm ci`、contracts gate、15/15 contract checks、Prisma generate、typecheck、Round 4 changed-file ESLint、`lint:saas` 全部 exit 0；`npm test` 204 files / 2186 passed / 1 existing skipped。
- build：`build:saas` 9 workspaces / 18 fresh artifacts；renderer/main/preload build 与 `compile:electron` exit 0，仅既有 Vite/eval/chunk warning。
- 当前状态：`REVIEW_PENDING_INTEGRATION`。本轮代码与证据已完整，但协调者将先合并 P03（会触及共享 package/stage 边界）；合并后必须以最终集成 HEAD 重新冻结 raw/stage 并复跑集成门，再交同一 Reviewer Round 4。当前不启动 Reviewer 或 Tester。

## Round 4 集成重冻 · 2026-07-12 · P03 accepted merge 后

- 协调者将 P03 完整 accepted SHA 链合入共享分支，merge commit `43665ce1b7519ed5dbd47591d7efabd9d436f366`；package 同时保留 P02 external manifest digest 入口与 P03 Bee timeout 配置。
- 合并后旧 P02 trusted launcher 按预期 exit 86，原因是 current `package.json` 与旧 codeEvidence Git blob 不一致；没有把旧证据误写为继续有效。
- `20b95e3c test(db): capture P03 merge evidence invalidation` 为集成 Red：旧 manifest 的 codeEvidenceSha 不包含 P03 merge（merge-base exit 1），正式 launcher 因 package mismatch exit 86，2/2 失败。
- 以 `20b95e3c1324fd368e8345d2b362d972c43c745c` 为新的 post-merge implementation source 后，集成测试 2/2 Green。
- raw evidence commit `b07694d936097c0d91fd6e2b6aca48abe80cbfb1`；runIds：contracts `150f5ba5-1628-4d80-9efb-07c9ab826672`、preflight `f6c65487-b55a-4765-9344-6ea40abe2a3c`、integration `d524744f-44f2-4689-84c9-3ddbe2045e64`、validation `88a78d74-90d9-4e4c-9e9e-5887389c235c`。
- stage 在 `b07694d9...` 上 PASS，invocationId `bb9fab84-6c45-4b90-9972-dd58877906f4`；runner=`af982ca6...`，manifest=expectedManifest=`658fac5b...`。
- 当前状态仍为 `REVIEW_PENDING_INTEGRATION`：提交 stage/docs 后复跑 P02 全门与 P03 targeted static，最终 clean 后才交同一 Reviewer；不启动 Tester、不改总体表。

### Post-merge 集成回归与共享阻断

- P02：evidence/bootstrap/provenance/P03-merge 组合 4 files / 32 tests PASS；DB unit 8 files / 74 tests PASS；真实 PostgreSQL 17 integration 4 files / 24 tests PASS，runId `08053ddd-f866-434f-8073-662a35e675e6`、skipped=0、container removed。
- P02 stage：集成 HEAD 重跑 invocationId `00329847-84cd-433b-9351-3db8f5f528bd` PASS，runner=`af982ca6...`，manifest=expectedManifest=`658fac5b...`；contracts 15/15、typecheck、changed-file ESLint、`lint:saas` 均 PASS。
- P03 static：不依赖 exact-five raw 目录的 4 targeted files / 34 tests PASS；Docker/Helm static checker PASS；Bee timeout/package 合并未破坏这些路径。
- P03 evidence blocker：共享 worktree 缺 ignored `.reports/supply-chain/20260712_PR3部署供应链证据`，`tests/deploy-supply-chain-evidence-files.test.ts` 有 36 项 ENOENT；第 37 项识别 product source 从 accepted `187da9a...` 错误推进到 P02 docs commit `613bbe91...`。
- 官方 `npm test` 如实 exit 1：1 file / 37 failed，其他 206 files / 2199 passed / 1 existing skipped。明确排除该 P03 external-evidence 文件的代码回归为 206 files / 2197 passed / 1 skipped，exit 0；不能用排除结果冒充官方 full PASS。
- build：`build:saas` 9 workspaces / 18 fresh artifacts；renderer/main/preload build 与 `compile:electron` PASS，仅既有 Vite/eval/chunk warning。
- 当前状态：`BLOCKED_BY_P03_SHARED_EVIDENCE_SOURCE`。协调者将让同一 P03 Developer 把 product-source resolver 收敛为仅实际 Docker image input 路径并重跑 exact-five。修复合入后 P02 必须最后一次 rebaseline raw/stage/full gates；本轮不唤 Reviewer、不启动 Tester、不改总体表。

## Round 4 最终 rebaseline · 2026-07-12 · P03 resolver/exact-five 稳定后

- P03 最终产品 source 为 `f93fdd59d98b029cb82da5cc4f5873520472bb7f`；exact-five invocation `5673b88b-2151-4f73-9f7c-b87a245a0d89` 的 5 项报告已在共享 worktree 存在，formal checker PASS。P03 后续 docs-only 提交后 resolver 仍返回 `f93fdd59...`。
- 旧 P02 evidence 的 targeted 基线精确复现 `2 failed / 7 passed`：`reviewer-round2-red` 和 `p03-merge-evidence` 各 1 项因旧 source 之后存在 P03 resolver/边界测试变更而失败，证明旧 manifest 没有伪绿。
- P02 本身无新产品代码改动，因此直接选择 clean integrated HEAD `21e141d47240988306fec7f45a7b23ae54a5f992` 作为最终 `codeEvidenceSha`；不制造无意义 code snapshot commit。
- `node scripts/db/validate.mjs` PASS；raw runIds：contracts `4ef139d8-37f2-4c67-9d54-7a6f906fb7bc`、preflight `fcd664b8-9d87-40df-b093-12f45da8e2c3`、integration `26c451e3-9683-40a9-90fd-0e8e9e8bfca5`、validation `4d73b8f7-ba20-4b5b-add8-d66990b6f731`。真实 PostgreSQL 17 integration 24/24、`skipped=0`、container removed。
- raw evidence-only commit 为 `c49b66cd479f1906586806c1cdd9043a11650656`。`npm run prisma:validate` 在该 clean commit PASS；stage invocation `4b004aa6-706f-452e-a608-7f292ae45bf5`，source=`c49b66cd...`，runner=`af982ca6...`，manifest=expectedManifest=`658fac5b...`。
- 本次 stage/docs 提交后从 clean HEAD 执行最终 full regression；结果追加在后续记录，Reviewer 通过前不启动 Tester，不修改总体表。

### Final clean-HEAD regression

- stage/docs commit：`668dd0eff6216c18a01d11085e0c8c93c1eaee1b`；以该 clean HEAD 启动所有最终命令。
- P02 evidence：`npx vitest run tests/db/tester-evidence-boundary.test.ts tests/db/validator-mutations.test.ts tests/db/evidence-bootstrap.test.ts tests/db/p03-merge-evidence.test.ts` → 4 files / 32 tests PASS；`npm run test:db:unit` → 8 files / 74 tests PASS。
- 真实 DB：`npm run test:db:integration` → 4 files / 24 tests PASS，runId `64a5c156-821d-419c-9e49-4c500bb380a5`，PostgreSQL `17.10`，`skipped=0`，container `removed=true`。
- P03 integrated：四文件 targeted → 4 files / 74 tests PASS（当前 source-boundary 已由原基线扩展）；`tests/deploy-product-source-boundaries.test.ts` 独立 19/19 PASS。`node scripts/check-supply-chain.mjs` → PASS，57 assets / exact 5 image evidence；`node scripts/check-docker-build.mjs --static` → PASS。
- contracts/stage：`npm run contracts:check && npm run test:contract` → 15/15 PASS；`npm run prisma:validate` → PASS，invocation `ca6f78f9-6ef4-460e-bbeb-bb50ad1b6432`。
- 官方 full：`npm test` → exit `0`，207 files PASS，2252 tests PASS，1 existing skipped，0 failed。旧 P03 exact-five ENOENT/source 阻断已确认消失，未使用 exclude 或忽略失败。
- quality/build：`npm run typecheck`、P02 scoped `npx eslint --ext ts,tsx --report-unused-disable-directives --max-warnings 0 libs/server/db/src tests/db tests/integration/db`、`npm run lint:saas` 均 PASS；`npm run build:saas` 为 9 workspaces / 18 fresh artifacts；`npm run build`、`npm run compile:electron` PASS，仅既有 Vite/eval/chunk warning。
- 交接前操作：提交本回归记录后，重跑 trusted validator 与 P02 关键 provenance/merge 测试，确认 clean tree 再唤起同一 Reviewer。Reviewer 通过前不启动 Tester，不修改总体表。

## Round 5 · 2026-07-12 · 协调者权威总表 provenance 闭环

### Red

- 协调者 commit `703b20a8` 只修改权威 `00-总体完成表.md`，trusted provenance 仍将其视为 non-evidence；revert `cde725ec` 由于逐提交历史审计仍失败。当前根回归精确为 `p03-merge-evidence` 与 `reviewer-round2-red` 各 1 项失败。
- `5f9dbcb5 test(db): capture coordinator table provenance gap`：在真实临时 Git 历史新增 7 项 governance corners。Red 为 13 项中唯一的“精确总表普通修改”失败，其余 12 项均按预期拒绝或通过。

### Green / Refactor

- `352d0caa fix(db): allow exact coordinator table updates`：`evidence-provenance.mjs` 将 name-status 与路径一起交给 allow predicate；validator 只对完整路径 `改造计划/20260711_V2单租户Web闭环开发/00-总体完成表.md` 且 status=`M` 放行。
- 不使用目录、basename、后缀或任意 docs allowlist。相邻 `05-计划自检报告.md`、嵌套同名、`.evil` 后缀、总表删除、总表 rename 到原本允许的 task 记录、copy 到相邻计划文档和代码改动均 fail-closed。
- targeted `tester-evidence-boundary + validator-mutations` 为 2 files / 34 tests PASS。

### Round 5 原生证据链

- codeEvidenceSha：`352d0caaa1c0ea992f6a48ee8c698d56df25309c`；raw evidence-only commit：`2f8e33e42d12249470a3b800612338ac448d0751`。
- raw runIds：contracts `e6c7b8ea-272a-42b8-ae50-e96642f34313`、preflight `a2d17b66-259c-4fe7-a51d-be188c309907`、integration `17184ecf-d751-4ec8-8569-8d7cf7be4b4f`、validation `7543b596-692a-4b5a-9b5b-27521b006e88`。真实 PostgreSQL 17 为 24/24、`skipped=0`、container removed。
- stage 在 `2f8e33e4...` 上 PASS，invocation `bf925a14-ae99-41e1-9aa0-dab7fbcda8b7`，runner=`af982ca6...`，manifest=expectedManifest=`658fac5b...`。
- stage/docs 提交后从 clean HEAD 重跑 trusted suite、官方 full、type/lint/build 并追加最终回归记录。本轮不修改总表勾选、Reviewer、Tester 或 P03 文件。

## B00-4 Round 6 · 2026-07-12 18:44 +0800 · `/root/p02_b00_4_developer` · 双平台 PostgreSQL policy candidate

### 范围与起点

- 执行基线为 Planner v4 commit `550b804f`，P03 B00-3A/3B/3C 协调接受 commit 为 `047f4ae0`；开工前 tracked worktree clean。
- 本轮只实现 P02 §9/§10 的 B00-4 产品候选；未重写 Prisma schema、已发布 migration、RLS、seed 或旧 evidence JSON，未执行 B00-6 snapshot，未修改 Reviewer/Tester/总体表/P03 live evidence/registry/signature。
- 环境为 Node `v24.18.0`、npm `11.16.0`、Docker client/server `28.3.3`、原生 `linux/amd64`。OCI index 实际解析的 amd64 digest 为 `sha256:9cc09bb9a1b9da469658a6fab7bbced9ece6ca99174e1b93c1c4cc1a12f741cf`；实际 pull 后 inspect 为 `linux/amd64`，RepoDigest 为 `postgres@sha256:9cc09b...741cf`。保留既有 arm64 digest `sha256:17b6c778de50f4bb9a878c36e736110fbcd9b7020377d6fdfdf20f7c0347e40a`。

### TDD Red → Green 提交链

- `de47cfa7 test(db): define PostgreSQL image policy v2`：首个纯单元 Red 因 `postgres-image-policy.mjs` 不存在而 `ERR_MODULE_NOT_FOUND / FAIL 1`；不依赖 Docker。
- `f2a002e7 test(ci): define PostgreSQL arm64 gate stages`：旧单 job workflow 的 platform/evidence/artifact 四项均失败。
- `d3d2c286 feat(db): add PostgreSQL image policy v2`：duplicate-key-safe 严格 v2 manifest、固定 `[linux/amd64, linux/arm64]` 顺序及唯一 selector；不保留 v1 兼容分支。
- consumer Red：在 v2 manifest 上原样运行旧 `test:db:preflight`，稳定得到 `Cannot read properties of undefined (reading 'replace') / FAILED 1`，证明 preflight 仍直读 v1 shape。
- `c0f7e586 test(db): define PostgreSQL runtime fact checks` → `b911a5fd fix(db): enforce approved PostgreSQL runtime facts`：三个 consumer 统一使用 selector；inspect OS/arch/RepoDigest、PG 17.10 patch、locale/ctype、timezone、SSL 均 fail closed。
- `7a527e92 test(db): define native platform artifact boundary` → `c8258311 ci(db): split PostgreSQL platform and evidence gates`：新增 source-bound artifact validator；CI 拆为 pre-freeze `db-platform-arm64` 与 post-freeze `db-evidence-arm64`，依赖方向固定为 evidence needs platform。
- `bbfd0ed3 test(db): define PostgreSQL cleanup fault evidence` → `1ee030b6 fix(db): fail closed on PostgreSQL cleanup faults`：stop fault 记录 `attempted=true, removed=false` 并令门失败；startup 前无 container 记录 `attempted=false`。
- `4baa4433 test(db): define migration lifecycle evidence`：真实 runner 27 项中新增 3 项因 lifecycle evidence 缺失失败，旧 24 项仍通过且 container removed。
- `ccc0bf86 test(db): verify migration recovery and concurrency`：空库/重复/已有 schema deploy；显式事务中的失败 migration 不留 partial table，`migrate resolve --rolled-back` 后修复重跑；两个并发 deploy 均 exit 0、只留一条 completed migration。
- `e2182d9b test(db): define PostgreSQL stage source boundary` → `647cc57b chore(db): bind PostgreSQL stage source fixtures`：policy/parser/consumer/runtime/workflow/tests 纳入 stage fixtures；manifest SHA-256 固定为 `e998e6bfe5b42594fa72040e8d2fef984ae5a01815e0069a8a27736e1bcab0d6`，package 与 scaffold checker 同步。
- `d5863371 test(db): expose coordinated stage fixture removal` → `a8761ab5 fix(db): require complete PostgreSQL stage fixtures`：即使删除 policy fixture并同时更新 package/checker digest，static gate 仍非零。
- `edec4dd3 test(db): reproduce structured migration artifact drift` → `6c18c277 fix(db): validate structured migration artifacts`：真实报告暴露旧 validator 仍期待 rollback boolean；修复为逐字段验证 structured rollback/concurrent facts。
- `6c8db08f refactor(db): derive integration evidence counts`：从 Vitest 原生输出提取当前 count，不硬编码 27；本轮本机最终 code candidate 与 P03 product source 均为 `6c8db08f5b6e80fb5b6926b6b89724fcae740196`。

### 本机 linux/amd64 最终 Green

- `npm run test:db:preflight`：PASS，runId `f893ec87-9174-405d-90bf-ce81d58da99d`；immutable ref 为 amd64 exact digest；PostgreSQL `17.10 (Debian 17.10-1.pgdg12+1)`，`lc_collate/lc_ctype=en_US.utf8`，timezone `Etc/UTC`，SSL `off`，container `4b8fd9...3937`，`attempted=true/removed=true`。
- `npm run test:db:integration`：5 files / 27 tests PASS，`skipped=0`，runId `3f839d29-7cc7-4118-80e1-901bc3e74bfd`；first/repeat/existing、rollback/repair、concurrent `[0,0]`，RLS/extension/seed/context 原有角度保持 Green；container `5a6b2f...1337`，`attempted=true/removed=true`。
- `validate-platform-artifact.mjs --source-sha 6c8db08f... --platform linux/amd64`：PASS；preflight/integration source、digest/reference、OS/arch、RepoDigests、PG settings、27/27、migration 与 cleanup 全部对账。
- B00-4 targeted：8 files / 81 tests PASS。全 DB unit 为 13 files、120 PASS / 2 FAIL；两项均为计划内旧 evidence 对当前 `package.json` 的 `trusted file mismatch`，不是本轮 policy/DB 失败。
- 官方 `npm test`：218 files 中 216 PASS / 2 FAIL；2404 tests 中 2396 PASS / 2 FAIL / 6 existing skipped。失败仍精确为 `p03-merge-evidence` 与 `reviewer-round2-red` 的旧 P02 evidence mismatch；未 exclude、未改断言、未写假 PASS。
- `node scripts/db/validate-static.mjs`、Node checks、`npm run typecheck`、P02 scoped ESLint、`npm run lint:saas`、`check-saas-scaffold`、Docker static、workflow YAML parse、contracts gate 与 15/15 contract checks均 PASS。无 P02/Testcontainers/Ryuk container 或 `/tmp/lobsterai-p02-*` fixture 残留。

### 外部门、交接与风险

- 当前状态：`WAITING_EXTERNAL_CI`，不是 B00-4/P02 `PASSED`。本轮未获推送授权且明确不推送，因此不能生成或虚构 arm64 GitHub run。
- `db-platform-arm64` 所需事实：实际 workflow checkout/source SHA、原生 runner `ubuntu-24.04-arm`、artifact 名 `db-platform-arm64-${SAAS_SOURCE_SHA}`、run URL/id、artifact digest、arm64 exact image/inspect、PG 17.10 settings、27/27、`skipped=0`、cleanup removed。本地 executable/P03 product candidate 是 `6c8db08f...`，后续 Developer 记录为 docs-only，不推进 product source；外部 CI 尚未触发，因此 workflow source SHA、run URL/id 与 artifact digest 均保持缺失，不填示例值。
- 获得同 source 的 arm64 Green/artifact 后交 P03 B00-5A。由于本轮修改 `scripts/db/**`、workflow 与 tests，P03 product source 已推进到 `6c8db08f...`，旧 exact-five/live artifact 必须作废重建。
- 只有 P03 B00-5A live/required artifact 稳定后，P02 Developer 才能按 §9.5/§10.8 执行 B00-6 两段 evidence freeze；当前旧 evidence 的两项失败正是预期 fail closed，不得提前重冻结或手改 JSON。

## B00-4 Round 7 · 2026-07-12 19:16 +0800 · `/root/p02_b00_4_developer` · Reviewer 返工

### Reviewer 结论与返工边界

- 独立 Reviewer commit `57440260` 给出 `CHANGES_REQUESTED_B00_4`：三项 P1 分别是 integration skip 可伪绿、pre-freeze CI/evidence 互锁、existing-schema 并非独立已有库；一项 P2 是 platform artifact 对 schema 和文件系统边界校验不严。
- 本轮只修复上述四项，不执行 B00-6 freeze，不改旧 P02 evidence JSON、P03 live evidence、Reviewer/Tester 文档或总体表，不把本机 amd64 结果冒充 arm64 外部证据。

### Red → Green 提交链

- skip 证据：`a0d6ab5d` Red 暴露跳过用例仍可通过；`ca1a4b0b` Green 切换为 Vitest JSON 机器结果；`83b87637` 追加失败路径会丢失 result counts 的 Red，`4c537820` Green 保留失败报告的完整 counts。
- existing-schema：`86d7930c` Red 要求独立已有库 migration；首次直接在非空 schema 执行 Prisma deploy 得到真实 `P3005`，证明不能把“第三次同库 deploy”写成已有库证据；`f1044e1e` 再以 artifact facts 绑定 Red，`bc15c739` Green 使用独立 `p02_existing_*` DB、baseline migration 与当前 migration 完成两阶段可管理升级。
- artifact 边界：`baeb2c90` Red 覆盖 unknown/missing/duplicate fields、symlink、FIFO 和超大文件；`165d65ac` Green 在读取前 `lstat`，要求普通非链接文件、真实路径不越界、非空且不超过 1 MiB，并严格校验根对象及所有嵌套对象 exact fields；cleanup 必须 `attempted=true/removed=true`。
- CI 双状态：`6c720657` Red 复现未冻结阶段官方 `npm test` 消费旧 evidence 且 evidence job 无条件运行的死锁；`d16d7820` Green 引入 `P02_EVIDENCE_PHASE=pre-freeze|post-freeze`、显式 `workflow_dispatch.p02_evidence_ready` 和 required aggregator。
- source fixtures：`03c15f11` Red 证明新 runner/test/CI 文件未入 stage 边界；`32cc4484` Green 绑定完整 fixtures，stage manifest SHA-256 为 `b851390fc303343ecf920d8c91c39c048728b1fdb8107cd2f508450b5c5facae`。

### 可机读证据与 corner cases

- integration runner 使用 Vitest `--reporter=json --outputFile=<temp>/integration.json`，严格校验 `passed + failed + skipped + todo = total`；任一 failed/skip/todo、JSON 不可解析或 counts 不一致都以 `FAILED/1` 结束。
- 在独立 detached worktree 对 `4c537820` 执行 Reviewer 的精确 `test.skip` mutation：exit `1`，报告 `FAILED`，`passed=26, skipped=1, total=27`，runId `8a096b36-53fc-4eed-bf2f-6d23b0ae9dd9`，cleanup `attempted=true/removed=true`；临时 worktree 已删除。
- 最终真实 amd64 integration 的 existing-schema facts：独立 DB，before tables 为 `_prisma_migrations,p02_preexisting_catalog`，after 新增 `agents,tenants`，完成 migration 数 `2`，marker 保留；before/after catalog SHA-256 分别为 `8e916f6a809ab5719497c8a6e4d4a2b0facf454b7575f42aff18279743d90518` / `f74e7b2391c8589c7375f85a4f9714cf20ceb7ff0e3d095f1dc0805b8e7ac584`。
- artifact mutation 定向 13/13 PASS；FIFO 在读取前拒绝，不再阻塞。CI required aggregator 状态表：platform 非 success 一律 FAIL；ready=false 且 evidence=skipped 为 `WAITING_POST_FREEZE/0`；ready=false 但 evidence 运行则 FAIL；ready=true 且 evidence=success 为 `POST_FREEZE_PASSED/0`；ready=true 时 evidence skipped/failure 均 FAIL；非法 ready 值 FAIL。

### 最终验证与当前状态

- executable HEAD `4c537820d8355887cc708d925d1728d4307f24c4`。`test:db:preflight` PASS，runId `6d46b4b5-d740-40f5-a97b-7f5317ff7baf`；`test:db:integration` 27/27 PASS、0 failed/skipped/todo，runId `c251820b-394f-4c0c-a27c-0059c3dfe5e0`；两者 cleanup 均 `attempted=true/removed=true`。严格 platform artifact validator 对上述报告 PASS。
- Reviewer 返工定向回归：9 files / 98 tests PASS。`node --check`、两份 workflow YAML parse、`validate-static.mjs`、`scaffold:check`、`docker:static`、`typecheck`、`lint:saas`、`git diff --check` 均 PASS。无本轮 Testcontainers/PostgreSQL 容器或临时 fixture 残留。
- `P02_EVIDENCE_PHASE=pre-freeze npm test` 为 220 files / 2418 passed / 6 existing skipped / 0 failed，exit `0`。默认 post-freeze `npm test` 如实 exit `1`：218 files 通过、2 files 失败，2416 passed / 2 failed / 6 existing skipped；仅 `p03-merge-evidence` 与 `reviewer-round2-red` 因当前 source 之后 `package.json` 已改而报 `trusted file mismatch package.json`。这是 B00-6 前必须保留的 fail-closed，不是用 exclude 伪造 full PASS。
- 当前状态仍为 `WAITING_EXTERNAL_CI`：未推送、未有 arm64 workflow run URL/id/artifact digest，未执行 B00-6 freeze，不得标记 P02/B00-4 完成。请独立 Reviewer 重新审核四项返工；外部 arm64 证据仍由后续协调流程补齐。

## B00-4 Round 8 · 2026-07-12 19:49 +0800 · `/root/p02_b00_4_developer` · Reviewer Round 6 返工

### Reviewer 结论与边界

- 独立 Reviewer commit `bb6149a1` 仍给出 `CHANGES_REQUESTED_B00_4`，三项 P1 为：Vitest 真失败时 runner 先抛错导致失败 artifact 丢失计数；workflow 永久注入 pre-freeze 导致 B00-6 后反向死锁；existing-schema 没有可重算 catalog payload/hash 与批准 migration history/checksum。
- 本轮不改 Reviewer/Tester/总体表、旧 P02 evidence JSON 或 P03 live evidence，不执行 B00-6，不推送或伪造 arm64 证据。

### P1-A · 真失败 artifact Red → Green

- `88f42a4d test(db): expose failed integration artifact loss`：detached worktree 注入真实第 28 项 Vitest failure，旧 runner exit `1` 且 cleanup removed，但最终 `checks.testResults=undefined`；helper 的 skip/todo/failure 三项 Red 也证明“解析”与“全绿策略”被错误耦合。
- `c174c3cc` 固化完整 `FAILED` artifact 先被 strict schema 拒绝的 Red；`88bd3d43` 固化坏 JSON 后 base evidence/output hash/临时目录清理 Red。
- `d2604c09 fix(db): preserve failed integration evidence` Green：无论 Vitest process exit 为何，runner 都在 `finally` 先解析 outputFile、保存 provider/migration/RLS/role/seed 与真实 counts，再聚合 process/parse/policy/cleanup/write 错误并以非零退出。strict validator 可识别结构完整的 `FAILED` integration artifact，但 required gate 仍 exit `1`。
- 最终隔离故障回归 2/2 PASS：真失败 artifact 保留 `{passed:27, failed:1, skipped:0, todo:0, total:28}`、`checksPassed=27/checksTotal=28`与 cleanup removed；坏 JSON mutation 保留 base facts 及 64hex output hash、明确 parse error，两者都清空 `lobsterai-p02-vitest-report-*`。

### P1-B · CI pre/post 双向可达 Red → Green

- `373e97f6 test(ci): expose post-freeze workflow deadlock` 为 resolver、workflow 命令环境和反向状态建立 7 个 Red；`ec10afca` 以 stage fixture Red 防止 resolver 和 failure runner 逃出 source boundary。
- `4089f907 ci(db): derive trusted evidence phase from source` Green：scaffold 与 main CI 的官方 `npm test` 统一消费 `p02-evidence-phase` output，不再写死 pre-freeze。push/PR 在 trusted validator 有效时派生 post-freeze，source drift 时派生 pre-freeze；dispatch `true+valid` 才是 post，`false+stale` 才是 pre，`true+stale`、`false+valid`、非法 input 均非零。evidence job 与 required aggregator 原 fail-closed truth table 保持不变，无 `continue-on-error` 或 `|| true`。
- `8e2c9ff3` Red 追加非法 validator 失败分类；`c6f10f3a fix(ci): reject unaudited evidence phase failures` Green 只允许精确 trusted-file mismatch 或纯 provenance `non-evidence change after source SHA` 进入 pre-freeze，bootstrap integrity/missing trusted file、坏 manifest、schema/digest/runner 异常都使 resolver 自身失败，不把损坏伪装成“未冻结”。

### P1-C · existing-schema history/checksum Red → Green

- `f5a4c3d1 test(db): expose unverifiable migration history`：旧 validator 对 legacy 任意 `1×64/2×64` hash 真实 exit `0`；Red 同时定义 canonical before/after payload/hash、两条 ordered history，以及 fake hash、换 name/checksum、same-count duplicate、missing/extra、unfinished/rolled-back 共 9 类 mutation。`a6a10694` 将新 helper 纳入 stage source-boundary Red。
- `aa843532 fix(db): bind existing schema migration history` Green：共享 helper 从 canonical payload 重算 SHA-256，从 baseline SQL 与仓内 current migration SQL 重算批准 checksum；lifecycle 从 `_prisma_migrations` 有序读取 `migrationName/checksum/finished/rolledBack`；validator 同时执行 exact nested schema、payload/hash、名称/顺序/checksum/完成状态与 count 对账。24/24 artifact corners PASS。
- 真实值：before payload hash `9c770abd6b242f5d6f8fe7eec596e0f940aa3dc5214af6bdddd23bc53ce8dc24`，after `1b35b00c013a4970a5d706765dd716e3fe3488783ac960fbe57b0a3528a9672c`；baseline checksum `07df3c3d18d5825b4ff89edee462bb160422f5132624df82bdca8800e582431e`，current checksum `129f3ce0dd1164f30c1ea185b956989198457f6a2b10954a5eb9c160f5c31aca`；两条均 `finished=true/rolledBack=false`。

### clean executable HEAD 验证与交接

- executable/P03 product source 均为 `aa84353218b1fd37050b31450855f985a435f74c`；stage manifest SHA-256 为 `fb1c494ff78d6cd1ac72ae38216293239e0fdfc81bd642535fd711ad64a06c5c`。
- 真实 linux/amd64 preflight PASS，runId `a7e8d24f-e8cd-4d13-8bfc-fa03d416b2f6`；integration 27/27 PASS、0 failed/skipped/todo，runId `bff17853-b224-4b87-9ff4-541db019dfc1`；两个 container 均 cleanup `attempted=true/removed=true`。strict platform artifact validator 对同 source 的报告 PASS。
- 定向回归 10 files / 124 tests PASS；Node syntax、workflow YAML parse、`validate-static`、`scaffold:check`、`docker:static`、`typecheck`、`lint:saas`、`git diff --check` 全部 PASS。无本轮 PostgreSQL/Testcontainers/Ryuk、failure worktree 或 Vitest temp report 残留。
- `P02_EVIDENCE_PHASE=pre-freeze npm test` exit `0`：220 files / 2441 passed / 6 existing skipped / 0 failed。默认 post-freeze `npm test` 如实 exit `1`：218 files 通过、2 files 失败，2439 passed / 2 failed / 6 existing skipped；仅旧 evidence 对当前 `package.json` 的 trusted mismatch，必须保留到 B00-6。
- 当前仍为 `WAITING_EXTERNAL_CI`：没有同 source 原生 arm64 run URL/id/artifact digest，未做 B00-6，不得标记 B00-4/P02 PASS。请新的独立 Reviewer 审核 `88f42a4d..aa843532`；当前不授权 Tester。

## B00-4 Round 9 · 2026-07-12 20:08 +0800 · `/root/p02_b00_4_developer` · Reviewer Round 7 返工

### Reviewer 结论与限定范围

- 独立 Reviewer commit `f09ea3b7` 继续给出 `CHANGES_REQUESTED_B00_4`，本轮只修两项 P1：真实 provenance drift JSON 在 stderr 而现有 classifier 读 stdout；主 CI PR 默认 checkout 合成 merge SHA，与 trusted evidence first-parent 契约冲突。
- 本轮未改 DB runner/migration/artifact validator、Reviewer/Tester/总体表、旧 P02 evidence JSON 或 P03 live evidence，未执行 B00-6，未推送或伪造 arm64 证据。

### P1-A · 真实 launcher 诊断协议 Red → Green

- `0e379318 test(ci): expose real provenance diagnostics` Red：从历史有效冻结 HEAD `6c7bf0b9` 建立 detached worktree，线性追加普通 `src/p02-phase-drift-probe.ts` 提交；真实 trusted launcher exit `1`、stdout 为空、stderr 为唯一 `FAILED` JSON，含 code/stage 两条相同 commit/path 的 `non-evidence change after source SHA`。旧 classifier 抛 `failed outside the auditable source-drift states`，精确复现 Reviewer finding。
- 同一 Red 同时固定协议反向：JSON 在 stdout、前后噪声、多 JSON、stdout/stderr 冲突、status 0 但带 stderr、bootstrap/trusted mismatch、manifest/schema/digest 等均不得判为 stale。
- `c85b1161 fix(ci): parse trusted evidence diagnostics strictly` Green：status `0` 只接受 stderr 空且 stdout 唯一 `PASS` JSON；status `1` 只接受 stdout 空且 stderr 唯一 exact-fields `FAILED` JSON，必须恰有 code/stage 两条、同一 40hex commit 与变更路径的完整 provenance drift，才返回 stale；其余全部 fail。真实 launcher 与合成协议矩阵 23/23 PASS。

### P1-B · PR head checkout/source 对账 Red → Green

- `bcbbc805 test(ci): expose synthetic PR merge checkout` Red：workflow mutation 证明主 CI test job 缺少 `ref=${{ github.event.pull_request.head.sha || github.sha }}` 且两个 phase step 未传 source SHA。临时 Git 双父 fixture 从有效 PR head 与独立 base 构造合成 merge：PR head launcher PASS，merge SHA 因 evidence source 不在 first-parent 而 exit `1`，且不得被 classifier 当成 stale。
- `c039f9ab test(ci): bind evidence phase to checked out source` Red：要求 resolver 对工作流预期 source 与实际 `git rev-parse HEAD` 做 40hex 精确对账，旧实现没有该边界。
- `8447120b fix(ci): resolve evidence on the PR head` Green：主 CI test 与 SaaS scaffold 均使用 full history 并显式 checkout `${{ github.event.pull_request.head.sha || github.sha }}`；push/dispatch 继续回退 `github.sha`。两个 phase step 将同一表达式传为 `P02_SOURCE_SHA`，resolver 在运行 trusted launcher 前必须与实际 HEAD 完全相等。phase/checkout/双父拓扑 2 files / 33 tests PASS。
- workflow、resolver 和两份修改测试早已在 `prisma:validate` fixture 集合中，本轮没有新 fixture 路径，因此 stage manifest 内容与 SHA-256 `fb1c494ff78d6cd1ac72ae38216293239e0fdfc81bd642535fd711ad64a06c5c` 不需变更。

### clean executable HEAD 回归与交接

- executable/P03 product source 为 `8447120bd5bab0ad1520efaf38051a14c2d69e56`。本轮仅改 CI/resolver/tests，不重复生成或冒充新 source 的 amd64 DB 报告；上一轮报告仅属于 `aa843532...`，对新 source 已作废。
- 真实 failure runner 2/2 PASS：注入第 28 项失败与 malformed JSON 两条路径均保留结构化失败 artifact 并清理容器/临时报告；其中 parse-failure 场景的底层 27 项 DB 测试全绿后才人为损坏 JSON。history/artifact/phase/PR 定向 4 files / 59 tests PASS，existing-schema 10 类 mutation 仍全部拒绝。
- `P02_EVIDENCE_PHASE=pre-freeze npm test` exit `0`：220 files / 2452 passed / 6 existing skipped / 0 failed。默认 post-freeze `npm test` 如实 exit `1`：218 files 通过、2 files 失败，2450 passed / 2 failed / 6 existing skipped；仅 B00-6 前旧 evidence `trusted file mismatch package.json`。
- workflow YAML parse、`validate-static`、`scaffold:check`、`docker:static`、`typecheck`、`lint:saas`、`git diff --check` 全部 PASS。无本轮 failure/phase/PR worktree、PostgreSQL/Testcontainers/Ryuk 或 Vitest temp report 残留。
- 当前状态仍是 `WAITING_EXTERNAL_CI`：没有 `8447120b...` 同 source 原生 arm64 run URL/id/artifact digest，未执行 B00-6，不得标记 B00-4/P02 PASS。请新的独立 Reviewer 审查 `0e379318..8447120b`；当前不授权 Tester。

## B00-4 Round 10 · 2026-07-12 20:22 +0800 · `/root/p02_b00_4_developer` · Reviewer Round 8 单项返工

### 唯一 P1 与 Red

- 独立 Reviewer commit `ac873260` 指出 classifier 仍把 stale provenance 硬编码为恰好两条/单路径，且当前 candidate 的真实 `trusted file mismatch package.json` exit `86` 不能进入 pre-freeze。本轮只修该 classifier，不扩展到 DB/workflow/evidence。
- `1f4bda66 test(ci): expose complete stale evidence sets` Red 包含两类真实 CLI：当前 clean candidate 的 push/PR/dispatch(false) 都因 launcher exit `86` 而错误 exit `1`；从有效冻结 `6c7bf0b9` 同一线性提交新增两个 `src/*.ts` 时，validator 返回 code/stage × 两路径共 4 条纯 provenance error，再追加第二个线性提交后累计 6 条，旧 classifier 均抛错。
- Red 同时固定反向：缺 code/stage 配对、奇数、不同路径错配、重复 stage、provenance 混入 digest/schema 失败、非唯一 JSON、噪声/冲突流、非 package trusted mismatch 均必须 fail。

### Green 与验证

- `43722bf0 fix(ci): classify complete stale evidence sets` Green：exit `86` 只接受 stdout 空且 stderr 精确等于单行 `P02 evidence bootstrap: trusted file mismatch package.json`；exit `1` 的唯一 JSON 诊断不再限制 errors 数量，而是以 `change status + path + 40hex commit` 为键分组。每组必须恰有一条 `codeEvidenceSha` 和一条 `stageEvidenceSha`，两者字段同构、无重复；影响键集合只要非空就可包含任意多路径/线性提交。
- clean Green HEAD `43722bf01d09a4c6d62898a6aafb9be3ae2317b1` 的真实 resolver 矩阵：push、pull_request、workflow_dispatch(false) 均 exit `0`、`phase=pre-freeze`、`trustedEvidenceExitCode=86`；workflow_dispatch(true) exit `1`。有效冻结 fixture 的 4 条/6 条纯 provenance 集合均正确 stale，所有上述反向仍 fail。
- phase + workflow 回归 2 files / 38 tests PASS；workflow YAML parse、`validate-static`、`scaffold:check`、changed-test ESLint、`git diff --check` 全部 PASS。本轮未新增 fixture 路径，stage manifest SHA-256 仍为 `fb1c494ff78d6cd1ac72ae38216293239e0fdfc81bd642535fd711ad64a06c5c`。
- 本轮未运行无关 DB 容器测试，未改 DB 实现；无 phase worktree/temp 残留。当前仍为 `WAITING_EXTERNAL_CI`，没有 `43722bf0...` 同 source arm64 证据，未执行 B00-6，不得标记 B00-4/P02 PASS 或启动 Tester。请新的独立 Reviewer 审查 `1f4bda66..43722bf0`。

## B00-4 Round 11 · 2026-07-12 20:42 +0800 · `/root/p02_b00_4_developer` · Tester Round 1 返工

### Tester Red 与最小修复

- 独立 Tester commits `6b709486` / `c7830ff5` 给出历史结论 `LOCAL_TESTS_FAILED_B00_4`。Developer 未修改或弱化 Tester test/报告；原样重放 `p02-b00-4-tester-artifact-bindings` 精确为 1 PASS / 3 FAIL：根 platform、声明 serverMajor、cleanup containerId 三个矛盾 artifact 都被旧 validator 错误 exit `0`。
- `c418f899 test(db): bind Tester artifact fact fixtures` 先以 source-boundary Red 要求新 Tester test 进入 `prisma:validate` fixtures。
- `f6f88fa9 fix(db): bind platform artifact runtime facts` 做三项最小跨字段不变量：顶层 platform 归一化后必须同时等于请求 platform 与 provider dockerPlatform；provider.serverMajor 必须为与 serverVersion/批准 policy 严格解析值同型相等的整数 `17`；cleanup.containerId 必须是非空字符串且精确等于同报告 provider.containerId，FAILED/BLOCKED 若使用 null 则只允许两侧一致 null，PASS 不允许 null。
- 真实 amd64 首轮还暴露 Node 顶层事实为 `linux/x64`，不是 Docker canonical `linux/amd64`；`436f12f5 fix(db): normalize Node report platform aliases` 只在 validator 内将必须带 `linux/` 前缀的 Node arch 归一化，不放宽非 Linux、缺失/多余段或其他 platform。
- source fixture 集合已增加 Tester test，stage manifest SHA-256 从 `fb1c494f...` 更新为 `de20879e77d1bb9adc98248203164360f4cd47b8d5abddd6b852385efd244505`；package/scaffold command 同步。已审计既有跨字段：sourceSha 对请求、image/digest/immutable reference 对 policy、test counts 内部等式均已绑定；preflight/integration runId 设计上各自独立，只需非空，未扩展新不变量。

### 最终验证与交接

- Tester 原 4 项现为 4/4 PASS；既有 `postgres-platform-artifact` 24/24 PASS。结合 stage/scaffold 定向为 4 files / 37 tests PASS。Tester 的历史 FAIL 报告未被改写，需在 Reviewer 通过后回到同一 Tester 复测。
- clean implementation/product source `436f12f51247b15ace860d986e37073dd24a8091`：真实 linux/amd64 preflight PASS，runId `fe4f49f1-4b81-47dc-bd64-e70f059bc2bd`；integration 27/27 PASS、0 failed/skipped/todo，runId `9f90f690-64a4-4df0-9262-4da22ffd1821`；两个 container 均 cleanup `attempted=true/removed=true` 且 cleanup/provider ID 相等。strict platform artifact validator 对同 source 报告 PASS。
- workflow YAML parse、Node syntax、`validate-static`、`scaffold:check`、Tester/相关 TS ESLint、`git diff --check` 全部 PASS。无本轮 PostgreSQL/Testcontainers/Ryuk 或 Tester temp artifact 残留。
- 当前仍为 `WAITING_EXTERNAL_CI`：没有 `436f12f5...` 同 source arm64 run URL/id/artifact digest，未执行 B00-6，不得标记 B00-4/P02 PASS。请新的独立 Reviewer 审核 `c418f899..436f12f5`；Reviewer 通过后再交回同一 Tester 复测。

## B00-4 Round 12 · 2026-07-12 20:53 +0800 · `/root/p02_b00_4_developer` · Reviewer Round 10 单项 P2 返工

### Red → Green

- 独立 Reviewer commit `caff394e` 指出唯一 P2：顶层 Node platform 和 raw runnerArch 错误复用 Docker 宽 alias parser，导致不可能的 Node 原始事实仍可假绿。本轮未改 Tester 原断言/报告，未修其他 artifact 不变量。
- `7ddafa09 test(db): expose runtime platform aliases` 为 test-only Red：保留 canonical `linux/arm64 + runnerArch=arm64` 和 `linux/amd64 + runnerArch=x64` 有效对照；定向将两份报告改为顶层 `linux/x86_64`、`linux/aarch64`或 runnerArch `x86_64`、`amd64`、`aarch64`。旧 validator 的 focused 结果为 3 PASS / 5 FAIL，五个宽 alias 均是“期望 exit 1，实际 exit 0”；bare top `amd64|x86_64|aarch64` 的既有拒绝也被锁定。
- `e0e984dc fix(db): narrow runtime platform parsing` Green：Docker requested/provider dockerPlatform 仍使用原 canonical policy parser，不修其 alias 集。strict artifact validator 新建独立窄 parser：顶层仅允许 `linux/amd64`、`linux/arm64` 及真实 Node 必要特例 `linux/x64 → linux/amd64`；raw runnerArch 仅允许 Node 真实 `x64|arm64`。`linux/x86_64`、`linux/aarch64`、bare alias 和 runner 宽 alias 全部 FAILED/1。

### 验证与交接

- Tester 原 4 项 + 扩展后 artifact 33 项合计 2 files / 37 tests PASS；Tester test blob/历史 FAIL 报告未改。本轮只修已在 fixture 中的 validator/test 内容，无新 fixture 路径，stage manifest SHA-256 仍为 `de20879e77d1bb9adc98248203164360f4cd47b8d5abddd6b852385efd244505`。
- clean implementation/product source `e0e984dcef235fc601a7156d137383e8bef5b6fb`：真实 linux/amd64 preflight PASS，runId `af9dbd75-4d6e-4e5a-bc06-619c21f599af`；integration 27/27 PASS、0 failed/skipped/todo，runId `ab7738bf-0255-48a2-8b5d-97441ada9253`；真实顶层 `linux/x64`、runnerArch `x64`、provider canonical `linux/amd64` 经窄映射对账，strict artifact PASS，cleanup 均 removed。
- workflow YAML parse、Node syntax、`validate-static`、`scaffold:check`、相关 TS ESLint、`git diff --check` 全部 PASS。当前仍为 `WAITING_EXTERNAL_CI`：没有 `e0e984dc...` 同 source arm64 run URL/id/artifact digest，未执行 B00-6，不得标记 B00-4/P02 PASS 或回交 Tester。请新的独立 Reviewer 审查 `7ddafa09..e0e984dc`。
