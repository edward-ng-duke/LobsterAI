# P02 · PR-2 Prisma/RLS scaffold

- 状态：TODO
- 依赖：P00；P01 统一 ID/字段命名。
- 权威输入：根 AGENTS、19、17 对应 V1/V2、16、附录 C；冲突以根 AGENTS 的生产断公网和附录 C 契约决策为准。

## 源码边界

prisma/schema.prisma、migrations/rls/seed、libs/server/db；不写完整 V2 repository。

## TDD：先写的失败测试

先写 schema/migration/preflight、ENABLE+FORCE 样例、非 superuser Testcontainers 失败测试。 Red 必须因目标行为缺失而失败；环境缺失另记 BLOCKED，不算 Red。

## Corner cases

citext/pgcrypto 缺失、重复 migrate、空 tenant context、SET 与 SET LOCAL、BYPASSRLS 误测。

## 实施与验收

Developer 按最小垂直切片逐个 Green，避免无关重构。验收命令：npm run prisma:validate；相关 Testcontainers；npm test；npm run typecheck。

## 回滚

只允许向前兼容/可逆的脚手架迁移；测试库重建验证，不对真实库手工 down。

## 完成证据

migration/preflight 输出、RLS 查询 0 行/报错证据、seed 无密钥扫描。 证据须绑定 SHA/runId/环境；所有触碰 TS 文件 changed-file ESLint 0 warning，相关 Vitest 全绿。

## Developer → Reviewer → Tester 循环

Developer 记录每轮 Red/Green 与 diff → 独立 Reviewer 对照本文件和源码审核；若 CHANGES_REQUESTED，退同一 Developer 修复并进入下一轮 → Reviewer APPROVED_FOR_TEST 后独立 Tester 新增 corner-case/回归测试；Tester FAILED 必须退 Developer 且修复后重新 Reviewer → Tester PASSED 后由协调者核验证据并勾总体表。任何角色不得自行勾 Todo。

