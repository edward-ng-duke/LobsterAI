# 测试报告

> 初始为空白；仅在 Reviewer APPROVED_FOR_TEST 后由独立 Tester 填写。

- Tester Agent：
- SHA/环境/namespace/digest：
- 结论：\`FAILED | PASSED\`

| 用例 ID | 独立角度 | 测试文件/脚本 | 结果 | 证据 |
|---|---|---|---|---|

## 回归、非功能与交接

## B00-4 Round 1 · 2026-07-12 20:34 +0800 · `/root/p02_b00_4_tester`

- 启动门：Reviewer commit `ad8855244b5db9b275641abc5b48692e7cb12a92` 明确允许新的独立 Tester 只追加本地 corner tests。实现 source 为 `43722bf01d09a4c6d62898a6aafb9be3ae2317b1`；本 Tester 未参与 Planner、Developer、Reviewer 或协调者角色。
- Tester Red commit：`6b709486` (`test(db): expose platform artifact fact drift`)，只新增 `tests/db/p02-b00-4-tester-artifact-bindings.test.ts`，未修改实现、旧测试、workflow、evidence JSON、P03 live evidence 或总表。
- 最终结论：`LOCAL_TESTS_FAILED_B00_4`。独立有效对照通过，三个互相矛盾的机器证据均被 `validate-platform-artifact.mjs` 错误接受为 exit `0/PASS`，可导致原生平台证据假绿。依 fail-fast 协议立即退回同一 P02 Developer，本轮不继续添加其他 Green corner，不由 Tester 修实现。

### 独立 Red 与阻断缺陷

| 严重级 | 用例/要求 | 实现位置 | 期望 | 实际 | 完成证据 |
|---|---|---|---|---|---|
| P1 | B00-4 平台 artifact 根事实绑定 | `scripts/db/validate-platform-artifact.mjs:119-120,226-263` | `report.platform` 必须精确等于请求的归一化原生平台，并与 provider Docker/runner 事实一致。 | 将有效 preflight 的顶层 `platform` 改为 `linux/amd64`，同时 provider/request 仍为 `linux/arm64`，validator 仍 exit `0`。 | 新 Red 修复后 exit `1`；同时保留完整 artifact 对照 exit `0`。 |
| P1 | B00-4 PostgreSQL major/patch 双重事实 | `scripts/db/postgres-image-policy.mjs:203-228`；`validate-platform-artifact.mjs:262` | artifact 声明的 `provider.serverMajor` 必须为整数 `17`，并与从 `SHOW server_version=17.10...` 解析的 major 一致。 | 将 integration `provider.serverMajor` 改为 `16`，保留 `serverVersion=17.10...`，validator 仍 exit `0`；当前 helper 只返回重新解析的 `17`，忽略被验 artifact 的声明值。 | 新 Red 修复后 exit `1`，并增加 non-integer/string/NaN-equivalent JSON 边界。 |
| P1 | B00-4 cleanup 容器绑定 | `scripts/db/validate-platform-artifact.mjs:113-115,240` | `cleanup.containerId` 必须非空且精确等于同份报告的 `checks.provider.containerId`；`attempted=true/removed=true` 不能证明清理了另一个容器。 | 将 integration cleanup ID 改为 `unrelated-container-id`，provider 仍为 `database-container-id`，validator 仍 exit `0`。 | 新 Red 修复后 exit `1`，并覆盖空 ID、类型错误及 preflight/integration 各自绑定。 |

### 新鲜验证

- `npx vitest run tests/db/p02-b00-4-tester-artifact-bindings.test.ts --reporter=verbose`：exit `1`，1 file / 4 tests，1 PASS / 3 FAIL。完整独立 fixture 通过；三个 mutation 都因“预期 exit 1，实际 exit 0”稳定失败，不是语法、Docker 或网络失败。
- `npx vitest run tests/db/postgres-platform-artifact.test.ts --reporter=verbose`：exit `0`，1 file / 24 tests PASS。旧套件的 regular file/symlink/FIFO/1 MiB/duplicate key/strict FAILED/count/history/hash 边界仍为 Green，证明新 Red 是新的交叉事实缺口。
- `npx eslint --ext ts,tsx --report-unused-disable-directives --max-warnings 0 tests/db/p02-b00-4-tester-artifact-bindings.test.ts`：exit `0`，0 warnings。`git diff --check`：exit `0`。
- 本轮只运行纯本地 artifact CLI fixture，未启动 PostgreSQL/Testcontainers，未创建或删除任何共享 Docker 资源。所有 `lobsterai-p02-tester-artifact-*` 临时目录由 `afterEach` 删除。

### 退回 Developer 与硬门边界

- 同一 P02 Developer 应使上述三个 Red 全部 Green，并保留现有 24/24 artifact 边界。修复应在 validator 明确做 cross-field equality/type checks，不应通过放松 fixture 或删除 mutation 消除 Red。
- 新 Tester test commit 已改变 tracked executable/source boundary。依 P02 §10.4/§10.8，Developer 修复后必须同步 stage fixtures/external digest/source allowlist，作废当前 candidate 的旧 platform/live/freeze evidence，重跑本地 amd64、同 source 原生 arm64、P03 B00-5A 与 P02 B00-6，再交新的独立 Reviewer 和独立 Tester。
- 因已有 P1 Red，本轮未运行真实 amd64 preflight/integration、failure probes、官方全量或 post-freeze default；不将未运行项写为 PASS。
- 本 Tester 没有原生 arm64 run URL/id/artifact digest，也未执行 arm64 容器。本结论不是 B00-4/P02 `PASSED`，不允许进入 B00-6 freeze，不允许协调者勾选总表。剩余 policy/runtime/phase/migration/cleanup corner matrix 留待 Developer 修复、新 Reviewer 通过后由新的独立 Tester 继续。
