# 测试报告

> 初始为空白；仅在 Reviewer APPROVED_FOR_TEST 后由独立 Tester 填写。

- Tester Agent：
- SHA/环境/namespace/digest：
- 结论：\`FAILED | PASSED\`

| 用例 ID | 独立角度 | 测试文件/脚本 | 结果 | 证据 |
|---|---|---|---|---|

## 回归、非功能与交接

## B00-4 Round 1 · 2026-07-12 20:34 +0800 · `/root/p02_b00_4_tester`

- 启动门：Reviewer commit `ad8855244b5db9b275641abc5b48692e7cb12a92` 明确允许新的独立 Tester 只追加本地 corner tests。实现 source 为 `43722bf01d09a4c6d62898a6aafb9be3ae2317b1`；本 Tester 未参与 Planner、Developer、Reviewer 或协调者角色。
- Tester Red commit：`6b709486` (`test(db): expose platform artifact fact drift`)，只新增 `tests/db/p02-b00-4-tester-artifact-bindings.test.ts`，未修改实现、旧测试、workflow、evidence JSON、P03 live evidence 或总表。
- 最终结论：`LOCAL_TESTS_FAILED_B00_4`。独立有效对照通过，三个互相矛盾的机器证据均被 `validate-platform-artifact.mjs` 错误接受为 exit `0/PASS`，可导致原生平台证据假绿。依 fail-fast 协议立即退回同一 P02 Developer，本轮不继续添加其他 Green corner，不由 Tester 修实现。

### 独立 Red 与阻断缺陷

| 严重级 | 用例/要求 | 实现位置 | 期望 | 实际 | 完成证据 |
|---|---|---|---|---|---|
| P1 | B00-4 平台 artifact 根事实绑定 | `scripts/db/validate-platform-artifact.mjs:119-120,226-263` | `report.platform` 必须精确等于请求的归一化原生平台，并与 provider Docker/runner 事实一致。 | 将有效 preflight 的顶层 `platform` 改为 `linux/amd64`，同时 provider/request 仍为 `linux/arm64`，validator 仍 exit `0`。 | 新 Red 修复后 exit `1`；同时保留完整 artifact 对照 exit `0`。 |
| P1 | B00-4 PostgreSQL major/patch 双重事实 | `scripts/db/postgres-image-policy.mjs:203-228`；`validate-platform-artifact.mjs:262` | artifact 声明的 `provider.serverMajor` 必须为整数 `17`，并与从 `SHOW server_version=17.10...` 解析的 major 一致。 | 将 integration `provider.serverMajor` 改为 `16`，保留 `serverVersion=17.10...`，validator 仍 exit `0`；当前 helper 只返回重新解析的 `17`，忽略被验 artifact 的声明值。 | 新 Red 修复后 exit `1`，并增加 non-integer/string/NaN-equivalent JSON 边界。 |
| P1 | B00-4 cleanup 容器绑定 | `scripts/db/validate-platform-artifact.mjs:113-115,240` | `cleanup.containerId` 必须非空且精确等于同份报告的 `checks.provider.containerId`；`attempted=true/removed=true` 不能证明清理了另一个容器。 | 将 integration cleanup ID 改为 `unrelated-container-id`，provider 仍为 `database-container-id`，validator 仍 exit `0`。 | 新 Red 修复后 exit `1`，并覆盖空 ID、类型错误及 preflight/integration 各自绑定。 |

### 新鲜验证

- `npx vitest run tests/db/p02-b00-4-tester-artifact-bindings.test.ts --reporter=verbose`：exit `1`，1 file / 4 tests，1 PASS / 3 FAIL。完整独立 fixture 通过；三个 mutation 都因“预期 exit 1，实际 exit 0”稳定失败，不是语法、Docker 或网络失败。
- `npx vitest run tests/db/postgres-platform-artifact.test.ts --reporter=verbose`：exit `0`，1 file / 24 tests PASS。旧套件的 regular file/symlink/FIFO/1 MiB/duplicate key/strict FAILED/count/history/hash 边界仍为 Green，证明新 Red 是新的交叉事实缺口。
- `npx eslint --ext ts,tsx --report-unused-disable-directives --max-warnings 0 tests/db/p02-b00-4-tester-artifact-bindings.test.ts`：exit `0`，0 warnings。`git diff --check`：exit `0`。
- 本轮只运行纯本地 artifact CLI fixture，未启动 PostgreSQL/Testcontainers，未创建或删除任何共享 Docker 资源。所有 `lobsterai-p02-tester-artifact-*` 临时目录由 `afterEach` 删除。

### 退回 Developer 与硬门边界

- 同一 P02 Developer 应使上述三个 Red 全部 Green，并保留现有 24/24 artifact 边界。修复应在 validator 明确做 cross-field equality/type checks，不应通过放松 fixture 或删除 mutation 消除 Red。
- 新 Tester test commit 已改变 tracked executable/source boundary。依 P02 §10.4/§10.8，Developer 修复后必须同步 stage fixtures/external digest/source allowlist，作废当前 candidate 的旧 platform/live/freeze evidence，重跑本地 amd64、同 source 原生 arm64、P03 B00-5A 与 P02 B00-6，再交新的独立 Reviewer 和独立 Tester。
- 因已有 P1 Red，本轮未运行真实 amd64 preflight/integration、failure probes、官方全量或 post-freeze default；不将未运行项写为 PASS。
- 本 Tester 没有原生 arm64 run URL/id/artifact digest，也未执行 arm64 容器。本结论不是 B00-4/P02 `PASSED`，不允许进入 B00-6 freeze，不允许协调者勾选总表。剩余 policy/runtime/phase/migration/cleanup corner matrix 留待 Developer 修复、新 Reviewer 通过后由新的独立 Tester 继续。

## B00-4 Round 2 · 2026-07-12 21:04 +0800 · `/root/p02_b00_4_tester`

- 复测启动门：Reviewer commit `e85bce97fa9b05529bb10dab2fd6c0faf4db46de` 给出 `LOCAL_QUALITY_APPROVED_FOR_TESTER_RECHECK_WAITING_ARM64_B00_4`，允许同一 Tester 重放 Round 1 Red 并追加少量本地组合 corner。Developer implementation/product source 为 `e0e984dcef235fc601a7156d137383e8bef5b6fb`。
- Tester corner commit：`c87ae6732d6699d4393c90018a2540e44ed29d7f` (`test(db): cover artifact fact combinations`)。只追加 Tester mutation，未修改 validator、policy、workflow、Developer/Reviewer 记录、evidence JSON、P03 live evidence 或总表。因 tracked test 已变，本轮所有真实 amd64 报告均在 `c87ae673...` 上重新生成，未复用 Reviewer 旧报告。
- 限定结论：`LOCAL_TESTS_PASSED_WAITING_EXTERNAL_ARM64_B00_4`。本地 linux/amd64 的 Round 1 三项缺陷、runtime alias、严格 artifact、PostgreSQL 17.10、migration/RLS/cleanup、phase 与官方 pre-freeze 回归均通过，未发现新的本地实现缺陷。

### Red 历史与独立组合 corner

- 在隔离 `git archive 6b709486` 中重放原 Tester 文件：1 PASS / 3 FAIL，exit `1`；三个失败仍精确为 platform/serverMajor/cleanup-container 矛盾被旧 validator 错误接受。archive 和临时目录已删除，未修改共享树。
- 当前 HEAD 的 Tester 文件 9/9 PASS：原四项加上 `linux/x64`/`x64` 不得跨配 arm64、`serverMajor='17'` 类型错误、PASS 下 null/空 container ID 拒绝，以及结构化 FAILED 下成对 null ID 只能到达 required-gate 失败而不被误判为 PASS。
- `tests/db/p02-b00-4-tester-artifact-bindings.test.ts` + `postgres-platform-artifact.test.ts`：2 files / 42 tests PASS。后者包含 canonical arm64、canonical amd64 + Node raw `x64`、宽 alias 拒绝、strict FAILED、regular/symlink/FIFO/size、counts/history/hash 等边界。
- policy 独立回归：1 file / 24 tests PASS；Docker selector 仍可正确处理其自身的 `linux/x86_64|linux/aarch64` alias，未被严格 runtime parser 误伤。

### 真实 linux/amd64 与反向故障证据

- `npm run test:db:preflight`：exit `0/PASS`，runId `7bbac4cd-7c5b-452d-888e-30c919eb890c`；source `c87ae673...`，Node `linux/x64`、Docker/inspect `linux/amd64`，digest `sha256:9cc09b...f741cf`，PostgreSQL `17.10`，locale/timezone/SSL 均匹配，container `74d8af3d...0a152` removed=true。
- `npm run test:db:integration`：exit `0/PASS`，runId `b90f81d4-71fd-4bd8-b3b2-5e38ad36e051`；27/27 PASS，failed/skipped/todo=0。空库+重复+已有 schema migration、历史 checksum、rollback repair、并发 deploy、非特权 RLS、seed 和 Tester 并发/context/extension corner 均通过；container `2dd553aa...e0090` removed=true。
- strict artifact validator 在 `.reports/db` 上以 source `c87ae673...`/platform `linux/amd64` exit `0/PASS`，精确读回上述两个 runId、27/27 和批准 digest。两个 container ID 后续 `docker inspect` 均不存在。
- phase/artifact/policy/cleanup/stage 组合 targeted：8 files / 115 tests PASS。`node --test tests/db/integration-failure-artifact.test.mjs`：2/2 PASS，验证真实 Vitest 失败与 JSON parse 失败都保留 base evidence/cleanup，不被终态 artifact 掩盖。

### 官方回归、静态门与交接

- `P02_EVIDENCE_PHASE=pre-freeze npm test`：exit `0`，221 files PASS，2475 tests PASS / 6 skipped / 0 failed；pretest 的 native rebuild、workspace clean build 和 contracts check-mode 同时成功。
- 默认 `npm test`：精确 219 files PASS / 2 files FAIL，2473 tests PASS / 2 FAIL / 6 skipped。两个失败仅为 `tests/db/p03-merge-evidence.test.ts` 和 `tests/db/reviewer-round2-red.test.ts` 报 `P02 evidence bootstrap: trusted file mismatch package.json`，与尚未 B00-6 freeze 的预期 post-freeze 状态一致；无第三项失败。
- 两份 workflow YAML parse、`node --check scripts/db/validate-platform-artifact.mjs`、`node scripts/db/validate-static.mjs`、`npm run --silent scaffold:check`、`npm run --silent lint:saas`、`git diff --check c7830ff5..HEAD` 全部 exit `0`。真实 resolver 在 `push`/current source 上输出 `PASS, phase=pre-freeze, trustedEvidenceExitCode=86`。
- 提交报告前 tracked worktree clean，无 `lobsterai-p02-*` 临时目录或本 Tester PostgreSQL/Testcontainers 容器残留。`.reports` 仍为 ignored 本地机器输出，未加入提交。
- 本 Tester 未运行 linux/arm64，没有与 `c87ae673...` 同 source 的 GitHub workflow run URL/id/artifact digest。因此本轮只是 `LOCAL_TESTS_PASSED_WAITING_EXTERNAL_ARM64_B00_4`，不是 B00-4/P02 `PASSED`；不得进入 P03 B00-5A/P02 B00-6 最终 freeze，不得协调接受 P02，不得勾选总表。后续必须以同 source 原生 arm64 证据为硬门；任何新 executable/source 变更都使本轮 amd64 事实作废。
