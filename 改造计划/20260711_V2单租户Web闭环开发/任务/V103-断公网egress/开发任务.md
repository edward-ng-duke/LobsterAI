# V103 · 生产断公网与沙箱 egress PoC

- 状态：TODO
- 依赖：V101；批准内网 Service 清单；NetworkPolicy 权限。
- 权威输入：根 AGENTS、19、17 对应 V1/V2、16、附录 C；冲突以根 AGENTS 的生产断公网和附录 C 契约决策为准。

## 源码边界

chart NetworkPolicy、探针 corpus、内网 audit/egress service；禁止新增公网出口。

## TDD：先写的失败测试

先写公网、metadata、K8s API、DB/Redis、其他 Pod 必须拒绝且批准内网模型/Cowork/DNS可达的测试。 Red 必须因目标行为缺失而失败；环境缺失另记 BLOCKED，不算 Red。

## Corner cases

IPv4/IPv6、DNS rebinding、CNAME、IP literal、HTTP redirect、DoH、hostNetwork、namespace/CIDR 粗放行。

## 实施与验收

Developer 按最小垂直切片逐个 Green，避免无关重构。验收命令：集群 allow/deny 探针；helm lint；网络审计报告 schema 校验。

## 回滚

回退 policy 必须保持 default deny；不得以放宽公网恢复服务。

## 完成证据

每个目标的实际 connect 结果、NetworkPolicy、审计日志；公网全失败。 证据须绑定 SHA/runId/环境；所有触碰 TS 文件 changed-file ESLint 0 warning，相关 Vitest 全绿。

## Developer → Reviewer → Tester 循环

Developer 记录每轮 Red/Green 与 diff → 独立 Reviewer 对照本文件和源码审核；若 CHANGES_REQUESTED，退同一 Developer 修复并进入下一轮 → Reviewer APPROVED_FOR_TEST 后独立 Tester 新增 corner-case/回归测试；Tester FAILED 必须退 Developer 且修复后重新 Reviewer → Tester PASSED 后由协调者核验证据并勾总体表。任何角色不得自行勾 Todo。

