# 审核意见

> 初始为空白模板。独立 Reviewer 必须读计划、全部 diff、Developer Red/Green 和原始网络证据；不得复用 Developer runId 或代填 Tester 结论。

## Round 1 · YYYY-MM-DD HH:mm · Reviewer/commit

- Reviewer Agent：
- 审核基线/commits：
- Developer runId（只作输入）：
- Reviewer 独立新 runId：
- context/cluster UID/namespace/CNI/RuntimeClass/node/image digest：
- approved endpoint registry version/hash：
- 结论：`BLOCKED | CHANGES_REQUESTED | APPROVED_FOR_TEST`

### 独立重放与需求核对

- Reviewer 新 runId 的 preflight、approved allow、public/metadata/K8s API/lateral deny、删 allow mutation、CNI/节点/网关审计重算：
- P00～P04/V101 前置、Day-0、AC01～AC24、PASS/FAILED/BLOCKED、旁路矩阵、证据/cleanup/回滚：

### 问题清单

| 严重级 | 用例/需求 | 文件:行 | 实际/期望 | 复现命令 | 要求证据 |
|---|---|---|---|---|---|

### 交接 Tester（仅 `APPROVED_FOR_TEST`）

- Reviewer 独立 runId 与证据路径：
- Tester 必须使用第三个全新 runId 重放的完整矩阵：
- 建议新增反向角度：

### BLOCKED 说明

- 缺独立集群/CNI/批准端点/审计权限或日志时写明缺口和非零证据，不得把环境不可用写成 `APPROVED_FOR_TEST`。
