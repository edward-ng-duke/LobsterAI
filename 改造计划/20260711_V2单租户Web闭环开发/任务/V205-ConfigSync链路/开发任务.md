# V205 · Config Sync 服务端链路化

- 状态：TODO
- 依赖：V106、V204。
- 权威输入：根 AGENTS、19、17 对应 V1/V2、16、附录 C；冲突以根 AGENTS 的生产断公网和附录 C 契约决策为准。

## 源码边界

Node config-sync module + runtime-orchestrator entrypoint/initContainer；不建立独立 configsync 服务。

## TDD：先写的失败测试

先写同输入 golden、secret 注入边界、原子落盘、workspace 文件 owner 和失败不留半配置测试。 Red 必须因目标行为缺失而失败；环境缺失另记 BLOCKED，不算 Red。

## Corner cases

并发 render、路径编码、缺 env/secret、旧配置损坏、重试、权限位、AGENTS managed block 幂等。

## 实施与验收

Developer 按最小垂直切片逐个 Green，避免无关重构。验收命令：golden unit；容器/Pod integration；密钥扫描；typecheck。

## 回滚

回退到上一 config schema/镜像 digest；失败 Pod 不启动，不能使用半配置。

## 完成证据

golden diff、Node import graph、initContainer 日志、配置无明文 secret。 证据须绑定 SHA/runId/环境；所有触碰 TS 文件 changed-file ESLint 0 warning，相关 Vitest 全绿。

## Developer → Reviewer → Tester 循环

Developer 记录每轮 Red/Green 与 diff → 独立 Reviewer 对照本文件和源码审核；若 CHANGES_REQUESTED，退同一 Developer 修复并进入下一轮 → Reviewer APPROVED_FOR_TEST 后独立 Tester 新增 corner-case/回归测试；Tester FAILED 必须退 Developer 且修复后重新 Reviewer → Tester PASSED 后由协调者核验证据并勾总体表。任何角色不得自行勾 Todo。

