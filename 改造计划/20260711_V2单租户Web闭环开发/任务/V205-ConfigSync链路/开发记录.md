# V205 开发记录

> 仅由独立 Developer 在 V107 GO、V106/V204 均被协调者接受后按 Round 追加。保留所有 Red/失败/回滚历史；Planner 冻结文档不代表实现开工或通过。

## 元数据与开工硬门

- 任务 ID：`V205`
- 状态：`BLOCKED_BY_PREREQUISITES | DEV_IN_PROGRESS | REVIEW_PENDING`
- Developer Agent：
- 开工 HEAD / merge-base：
- 计划版本 / 计划提交 SHA：
- Node/npm/OS/arch：
- PostgreSQL/K8s/RuntimeClass/OpenClaw/runtime image digest：
- feature flag / config schema / renderer version：
- 证据 runId / `docs/poc/YYYYMMDD_V205_ConfigSync服务端链路证据/<runId>/`：
- 独立 PG schema / namespace / PVC / temp root：
- 与本任务无关的 worktree 变更（只读列出，不提交）：

| 硬门 | 协调者接受 SHA/GO | Reviewer | Tester | 新鲜 gate / report hash | 结论 |
|---|---|---|---|---|---|
| V107 GO（含 P00～P04、V1.0～V1.6） |  |  |  | `scaffold/contracts/prisma/supply-chain/docker/helm/poc` |  |
| V106 pure renderer/golden/effect inventory |  |  |  | import/golden/shadow hash |  |
| V204 PG facade/Cowork/OpenClaw adapter |  |  |  | contract/integration hash |  |

任一行不完整时只记录 `BLOCKED`、missing artifact/owner/解除证据；禁止创建 V205 Red、migration、实现或 production manifest commit。

## 交付范围与复用账本

| 责任 | 权威输入/计划位置 | 实际文件/包 | shadow/canary/active | owner / 备注 |
|---|---|---|---|---|
| V106 pure renderer唯一实现 |  |  |  | 禁止server fork |
| PG snapshot provider/source registry |  |  |  |  |
| SecretRef/三向对账 |  |  |  | 无secret value |
| revision/apply ledger + migration/RLS |  |  |  | 不建替身领域表 |
| diff/no-op/reload/rebuild policy |  |  |  |  |
| orchestrator command/initContainer |  |  |  | 无独立deployable |
| `/state` effects/commit barrier |  |  |  |  |
| `/workspace/state` effects/managed files |  |  |  | D5 writer lease |
| leader/lease/fence/CAS |  |  |  |  |
| real OpenClaw validator |  |  |  | pinned version/digest |
| golden/shadow/mutation/security tests |  |  |  |  |
| V204调用 / V206边界 |  |  |  | 不改V206 controller |

## Round 1 · YYYY-MM-DD HH:mm · Agent/commit

### 开工门与三态

- 前置重放命令/exit/CST+UTC/report hash：
- V106实际 package/schema/renderer/golden/effect inventory 路径与source hash：
- V204实际 tenant/session/workspace/agent/config source接口与接受SHA：
- PG/Secret/K8s/OpenClaw必需输入缺口：
- 本轮结论：`DEV_IN_PROGRESS | REVIEW_PENDING | BLOCKED`

### Red

| T/AC | 先写测试或 mutation | 目标缺失导致的失败 | 命令 / exit | 隔离位置 / 证据 |
|---|---|---|---|---|
|  |  |  |  |  |

环境/依赖 BLOCKED、语法/导入错误、mock gateway 或旧报告不得填为有效 Red。mutation 的临时 worktree/schema/namespace/PVC 与 cleanup 必须记录。

### Green / Refactor

| T/AC | 最小实现 | 文件:行 | commit | Green命令/exit | 与Red对应证据 |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

若为 V106 renderer 机械搬迁，记录搬迁前后 source hash、desktop/server import target与golden byte hash；不得只写“复用完成”。

### PG snapshot、source completeness 与 SecretRef

- tenant role / transaction isolation / RLS / source query inventory：
- tenant/session/workspace/agent UUID↔logical_id 校验：
- provider/agent/IM/MCP/skill/plugin/sandbox/workspace/cron source registry `missing/extra`：
- 并发读取前后 desired revision/source hash 与 torn-read 结果：
- public snapshot/input revision/source hash/schema/renderer/runtime版本：
- SecretRef kind/version/envName、placeholder、实际注入三向计数/hash：
- 确认 PG/ConfigMap/argv/golden/log 未含 secret value、正文、真实路径：
- 若 source/schema 缺失：`BLOCKED_SERVER_SOURCE_SCHEMA` / owner / 解除证据：

### Migration、revision、CAS 与幂等

- accepted migration checksum / 新 migration / catalog/RLS registry hash：
- 空库、upgrade、repeat deploy、drift、lock budget、中断恢复：
- desired/applied/LKG revision 与 plan/file/ref-set hash：
- same revision same hash / same revision different hash / stale revision：
- PG CAS SQL/Prisma条件与 row count；无 admin/unsafe fallback：

### Leader、多 Pod 单写者与 fencing

- leader election provider/lease identity/renew/expiry：
- workspace lease key/holder/fencing token/挂载模式：
- 双leader、旧leader恢复、10并发same revision、两Pod同workspace结果：
- 锁顺序/timeout/SIGTERM/网络分区/cleanup：
- applied revision与最终 lease/fence/readback hash：

### Atomic apply、manifest barrier 与 LKG

- `/state`、`/workspace/state` allowed roots/realpath/mode/UID/GID/pre-hash：
- stage path/runId/fsync/next hash/real gateway validate：
- workspace/state commit顺序与最终manifest：
- prepare失败 rename count：
- partial rename/SIGTERM/ENOSPC/只读PVC恢复 changed/restored/unrestored：
- LKG from/to revision、兼容检查、fallback状态：
- gateway entrypoint barrier、启动与cleanup结果：
- production activation：`disabled | shadow | canary | active`（需写授权门）：

### Diff、no-op、reload/rebuild

- previous/next plan hash 与 exact pointer/file diff：
- 动作：`NO_OP | CONTROL_PLANE_ONLY | HOT_RELOAD | POD_REBUILD_REQUIRED` / reason：
- NO_OP 的 mtime/revision/PID 证据：
- HOT_RELOAD pinned gateway PID/采纳行为/SLA/turn状态证据：
- rebuild 的 V204 drain/capsule/sessionKey/历史与PVC不丢证据：
- 未知字段默认 rebuild 与错误热更 mutation：

### Golden、shadow、真实 gateway、安全与跨平台

- V106 fixture manifest/source hash/scenario count：
- PG→server snapshot→同一renderer golden diff：
- desktop/V106/V205 dry-run shadow hash与 exact approved differences：
- pinned OpenClaw version/runtime image digest/真实命令/ready/invalid mutation：
- Windows pathFlavor与Linux container path/UNC/Unicode/CRLF结果：
- secret encoded variants/evidence/image-layer scan：
- 内网 allow + 公网/metadata/K8s API/非批准内网/runtime install deny：
- audit/log/metric/trace redaction：
- 证据分类确认：`fixture ≠ shadow ≠ pod apply ≠ gateway boot ≠ V2`：

### 自测与证据

| 门 | 命令 | CST/UTC | exit | SHA/runId/digest | 报告/hash | 结论 |
|---|---|---|---:|---|---|---|
| V205 targeted Vitest |  |  |  |  |  |  |
| boundary/import audit |  |  |  |  |  |  |
| golden/completeness/secret |  |  |  |  |  |  |
| PG17/Testcontainers/migration/RLS |  |  |  |  |  |  |
| container/Pod/init/gateway |  |  |  |  |  |  |
| concurrency/lease/CAS/LKG/rollback |  |  |  |  |  |  |
| changed-file ESLint |  |  |  |  |  |  |
| typecheck / npm test |  |  |  |  |  |  |
| build:saas / build / compile:electron |  |  |  |  |  |  |
| prerequisite cumulative gates |  |  |  |  |  |  |
| `git diff --check` / scope / secret scan |  |  |  |  |  |  |

### AC01～AC30 / T01～T35 完整性与回滚演练

- AC/T `PASS / FAILED / BLOCKED` 清单与 owner：
- 纯逻辑 branch coverage / secret/path/CAS/fence/rollback安全coverage：
- shadow→canary→active 当前阶段与进入门：
- feature flag/image/chart回滚、ledger forward-only、LKG/PVC恢复实测：
- commits（英文 Conventional Commits、无无关文件、无Co-Authored-By）：
- worktree/namespace/schema/PVC/temp/secret fixture cleanup：

### 交接 Reviewer

列出稳定 review SHA、merge-base、全部 commits/diff、有效 Red→Green、前置接受证据、runId/namespace/digest、30 AC/T01～T35映射、known risks与 Reviewer 必跑 mutations。Developer 不得填写 `APPROVED_FOR_TEST` 或 Tester 结论。
