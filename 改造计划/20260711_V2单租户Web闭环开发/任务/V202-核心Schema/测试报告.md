# V202 测试报告

> 仅在独立 Reviewer `APPROVED_FOR_TEST` 后由独立 Tester 按 Round 追加。Tester 不复用 Planner/Developer/Reviewer Agent、runId、container、seed、连接池、进程、报告或未清资源；新增测试属于交付，不能只复述 Developer 自测。

## Round 1 · YYYY-MM-DD HH:mm · Tester Agent/commit

### 0. 测试元数据与三态

- Tester Agent：
- Reviewer `APPROVED_FOR_TEST` Round / commit：
- 独立 checkout SHA：
- test runId / PG17 container ID：
- Developer/Reviewer runId（仅证明不同）：
- P02 accepted SHA / V107 packageHash + validity：
- PostgreSQL image exact tag/platform/digest / server version：
- app role / table owner / inherited memberships：
- 开始/结束时间（CST / UTC）：
- 结论：`PASSED | FAILED | BLOCKED`

依赖、Docker、镜像、磁盘、权限或有效 V107 GO 缺失=`BLOCKED`；环境完整但断言失败=`FAILED`；只有所有必需项和新增 corner 全绿、skipped=0、cleanup=0 才可 `PASSED`。

### 1. 独立累计门

| 命令 | started/finished | exit/status | tests/skipped | sourceSha | report hash |
|---|---|---|---|---|---|
| `npm ci` |  |  |  |  |  |
| contracts check/test |  |  |  |  |  |
| Prisma generate/preflight/unit/integration/validate |  |  |  |  |  |
| typecheck/changed-file ESLint/lint:saas |  |  |  |  |  |
| npm test/build:saas/build/compile:electron |  |  |  |  |  |
| diff/status/scope audit |  |  |  |  |  |

### 2. 独立 schema / migration / RLS 矩阵

| 用例 ID | 独立角度 | 预期 | 实际 | 结果 | 证据 |
|---|---|---|---|---|---|
| V202-QA-01 | P02→V202、空库、重复 deploy checksum/history | 三路 clean |  |  |  |
| V202-QA-02 | 四表 tenant NOT NULL + registry/policy/catalog | 差异 0 |  |  |  |
| V202-QA-03 | 非 owner/non-super/non-BYPASSRLS A/B CRUD | 0 泄露 |  |  |  |
| V202-QA-04 | missing/invalid context、commit/rollback/throw 后复用 | fail closed/0 leak |  |  |  |
| V202-QA-05 | 跨租户 session-agent-parent-message-capsule | FK/RLS 拒绝 |  |  |  |
| V202-QA-06 | soft delete 保留子项；hard purge cascade/set-null | 语义匹配计划 |  |  |  |
| V202-QA-07 | dual `main`、UUID/logical roundtrip、同租户 duplicate | A/B成功；duplicate失败 |  |  |  |
| V202-QA-08 | 历史列、epoch/JSON/boolean/tool subtype fixture | 无静默丢失/误映射 |  |  |  |
| V202-QA-09 | schema/migration/generated/RLS/catalog drift mutation | gate 非零 |  |  |  |

### 3. 独立顺序、cursor、幂等与事务矩阵

| 用例 ID | 独立角度 | 关键阈值 | 结果 | 证据 |
|---|---|---|---|---|
| V202-QA-10 | 多条相同 created_at、UUID tiebreaker、before/after | 全量恰一次、稳定顺序 |  |  |
| V202-QA-11 | 翻页间插入/删除、NULL/shift sequence | cursor 无重无漏；不用 sequence |  |  |
| V202-QA-12 | app role 篡改 id/created_at/tenant/session | 全拒绝 |  |  |
| V202-QA-13 | 并发 append + rollback/timeout/deadlock | sequence 唯一单调，gap 可解释 |  |  |
| V202-QA-14 | session/message 同幂等 key 并发与请求冲突 | 每 scope 一副作用 |  |  |
| V202-QA-15 | capsule 相同 revision 并发 CAS | 恰一成功，无 lost update |  |  |
| V202-QA-16 | session+first message、fork+capsule 中途失败 | 原子回滚，无孤儿 |  |  |

### 4. 大数据 migration、恢复与生产断公网

| 用例 ID | rows/size | 场景 | lock/duration/探针 | 结果 | artifact |
|---|---:|---|---|---|---|
| V202-QA-17 |  | keyset batch backfill + repeat |  |  |  |
| V202-QA-18 |  | backfill/validate/index 中断后续跑 |  |  |  |
| V202-QA-19 |  | concurrent read/write during expand/contract |  |  |  |
| V202-QA-20 |  | lock_timeout/statement_timeout 超限 |  |  |  |
| V202-QA-21 |  | app rollback，数据/RLS仍在 |  |  |  |
| V202-QA-22 |  | production seed/runtime public egress attempt | 必须拒绝 |  |  |

不得以小空库结果替代锁测试；invalid index、dirty migration、半 backfill、RLS 未 FORCE、读写探针中断任一出现即 FAILED。生产不运行 `npm/pip install` 或公网拉镜像；Testcontainers 只在测试/构建环境或批准内网 registry。

### 5. Tester 新增 corner / mutation

至少新增 Reviewer 未覆盖的 3 项，其中必须各含一个并发、迁移恢复、安全/RLS角度。

| 新用例 | Reviewer 未覆盖角度 | Red 预期 | Green 结果 | commit | 证据 |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

建议候选：UUID 边界/Unicode normalization、微秒相同 timestamp、多连接相反锁序 deadlock、idempotency NULL/空串、partial unique 在软删后语义、复合 FK `SET NULL(column)`、role 间接继承、future clock、JSON 超大对象、signal 中断后的 migration/容器清理。不得把修改错误测试期望冒充实现修复。

### 6. Seed、证据完整性与 cleanup

- deterministic A/B/main/session/message/capsule seed repeat hash：
- production seed reject / secret scan / public URL scan：
- evidence root：`docs/db/20260712_V202_PostgreSQL核心Schema证据/<testRunId>/`
- source/plan/P01/P02/V107/image/runner/report hash 重算：
- containers/volumes/networks/test roles/schemas/temp dirs 清理：
- skipped / stale / mismatch / cleanup outstanding count：

### 7. 失败返工与最终交接

| 失败 T/QA | 状态 | Owner | 复现 | 失效下游 | 最早重验条件 |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

- Tester conclusion：`PASSED | FAILED | BLOCKED`
- final test commit / report hash：
- `FAILED` 退原 Developer；修复后重新独立 Reviewer，再由新 Tester runId 全回归。`BLOCKED` 解除后也不得续写旧 PASS。
- `PASSED` 只表示 Tester 交付候选通过；总体协调者仍须核对依赖有效性、全部 AC/T/QA、证据 hash、生产断公网与 scope，才可接受并更新总表。
- 明示禁止：Tester 不更新 `00-总体完成表.md`，不启动/实现 V203，不覆盖旧 FAILED/BLOCKED 证据。
