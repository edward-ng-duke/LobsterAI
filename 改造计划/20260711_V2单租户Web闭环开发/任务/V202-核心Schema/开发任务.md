# V202 · Postgres 核心 schema

- 状态：TODO
- 依赖：V107=go、P02。
- 权威输入：根 AGENTS、19、17 对应 V1/V2、16、附录 C；冲突以根 AGENTS 的生产断公网和附录 C 契约决策为准。

## 源码边界

Prisma models/migrations for sessions/messages/capsules/agents；不迁移完整桌面数据。

## TDD：先写的失败测试

先写 tenant_id 非空、UUID+logical_id、immutable(created_at,id) cursor、FK/index/migration 幂等失败测试。 Red 必须因目标行为缺失而失败；环境缺失另记 BLOCKED，不算 Red。

## Corner cases

logical_id main、同租户唯一/跨租户同名、同时间 cursor、软删、超长内容、capsule、sequence 竞态预置。

## 实施与验收

Developer 按最小垂直切片逐个 Green，避免无关重构。验收命令：prisma validate/migrate；Testcontainers schema/constraint；npm test。

## 回滚

expand/contract：先新增兼容表列；应用回滚不 drop 数据。

## 完成证据

schema/migration、约束/索引、tenant_id 与双键测试报告。 证据须绑定 SHA/runId/环境；所有触碰 TS 文件 changed-file ESLint 0 warning，相关 Vitest 全绿。

## Developer → Reviewer → Tester 循环

Developer 记录每轮 Red/Green 与 diff → 独立 Reviewer 对照本文件和源码审核；若 CHANGES_REQUESTED，退同一 Developer 修复并进入下一轮 → Reviewer APPROVED_FOR_TEST 后独立 Tester 新增 corner-case/回归测试；Tester FAILED 必须退 Developer 且修复后重新 Reviewer → Tester PASSED 后由协调者核验证据并勾总体表。任何角色不得自行勾 Todo。

