# V202 审核意见

> 仅独立 Reviewer 在 Developer `REVIEW_PENDING` 后按 Round 追加。Reviewer 不复用 Planner/Developer Agent、runId、container、seed、报告或临时目录，不代写实现、不改总体表、不把 V203 repository 缺失当作 V202 缺陷。

## Round 1 · YYYY-MM-DD HH:mm · Reviewer Agent/commit

### 0. 审核元数据与启动门

- Reviewer Agent：
- Developer final SHA / evidence SHA：
- 独立 checkout SHA：
- review runId / PG17 container ID：
- P02 accepted SHA / V107 packageHash + validity：
- image exact tag/platform/digest / server version：
- 开始/结束（CST / UTC）：
- 结论：`CHANGES_REQUESTED | BLOCKED | APPROVED_FOR_TEST`

V107 非有效 GO、P02 非最终接受、Docker/镜像/权限缺失时写 `BLOCKED`；环境完整但断言失败写发现并 `CHANGES_REQUESTED`。不得只读 Developer 报告后批准。

### 1. 独立范围、血缘与 schema 审计

| 审核项 | 结果 | 独立证据 |
|---|---|---|
| 只追加 migration，P02 published checksum 未变 |  |  |
| 四核心表字段/历史 map/UUID/时区/check 完整 |  |  |
| tenant 前缀 unique/index 与复合 FK/delete 正确 |  |  |
| RLS registry/SQL/catalog 四方集合相等 |  |  |
| soft delete 与最终 hard cascade 不混淆 |  |  |
| cursor/sequence/idempotency/CAS 语义未漂移 |  |  |
| owner/workspace 未创建并行任务替身 |  |  |
| 无 `coworkStore`/repository/V203、总表或其它任务越界 |  |  |
| 证据 source/plan/P02/V107/image/run hash 可重算 |  |  |

### 2. 必做独立 mutation

所有 mutation 在隔离副本/临时 schema 运行，不能污染共享树或复用 Developer 数据。

| T ID | mutation | 预期 | 实际 | 捕获 | 证据 |
|---|---|---|---|---|---|
| T03 | 任一核心表删 tenant NOT NULL/registry | 非零 |  |  |  |
| T04 | 删 FORCE 或 WITH CHECK | FAILED |  |  |  |
| T05 | app role 赋 BYPASSRLS/owner membership | FAILED |  |  |  |
| T06 | logical main 改全局唯一/字符串 PK | FAILED |  |  |  |
| T09 | 子表 single FK 后跨租户挂接 | FAILED |  |  |  |
| T12 | cursor 改 sequence 或 `>=` 边界 | FAILED |  |  |  |
| T13 | 更新 message created_at/id | FAILED |  |  |  |
| T14 | allocator 改 `MAX(sequence)+1` | 并发 FAILED |  |  |  |
| T15 | 删除幂等 partial unique | duplicate FAILED |  |  |  |
| T18 | 只测空库/篡改旧 migration | FAILED |  |  |  |
| T20 | schema/generated/registry/catalog drift | 非零 |  |  |  |
| T21 | 锁超时/中断留下 invalid index | FAILED |  |  |  |
| T23 | skip/Testcontainers→SQLite/admin | BLOCKED/FAILED |  |  |  |
| T26 | 加 repository/coworkStore 实现 | scope FAILED |  |  |  |

Reviewer 另加 mutation：

### 3. 独立 PostgreSQL 17 重放

| 场景 | app role/runId | 结果/阈值 | skipped | artifact/hash |
|---|---|---|---:|---|
| empty、P02 upgrade、repeat migration |  |  |  |  |
| large backfill + concurrent read/write + lock budget |  |  |  |  |
| A/B CRUD、missing context、连接复用 |  |  |  |  |
| FK、self/cross parent、set-null/cascade |  |  |  |  |
| dual main + historical map/checks |  |  |  |  |
| same-time cursor before/after/delete boundary |  |  |  |  |
| sequence/idempotency/capsule CAS concurrency |  |  |  |  |
| seed repeat/prod reject/secret/public URL scan |  |  |  |  |
| cleanup：container/volume/network/role/schema |  |  |  |  |

### 4. 审核发现

| 严重级 | T/AC | 文件:行或 artifact | 实际 / 期望 | 复现命令 | Owner | 要求证据 |
|---|---|---|---|---|---|---|
|  |  |  |  |  |  |  |

任何 P0/P1、无有效 Red、必需 FAILED/BLOCKED、skip、RLS/tenant/FK 缺口、cursor 重复/遗漏、migration drift/长锁、报告/cleanup 缺口、生产公网依赖或 V203 越界都不得 `APPROVED_FOR_TEST`。

### 5. 累计门与 Reviewer 结论

- contracts / Prisma generate+validate / DB unit+integration：
- typecheck / changed-file ESLint / lint:saas：
- npm test / build:saas / build / compile:electron：
- diff/status、遗留非 V202 改动归属：
- `PASS/FAILED/BLOCKED` 计数：
- 最终结论：

### 6. 交接 Tester

仅 `APPROVED_FOR_TEST` 时填写：

- Reviewer approved SHA / report hash：
- Tester 必须使用的新 runId/container/seed：
- Tester 必须重放：PG17 非特权 RLS、P02 upgrade/repeat、大数据锁、同时间 cursor、幂等/sequence/CAS、drift、rollback、seed、断公网/cleanup。
- Reviewer 未覆盖、Tester 必须新增的角度：
- 明示：Reviewer 批准不是 V202 ACCEPTED，Tester/协调者仍可 `FAILED/BLOCKED`。
