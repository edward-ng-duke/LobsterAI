# V207 · 基础 Artifacts 只读预览

- 状态：TODO
- 依赖：V202、V204、V206；内网对象存储或受控内联方案。
- 权威输入：根 AGENTS、19、17 对应 V1/V2、16、附录 C；冲突以根 AGENTS 的生产断公网和附录 C 契约决策为准。

## 源码边界

artifact metadata API/DB、apps/web renderers；仅 text/Markdown/code/image、HTML 内部 sandbox iframe。

## TDD：先写的失败测试

先写 metadata、MIME/size、sanitization、CSP/iframe、加载失败与旧内容陈旧提示测试。 Red 必须因目标行为缺失而失败；环境缺失另记 BLOCKED，不算 Red。

## Corner cases

空/损坏/超大图片、原子 rename、恶意 HTML/SVG、Unicode 文件名、缓存、过期 URL、未知 MIME。

## 实施与验收

Developer 按最小垂直切片逐个 Green，避免无关重构。验收命令：unit/sanitizer；API integration；Playwright CSP/iframe；visual smoke。

## 回滚

关闭 HTML/各类型 preview feature flag；metadata 不删除，blob 不公开。

## 完成证据

各类型成功/失败 trace、CSP headers、sandbox 属性、安全负向报告。 证据须绑定 SHA/runId/环境；所有触碰 TS 文件 changed-file ESLint 0 warning，相关 Vitest 全绿。

## Developer → Reviewer → Tester 循环

Developer 记录每轮 Red/Green 与 diff → 独立 Reviewer 对照本文件和源码审核；若 CHANGES_REQUESTED，退同一 Developer 修复并进入下一轮 → Reviewer APPROVED_FOR_TEST 后独立 Tester 新增 corner-case/回归测试；Tester FAILED 必须退 Developer 且修复后重新 Reviewer → Tester PASSED 后由协调者核验证据并勾总体表。任何角色不得自行勾 Todo。

