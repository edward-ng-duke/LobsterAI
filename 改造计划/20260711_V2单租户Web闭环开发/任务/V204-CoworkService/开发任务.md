# V204 · Cowork Service 与 OpenClaw adapter

- 状态：TODO
- 依赖：V102、V105、V203。
- 权威输入：根 AGENTS、19、17 对应 V1/V2、16、附录 C；冲突以根 AGENTS 的生产断公网和附录 C 契约决策为准。

## 源码边界

apps/api Cowork domain/API/WS、OpenClaw runtime adapter；不实现 V4 orchestrator。

## TDD：先写的失败测试

先写 session create+首prompt、turns、history、chat.*与sessions.*真 RPC、10事件、permission union 测试。 Red 必须因目标行为缺失而失败；环境缺失另记 BLOCKED，不算 Red。

## Corner cases

1 session多key、多副本回指、requestId 未命中、abort/permission竞态、gateway crash、重复事件。

## 实施与验收

Developer 按最小垂直切片逐个 Green，避免无关重构。验收命令：unit/contract/Testcontainers；真实内网 gateway integration；eslint/typecheck。

## 回滚

feature flag 禁用 Web Cowork；保留数据库记录并关闭新请求，drain active turn。

## 完成证据

REST/WS contract、真实 RPC 日志、10事件顺序、permission/错误路径。 证据须绑定 SHA/runId/环境；所有触碰 TS 文件 changed-file ESLint 0 warning，相关 Vitest 全绿。

## Developer → Reviewer → Tester 循环

Developer 记录每轮 Red/Green 与 diff → 独立 Reviewer 对照本文件和源码审核；若 CHANGES_REQUESTED，退同一 Developer 修复并进入下一轮 → Reviewer APPROVED_FOR_TEST 后独立 Tester 新增 corner-case/回归测试；Tester FAILED 必须退 Developer 且修复后重新 Reviewer → Tester PASSED 后由协调者核验证据并勾总体表。任何角色不得自行勾 Todo。

