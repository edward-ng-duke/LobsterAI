# 测试报告

> 初始为空白；仅在 Reviewer APPROVED_FOR_TEST 后由独立 Tester 填写。

- Tester Agent：`/root/p03_tester_final`
- Reviewer 批准基线：`79475f6a9d5386d62465d15049517517275c1ad8`
- Tester 测试提交：`3a327fa1`、`fe35f595`
- 产品 source：`3026eb03a5fc012279165bf8af5e25ec1a890dd3`
- exact-five invocation：`0f5ce7fa-da9a-4525-ae6c-a24cbab58792`
- 环境：macOS Darwin arm64；Node `v24.15.0`；Docker client/server `29.4.0`（OrbStack）；namespace `N/A`（本轮未连接集群）
- Round 1 结论：`FAILED`（以下三项已在 Round 2 关闭）

独立 Tester 新增 19 项测试（证据 corner 14 项、product-source 3 项、Helm/SIGTERM 2 项）。最终 targeted 为 4 files / 56 tests，其中 53 PASS、3 FAIL。三个失败都不是环境阻断：隔离 fixture 中的正式 `scripts/check-supply-chain.mjs` 对协调篡改返回 `PASSED`、exit `0`。

| 用例 ID | 独立角度 | 测试文件/脚本 | 结果 | 证据 |
|---|---|---|---|---|
| `P03-TEST-01` | schema root/深层 unknown、错类型、缺 required | `tests/deploy-supply-chain-evidence-files.test.ts` | PASS | report/root/runtime/plugin item mutation 均非零；wrong Critical count type 与缺 runtime status 均被 Ajv 拒绝。 |
| `P03-TEST-02` | evidence absolute/`..`/symlink/directory/FIFO/替换/hash 协同 | 同上 | PASS | absolute 与 FIFO 均非零；FIFO 在 5 秒内返回且未阻塞读取；用 API 的有效 SBOM 替换 Web 并协调 hash 仍被 subject 对账拒绝。 |
| `P03-TEST-03` | SPDX 多 subject / 缺 relationship | 同上 | PASS | 重复或删除 `DESCRIBES` 后协调文件 hash，正式 checker 均非零。 |
| `P03-TEST-04` | SPDX 重复根 package identity | 同上 | **FAIL / P2** | 复制 Web root package（相同 `SPDXID`）并协调 SBOM SHA-256；实际 checker `PASSED`/exit `0`，期望拒绝重复 package identity。最小失败行：测试文件约 421-440 行。 |
| `P03-TEST-05` | Grype tag/repoDigest/manifestDigest/大小写 severity/Critical 对账 | 同上及既有 evidence mutations | PASS | target/tag/repoDigest/manifestDigest 漂移、wrapper/raw severity 不一致及 Critical 计数漂移均非零。 |
| `P03-TEST-06` | Grype exact duplicate match + wrapper 同步重复 | 同上 | **FAIL / P2** | 原始 `matches[0]` 与 wrapper finding 同步复制、协调 scan SHA-256；实际 checker `PASSED`/exit `0`，期望拒绝同一漏洞/制品 identity 的完全重复记录。最小失败行：约 443-461 行。 |
| `P03-TEST-07` | 本地 Docker image 缺失 / inspect digest 漂移 | 同上 | PASS | 通过隔离 PATH 的假 Docker 分别返回 unavailable 与错误 metadata；checker 均非零并给出对应诊断。 |
| `P03-TEST-08` | plugin declared exact set、required skipped、duplicate、unknown | 同上 | PASS | required Bee skipped、重复 ID、unknown 替换声明 ID 均非零。 |
| `P03-TEST-09` | optional plugin status 与绑定 final-image 目录一致性 | 同上 + hardened `docker run` | **FAIL / P1** | 当前 bound OpenClaw image 中 `moltbot-popo` 目录实际 absent、Bee present；仅把报告 POPO 从 `skipped` 改为 `installed` 后正式 checker 仍 `PASSED`/exit `0`。期望 supply-chain gate 重新核对最终镜像目录，而不是只信任 wrapper。最小失败行：约 493-530 行。 |
| `P03-TEST-10` | product-source：test/角色记录允许，产品 edit/rename/merge 不跳过 | `tests/deploy-product-source-boundaries.test.ts` | PASS | test 与开发/审核/测试记录 commit 保持 baseline；产品编辑、`tests→src` rename、含产品变更的 no-ff merge 均解析到包含产品变更的新 source。3/3 PASS。 |
| `P03-TEST-11` | Helm security/resources/probes/NetworkPolicy | `tests/deploy-helm-policy.test.ts` | PASS | 新增组合 mutation 同时缺 SA token/security/resources/readiness/liveness，render scan 给出全部拒绝诊断；既有明确 TCP edge 与 broad/missing-port mutation 保持通过。 |
| `P03-TEST-12` | SIGTERM/health 负向与重复信号 | `tests/deploy-docker-policy.test.ts` | PASS | live gateway 负向端口仍非零；Runtime Orchestrator 连续 SIGTERM/SIGINT 仅关闭一次并清理两类 listener。 |

## 回归、非功能与交接

- 当前未篡改 baseline 的正式 `node scripts/check-supply-chain.mjs`：PASS，57 assets / 5 image evidence，publish/signature 保持 `BLOCKED`。
- exact-five report：mode `0600`、SHA-256 `612940682fa2481eafc608c78aebdcec65d143319d9830b9b65a56c5f06b5ab1`；五镜像 `docker image inspect` 全部为 `linux/arm64`、`10001:10001`，digest 与 report 一致：
  - web `sha256:b58c71f8b37cb2c94514308a4b6a327cd09b8cf1faf9dc5501088bd380fdc5af`
  - api `sha256:160557cccd6f2a8ed92b6461aeec2a781dfe5a07416f229cbaa30278a08a0917`
  - worker `sha256:16d559c8e7452e9e357d0138d3f4ad33cd070ae90451b75bd870776efeff043d`
  - runtime-orchestrator `sha256:695e4da2caae8a752e58fa09eb8e72cbdd9ed992cf25a2bbeec2daebb3b01bf5`
  - openclaw-runtime `sha256:ec9057acd2b3be8dacbdc76198093e173f7988e003b809b86a9990e1e7d196fc`
- `npx vitest run tests/deploy-supply-chain-evidence-files.test.ts tests/deploy-product-source-boundaries.test.ts tests/deploy-docker-policy.test.ts tests/deploy-helm-policy.test.ts`：4 files；56 tests；53 PASS / 3 FAIL，exit `1`。
- Green 对照单独执行：product-source + Docker/Helm 为 3 files / 19 tests PASS；changed-test ESLint 与 `git diff --check` PASS。
- 未重跑约 40 分钟的五镜像 build：现有 exact-five evidence 有效且缺陷发生在 formal evidence re-validation 层；重建镜像不能关闭上述绕过。
- 未运行全量 `npm test`：targeted 已出现确定性产品 Red，按 Tester 协议立即停止扩展并退回 Developer；不得把本轮写成 PASSED。
- 返工要求：供应链 checker 应拒绝 SPDX 重复 package identity、Grype 完全重复 match，并将 `pluginInstallations` 与绑定 final image 的 `/opt/openclaw/third-party-extensions/<id>/package.json` 重新核对。Developer 修复后须重新 Reviewer；Tester 不修改业务实现、总体表或 shared main。

## Round 2 · Tester 返工复测

- 复测 Tester：`/root/p03_tester_final`
- Reviewer 批准 HEAD：`0b7215a6c2667be8a2cf376d7979171ce3f98253`
- 产品 source：`187da9a20b742c318e947a96cf0dd66c7cf9eca9`
- exact-five invocation：`64595381-cec8-46b2-a8b8-4631e96dfb9b`
- 当前结论：`PASSED`

### 原 Red 与对称 corner

| 复测项 | 独立结果 | 结论 |
|---|---|---|
| SPDX 重复 root/package identity | Tester 原 mutation 现在由 formal checker 非零拒绝；多/缺 `DESCRIBES` 仍拒绝。 | CLOSED |
| Grype raw match 与 wrapper finding 同步重复 | 完全重复 identity 现在非零；identity 包含 `artifactId`，raw/wrapper 使用完整 finding multiset 双向对账。 | CLOSED |
| POPO absent 却改报 installed | bound final image 中 POPO 实查 absent，报告为 `skipped`；将其改报 `installed` 的原 mutation 现在非零。 | CLOSED |
| NIM present 却改报 skipped | bound final image 中 NIM 实查为 regular file，报告为 `installed`；44d1 反向 mutation 非零。 | CLOSED |
| required Bee present/installed | bound final image 中 Bee 实查为 regular file，报告为 required + `installed`；required skipped mutation 非零。 | CLOSED |

### 独立命令与证据

- Node `v24.15.0`：`npx vitest run tests/deploy-supply-chain-evidence-files.test.ts tests/deploy-product-source-boundaries.test.ts tests/deploy-docker-policy.test.ts tests/deploy-helm-policy.test.ts` → 4 files / **58 tests PASS**，exit `0`，39.79s。
- `node scripts/check-supply-chain.mjs` → PASS，57 assets / exact 5 image evidence / publish `BLOCKED`；`node scripts/check-docker-build.mjs --static` → PASS。
- report mode `0600`、SHA-256 `613fe5f8478608d9723c5cf0296cb4de090dc488200c9a36f3a902cd366c67bc`；五份 SBOM 与五份 Grype 文件逐项实算 hash 全部匹配。raw Grype Critical 合计 `0`、wrapper Critical 合计 `0`、空 `artifactId` 合计 `0`。
- 五镜像 `docker image inspect` 均为 `linux/arm64`、`10001:10001`，Id/RepoDigest 与 report 一致：
  - web `sha256:67040e3975c3671600a6513c6fbdd2a60ff2342e7fe857617183b2d58883b21e`
  - api `sha256:36e5261c5660af60d4dc36924d56430fc891c36feaba407345570c930b917991`
  - worker `sha256:d7b6cb8f4f2eff334dc97a2a0f3f7a2f56b3a7eb73c6e2b8645097fa02838444`
  - runtime-orchestrator `sha256:01a874d08203cdd271c1d7df529226b10f7cdcc01a4cf66c3d8d773b3f740992`
  - openclaw-runtime `sha256:b3ed4a874b943c4aa9d942feffd6e49cd8f148486d88b16fbd8fa3da0b05a793`
- hardened 一次性容器使用 `--network=none --read-only --cap-drop=ALL --security-opt=no-new-privileges --user=10001:10001` 实查 final image：`moltbot-popo=absent`、`openclaw-nim-channel=present-regular`、`openclaw-netease-bee=present-regular`；与报告的 skipped/installed/installed 完全一致，容器 `--rm` 无残留。
- `resolveProductSourceSha()` 独立求值为 `187da9a2...`；其后的改动只有 evidence test source 绑定与角色记录。独立 boundary tests 继续证明产品 edit、`tests→src` rename、含产品变更的 no-ff merge 不会跳过。
- fresh `npm run supply-chain:check` → PASS，invocation `e1a1f0ce-4b0a-4cc8-a009-ae9109cc00c6`；fresh `npm run helm:lint` → PASS，invocation `8bc991d7-dc42-48d5-9ba7-1fe318974ea7`。Helm 使用官网包 `v3.21.0+ge0878d4` 并在执行前按发布 SHA-256 校验，临时工具目录随后删除。
- 未重跑约 40 分钟的五镜像 build：当前 exact-five report、本地镜像、SBOM/Grype 与 formal checker 均有效，复测目标是身份/目录返工层。未改业务、总体表或 shared main；signature/publish 继续诚实保持 `BLOCKED`。

## B00-3A 独立 Tester 验证 · 2026-07-12

- 独立 Tester：`/root/p03_3a_tester`
- Reviewer 局部许可 commit：`d610a76915cad4713675d71ba0551353f6f114ba`
- Tester 新增测试 commit：`a9dd7a29 test(supply-chain): add independent parser corners`
- 环境：Linux `6.8.0-124-generic` x86_64；Node `v24.18.0`
- 结论：`FAILED_B00_3A`。这是 B00-3A 局部失败结论，不是 P03 任务级 `PASSED`。

### 独立 corner 结果

| 独立角度 | 结果 | 证据 |
|---|---|---|
| root commit 命令与 fail-closed | PASS | 受控 runner 精确观测到 `git diff-tree --root --no-commit-id -r --name-status -z -M -C <root>`，raw 调用无 `encoding`且 `maxBuffer=16 MiB`；非零返回后 resolver 保守返回 root commit。 |
| 默认 factory 等价性 | PASS | 真实临时 Git repo 上 `createProductSourceShaResolver()()` 与公开 `resolveProductSourceSha()` 返回同一 baseline。 |
| 真实 Git 病理路径与清理 | PASS | 实际 commit/diff 覆盖 emoji、CR、leading dash、`docs/\uFEFFname.md` 与 `\uFEFFdocs/...`；frozen 路径不推进，伪装 frozen 的未知路径 fail closed 推进。用例显式对账 `process.env`和 `/tmp/lobsterai-p03-product-source-*`，执行后无临时目录残留。 |
| runner 异常 stdout/错误组合 | **FAIL / P2** | raw name-status 对 string、`Uint8Array`、`status:0 + error` 正确 fail closed；但文本 `rev-list` 对 Buffer/null stdout 抛 `TypeError`，对 `status:0 + error/signal` 继续信任 stdout，最终错误返回旧 frozen baseline，而非当前 analyzed commit。 |

### 稳定复现

Node 24 下只运行新增 4 项：

```bash
PATH="$HOME/.nvm/versions/node/v24.18.0/bin:$PATH" \
  npx vitest run tests/deploy-product-source-boundaries.test.ts --reporter=verbose \
  -t "uses the exact root|keeps the default factory|preserves real Git|fails closed for abnormal"
```

结果：3 PASS / 1 FAIL / 40 skipped，exit `1`。失败聚合输出稳定显示：

- Buffer stdout：`TypeError: result.stdout.trim is not a function`；
- null stdout：`TypeError: Cannot read properties of null (reading 'trim')`；
- `status:0 + error`：实际返回 frozen baseline；
- `status:0 + signal`：实际返回 frozen baseline。

根因位于 `scripts/check-supply-chain.mjs:120-123`：文本 `gitOutput()` 仅检查 `status === 0`，未检查 `error`、`signal` 和 `typeof stdout === 'string'`，且直接调用 `stdout.trim()`。Tester 未修改生产代码；当前实现本身已稳定 Red，无需再做人工 mutation。

### 规定门禁

- 全 B00-3A targeted：`npx vitest run tests/deploy-product-source-boundaries.test.ts --reporter=verbose` → 1 file / 44 tests，43 PASS / 1 FAIL，exit `1`。原 40 项全部 PASS，唯一失败为新增防御性 corner。
- `node --check scripts/check-supply-chain.mjs` → exit `0`。
- `node scripts/check-docker-build.mjs --static` → exit `0`，status `PASSED`，exact five 全部通过。
- `npx eslint --ext ts,tsx --report-unused-disable-directives --max-warnings 0 tests/deploy-product-source-boundaries.test.ts` → exit `0`。
- `git diff --check d610a769..a9dd7a29` → exit `0`。
- 未执行官方全量 `npm test`：B00-3A targeted 已出现确定性产品 Red，按局部 Tester 协议立即退回，不用其他 B00/P02 结果混淆本结论。

### 交接 Developer

Developer 应仅修复文本 Git runner 返回值验证：任一非零 status、`error`、`signal` 或非 string stdout 都必须返回 `undefined`，不得抛异常或继续信任输出；使已知 analyzed commit 路径 fail closed 到该 commit。修复后须重跑新增 4 项、全 44 项 targeted、Node check、Docker static、changed-TS ESLint 与 diff-check，再交新独立 Reviewer/Tester。本轮未改 script、计划、总表、审核意见、B00-3B 或 B00-3C，且未推送。

## B00-3A 独立 Tester 修复复测 Round 2 · 2026-07-12

- 复测 Tester：`/root/p03_3a_tester_recheck`
- 旧 Red 基线：`57e4bfc306411ce3a8121741aa82afba8a6a4e11`；Developer 修复：`070b57731fcc8cb487ea4a80c93f7556f8614893`；Reviewer 局部复测许可 HEAD：`7a56f4e457b7c80d7c518e10ce2f69e811100c82`。
- 环境：Linux `6.8.0-124-generic` x86_64；Node `v24.18.0`。
- 结论：`PASSED_B00_3A_LOCAL_GATE`。这只是 B00-3A Tester 失败修复的本地局部门禁结论，**不是 P03 任务级 `PASSED`**，也不代表 B00-3B/3C、P02、live evidence 或 registry/signature 验收。

### Red 重放与当前 Green

- 从 `57e4bfc3` 创建仓库外 `git archive` 隔离副本，只重放 Tester 新增 4 个 corner：3 PASS / 1 FAIL / 40 skipped，exit `1`。唯一失败为原异常-runner 聚合：Buffer/null stdout 分别抛 `TypeError`，`status:0 + error/signal` 错误返回旧 frozen baseline。隔离目录随后删除，当前工作树未被修改。
- 当前 HEAD 重跑同 4 个 corner：4 PASS / 40 skipped，exit `0`。文本 runner 的 Buffer/null stdout、`status:0 + error`、`status:0 + signal` 均 fail closed 到当前 analyzed commit；root diff-tree 精确命令、默认 factory 等价性、真实 Git 病理路径与清理三个独立角度不回归。
- 当前 HEAD 全 B00-3A targeted：`npx vitest run tests/deploy-product-source-boundaries.test.ts --reporter=verbose` → 1 file / **44 tests PASS**，exit `0`。其中非零 status、ENOENT/error、signal、stdout overflow、malformed/raw 非 Buffer 结果全部 fail closed，原 40 项无回归。

### 规定局部门禁

- `node --check scripts/check-supply-chain.mjs` → exit `0`。
- `node scripts/check-docker-build.mjs --static` → exit `0`，status `PASSED`，images 为 web/api/worker/runtime-orchestrator/openclaw-runtime。
- `npx eslint --ext ts,tsx --report-unused-disable-directives --max-warnings 0 tests/deploy-product-source-boundaries.test.ts` → exit `0`。
- `git diff --check 57e4bfc3..7a56f4e4` → exit `0`。
- 未运行官方全量 `npm test`：本轮仅按指定 B00-3A 失败修复范围执行 44 项 targeted 及规定静态门禁，不以局部 Green 替代 P03 后续唯一线性硬门。本轮未改代码、测试、计划、总表或审核意见，且不推送。

## B00-3B 独立 Tester 验证 · 2026-07-12

- 独立 Tester：`/root/p03_3b_tester`。
- Reviewer 局部许可 HEAD：`030717702bfa2f1701d7d4181c964887d2320ca0`；Tester 新增测试 commit：`f3a967ac test(supply-chain): add independent hermetic fixture corners`。
- 环境：Linux `6.8.0-124-generic` x86_64；所有有效门禁均显式使用 Node `v24.18.0`。
- 结论：`PASSED_B00_3B_LOCAL_GATE`。这只是 B00-3B hermetic fixture 的局部 Tester 门禁，**不是 P03 任务级 `PASSED`**，不代表 B00-3C、P02、live evidence 或 registry/signature 验收。

### 独立 corner 与结果

| 独立角度 | 结果 | 证据 |
|---|---|---|
| 多 fixture 并存隔离 | PASS | 同时创建 3 个独占 root，以不同 Git author/committer time 生成 3 个不同 `sourceSha`；report 和 Docker state 只含各自 source prefix，三个 checker 均 exit `0`。 |
| cleanup 幂等与构造失败 | PASS | 同一 fixture 连续 cleanup 两次无异常且不删另一 root，另一 checker 仍绿；隔离 `TMPDIR` 内以空 PATH 注入 Git 构造失败，抛错后无 `lobsterai-p03-evidence-*` 残留。 |
| 权限、token 与 secret 泄漏 | PASS | report + 5 SPDX + 5 Grype 共 11 个生成文件均为 mode `0600`；无 `__[A-Z0-9_]+__` 未替换 token，不含注入的 `NPM_TOKEN`/`DOCKER_AUTH_CONFIG` sentinel 或 private-key header。 |
| fake Docker 严格 CLI | PASS | exact hardened run exit `0`；空 argv、`version`、inspect 缺参/未知 tag、run 缺参、非 read-only、script 漂移、坏 JSON 与把 report 冒充 state 均精确 exit `64`。 |
| 恶意 env state/report 协调 | PASS | 外部 state 把 optional POPO 协调为 installed，同时篡改 report；`runChecker()` 仍强制内部 state path 并以 plugin directory/status mismatch 非零阻断，内部 state bytes 不变。 |

### Red 自证、回归与范围

- Node 24 下单独新增文件：`npx vitest run tests/deploy-supply-chain-evidence-fixture-tester-corners.test.ts --reporter=verbose` → 1 file / **5 tests PASS**，exit `0`。
- 主 worktree 全 B00-3B targeted：新增 5 项 + 原 evidence 47 项共 2 files / **52 tests PASS**，exit `0`；两次完整重放均绿。
- 从 `f3a967ac` 创建仓库外 `git archive`，确认无 `.reports`、无 Git objects，只链接当前 `node_modules`：同样 2 files / **52 tests PASS**。在该归档中临时将 fake Docker 未知命令从 exit `64` mutation 为 exit `0`，定向 CLI 测试稳定 **1 FAIL / exit 1**（期望 64，实际 0）；恢复 helper 后新增 5/5 PASS，归档目录随后删除。
- `node --check scripts/check-supply-chain.mjs`、`node --check scripts/check-docker-build.mjs`、`node scripts/check-docker-build.mjs --static`、changed-test ESLint、禁用模式 `rg`、临时目录残留检查与 diff-check 均 exit `0`；Docker static 报告 exact five `PASSED`。
- 本轮只新增 Tester 专用 test 并追加本报告；未修改 helper/checker/template、B00-3C、P02、计划、总表或审核意见，未运行任务级全量 `npm test`，不推送。

## B00-3C 独立 Tester 验证 · 2026-07-12

- 独立 Tester：`/root/p03_3c_tester`。
- Reviewer 局部许可 commit：`45e399b04f5e8c505da3b0aa239090fbfd794e2f`；Tester test-only commit：`dcb2c93b test(ci): expose B00-3C gate bypasses`。
- 环境：Linux `6.8.0-124-generic` x86_64；Node `v24.18.0`。
- 结论：`FAILED_B00_3C_LOCAL_GATE`。这是 B00-3C 局部 Tester 失败结论，**不是 P03 任务级 `PASSED`**，不代表 P02、live evidence、registry/signature 或总表接受。

### 独立新增覆盖与结果

Tester 新增 `tests/b00-3c-bootstrap-ci-tester-corners.test.ts` 与 `tests/b00-3c-web-fixture-tester-corners.test.ts`，共 41 项独立 corner。直接联合运行稳定为 **2 files / 41 tests：35 PASS / 6 FAIL**，exit `1`。6 个 Red 均由正式 `validateMainCiWorkflow()` 错误接受 mutation 引起；未修改 Developer/Reviewer 的既有测试或实现来迁就结果。

| 独立角度 | 结果 | 证据 |
|---|---|---|
| 根 `pretest` 真实 shell 顺序、逐步 fail-fast、check-only、Vitest 不提前启动 | PASS | 用隔离 PATH 中四个受控可执行文件重放 checked-in `pretest`；成功调用严格为 native rebuild → clean → build → `generate.mjs --check` → Vitest。四个前置分别注入 exit 23 时，只执行到失败点且不启动后续命令。 |
| Node/Linux 冻结语义 | PASS | 当前进程精确为 Linux x64 / Node 24；checked-in workflow baseline 本身仍为 `setup-node 24.x`。 |
| CI 五个关键 step 的唯一性、顺序、`continue-on-error` 与命令 step `if` | 部分 PASS | checkout/setup/cache/`npm ci`/`npm test` 的重复 mutation 全部拒绝；任一 step 声明 `continue-on-error`（包括 `false`）全部拒绝；`npm ci`/`npm test` 的 `if` 拒绝。 |
| CI action step `if` fail-closed | **FAIL / P2** | 分别给 checkout、setup-node、cache 唯一步骤增加 `if: ${{ false }}`，正式 validator 三次都返回空 errors，mutation 被错误接受。 |
| CI test job `continue-on-error` fail-closed | **FAIL / P1** | 给 `jobs.test` 增加 `continue-on-error: true`，正式 validator 返回空 errors；官方测试失败可被工作流级容错语义降级。 |
| CI PR path 与唯一依赖 | 部分 PASS / **FAIL / P1** | `needs` 为空、多依赖或改依赖均拒绝，`pull_request.paths` 拒绝；但 `pull_request.paths-ignore: [scripts/**]` 被错误接受，会违反 scripts/tests-only PR 必须执行 test job 的冻结要求。 |
| CI Node 版本冻结 | **FAIL / P2** | 把唯一 setup-node 的 `node-version` 从 `24.x` 改为 `22.x`，正式 validator 返回空 errors，未锁定 B00-3C 所需 Node 24 语义。 |
| Web fixture 畸形输入 | PASS | malformed JSON、`references` 为 object、缺 path、数字 path、空 path 均在无残留状态拒绝；无 `extends` 的合法最小 project 可构造并幂等清理。独立诊断另确认 TypeScript 允许 `references:null`，因此未把该合法语义误报为缺陷。 |
| Web fixture alias/cycle/symlink | PASS | 相对 alias 与绝对 in-repo alias 指向同一 canonical project 时生成互相隔离的实体副本，单一 package link 稳定指向排序后首副本；写入不回源、不串写。excluded output symlink 不复制，非 excluded nested symlink 仍 fail closed。 |
| Web fixture copy/link failure cleanup | PASS | 两个 project 使用重复 `@lobsterai` package name 触发链接失败、Linux FIFO 触发复制失败，constructor 都删除部分构造 root；无 `lobsterai-web-build-*` 残留。 |
| 并发、二次运行与主输出不变 | PASS | 两个独立 `npm run build --workspace @lobsterai/web` 子进程并发成功，再在首 fixture 二次构建成功；构建前后 9 个 authoritative output 路径按类型/权限/大小/SHA-256 快照完全相同，cleanup 幂等且 `/tmp` 无残留。 |

### 回归、静态门禁与返工要求

- 联合 Tester Red：`npx vitest run tests/b00-3c-bootstrap-ci-tester-corners.test.ts tests/b00-3c-web-fixture-tester-corners.test.ts --reporter=verbose` → **35 PASS / 6 FAIL**，exit `1`；失败稳定集中在 CI validator 上述四类缺口。
- Web Tester 文件单独执行为 **1 file / 11 tests PASS**。Developer/Reviewer 既有 bootstrap、CI、fixture boundary 与两个 Web consumer 定向为 **5 files / 30 tests PASS**，说明本轮没有破坏既有 Green。
- `node scripts/contracts/generate.mjs --check` → PASS（158 routes / 32 channels / 184 inventory / 321 bridge properties）；执行前后 tracked diff SHA-256 同为 empty-diff hash，check-only 未写 tracked 内容。
- 主 CI YAML 独立解析仍为 `needs=[lint]`、5 steps、Node `24.x`。generator Node check、2 个新增 TS 的 CI 等价 ESLint、`git diff --check 45e399b0..dcb2c93b` 与临时目录残留检查均 exit `0`。
- 未执行无输出/已有输出的官方全 B00-3C targeted、官方 full `npm test` 或 P03/P02 后续门：独立 targeted 已出现确定性产品 Red，按 Tester 协议停止扩大耗时验证并退回 Developer，不能用其他 Green 覆盖失败。
- Developer 返工应保持 checked-in workflow 当前结构不变，仅强化 `tests/helpers/ci-workflow-evidence.ts` 及其类型：五个关键 step 任一自有 `if` 都拒绝；`jobs.test` 任一 `continue-on-error` 都拒绝；`pull_request.paths` 与 `paths-ignore` 都拒绝；唯一 setup-node 必须精确冻结 `node-version: 24.x`。修复必须先重放本 test-only Red，再交新的独立 Reviewer；Tester 不修改该实现、计划、总表或 P02，且不推送。

## B00-3C 独立 Tester 修复复测 Round 2 · 2026-07-12

- 复测 Tester：`/root/p03_3c_tester`。
- Developer CI validator 修复：`3a1168be8d900e74f3abcd62698135a9e361c0b5`；Reviewer harness 返工许可：`871605624cfa300a80fca034cf236fdeda1fc1e4`；Tester harness 修复：`18e9aa153a1f67ab468369957acbb62bda9a0e68`。
- 环境：Linux `6.8.0-124-generic` x86_64；Node `v24.18.0`。
- 结论：`PASSED_B00_3C_LOCAL_GATE`。Round 1 的 6 个 CI validator Red 已关闭，Tester 自身跨文件并发误报也已关闭；这仍只是 **B00-3C 本地局部门禁**，不是 P03 任务级 `PASSED`，不代表 P02、live evidence、registry/signature 或总体完成表接受。

### Round 1 Red 关闭与 Tester harness 修正

- 两份 Tester 文件当前为 **2 files / 41 tests PASS**。checkout/setup-node/cache 的 step-level `if`、test job `continue-on-error`、`pull_request.paths-ignore` 与 Node 22 mutation 六项现在全部由正式 validator 非空拒绝；未删除或弱化原 Tester 断言。
- 在 `87160562` 上把 Tester 两文件和原 11 文件放进同一默认并发 Vitest 进程，精确复现 **13 files / 121 tests = 120 PASS / 1 FAIL**。唯一失败是 Tester 最终对共享 `/tmp/lobsterai-web-build-*` 做全局集合等值，误把其他文件合法 root 的生命周期当成残留变化，不是产品 helper 越界。
- `18e9aa15` 只修改 Tester 自己的 Web 文件：并发构建使用测试专属 `TMPDIR`，最终只跟踪自身 `first.root`、`second.root` 和专属目录；两个 build 子进程运行期间，另在共享 tmp 创建并删除一个合法同前缀 owner，显式证明外部 owner 生命周期不会伪报。仍保留主 checkout 输出快照、二次 build、自身 root 逐项不存在、专属目录零残留与 cleanup 幂等断言；未设置 `fileParallelism:false`、全局/套件串行、顺序依赖或提高既有 60 秒局部 timeout。

### 默认并发、clean checkout 与重复状态

- 当前提交上同一 13 文件默认并发命令连续两轮均为 **13 files / 121 tests PASS**，每轮约 10 秒；没有靠重试、串行或文件排序取得 Green。
- 先运行 `scripts/clean-saas-build.mjs`，逐项确认 registry 的 9 个 workspace 均无 `dist`、`dist-types`、`tsconfig.tsbuildinfo`；随后通过官方 `npm test -- ...` 运行原 B00-3C 11 文件，真实执行 native rebuild → workspace clean → workspace build → contracts `--check`，得到 **11 files / 80 tests PASS**。
- 确认 9 个 workspace 的 buildinfo 已重建后，不删除任何输出，以相同官方入口重复第二轮，仍为 **11 files / 80 tests PASS**；两轮 contracts 统计均为 158 routes / 32 channels / 184 inventory / 321 bridge properties。
- 两个独立 Vitest 进程同时运行既有 Web consumer：`tests/scaffold-web-build.test.ts` 为 **1/1 PASS**，`tests/scaffold-tester-corners.test.ts` 为 **5/5 PASS**。Tester 用例内部两个独立 npm/Vite build 也并发成功并在首 fixture 二次成功。

### 只读、静态与残留门禁

- 新鲜 `node scripts/contracts/generate.mjs --check` PASS；14 个 managed contract 输出的 SHA-256、size、mtime、ctime 前后逐项完全相同，确认 check-only 只读。
- 主 CI YAML 独立解析为 `needs=[lint]`、唯一 5 steps、Node `24.x`、无 test job `if` / `continue-on-error`、无 PR `paths` / `paths-ignore`。generator Node check、9 个 B00-3C 相关 TS 文件的 CI 等价 ESLint、`git diff --check 87160562..18e9aa15` 均 exit `0`。
- 最终 tracked worktree clean；`/tmp` 无 `lobsterai-web-build-*`、`lobsterai-b00-3c-*`、fixture source 或 P00 Tester 临时目录残留。主 checkout authoritative output 在 Tester Web 并发与二次 build 前后的类型、权限、大小和 SHA-256 快照不变。
- 未运行已知受 P02 provenance 重冻结事项阻断的官方 full `npm test`；本轮严格完成 B00-3C 局部门禁，不以局部 Green 替代 P02/P03 后续硬门。本轮未修改产品 helper、既有测试、Developer/Reviewer 记录、计划总表或 P02，且不推送。
