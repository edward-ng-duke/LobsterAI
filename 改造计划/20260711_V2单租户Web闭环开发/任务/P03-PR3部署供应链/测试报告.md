# 测试报告

> 初始为空白；仅在 Reviewer APPROVED_FOR_TEST 后由独立 Tester 填写。

- Tester Agent：`/root/p03_tester_final`
- Reviewer 批准基线：`79475f6a9d5386d62465d15049517517275c1ad8`
- Tester 测试提交：`3a327fa1`、`fe35f595`
- 产品 source：`3026eb03a5fc012279165bf8af5e25ec1a890dd3`
- exact-five invocation：`0f5ce7fa-da9a-4525-ae6c-a24cbab58792`
- 环境：macOS Darwin arm64；Node `v24.15.0`；Docker client/server `29.4.0`（OrbStack）；namespace `N/A`（本轮未连接集群）
- Round 1 结论：`FAILED`（以下三项已在 Round 2 关闭）

独立 Tester 新增 19 项测试（证据 corner 14 项、product-source 3 项、Helm/SIGTERM 2 项）。最终 targeted 为 4 files / 56 tests，其中 53 PASS、3 FAIL。三个失败都不是环境阻断：隔离 fixture 中的正式 `scripts/check-supply-chain.mjs` 对协调篡改返回 `PASSED`、exit `0`。

| 用例 ID | 独立角度 | 测试文件/脚本 | 结果 | 证据 |
|---|---|---|---|---|
| `P03-TEST-01` | schema root/深层 unknown、错类型、缺 required | `tests/deploy-supply-chain-evidence-files.test.ts` | PASS | report/root/runtime/plugin item mutation 均非零；wrong Critical count type 与缺 runtime status 均被 Ajv 拒绝。 |
| `P03-TEST-02` | evidence absolute/`..`/symlink/directory/FIFO/替换/hash 协同 | 同上 | PASS | absolute 与 FIFO 均非零；FIFO 在 5 秒内返回且未阻塞读取；用 API 的有效 SBOM 替换 Web 并协调 hash 仍被 subject 对账拒绝。 |
| `P03-TEST-03` | SPDX 多 subject / 缺 relationship | 同上 | PASS | 重复或删除 `DESCRIBES` 后协调文件 hash，正式 checker 均非零。 |
| `P03-TEST-04` | SPDX 重复根 package identity | 同上 | **FAIL / P2** | 复制 Web root package（相同 `SPDXID`）并协调 SBOM SHA-256；实际 checker `PASSED`/exit `0`，期望拒绝重复 package identity。最小失败行：测试文件约 421-440 行。 |
| `P03-TEST-05` | Grype tag/repoDigest/manifestDigest/大小写 severity/Critical 对账 | 同上及既有 evidence mutations | PASS | target/tag/repoDigest/manifestDigest 漂移、wrapper/raw severity 不一致及 Critical 计数漂移均非零。 |
| `P03-TEST-06` | Grype exact duplicate match + wrapper 同步重复 | 同上 | **FAIL / P2** | 原始 `matches[0]` 与 wrapper finding 同步复制、协调 scan SHA-256；实际 checker `PASSED`/exit `0`，期望拒绝同一漏洞/制品 identity 的完全重复记录。最小失败行：约 443-461 行。 |
| `P03-TEST-07` | 本地 Docker image 缺失 / inspect digest 漂移 | 同上 | PASS | 通过隔离 PATH 的假 Docker 分别返回 unavailable 与错误 metadata；checker 均非零并给出对应诊断。 |
| `P03-TEST-08` | plugin declared exact set、required skipped、duplicate、unknown | 同上 | PASS | required Bee skipped、重复 ID、unknown 替换声明 ID 均非零。 |
| `P03-TEST-09` | optional plugin status 与绑定 final-image 目录一致性 | 同上 + hardened `docker run` | **FAIL / P1** | 当前 bound OpenClaw image 中 `moltbot-popo` 目录实际 absent、Bee present；仅把报告 POPO 从 `skipped` 改为 `installed` 后正式 checker 仍 `PASSED`/exit `0`。期望 supply-chain gate 重新核对最终镜像目录，而不是只信任 wrapper。最小失败行：约 493-530 行。 |
| `P03-TEST-10` | product-source：test/角色记录允许，产品 edit/rename/merge 不跳过 | `tests/deploy-product-source-boundaries.test.ts` | PASS | test 与开发/审核/测试记录 commit 保持 baseline；产品编辑、`tests→src` rename、含产品变更的 no-ff merge 均解析到包含产品变更的新 source。3/3 PASS。 |
| `P03-TEST-11` | Helm security/resources/probes/NetworkPolicy | `tests/deploy-helm-policy.test.ts` | PASS | 新增组合 mutation 同时缺 SA token/security/resources/readiness/liveness，render scan 给出全部拒绝诊断；既有明确 TCP edge 与 broad/missing-port mutation 保持通过。 |
| `P03-TEST-12` | SIGTERM/health 负向与重复信号 | `tests/deploy-docker-policy.test.ts` | PASS | live gateway 负向端口仍非零；Runtime Orchestrator 连续 SIGTERM/SIGINT 仅关闭一次并清理两类 listener。 |

## 回归、非功能与交接

- 当前未篡改 baseline 的正式 `node scripts/check-supply-chain.mjs`：PASS，57 assets / 5 image evidence，publish/signature 保持 `BLOCKED`。
- exact-five report：mode `0600`、SHA-256 `612940682fa2481eafc608c78aebdcec65d143319d9830b9b65a56c5f06b5ab1`；五镜像 `docker image inspect` 全部为 `linux/arm64`、`10001:10001`，digest 与 report 一致：
  - web `sha256:b58c71f8b37cb2c94514308a4b6a327cd09b8cf1faf9dc5501088bd380fdc5af`
  - api `sha256:160557cccd6f2a8ed92b6461aeec2a781dfe5a07416f229cbaa30278a08a0917`
  - worker `sha256:16d559c8e7452e9e357d0138d3f4ad33cd070ae90451b75bd870776efeff043d`
  - runtime-orchestrator `sha256:695e4da2caae8a752e58fa09eb8e72cbdd9ed992cf25a2bbeec2daebb3b01bf5`
  - openclaw-runtime `sha256:ec9057acd2b3be8dacbdc76198093e173f7988e003b809b86a9990e1e7d196fc`
- `npx vitest run tests/deploy-supply-chain-evidence-files.test.ts tests/deploy-product-source-boundaries.test.ts tests/deploy-docker-policy.test.ts tests/deploy-helm-policy.test.ts`：4 files；56 tests；53 PASS / 3 FAIL，exit `1`。
- Green 对照单独执行：product-source + Docker/Helm 为 3 files / 19 tests PASS；changed-test ESLint 与 `git diff --check` PASS。
- 未重跑约 40 分钟的五镜像 build：现有 exact-five evidence 有效且缺陷发生在 formal evidence re-validation 层；重建镜像不能关闭上述绕过。
- 未运行全量 `npm test`：targeted 已出现确定性产品 Red，按 Tester 协议立即停止扩展并退回 Developer；不得把本轮写成 PASSED。
- 返工要求：供应链 checker 应拒绝 SPDX 重复 package identity、Grype 完全重复 match，并将 `pluginInstallations` 与绑定 final image 的 `/opt/openclaw/third-party-extensions/<id>/package.json` 重新核对。Developer 修复后须重新 Reviewer；Tester 不修改业务实现、总体表或 shared main。

## Round 2 · Tester 返工复测

- 复测 Tester：`/root/p03_tester_final`
- Reviewer 批准 HEAD：`0b7215a6c2667be8a2cf376d7979171ce3f98253`
- 产品 source：`187da9a20b742c318e947a96cf0dd66c7cf9eca9`
- exact-five invocation：`64595381-cec8-46b2-a8b8-4631e96dfb9b`
- 当前结论：`PASSED`

### 原 Red 与对称 corner

| 复测项 | 独立结果 | 结论 |
|---|---|---|
| SPDX 重复 root/package identity | Tester 原 mutation 现在由 formal checker 非零拒绝；多/缺 `DESCRIBES` 仍拒绝。 | CLOSED |
| Grype raw match 与 wrapper finding 同步重复 | 完全重复 identity 现在非零；identity 包含 `artifactId`，raw/wrapper 使用完整 finding multiset 双向对账。 | CLOSED |
| POPO absent 却改报 installed | bound final image 中 POPO 实查 absent，报告为 `skipped`；将其改报 `installed` 的原 mutation 现在非零。 | CLOSED |
| NIM present 却改报 skipped | bound final image 中 NIM 实查为 regular file，报告为 `installed`；44d1 反向 mutation 非零。 | CLOSED |
| required Bee present/installed | bound final image 中 Bee 实查为 regular file，报告为 required + `installed`；required skipped mutation 非零。 | CLOSED |

### 独立命令与证据

- Node `v24.15.0`：`npx vitest run tests/deploy-supply-chain-evidence-files.test.ts tests/deploy-product-source-boundaries.test.ts tests/deploy-docker-policy.test.ts tests/deploy-helm-policy.test.ts` → 4 files / **58 tests PASS**，exit `0`，39.79s。
- `node scripts/check-supply-chain.mjs` → PASS，57 assets / exact 5 image evidence / publish `BLOCKED`；`node scripts/check-docker-build.mjs --static` → PASS。
- report mode `0600`、SHA-256 `613fe5f8478608d9723c5cf0296cb4de090dc488200c9a36f3a902cd366c67bc`；五份 SBOM 与五份 Grype 文件逐项实算 hash 全部匹配。raw Grype Critical 合计 `0`、wrapper Critical 合计 `0`、空 `artifactId` 合计 `0`。
- 五镜像 `docker image inspect` 均为 `linux/arm64`、`10001:10001`，Id/RepoDigest 与 report 一致：
  - web `sha256:67040e3975c3671600a6513c6fbdd2a60ff2342e7fe857617183b2d58883b21e`
  - api `sha256:36e5261c5660af60d4dc36924d56430fc891c36feaba407345570c930b917991`
  - worker `sha256:d7b6cb8f4f2eff334dc97a2a0f3f7a2f56b3a7eb73c6e2b8645097fa02838444`
  - runtime-orchestrator `sha256:01a874d08203cdd271c1d7df529226b10f7cdcc01a4cf66c3d8d773b3f740992`
  - openclaw-runtime `sha256:b3ed4a874b943c4aa9d942feffd6e49cd8f148486d88b16fbd8fa3da0b05a793`
- hardened 一次性容器使用 `--network=none --read-only --cap-drop=ALL --security-opt=no-new-privileges --user=10001:10001` 实查 final image：`moltbot-popo=absent`、`openclaw-nim-channel=present-regular`、`openclaw-netease-bee=present-regular`；与报告的 skipped/installed/installed 完全一致，容器 `--rm` 无残留。
- `resolveProductSourceSha()` 独立求值为 `187da9a2...`；其后的改动只有 evidence test source 绑定与角色记录。独立 boundary tests 继续证明产品 edit、`tests→src` rename、含产品变更的 no-ff merge 不会跳过。
- fresh `npm run supply-chain:check` → PASS，invocation `e1a1f0ce-4b0a-4cc8-a009-ae9109cc00c6`；fresh `npm run helm:lint` → PASS，invocation `8bc991d7-dc42-48d5-9ba7-1fe318974ea7`。Helm 使用官网包 `v3.21.0+ge0878d4` 并在执行前按发布 SHA-256 校验，临时工具目录随后删除。
- 未重跑约 40 分钟的五镜像 build：当前 exact-five report、本地镜像、SBOM/Grype 与 formal checker 均有效，复测目标是身份/目录返工层。未改业务、总体表或 shared main；signature/publish 继续诚实保持 `BLOCKED`。

## B00-3A 独立 Tester 验证 · 2026-07-12

- 独立 Tester：`/root/p03_3a_tester`
- Reviewer 局部许可 commit：`d610a76915cad4713675d71ba0551353f6f114ba`
- Tester 新增测试 commit：`a9dd7a29 test(supply-chain): add independent parser corners`
- 环境：Linux `6.8.0-124-generic` x86_64；Node `v24.18.0`
- 结论：`FAILED_B00_3A`。这是 B00-3A 局部失败结论，不是 P03 任务级 `PASSED`。

### 独立 corner 结果

| 独立角度 | 结果 | 证据 |
|---|---|---|
| root commit 命令与 fail-closed | PASS | 受控 runner 精确观测到 `git diff-tree --root --no-commit-id -r --name-status -z -M -C <root>`，raw 调用无 `encoding`且 `maxBuffer=16 MiB`；非零返回后 resolver 保守返回 root commit。 |
| 默认 factory 等价性 | PASS | 真实临时 Git repo 上 `createProductSourceShaResolver()()` 与公开 `resolveProductSourceSha()` 返回同一 baseline。 |
| 真实 Git 病理路径与清理 | PASS | 实际 commit/diff 覆盖 emoji、CR、leading dash、`docs/\uFEFFname.md` 与 `\uFEFFdocs/...`；frozen 路径不推进，伪装 frozen 的未知路径 fail closed 推进。用例显式对账 `process.env`和 `/tmp/lobsterai-p03-product-source-*`，执行后无临时目录残留。 |
| runner 异常 stdout/错误组合 | **FAIL / P2** | raw name-status 对 string、`Uint8Array`、`status:0 + error` 正确 fail closed；但文本 `rev-list` 对 Buffer/null stdout 抛 `TypeError`，对 `status:0 + error/signal` 继续信任 stdout，最终错误返回旧 frozen baseline，而非当前 analyzed commit。 |

### 稳定复现

Node 24 下只运行新增 4 项：

```bash
PATH="$HOME/.nvm/versions/node/v24.18.0/bin:$PATH" \
  npx vitest run tests/deploy-product-source-boundaries.test.ts --reporter=verbose \
  -t "uses the exact root|keeps the default factory|preserves real Git|fails closed for abnormal"
```

结果：3 PASS / 1 FAIL / 40 skipped，exit `1`。失败聚合输出稳定显示：

- Buffer stdout：`TypeError: result.stdout.trim is not a function`；
- null stdout：`TypeError: Cannot read properties of null (reading 'trim')`；
- `status:0 + error`：实际返回 frozen baseline；
- `status:0 + signal`：实际返回 frozen baseline。

根因位于 `scripts/check-supply-chain.mjs:120-123`：文本 `gitOutput()` 仅检查 `status === 0`，未检查 `error`、`signal` 和 `typeof stdout === 'string'`，且直接调用 `stdout.trim()`。Tester 未修改生产代码；当前实现本身已稳定 Red，无需再做人工 mutation。

### 规定门禁

- 全 B00-3A targeted：`npx vitest run tests/deploy-product-source-boundaries.test.ts --reporter=verbose` → 1 file / 44 tests，43 PASS / 1 FAIL，exit `1`。原 40 项全部 PASS，唯一失败为新增防御性 corner。
- `node --check scripts/check-supply-chain.mjs` → exit `0`。
- `node scripts/check-docker-build.mjs --static` → exit `0`，status `PASSED`，exact five 全部通过。
- `npx eslint --ext ts,tsx --report-unused-disable-directives --max-warnings 0 tests/deploy-product-source-boundaries.test.ts` → exit `0`。
- `git diff --check d610a769..a9dd7a29` → exit `0`。
- 未执行官方全量 `npm test`：B00-3A targeted 已出现确定性产品 Red，按局部 Tester 协议立即退回，不用其他 B00/P02 结果混淆本结论。

### 交接 Developer

Developer 应仅修复文本 Git runner 返回值验证：任一非零 status、`error`、`signal` 或非 string stdout 都必须返回 `undefined`，不得抛异常或继续信任输出；使已知 analyzed commit 路径 fail closed 到该 commit。修复后须重跑新增 4 项、全 44 项 targeted、Node check、Docker static、changed-TS ESLint 与 diff-check，再交新独立 Reviewer/Tester。本轮未改 script、计划、总表、审核意见、B00-3B 或 B00-3C，且未推送。
