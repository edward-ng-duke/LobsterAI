# P03 · PR-3 Docker/Helm/supply-chain scaffold

- 状态：TODO
- 依赖：P00；内部 registry/签名未定时显式 blocked。
- 权威输入：根 AGENTS、19、17 对应 V1/V2、16、附录 C；冲突以根 AGENTS 的生产断公网和附录 C 契约决策为准。

## 源码边界

docker/*、charts/lobsterai、docs/supply-chain、inventory/check 脚本；生产/debug 镜像分离。

## TDD：先写的失败测试

先写缺非 root、latest tag、无 digest/SBOM、生产 noVNC、NetworkPolicy/Quota 缺失的失败检查。 Red 必须因目标行为缺失而失败；环境缺失另记 BLOCKED，不算 Red。

## Corner cases

可选插件、git/npm 私有源、大小写技能路径、native module、多架构、Secret 误入镜像层。

## 实施与验收

Developer 按最小垂直切片逐个 Green，避免无关重构。验收命令：npm run docker:build:check；npm run helm:lint；npm run supply-chain:check。

## 回滚

按镜像 digest/chart version 回滚；删除不完整 manifest，不能保留未审计镜像。

## 完成证据

镜像/Helm lint、SBOM、manifest、密钥扫描及内部源清单。 证据须绑定 SHA/runId/环境；所有触碰 TS 文件 changed-file ESLint 0 warning，相关 Vitest 全绿。

## Developer → Reviewer → Tester 循环

Developer 记录每轮 Red/Green 与 diff → 独立 Reviewer 对照本文件和源码审核；若 CHANGES_REQUESTED，退同一 Developer 修复并进入下一轮 → Reviewer APPROVED_FOR_TEST 后独立 Tester 新增 corner-case/回归测试；Tester FAILED 必须退 Developer 且修复后重新 Reviewer → Tester PASSED 后由协调者核验证据并勾总体表。任何角色不得自行勾 Todo。

