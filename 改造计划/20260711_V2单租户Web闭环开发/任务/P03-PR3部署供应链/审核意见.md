# 审核意见

> 初始为空白；Reviewer 独立审核并按 Round 追加。

## Round 1
- Reviewer Agent：`/root/p03_reviewer`
- 审核时间：`2026-07-11 14:24 +08:00`
- 基线/commit：`cb37e1da^..2d742d688f3d033485cc674d4889c3199d6345d6`；独立复现均绑定 HEAD `2d742d688f3d033485cc674d4889c3199d6345d6`
- 结论：`CHANGES_REQUESTED`

Planner 已冻结的 15 项 AC 缺一不可。本轮静态实现和 21 个 targeted tests 有实质进展，但五镜像/SBOM 动态门在修复后没有完整执行，且供应链门可在零镜像证据时假绿；因此不得交 Tester。

| 严重级 | 用例/需求 | 文件:行 | 实际/期望 | 复现 | 要求证据 |
|---|---|---|---|---|---|
| P1 | P03-AC13、P03-AC15、P03-T12/T14 | `docs/supply-chain/skills-and-plugins.manifest.json:693`；`scripts/check-supply-chain.mjs:214-225,239-264,296-301`；`scripts/saas-stage-gates.json:41-56` | 当前 `imageEvidence` 是空数组，但 checker 不要求恰好覆盖五个生产镜像，`supply-chain:check` 仍以 PASS/0 结束并明确输出 `imageEvidenceChecked:0`。因此“缺 SBOM/digest/source SHA 必须失败”没有成为真实门，ACTIVE gate 可假绿。期望 inventory/SBOM policy 双向要求 web/api/worker/runtime-orchestrator/openclaw-runtime 五项、拒绝缺失/重复/幽灵证据，并绑定同一 HEAD。 | 在 HEAD 直接执行 `node scripts/check-supply-chain.mjs` 得 `status=PASSED, assetsChecked=57, imageEvidenceChecked=0`；随后 `npm run supply-chain:check` exit 0，stage report sourceSha 为当前 HEAD。 | 修复 checker/schema/manifest/runner，新增“空/少一个/重复/额外/旧 SHA/SBOM digest 不匹配”隔离副本 mutation；提交当前 HEAD 五镜像 machine-readable evidence，使 `imageEvidenceChecked=5`，并提供 supply-chain gate PASS/0 报告。 |
| P1 | P03-AC13、P03-T12 | `scripts/check-docker-build.mjs:193-204`；`scripts/check-supply-chain.mjs:214-237` | Docker 门只用 Syft 生成 SPDX 组件清单，没有执行 Trivy/Grype 等漏洞扫描，也没有把任何真实 scan report/critical finding count 接入最终报告。`validateScanReport` 只被单测 fixture 调用，不能证明构建出的镜像没有 Critical 漏洞；waiver 也没有与具体镜像 finding 关联应用。期望真实镜像扫描 fail closed，Critical 默认阻断，有效例外需 owner/reason/expiresAt 并精确关联 finding。 | `rg -n "syft|trivy|grype|validateScanReport|criticalFindings" scripts/check-docker-build.mjs scripts/check-supply-chain.mjs`：构建链只有 Syft，Docker report 返回值无 vulnerability report/critical count；当前 manifest 亦无 image evidence。 | 提供固定版本漏洞扫描器及校验来源；五镜像扫描报告/hash、0 Critical（或合规未过期 waiver）绑定 image digest/source SHA；mutation 必须证明 Critical、未知 finding、过期/错配 waiver 均让真实 gate 非零。 |
| P1 | P03-AC01～AC05、P03-AC13、P03-AC15 | `开发记录.md:51-56,63,67-70`；`scripts/check-docker-build.mjs:141-204,228-260` | Developer 明确记录修复 `8242bcf5` 后未重跑完整五镜像；只有旧 SHA 的 web build，API/worker/orchestrator/OpenClaw 与所有 SBOM 均缺。Reviewer 当前环境复现 `npm run docker:build:check` exit 2/BLOCKED（Syft 不可用），无法独立补证。静态 policy tests 不能证明 Dockerfile 可构建、OpenClaw Linux runtime/原生模块/裸入口完整、五镜像非 root/断网/只读根/health/SIGTERM/history/SBOM 实际成立。 | HEAD：`npm run docker:build:check` → `BLOCKED: missing or unusable syft`，exit 2；Developer 记录第 54、63、68 行也自证未完成。 | 在干净 checkout/批准构建环境、无残留镜像依赖下，以固定 Docker/Syft/漏洞扫描器版本完成五镜像 gate PASS/0。证据至少含每镜像 digest、effective UID:GID、health/start、`--network=none`+read-only+drop ALL smoke、worker/orchestrator SIGTERM、history/layer secret scan、SBOM hash；OpenClaw 另含 pinned version、gateway 启动/ready、Linux arch/native module、入口与禁 GUI/macOS/tenant/secret 内容扫描。所有报告必须绑定返工后的同一 commit SHA。 |
| P1 | P03-AC03、P03-AC05、P03-T06 | `docker/openclaw-runtime/healthcheck.mjs:1-5`；`scripts/check-docker-build.mjs:168-175` | OpenClaw 的 HEALTHCHECK 仅重复读取镜像内 `package.json` 并比对版本；gateway 即使死锁、端口未监听或 readiness 失败仍会被判 healthy。Docker smoke 又只执行 `openclaw.mjs --version`，从未按生产 entrypoint 注入 `/state`、`/workspace`、token 并启动到 gateway ready。期望探针验证真实 gateway 健康/就绪，启动 smoke 证明生产入口和依赖可用。 | 静态检查以上两处即可复现：没有 HTTP/TCP/WS gateway probe；OpenClaw 分支不创建运行中容器、不调用 `waitForHealthy`。 | 将 runtime 纳入真实离线启动与 readiness/liveness smoke；增加“gateway 不监听/health 永远真/入口缺依赖”负向用例，并提交当前 SHA 的 ready/stop 日志。 |
| P2 | P03-AC12、P03-T10 | `scripts/check-supply-chain.mjs:121-136`；`src/main/computerUse/computerUseKit.ts:38-70` | `kit:computer-use` 的发现和 integrity 只读取 `computerUseRuntime.ts`；真正的 Kit 默认 metadata、bundle hash、skill list 与 MCP refs 在 `computerUseKit.ts`，修改这些字段不会造成 inventory drift。故“Kit defaults 双向覆盖/完整性漂移阻断”证据不足。 | 复制仓库后仅修改 `buildComputerUseMarketplaceKit()` 的 `skills`/`mcpServers`/bundle metadata，再执行 checker；发现值和 digest 均不读取该文件，门仍可通过。 | 发现器以真实 Kit 注册/构建事实源生成结构化条目并对所有影响 runtime 的输入计算确定性 integrity；新增仅改 bundle/skill/MCP ref 即失败的 mutation。 |
| P2 | P03-AC10 | `charts/lobsterai/templates/networkpolicies.yaml:32-58`；`scripts/check-helm.mjs:169-184` | `allow-service-plane` 允许所有 `app.kubernetes.io/part-of=lobsterai` Pod 之间全端口双向通信，不是 AC10 要求的最小、明确 service/port 规则；render checker 只检查 default-deny 是否存在，无法阻断这一宽放行。 | 渲染模板可见 ingress/egress 均无 `ports`，source/destination 都是全体 lobsterai Pods；将策略扩大到任意内部端口仍不会触发现有 `scanRenderedManifests`。 | 收敛到实际必需 component→component+port 规则；添加删除端口/扩大 selector/全内部互通的负向 render mutation，并给 Helm lint/template PASS/0 报告。 |

### 15 AC 审核摘要

- `AC06-AC09/AC11/AC14`：静态实现方向基本符合生产/debug 分离、digest/schema、安全上下文、Secret 引用、独立 SA/RBAC 占位和 publish BLOCKED 边界；但批准仍依赖同 SHA 的真实 Helm lint/template 报告。Reviewer 当前 `npm run helm:lint` 因缺 Helm exit 2/BLOCKED，Developer 所述 report 未提交且没有绑定最终 HEAD，不能作为本轮可复核证据。
- `AC10`：default deny/ResourceQuota 存在，但 service-plane 规则不够最小，见 P2。
- `AC12`：57 项当前清单能够自洽，但 Kit 事实源完整性有缺口，见 P2。
- `AC01-AC05/AC13/AC15`：动态证据缺失或门禁可绕过，均未通过。
- 回归：`npx vitest run tests/deploy-docker-policy.test.ts tests/deploy-helm-policy.test.ts tests/deploy-supply-chain-policy.test.ts tests/scaffold-stage-gates.test.ts` 为 4 files / 21 tests PASS；这只证明现有静态/mutation 用例，不替代五镜像、Helm、漏洞扫描与全量 `npm test`。Developer 也记录未运行 `npm test`，返工后必须补全。

### 交接 Tester

不交接。Developer 修复以上 P1/P2、在返工后单一 HEAD 重跑 Planner §6 全部命令，并把可复核证据追加到 `开发记录.md` 后，须由独立 Reviewer 开新 Round 复审；只有 15 AC 全部有当前 SHA 的直接证据时才能给出 `APPROVED_FOR_TEST`。

## Round 2 · 2026-07-12 00:22 · `/root/p03_reviewer_final` / `b38282e8`

- 审核范围：完整差异链 `a6066c31..b38282e849877e0d16d5850d23f34f270726ef23`；产品证据 source `e9940762c8ab4cc9b14495de23b43e99dc2a7e6c`；Docker invocation `489a1f9b-bbfe-457e-a630-8b2caca294e1`。
- 结论：`CHANGES_REQUESTED`。

五镜像本身的本轮证据真实且未混用旧 invocation：Reviewer 将 report、五个本地镜像、五条 BuildKit history 和十份 SBOM/Grype 文件交叉核对，确认 exact set 为 `web/api/worker/runtime-orchestrator/openclaw-runtime`，五条 build 均连续发生、`NoCache=true`、`VCSRevision=e9940762...`、status `completed`，BuildKit attachment/image inspect/report digest 三方一致；report mode `0600`、SHA-256 `005a18ea66cd609e998dacb7378da68d617b4de50670f7b861675ef6692470c8`，十份文件 hash 与 report 一致，原始 Grype 报告均为 Critical `0`。但 ACTIVE 供应链门不验证这些文件与声明之间的真实性；下列 P1 mutation 证明报告与磁盘证据不一致时仍可能返回成功，因此 AC13/AC15 仍未满足。

| 严重级 | 用例/需求 | 文件:行 | 实际 / 期望 | 独立复现 | 要求证据 |
|---|---|---|---|---|---|
| P1 | P03-AC13、P03-AC15、P03-T12/T14 | `scripts/check-supply-chain.mjs:253-349,411-424`；`libs/shared/contracts/assets/supply-chain-inventory.schema.json:75-225`；`scripts/check-docker-build.mjs:241-296` | schema 对嵌套 evidence 声明了 `additionalProperties:false`，Docker checker 也在生成时计算文件 hash；但真实 `supply-chain:check` 只从 wrapper report 取 `images` 后执行手写字段检查，从未加载 schema，也不读取 `sbom.path`/`vulnerabilityScan.path`、不重算 hash、不解析 SPDX/Grype schema/subject digest、不将 wrapper findings 与 Grype `matches` 对账、不将 `criticalFindings` 与 findings 计数对账。故报告字段与磁盘证据发生漂移时，门禁仍可能返回成功。期望 gate 对 report 与所有引用文件 fail closed：schema 校验、受控相对路径、存在性、实际 SHA-256、SPDX/Grype JSON 结构、扫描 subject/image digest/source SHA、findings/Critical 计数均双向一致。 | 在 detached `e9940762` 副本复制 invocation `489a...` 后直接运行 `node scripts/check-supply-chain.mjs`：baseline exit `0`；加入顶层 `images[0].tag` exit `1`（仅这一层有效）；加入 `images[0].runtimeEvidence.unexpectedNested` exit `0`；加入 report 根 unknown exit `0`；把 SBOM/Grype path 指向内容分别为 `not an SPDX document`/`not a Grype report` 的文件 exit `0`；同时把 path 与 hash 改成两份测试文件的真实 hash exit `0`；把 web 的 `digest`、`image@digest`、SBOM/scan imageDigest、两份 hash 同步改成不存在的 `sha256:ffff...`，并把 wrapper findings 清空、Critical 改 0，仍 exit `0`；仅将整组 source SHA 改成 `ffff...` 会正确 exit `1`。 | 在 checker CLI/stage runner 层新增隔离副本 mutation，逐项证明上述嵌套 unknown、根 unknown、缺失/越界 path、坏 JSON、错误 schema、文件 hash 漂移、SPDX/Grype subject digest 漂移、wrapper findings/Critical 与真实 Grype 内容漂移均非零；随后以修复后的产品 source 生成 exact-five 新报告并使真实 `supply-chain:check` exit `0`。 |
| P2 | P03-AC12（可选插件状态）、交接明确要求 POPO 诚实 | `scripts/ensure-openclaw-plugins.cjs:729-733,746-770`；`docs/supply-chain/skills-and-plugins.manifest.json` 的 `openclaw-plugin:moltbot-popo` | POPO 安装失败时会正确 warning/skip，但循环结束仍无条件输出 `All 10 plugin(s) installed successfully.`；最终镜像实际 `popo=false`，manifest 却把该资产记为 `platform-reviewed + productionEligible:true`，机器证据没有 installed/skipped 状态。这与“可选插件诚实”冲突，也会误导日志消费者。期望汇总准确区分 installed/cached/skipped/failed，inventory/evidence 如实表达本 invocation 的 optional skip，不能把 9/10 写成 all success。 | `docker buildx history logs nnykfgo97txw7qhoyx4ti9ik4`：先出现 POPO registry socket hang up、`Failed to install optional plugin`、`Skipping moltbot-popo`，随后仍出现 `All 10 plugin(s) installed successfully.`；对最终 digest 在同一 hardened `docker run` 中枚举 `/opt/openclaw/third-party-extensions` 得 `popo:false, bee:true`。Bee 从 867.0s 到 1108.6s 成功，约 242 秒，确实低于 required 10 分钟边界。 | 新增 optional skip 汇总/manifest evidence 测试；真实构建日志不得再声称 all success，并在最终镜像/报告中机器可核对 POPO skipped、Bee installed。 |

### 独立动态与静态复核摘要

- 五镜像：五个 `docker image inspect` 均为 `linux/arm64`、`10001:10001`，digest 与 report/BuildKit attachment 一致；五条 BuildKit record 均 `NoCache=true`，不是失败 invocation `a4d...`/旧镜像证据。当前无 P03 容器残留。
- Hardened OpenClaw 实跑：`--network=none --read-only --cap-drop=ALL --security-opt=no-new-privileges --user=10001:10001`，受限 `/tmp`、`/state`、`/workspace` tmpfs，到 `healthy` 且日志含 `[gateway] ready`；inspect 确认所有约束真实生效；SIGTERM/stop 361ms、exit `0`、OOM false。缺 token exit `2` 且命中 required 错误；非法 `gateway.mode` exit `1` 且输出 `Invalid config`。容器均已清理。
- Runtime Orchestrator：返工实现安装幂等 SIGINT/SIGTERM handler；targeted real-server close 测试通过，invocation report 记录 stop 107ms、exit `0`、OOM false。
- Helm：固定 Darwin arm64 Helm `v3.21.0` 独立运行 `npm run helm:lint`，HEAD `b38282e8` exit `0`；17 documents、3 个负向 mutation，独立 stage invocation `d1f6db1b-4c4f-404b-bf60-a13df4c62bb0`。渲染静态复核确认 digest-only、四 Deployment、Web/API Service、resources、probe、securityContext、独立 orchestrator SA/只读 namespaced RBAC、default-deny/DNS/显式 component→API TCP 规则、ResourceQuota、sandbox fail-closed。
- Targeted：Node `v24.15.0` 下运行 Planner/返工相关 6 个参数，Vitest 实际 5 files / 43 tests PASS；`npm run docker:static` PASS。系统默认 Node 20 的首次 Vitest startup error 属环境版本不满足根 AGENTS 要求，不计产品失败。
- signature/publish：manifest/report 均保持 `BLOCKED`，trusted identities 为空；未发现签名、push、deploy 冒充。
- HEAD/source 说明：`b38282e8` 相比产品 source `e9940762` 仅追加 `开发记录.md`，因此本轮没有把 docs-only 祖先差异误判成五镜像混证；但现 HEAD 直接执行 `npm run supply-chain:check` 确实因五项 `source SHA mismatch` exit `1`。在 `e9940762` detached 副本，未篡改报告 baseline exit `0`。修复 P1/P2 后属于产品代码变化，必须以新的产品 source 重建/重验，不能继续沿用 `e9940762`。
- 因 P1 已稳定复现，本轮未重复耗时约 40 分钟的五镜像 build，也未重复 Developer 已报告的全量 2110 tests；这不影响退回结论。`git diff --check a6066c31..b38282e8` PASS。

### 交接 Tester

不交接。P1 证明 ACTIVE supply-chain gate 在 SBOM、Grype、image digest 与 findings 声明和磁盘事实不一致时仍可 exit `0`，属于 Planner 明确规定必须退回的证据完整性缺口。Developer 修复 P1 与 POPO 状态汇总后，应按新产品 source 重新生成 exact-five evidence、运行 Planner §6 全门禁与回归，再由独立 Reviewer 开新 Round；本轮结论只能是 `CHANGES_REQUESTED`。

## Round 3 · 2026-07-12 01:22 · `/root/p03_reviewer_final` / `2a9cf91d`

- 复审范围：`37808add..2a9cf91d79cbcd7e74d355b0d61669f3addb43c8`；产品 source `3026eb03a5fc012279165bf8af5e25ec1a890dd3`；Docker invocation `0f5ce7fa-da9a-4525-ae6c-a24cbab58792`。
- 结论：`APPROVED_FOR_TEST`。
- P0/P1：未发现。Round 2 的 P1/P2 已按正式 checker、磁盘证据和最终镜像三层独立复核关闭。

### Round 2 退回项关闭结果

| 原问题 | 独立复核结果 | 结论 |
|---|---|---|
| P1：声明 schema 未进入正式 gate | `scripts/check-supply-chain.mjs` 现以 Ajv 2020 同时编译 inventory schema 与 `dockerBuildReport` schema；report 根 unknown、image 根 unknown、嵌套 runtime unknown 都由实际 checker 拒绝。schema 本身对 report、image evidence、build/runtime/history/SBOM/vulnerability/plugin installation 各层使用 `additionalProperties:false`。 | `CLOSED` |
| P1：路径、摘要和原始 SBOM/Grype 内容未对账 | evidence path 只接受证据目录内单一 filename；缺失、`..` 越界、symlink、目录/非 regular file 均拒绝。checker 从磁盘重算 SHA-256、解析 JSON，校验 SPDX 2.3 document/root package/source tag/manifest digest，并校验 Grype image target/tag/repo digest/manifest digest/scanner、raw matches 与 wrapper findings/Critical 计数。最终还以 `docker image inspect` 对账本机 immutable digest。 | `CLOSED` |
| P1 mutation 可在同步改 path/hash、全 `ffff` digest、清空 findings 后成功 | Node `v24.15.0` 独立运行 `tests/deploy-supply-chain-evidence-files.test.ts`：23/23 PASS。其中 19 个负向 consistency mutation 全部得到非零，覆盖 report/嵌套 unknown、路径缺失/越界/symlink/non-regular、坏 JSON、SPDX/Grype 结构与 subject/digest 漂移、disk hash 漂移、wrapper findings 清空、同步不存在 image digest、Critical 计数漂移；其余为未修改 exact-five baseline、source resolver 与 plugin evidence/summary 断言。正式 `node scripts/check-supply-chain.mjs` 也在未修改证据上 exit `0`。 | `CLOSED` |
| P2：POPO skip 后仍宣称 all installed | installer 改为分别累计 installed/skipped，并输出 `required=<n>, installed=<n>, skipped=<n>`，不再存在 `All ${plugins.length} ... installed successfully`。新 report 的 OpenClaw `pluginInstallations` 恰为 10 项：9 `installed`、1 `skipped`；唯一 skipped 是 optional `moltbot-popo`，required Bee 为 installed，optional NIM 也为 installed。Reviewer 以 hardened、断网、只读、drop ALL、no-new-privileges 的最终镜像枚举 `/opt/openclaw/third-party-extensions`，确认 POPO 不存在而 Bee/NIM 存在，与机器报告一致。 | `CLOSED` |

### Exact-five 与供应链证据

- report：status `PASSED`、invocation `0f5ce7fa-da9a-4525-ae6c-a24cbab58792`、product source `3026eb03...`、mode `0600`、SHA-256 `612940682fa2481eafc608c78aebdcec65d143319d9830b9b65a56c5f06b5ab1`；image set 恰为 `web/api/worker/runtime-orchestrator/openclaw-runtime`。
- BuildKit：本轮五条 record `bsleoz37.../wqxp3kuc.../nsr9h9ji.../uzt0xaz6.../mj7b2wgw...` 按五镜像顺序连续完成，分别 18/18、18/18、18/18、18/18、25/25，全部 `NoCache=true`；五个 OCI attachment digest 与 report、`docker image inspect` 三方一致。BuildKit 的完整 checkout revision 为 `3fa5dec3`；`3026eb03..3fa5dec3` 只有 evidence test source 绑定更新，不含产品代码。
- 五个本地镜像均为 `linux/arm64`、effective user `10001:10001`，RepoDigest 与 report 一致。十份 SBOM/Grype 文件的 Reviewer 实算 SHA-256 与 report 全部一致；wrapper 与 raw Grype Critical 合计均为 `0`。
- 当前 signature/publish 继续明确 `BLOCKED`，trusted identity 未冻结；没有签名、push、deploy 的成功声明。

### Product source 与角色记录语义

- `resolveProductSourceSha()` 使用 Git pathspec 排除 `tests/**` 和 Developer/Reviewer/Tester 角色记录；当前独立求值为 `3026eb03...`。`3026eb03..HEAD` 仅包含 `tests/deploy-supply-chain-evidence-files.test.ts` 与 `开发记录.md`，所以新 evidence 没有放过后续产品代码提交。
- 正式 checker 还要求 report source 等于上述产品 source 且必须是 HEAD ancestor。产品代码一旦新增提交，就会成为 `git log -1` 的新产品 source，旧 report 因 source mismatch 失败；docs/test-only 提交不要求耗时五镜像重复构建。该语义与 Round 2 的 raw-HEAD mismatch 分开处理，不再把角色记录提交误判为产品漂移。

### 独立命令与阶段门

- Targeted：`npx vitest run tests/deploy-supply-chain-evidence-files.test.ts tests/deploy-supply-chain-policy.test.ts tests/deploy-docker-policy.test.ts tests/ensure-openclaw-plugins.test.ts`，4 files / 57 tests PASS。
- 正式 checker：`node scripts/check-supply-chain.mjs` exit `0`，57 assets / 5 image evidence / publish `BLOCKED`；`npm run docker:static` exit `0`。
- `npm run supply-chain:check` 在当前 HEAD 独立 exit `0`，Reviewer fresh invocation `5dbdcbf8-40b1-41af-8cfa-1c165df9a215`。Developer 记录的前序 fresh invocation `5effed87-91bc-4bc6-adf8-e0e4851fb380` 已被 Reviewer 的原子 stage report 覆盖；Reviewer 没有沿用其 stdout 结论。
- 固定 Helm `v3.21.0` 的 `npm run helm:lint` 在当前 HEAD 独立 exit `0`，Reviewer fresh invocation `8cdb878b-b6b5-4a91-96f4-59de6881ba7e`。Developer 记录的前序 invocation `1338082b-57dc-46d9-8a29-3202b8955896` 同样被 Reviewer replay 覆盖。
- Developer 记录的全量门：官方 `npm test` 为 198 files / 2133 tests PASS / 6 skipped；`contracts:validate`、`typecheck`、`lint:saas`、`build:saas`、renderer build、Electron compile 均 exit `0`。Reviewer 本轮未重复全量 2133 tests，而以新增 57 targeted、正式 checker、两个 ACTIVE stage gate、BuildKit/image/disk evidence 对账覆盖返工风险。
- `git diff --check 37808add..2a9cf91d` PASS；复审前 worktree clean。所有 Reviewer 临时容器均使用 `--rm`，无残留。

### 交接 Tester

允许交接独立 Tester。Tester 应优先从 Developer 未覆盖角度继续检验：协调修改 raw Grype 与 wrapper、plugin installation status 与镜像目录漂移、source resolver 在新增产品提交后的陈旧证据拒绝，以及 evidence 目录的 Unicode/大小写和并发原子写边界。Tester `PASSED` 前，协调者仍不得更新总体完成表或将 signature/publish 从 `BLOCKED` 改为完成。

## Round 4 · 2026-07-12 02:12 · `/root/p03_reviewer_final` / `7f808d29`

- 复审范围：Tester Red `3a327fa1` / `fe35f595` / `b134f47f` 及返工链 `44d1cf63..7f808d29adf522aed192834f867730d6a51238b4`；产品 source `187da9a20b742c318e947a96cf0dd66c7cf9eca9`；exact-five invocation `64595381-cec8-46b2-a8b8-4631e96dfb9b`。
- 结论：`APPROVED_FOR_TEST`。
- P0/P1：未发现。Tester 三项失败及新增的两个对称 corner 均已在正式 checker 路径关闭。

### Tester Red 与对称边界关闭

| 用例 | 独立复核 | 结论 |
|---|---|---|
| SPDX 重复 root/package identity | `validateSpdxDocument` 先要求 `packages[*].SPDXID` 全局为非空唯一值，再要求恰好一个 `DESCRIBES` 且其 identity 只对应一个 root package。重复 root、重复任意 package identity、多/少 subject 均由 formal checker 非零拒绝。 | `CLOSED` |
| Grype raw/wrapper 同步重复 | finding identity 现包含 vulnerability ID、`artifactId`、package、version；raw matches 和 wrapper findings 各自拒绝重复 identity，再用包含 severity 的完整 finding record multiset 双向对账。完全重复及相同 artifact identity/不同 severity 两类对称 mutation 均非零。新 exact-five 的 236 条 wrapper findings 均有非空 `artifactId`，raw/wrapper Critical 均为 `0`。 | `CLOSED` |
| POPO absent 但 report 改 installed | formal checker 不再只信 wrapper：对绑定 `lobsterai-p03-openclaw-runtime:<productSha12>` 执行 `--network=none --read-only --cap-drop=ALL --security-opt=no-new-privileges --user=10001:10001` 的一次性容器检查，逐项验证 final image 中 regular、non-symlink、contained `third-party-extensions/<id>/package.json`，并将 installed set 与 10 项 report status 对账。Reviewer 独立实查为 POPO absent、Bee present、NIM present；报告为 9 installed / 1 skipped，唯一 skipped 是 optional POPO，required Bee 为 installed。反向的“NIM present 但报告 skipped”也被拒绝。 | `CLOSED` |

### Targeted、formal checker 与 source resolver

- Node `v24.15.0`：`npx vitest run tests/deploy-supply-chain-evidence-files.test.ts tests/deploy-product-source-boundaries.test.ts tests/deploy-docker-policy.test.ts tests/deploy-helm-policy.test.ts`，4 files / 58 tests PASS，exit `0`。这包含 Tester 三个原 Red、Developer 新增 identity 对称 corner、产品 edit/rename/no-ff merge source 边界、Helm 组合缺失和 Runtime Orchestrator 重复信号清理。
- `node scripts/check-supply-chain.mjs`：exit `0`，57 assets / exact 5 image evidence / publish `BLOCKED`；`node scripts/check-docker-build.mjs --static`：exit `0`。
- `resolveProductSourceSha()` 当前独立求值为 `187da9a2...`；`187da9a2..HEAD` 只有 evidence test source 绑定与 `开发记录.md`。产品编辑、`tests→src` rename 及含产品变更的 no-ff merge 都在独立 boundary tests 中解析到新产品 source，test/角色记录提交才保持原 source。

### Current exact-five 证据

- report status `PASSED`、invocation `64595381-cec8-46b2-a8b8-4631e96dfb9b`、product source `187da9a2...`、mode `0600`、SHA-256 `613fe5f8478608d9723c5cf0296cb4de090dc488200c9a36f3a902cd366c67bc`，image set 恰为 `web/api/worker/runtime-orchestrator/openclaw-runtime`。
- 五条 BuildKit record `issoqok6.../u9yo2kajo.../wqer89xm.../ktluw8f6.../jkoo9zahu...` 连续完成，分别 18/18、18/18、18/18、18/18、25/25，全部 `NoCache=true`；完整 checkout revision `7ccc0a58` 仅比产品 source 多测试 source 绑定。五个 OCI attachment digest 与 report、当前 `docker image inspect` 三方一致。
- 五镜像均为 `linux/arm64`、effective user `10001:10001`。Reviewer 对十份当前 SBOM/Grype 文件重新计算 SHA-256，全部与 report 一致；五镜像 Critical 合计 `0`，没有空 `artifactId`。
- current digest：web `sha256:67040e3975c3671600a6513c6fbdd2a60ff2342e7fe857617183b2d58883b21e`；api `sha256:36e5261c5660af60d4dc36924d56430fc891c36feaba407345570c930b917991`；worker `sha256:d7b6cb8f4f2eff334dc97a2a0f3f7a2f56b3a7eb73c6e2b8645097fa02838444`；runtime-orchestrator `sha256:01a874d08203cdd271c1d7df529226b10f7cdcc01a4cf66c3d8d773b3f740992`；openclaw-runtime `sha256:b3ed4a874b943c4aa9d942feffd6e49cd8f148486d88b16fbd8fa3da0b05a793`。

### Fresh stages 与当前回归

- Developer fresh supply-chain stage `4db84280-c97c-4470-b721-86f3b4e090c3`、Helm stage `ea4ad9e9-228a-4d62-b0ef-dadabbb43baf` 已记录在当前开发记录。Reviewer 在 HEAD `7f808d29` 重新运行两个 ACTIVE gate，分别得到新的 invocation `7face109-ccc6-4367-a36c-7edd7fd6d00d` 与 `e8a86d24-9b18-4c4e-9c9d-993e3e2556f0`，均 exit `0`；后者使用固定 Helm `v3.21.0`。
- Developer 当前产品 source 全量门记录：`npm test` 199 files / 2154 tests PASS / 6 skipped；`contracts:validate`、`typecheck`、`lint:saas`、Docker static、`build:saas`、renderer build、`compile:electron` 均 exit `0`。Reviewer 本轮按风险聚焦重跑 58 targeted、formal checker、Docker static 与两个 ACTIVE stage gate，没有重跑约 40 分钟 exact-five build。
- `git diff --check 79475f6a..7f808d29` PASS；复审前 worktree clean。Reviewer 的插件实查容器使用 `--rm`，无测试容器残留。

### 交接 Tester

允许交回同一 Tester 复测其三项 Red 与对称 corner。Tester 应确认失败记录转绿后再完成其余独立安全/证据边界；Tester `PASSED` 前总体表仍仅能由协调者维护，signature/publish 继续保持 `BLOCKED`，不得因本地供应链门通过而改写为已签名或已发布。

## Round 5 · 2026-07-12 04:12 · `/root/p03_reviewer_final` / shared `703b20a8`

- 协调指定基线：`13552219379fffc807b3f96c0fdab500f0bdf2dd`；实际复审时共享 HEAD 已前进为其直接子提交 `703b20a8c64b3cdd04e96be7b813cf2d9e68a4bf`（`docs(db): accept P02 completion`）。
- P03 product source：`f93fdd59d98b029cb82da5cc4f5873520472bb7f`；exact-five invocation：`5673b88b-2151-4f73-9f7c-b87a245a0d89`。
- 结论：`CHANGES_REQUESTED` / `BLOCKED_BY_P02_REBASELINE`。P03 自身的 exact-five、formal checker 与 targeted 均通过，但实际当前共享 HEAD 的官方 `npm test` 非绿，且失败包含 P02 trusted provenance 的确定性拒绝；不满足“最终共享集成当前全绿”的批准条件。

### P03 当前正向证据

- 原生 report status `PASSED`、mode `0600`、SHA-256 `3c1561b6e2b7fd787f7496bc950e7656cc4d2133ed0ec86bfd8367cab43ae0e6`；image set 精确为 `web/api/worker/runtime-orchestrator/openclaw-runtime`，57 assets / 5 image evidence，五镜像 Critical 合计 `0`。
- 五条共享 BuildKit record `ljt3x2lu.../lygfbj25.../7mf4zn3w.../u6s8jfy2.../sebfpa4o...` 连续完成，分别 18/18、18/18、18/18、18/18、25/25，全部 `NoCache=true`；完整 checkout revision `304d3449`，product source resolver 当前仍返回 `f93fdd59...`。五个 OCI attachment digest 与 report、当前本地 `docker image inspect` 三方一致，镜像均为 `linux/arm64`、`10001:10001`。
- Reviewer 重新计算五份 SPDX 与五份 Grype 文件摘要，十项均与 report 一致。当前 digest：web `sha256:2531800b80c43b206fe463af0280837cff067cd527b9f2a08564d63fa596b358`；api `sha256:bd5101aac238d81bd895dcd1212704b3e6c39d216c08181b325c79dbf3ece309`；worker `sha256:514af31aa37d02a5f17737069fcd4bd2779db5dc48474c7bec109c60044b920c`；runtime-orchestrator `sha256:ca3e8102a129ba4b71dcb254708e428fcca21a9daa49cbcd6c76f114ba7fc594`；openclaw-runtime `sha256:538975319b88934a851eea2e4d39bdea51994f406c71845308201ae57cb2adfd`。
- OpenClaw `pluginInstallations` 恰为 10 项：9 `installed`、1 `skipped`；required `openclaw-netease-bee` 为 installed，唯一 skipped 为 optional `moltbot-popo`，optional NIM 为 installed。Reviewer 用 hardened、断网、只读一次性容器实查 final image，得到 POPO absent、Bee/NIM present，与 report 一致。
- report 与 manifest 均只声明 signature/publish `BLOCKED`，trusted identity 为空，waivers 为 `[]`；未把本地 build/SBOM 成功表述为已签名、已发布或已部署。
- Node `v24.15.0` 独立 targeted：`tests/deploy-docker-policy.test.ts`、`tests/deploy-supply-chain-policy.test.ts`、`tests/deploy-product-source-boundaries.test.ts`、`tests/deploy-supply-chain-evidence-files.test.ts` 为 4 files / 78 tests PASS。`node scripts/check-supply-chain.mjs` PASS（57/5/BLOCKED），Docker static PASS；fresh `npm run supply-chain:check` PASS，invocation `cba9a377-e891-46ac-b001-b4e807fc0580`。

### P02 联动与当前共享阻断

- 在任务指定的 `13552219` 链上，P02 Reviewer Round 4 已给出 `APPROVED / APPROVED_FOR_TEST`，P02 Tester 也记录 `PASSED`；两者都独立引用相同 P03 source `f93fdd59...` 与 invocation `5673b88b...`。因此 P02 最终证据与 P03 exact-five 的联动基线本身一致。
- 但实际当前 HEAD `703b20a8` 在 `13552219` 之后又修改 `改造计划/20260711_V2单租户Web闭环开发/00-总体完成表.md`。官方 `npm test` 中 `tests/db/p03-merge-evidence.test.ts` 与 `tests/db/reviewer-round2-red.test.ts` 均稳定 exit failure，正式诊断为 `codeEvidenceSha/stageEvidenceSha: non-evidence change after source SHA ... 00-总体完成表.md (703b20a8...)`。这证明 P02 trusted boundary 在实际 current HEAD 已不再通过，不能沿用 `13552219` 的全绿结论描述 `703b20a8`。
- 同次官方 full 结果：207 files，204 pass / 3 fail；2253 tests，2248 pass / 4 fail / 1 existing skip，exit `1`。另外两项失败是 P03 evidence mutation 在 full 并发下超过 5 秒；相同 P03 78 targeted 单独运行已 PASS，因此不把它们单独定性为新的 P03 功能缺陷，但官方全量门仍客观非绿。

### 完成要求

- 协调者需在实际共享 HEAD 重新建立 P02 trusted evidence/rebaseline，使 `00-总体完成表.md` 变更不再位于已签收 evidence source 之后的非 evidence 历史中；或将共享基线恢复到任务指定且已验证的 `13552219`，再按允许的角色记录顺序推进。
- 修复后必须在新的实际共享 HEAD 独立重跑 P02 trusted validator、P03 78 targeted/formal checker 与官方 `npm test`，全部 exit `0` 后再唤回 Reviewer。无需重建五镜像，前提是 P03 image-input resolver 仍为 `f93fdd59...` 且 exact-five 磁盘/本机证据未漂移。
- 本轮不交接新的最终批准；总体表仍只能由协调者维护，且任何总体表更新都必须尊重 P02 evidence provenance 门。

## Round 6 · 2026-07-12 11:58 +08:00 · `/root/p03_3a_spec_reviewer` / `a70136ff`

- 独立审核范围：BASE `9e3c75699e1c6144a052e8b1499c289ded065601` → HEAD `a70136ffe73d1e7ca1c675af8dc5526d5c4a57b1`；逐提交审阅 `e5f3513d` Red 与 `a70136ff` Green 的完整 diff。
- 结论：`CHANGES_REQUESTED_B00_3A`。本结论只是 B00-3A 规格符合性审核，**不是** P03 任务级 `APPROVED_FOR_TEST`。
- 严重级汇总：P0/P1 未发现；存在 2 个可执行 P2 测试契约缺口，因此不得进入 B00-3B。

### 发现

| 严重级 | 要求 | 文件:行 | 实际 / 期望 | 独立复现 | 要求修复 |
|---|---|---|---|---|---|
| P2 | P03 §9.2 要求“按以下契约测试” Git 非零、`spawnSync.error`、signal、输出超限及 parser `undefined` 均使 `changedPathsAgainstFirstParent()` 失败，resolver 随即 fail closed 到当前 commit。 | `tests/deploy-product-source-boundaries.test.ts:71-106`；实现分支在 `scripts/check-supply-chain.mjs:76-84,136-153` | 实现有相应防御分支，但 33 个 targeted tests 只直接覆盖 parser malformed/truncated/unknown/invalid UTF-8，没有任何用例使 name-status Git 调用真实命中 nonzero/error/signal/overflow，也没有断言 resolver 在这些分支返回当前 commit。这些 fail-closed 行为因而未被回归锁定。 | `rg -n 'maxBuffer|ENOBUFS|result\.error|result\.signal|SIGTERM|spawn error|nonzero' tests/deploy-product-source-boundaries.test.ts` 无匹配；定向 P03 boundary test 也找不到上述五类 subprocess fail-closed 用例。 | 以受控 fake Git/可注入 runner 或等价隔离方式，分别触发 nonzero、spawn error、signal、stdout overflow 与 parser `undefined`，对每项断言 `resolveProductSourceSha(root)` 返回正在分析的 commit；不得只检查私有 helper 返回值。 |
| P2 | P03 §9.2 固定 direct parser 输入 `R100\0old\0new\0` 与 `C100\0old\0new\0`，并要求按完整 status grammar 测试 `A/D/M/T/U/X/B` 及 `R0..R100`/`C0..C100`。 | `tests/deploy-product-source-boundaries.test.ts:71-106` | direct 正向用例只喂入 `A`、`M`、`R100` 和 `C087`；固定要求的 direct `C100` 缺失，`D/T/U/X/B` 也没有 parser 接受断言。真实 Git copy fixture 中查看到 `C100` 不等于按 Planner 要求直接锁定 parser grammar。 | `nl -ba tests/deploy-product-source-boundaries.test.ts | sed -n '71,106p'` 显示唯一 direct copy 记录为 `C087`，单路径正向记录只有 `A`/`M`。 | 增加精确 direct `C100` 用例和所有合法单路径 status 的接受用例；保留已有无相似度、`C101`、unknown `Z`、多/少/empty path 拒绝用例。 |

### 已符合项与证据

- 真实 TDD Red：在隔离 `git archive e5f3513d` 中重放 targeted，exit `1`，33 项中 19 PASS / 14 FAIL。失败由未导出 parser 与旧的行/Tab/quoted-path 解析错误引起；`core.quotepath=true/false` 的病理 frozen path 均错误推进 source，不是工具、网络、语法或 fixture 创建失败。Red commit 只修改一个 test 文件。
- Green 实现：name-status 调用与文本 `gitOutput()` 已分离，`spawnSync` 不设 `encoding`、直接返回 raw `Buffer`；两条命令形状均为 `--name-status -z -M -C`。parser 使用 ASCII status 和 `TextDecoder('utf-8', { fatal: true })`，不归一化 NFC/NFD；单路径与 R/C 双路径消费逻辑正确。
- 已覆盖并通过：空 Buffer、无尾 NUL、截断 rename、unknown `Z`、无/越界相似度、空/多余 path、非法 UTF-8；`core.quotepath=true/false`；中文、NFC/NFD、空格、Tab、引号、换行；frozen↔product rename/copy 两端；未知 UTF-8 产品路径推进 source。
- 范围与历史：`9e3c7569..a70136ff` 仅修改 `scripts/check-supply-chain.mjs`、定向 test 和 P03 `开发记录.md`；两提交是直线父子关系，无 merge，未开始 B00-3B/3C、P02、live evidence、registry/signature，也未冒充任务级签收。

### 本轮独立验证

- `node --check scripts/check-supply-chain.mjs` → exit `0`。
- `npx vitest run tests/deploy-product-source-boundaries.test.ts --reporter=verbose` → exit `0`，1 file / 33 tests PASS。
- 隔离 Red replay → exit `1`，19 PASS / 14 FAIL，失败原因如上。
- `node scripts/check-docker-build.mjs --static` → exit `0`，status `PASSED`，exact five 静态策略通过。
- `npx eslint --ext ts,tsx --report-unused-disable-directives --max-warnings 0 tests/deploy-product-source-boundaries.test.ts` → exit `0`。`.mjs` 被当前 ESLint ignore 规则排除，以 `node --check` 覆盖语法门。
- `git diff --check 9e3c7569..a70136ff` → exit `0`。本轮未运行官方 full `npm test`：该门按计划在 B00-3A/3B 与 P02 provenance 同步修复后要求，不用其已知的 B00-3B/P02 失败代替本轮定向判定。

### 交接

不交接 B00-3B。Developer 补齐上述两项 P2 契约测试，记录相应目标 Red/变异证据并在新 HEAD 重跑 targeted、语法、Docker static、changed-test ESLint 与 `git diff --check` 后，由新独立 Reviewer 复审 B00-3A。只有局部契约完整时才可写 `SPEC_APPROVED_FOR_QUALITY_REVIEW_B00_3A`；仍不得写任务级 `APPROVED_FOR_TEST`。

## Round 7 · 2026-07-12 12:11 +08:00 · `/root/p03_3a_spec_reviewer` / `969f40d4`

- 复审范围：Round 6 审核 commit `d85815433cb11529ca53f05865ce5c066ecdb52c` → Developer 返工 commit `969f40d4ae6f0ba4ad09124a4e37fb223ddd1c95`。
- 结论：`SPEC_APPROVED_FOR_QUALITY_REVIEW_B00_3A`。Round 6 的两项 P2 已全部关闭，未发现新 P0/P1/P2。
- 限定：这只是 B00-3A 局部规格符合性结论，**不是** P03 任务级 `APPROVED_FOR_TEST`、`PASSED` 或总表接受；B00-3B/3C、P02、live evidence 与 registry/signature 仍须按唯一线性顺序继续。

### Round 6 P2 关闭证据

| 原 P2 | 独立复审 | 结论 |
|---|---|---|
| subprocess nonzero/ENOENT/signal/overflow/parser `undefined` 未在 resolver 端锁定 | `tests/deploy-product-source-boundaries.test.ts:67-91,170-191` 的受控 fake Git 仅在 `diff/diff-tree --name-status` 阶段制造失败，普通 `rev-list` 通过绝对路径委托真实 Git。每例先在正常 PATH 断言 frozen commit 不推进 baseline，再通过生产导出 `resolveProductSourceSha()` 断言五类失败均 fail closed 到 analyzed commit。ENOENT 在第二次 `rev-list --parents` 成功后删除 fake executable，下一次生产 `spawnSync('git')` 真实产生 error；overflow 真实写入 17 MiB，signal 为子进程 SIGTERM，malformed 交给生产 parser 返回 `undefined`。 | `CLOSED` |
| direct grammar 缺 `C100` 及 `D/T/U/X/B` | `tests/deploy-product-source-boundaries.test.ts:134-167` 现直接喂入并断言 `A/D/M/T/U/X/B`、`R0/R100`、`C0/C100`，同时保留 `C087`中间值、NFC/NFD 不归一化及原 malformed/truncated/unknown/illegal UTF-8 拒绝集。 | `CLOSED` |

### 非自证与清理审计

- 新增的 5 项 resolver 用例后面，原 quotepath、Unicode、rename/copy、unknown product path、merge 等 33 项继续运行并全绿；因此 fake PATH 没有泄漏到后续用例。Reviewer 额外比对运行前后 shell PATH 与 `/tmp/lobsterai-p03-product-source-*` 目录集，得到 `PATH_RESTORED=1` 和 `TEMP_ROOT_SET_UNCHANGED=1`。
- Reviewer 在仓库外 `git archive 969f40d4` 中临时破坏生产 `gitNameStatusOutput` 错误分支与 parser-undefined 传递；只重放 5 项 resolver 用例时 5/5 全部转 Red，exit `1`。隔离目录随后删除，主工作树从未修改产品代码。这证明测试会触达生产 resolver，不是 fake 自证。
- `d8581543..969f40d4` 只修改定向 test 与 P03 `开发记录.md`，没有产品实现、B00-3B/3C、计划、总表或其他任务越界。

### 本轮命令证据

- `node --check scripts/check-supply-chain.mjs` → exit `0`。
- `npx vitest run tests/deploy-product-source-boundaries.test.ts --reporter=verbose` → exit `0`，1 file / 38 tests PASS；原 33 项保留，新增 5 项 resolver fail-closed。
- `node scripts/check-docker-build.mjs --static` → exit `0`，status `PASSED`，5 images checked。
- `npx eslint --ext ts,tsx --report-unused-disable-directives --max-warnings 0 tests/deploy-product-source-boundaries.test.ts` → exit `0`。
- `git diff --check d8581543..969f40d4` → exit `0`；复审前 tracked worktree clean。

### 交接

B00-3A 可以将本限定结论交给协调者进入下一个线性步骤 B00-3B。后续任何对 resolver 产品实现或本定向测试的修改都使本轮局部结论过期，必须重新审核；P03 最终签收仍只能在后续 B00 全部硬门满足后执行。

## Round 8 · 2026-07-12 12:19 +08:00 · `/root/p03_3a_quality_reviewer` / `5a596f51`

- 独立质量审核范围：实现基线 `9e3c75699e1c6144a052e8b1499c289ded065601`，产品实现 `a70136ffe73d1e7ca1c675af8dc5526d5c4a57b1`，测试 HEAD `969f40d4ae6f0ba4ad09124a4e37fb223ddd1c95`；实际审核 HEAD `5a596f51222ec55731f77b6c928a197671b8beca` 在其后只有审核记录。
- 结论：`QUALITY_CHANGES_REQUESTED_B00_3A`。本结论是 B00-3A 独立代码质量退回，**不交 Tester，且不是** P03 任务级 `APPROVED_FOR_TEST`、`PASSED` 或总表接受。
- 严重级汇总：Critical/P0、P1 未发现；存在 1 项 Important/P2 实现资源边界和 2 项明确 Minor/P3 测试质量缺口。

### 质量发现

| 严重级 | 文件:行 | 实际风险与独立复现 | 要求修复 |
|---|---|---|---|
| Important / P2 | `scripts/check-supply-chain.mjs:97-103`（`fields` 全量收集），并受 `:76-84` 的 16 MiB stdout 上限约束 | parser 在验证第一条 record 前，先为每个 NUL 创建一个 `Buffer.subarray` 对象；其空间不是接近输入 bytes，而是随字段数产生高倍对象放大。Node `v24.18.0` 下直接喂入 100,000 条合法且路径唯一的 `A\0p${index}\0` record：输入仅 888,890 bytes，成功返回 100,000 paths，但单次解析 RSS 增加 43,966,464 bytes、heapUsed 增加 44,933,680 bytes。另一次 200,000 条、800,000 bytes 的合法 grammar 输入把 RSS 从 65,159,168 推至 156,311,552 bytes。生产允许 name-status stdout 达 16 MiB，因此大型但合法仓库可在到达 maxBuffer 之前耗尽大量堆并崩溃；崩溃不是 resolver 返回当前 commit 的 fail-closed 结果。 | 改为单遍按 offset 消费 status/path，边解析边验证，不预先为每个 NUL 物化 `Buffer` view；只保留契约要求返回的 path strings。增加低于 16 MiB、字段数高的有界堆回归（建议隔离子进程并设置 heap limit），证明解析完成或按显式资源策略返回 `undefined`，而不是 OOM/进程中止。不得用删除 `maxBuffer` 或静默丢 path 规避。 |
| Minor / P3 | `tests/deploy-product-source-boundaries.test.ts:70-117` | fake Git 通过逐项拼接 `PATH/git` 找真实可执行文件，并只创建名为 `git` 的无扩展名 POSIX shebang 文件，再依赖 `chmod(0755)` 和运行中 `unlink`。这在 Linux/macOS 可运行，但 Windows 的真实可执行通常是 `git.exe`，无扩展名脚本不会因 chmod/shebang 成为 `spawnSync('git')` 可执行目标；新增的 5 项 failure-boundary 测试会在进入目标 name-status 分支前失败。当前两个 workflow 全是 Ubuntu，无法证明仓库声明支持的 Windows 官方测试路径。 | 使用平台无关的 runner 注入/mock 或真正跨平台的可执行 fixture 来制造 nonzero/error/signal/overflow/malformed，保留对生产 `resolveProductSourceSha()` 的端到端断言；至少增加 Windows 重放证据。若继续改 PATH，真实 Git 定位必须尊重平台可执行后缀，并在原 PATH 不存在时用 `delete process.env.PATH` 恢复，而不是赋值 `undefined`。 |
| Minor / P3 | `scripts/check-supply-chain.mjs:87-90`；`tests/deploy-product-source-boundaries.test.ts:123-169` | `ignoreBOM: true` 当前会正确保留路径开头的 U+FEFF；独立探针得到 `parseGitNameStatusZ(A\0<EF BB BF>docs/file\0)[0].codePointAt(0) === 0xfeff`。但定向测试只锁定 NFC/NFD 与非法 UTF-8，没有锁定这个容易被误读的选项。若未来删除该选项并回到 `TextDecoder` 默认行为，leading BOM 会被静默剥离，`\uFEFFdocs/...` 会从 unknown/fail-closed 路径误分类为 frozen `docs/`。 | 增加 raw bytes direct parser 用例，明确断言 leading UTF-8 BOM 作为文件名字符被保留，且 resolver 对 `\uFEFFdocs/...` 保持保守分类；不要把它当文本文件 BOM 删除。 |

Important/P2 的最小复现（先选择仓库要求范围内的 Node 24）：

```bash
node --input-type=module - <<'NODE'
import { parseGitNameStatusZ } from './scripts/check-supply-chain.mjs';
const input = Buffer.from(Array.from({ length: 100_000 }, (_, index) => `A\0p${index}\0`).join(''));
const before = process.memoryUsage();
const paths = parseGitNameStatusZ(input);
const after = process.memoryUsage();
console.log({ inputBytes: input.length, paths: paths?.length,
  rssIncrease: after.rss - before.rss, heapIncrease: after.heapUsed - before.heapUsed });
NODE
```

### 已确认正确的实现与测试面

- name-status 与普通文本 Git 调用已分离；两条命令精确使用 raw `Buffer`、`-z -M -C`，成功路径不 `trim()`、不按行/Tab 解析。`spawnSync.error`、非零、signal、stdout overflow、非 Buffer 与 parser `undefined` 均向 resolver 传播为当前 analyzed commit。
- status grammar 正确接受 `A/D/M/T/U/X/B` 与数值不超过 100 的 `R/C` similarity，拒绝无尾 NUL、截断、unknown、空/多 path 和非法 UTF-8。`TextDecoder` fatal 解码、BOM 保留与 NFC/NFD 不归一化的当前行为正确。
- root commit 使用 `diff-tree --root --no-commit-id -r`；非 root 使用 first-parent tree diff。merge 回归证明不会被 mainline test-only commit 遮蔽；rename/copy old/new 两端都会进入 frozen/product/unknown 分类。
- 导出面符合固定 API：`parseGitNameStatusZ` 与 `resolveProductSourceSha` 都从生产模块直接导出。Linux 上 fake Git 的 nonzero、真实 ENOENT、SIGTERM、17 MiB overflow 与 malformed 触达生产 resolver；`try/finally` 恢复 PATH，`afterEach` 删除所有临时 root，定向运行未发现子进程、定时器或临时目录泄漏。除上列 Windows 风险外，测试未使用 concurrent 模式，进程级 PATH mutation 不与同文件用例并发。

### 本轮独立验证

- Node `v24.18.0`：`npx vitest run tests/deploy-product-source-boundaries.test.ts --reporter=verbose` → exit `0`，1 file / 38 tests PASS。
- `node --check scripts/check-supply-chain.mjs` → exit `0`。
- `node scripts/check-docker-build.mjs --static` → exit `0`，`PASSED`，5 images checked。
- `npx eslint --ext ts,tsx --report-unused-disable-directives --max-warnings 0 tests/deploy-product-source-boundaries.test.ts` → exit `0`。
- parser 资源探针使用 Node `v24.18.0` 在独立短生命周期子进程中执行；只读取生产导出，没有修改工作树，数值见 Important/P2。当前审核只要求 38 项 targeted、语法、Docker static 与 changed-TS ESLint，未运行官方 full `npm test`。

### 交接

不交 Tester，也不沿用 Round 7 的局部交接许可。Developer 修复 Important/P2，并关闭两个明确 P3；在新 HEAD 重跑 38 项及新增资源/BOM/跨平台回归、Node check、Docker static、changed-TS ESLint 与 `git diff --check`，再由新的独立 Quality Reviewer 复审。任何 resolver 或本定向测试变更都会使本轮证据过期，但仍不等同于触发 P03 任务级最终签收。

## Round 9 · 2026-07-12 12:35 +08:00 · `/root/p03_3a_quality_reviewer` / `04af6c56`

- 复审范围：Round 8 审核基线 `975166bab43b688422ac9418a4498542dd54560c` → test-only `e00ee0936e78d83088a3df29023e7cc69a63ac85` → Green `04af6c56dbd88750ae91912cec4ad033b353cb3f`；完整差异只包含生产 parser、定向 test 与 P03 `开发记录.md`。
- 结论：`QUALITY_CHANGES_REQUESTED_B00_3A`。Round 8 的资源 Important/P2 与 BOM Minor/P3 已关闭，但 Windows fake Git Minor/P3 仍未形成可执行链；本轮仍**不交 Tester，且不是** P03 任务级 `APPROVED_FOR_TEST`。

### Round 8 关闭矩阵

| Round 8 项 | 独立复审证据 | 结论 |
|---|---|---|
| parser 全量 `fields: Buffer[]` 造成高倍内存放大 | `scripts/check-supply-chain.mjs:92-125` 现以单一 `cursor` 顺序定位 status/path NUL，逐字段即时验证与解码；没有 `fields` 数组，只保留返回契约所需的 path strings。Reviewer 从 `e00ee093` 创建独立 archive，在 Node `v24.18.0`、`--max-old-space-size=24` 下只重放 888,890 bytes / 100,000 unique paths：旧实现明确 `Reached heap limit`、`SIGABRT`，1 FAIL / 39 skipped；当前实现同条件 40/40 PASS，资源项再独立连续运行 5 次均 PASS（每次约 77–81 ms）。单遍扫描随 cursor 单调前进，复杂度为输入 bytes 与返回 paths 的线性空间/时间，不再按 NUL 数预存 Buffer view。 | `CLOSED` |
| leading UTF-8 BOM 未锁定 | `tests/deploy-product-source-boundaries.test.ts:190-199` 直接构造 `EF BB BF` + NFD path bytes，断言 parser 返回首字符 U+FEFF 且不归一化为 NFC；生产 `TextDecoder` 保持 `fatal: true, ignoreBOM: true`。当前定向测试通过。 | `CLOSED` |
| fake Git 的 Windows 可执行性 | 真实 Git 搜索现覆盖 `git/git.exe/git.cmd/git.bat`，原 PATH 的 `undefined` 恢复与 `win32` 不 skip 也已补齐；但生成入口在 Windows 仍只有 `git.cmd`，无法被生产无 shell spawn 执行，详见下节。 | `OPEN` |

### 剩余 Minor/P3：Windows launcher 在目标调用前即不可执行

- 文件与行：`tests/deploy-product-source-boundaries.test.ts:85-88,129-140` 只创建 `git.cmd`；生产实际入口 `scripts/check-supply-chain.mjs:71-84` 仍通过 `spawnSync('git', args, ...)` 且没有 `shell`。
- Node 24 官方文档明确：Windows 的 `.bat`/`.cmd` 不能作为无 terminal 的直接 executable 启动；必须使用带 `shell` 的 `spawn`、`exec`，或显式启动 `cmd.exe /c`（[Node.js v24 child_process: Spawning `.bat` and `.cmd` files on Windows](https://nodejs.org/download/release/latest-v24.x/docs/api/child_process.html#spawning-bat-and-cmd-files-on-windows)）。因此 PATH 仅指向 fixture 目录时，生产第一次 `rev-list --first-parent HEAD` 就找不到可直接执行的 `git.exe`；resolver 返回 `undefined`，不会进入 fake program，更不会到达 name-status failure 或 ENOENT 自删分支。`git.cmd` 内的 exit `86` / `del "%~f0"` 逻辑本身不能修复入口不可执行问题。
- 要求修复：使用不改变生产 Git 安全语义的平台无关 runner mock/injection，或提供能够被无 shell `spawnSync('git')` 直接启动的真实 Windows executable fixture；不得通过 `win32` skip，也不建议为了测试把生产 Git 调用改成带 shell 的字符串执行。必须给出 Windows Node 24 上五类 resolver 用例真实进入目标分支的重放证据，至少证明 ordinary `rev-list` 成功代理、name-status nonzero/error/signal/overflow/malformed 分别命中且 ENOENT 发生在代理成功之后。

### 本轮验证与清理

- Node `v24.18.0`：当前 `npx vitest run tests/deploy-product-source-boundaries.test.ts --reporter=verbose` → exit `0`，1 file / 40 tests PASS；旧实现资源 Red 与当前连续 5 次 Green 证据如上。
- `node --check scripts/check-supply-chain.mjs` → exit `0`。
- `node scripts/check-docker-build.mjs --static` → exit `0`，`PASSED`，5 images checked。
- `npx eslint --ext ts,tsx --report-unused-disable-directives --max-warnings 0 tests/deploy-product-source-boundaries.test.ts` → exit `0`；`git diff --check 975166ba..04af6c56` → exit `0`。
- targeted 前后 shell PATH 相同，`/tmp/lobsterai-p03-product-source-*` 集合相同；结束后无该前缀目录，也无命令行包含 `fake-git.cjs` 的 Node 子进程。测试未引入 concurrent/skip 或全局 timeout。
- 本轮未运行官方 full `npm test`：协调指定门限为 40 项 targeted、语法、Docker static、changed-TS ESLint 与 diff-check；不能用这些 Linux Green 替代缺失的 Windows 执行证据。

### 交接

Developer 只关闭上述 Windows P3 后再提交复审；资源 parser 与 BOM 不需重做，除非对应实现或测试再次变化。新的 Reviewer 必须核对 Windows 可直接执行入口和五类 failure 分支证据，随后重跑 40 项及规定门。只有全部关闭时才可写局部 `QUALITY_APPROVED_FOR_TEST_B00_3A`；P03 任务级最终签收仍受后续 B00 硬门约束。

## Round 10 · 2026-07-12 12:43 +08:00 · `/root/p03_3a_quality_reviewer` / `96e819f3`

- 复审范围：Round 9 审核 `ddf7f27d12b5165f60dea18e7f8d254b838231c2` → 最终整改 `96e819f39731b3b2081c0e1fc456559faa8ef47b`；完整差异只包含 `scripts/check-supply-chain.mjs`、定向 test 与 P03 `开发记录.md`。
- 结论：`QUALITY_APPROVED_FOR_TEST_B00_3A`。Round 9 唯一剩余 Windows P3 已关闭，Round 8 的资源/BOM 关闭结论保持有效；这是 **B00-3A 局部代码质量交 Tester 许可，不是** P03 任务级 `APPROVED_FOR_TEST`、`PASSED`、B00-3B/3C 许可或总体表接受。

### Round 9 P3 关闭证据

- 最小工厂 API：`scripts/check-supply-chain.mjs:119-164` 只新增 `createProductSourceShaResolver(runGit?)`，依赖面仅为 Git runner；默认值是模块私有 `defaultGitRunner`，原 `resolveProductSourceSha` 仍由无参数工厂创建，既有调用面不变。Reviewer 直接比较 `createProductSourceShaResolver()()` 与 `resolveProductSourceSha()`，二者均返回当前 `96e819f3...`。
- 生产默认语义不降级：`defaultGitRunner` 唯一实现仍是 `spawnSync('git', args, options)`，没有 `shell`、`NODE_ENV`、平台 launcher 或 test mode。注入只替换 subprocess 依赖，不跳过 first-parent 历史、路径 parser、分类或 fail-closed 决策。
- text/raw 共用 runner：闭包内 `gitOutput` 与 `gitNameStatusOutput` 都调用同一 `runGit`；前者显式 `encoding: 'utf8'`，后者保持 raw Buffer 并传 `maxBuffer: 16 MiB`。生产命令形状、root/first-parent、status grammar 与 parser 均未改变。
- 五类故障真实触达 resolver：`tests/deploy-product-source-boundaries.test.ts:93-136,234-255` 的 runner 对普通 Git 调用继续委托真实无 shell `spawnSync('git')`，只在 name-status 返回 nonzero、`ENOENT` error、`SIGTERM` signal、17 MiB/`ENOBUFS` overflow 或 malformed Buffer。每例都通过生产工厂获得 resolver，断言返回 analyzed commit，并锁定 `delegatedCalls=2`、`nameStatusCalls=1`、`nameStatusMaxBuffer=16 MiB`；若注入未进入生产路径，这些断言不能成立。
- 跨平台清理：旧 `PATH` 修改、git/git.exe/cmd/bat 搜索、`.cmd`、POSIX shebang/chmod、自删入口与 `win32` 分支均已删除；代码/测试无 `shell`、skip、concurrent 或全局 timeout。Windows 不再依赖平台脚本可执行语义，受控 runner 是纯依赖替换。
- 资源与 BOM 无回归：单遍 cursor parser 未改回 fields Buffer 数组；24 MiB / 100,000 records 用例与 U+FEFF/NFD direct 用例继续 Green。测试仍只维护 `temporaryRoots` 并在 `afterEach` 删除；无 PATH 全局状态或失败子进程资源。

### 本轮独立验证

- Node `v24.18.0`：`npx vitest run tests/deploy-product-source-boundaries.test.ts --reporter=verbose` → exit `0`，1 file / 40 tests PASS。
- `node --check scripts/check-supply-chain.mjs` → exit `0`；默认工厂/原生产导出 source SHA 对账 → PASS。
- `node scripts/check-docker-build.mjs --static` → exit `0`，`PASSED`，5 images checked。
- `npx eslint --ext ts,tsx --report-unused-disable-directives --max-warnings 0 tests/deploy-product-source-boundaries.test.ts` → exit `0`；`git diff --check ddf7f27d..96e819f3` → exit `0`。
- targeted 前后 shell PATH 相同，`/tmp/lobsterai-p03-product-source-*` 集合相同；没有残留平台 launcher、临时 root 或 fake Git 子进程。本轮未运行官方 full `npm test`，不以本局部批准替代后续 B00/P03 全量硬门。

### 局部交接

B00-3A 可交 Tester 做同范围独立复测。后续任何对 resolver、Git runner 工厂或本定向测试的修改都会使本轮局部批准过期；P03 任务级签收仍必须等待 B00-3B/3C、P02、live evidence 与 registry/signature 的唯一线性硬门完成。

## Round 11 · 2026-07-12 13:02 +08:00 · `/root/p03_3a_fix_reviewer` / `070b5773`

- 独立修复复审范围：Tester FAILED 记录 `57e4bfc306411ce3a8121741aa82afba8a6a4e11` → Developer 修复 `070b57731fcc8cb487ea4a80c93f7556f8614893`。完整差异只包含 `scripts/check-supply-chain.mjs` 中文本 Git 结果形状防御与 P03 `开发记录.md` 追加；`a9dd7a29..070b5773` 对 Tester 定向测试无差异。
- 结论：`FIX_APPROVED_FOR_TESTER_RECHECK_B00_3A`。Tester P2 已关闭，未发现新 P0/P1/P2/P3。这只是 **B00-3A Tester 失败修复的局部复测许可**，不是 P03 任务级 `APPROVED_FOR_TEST`、`PASSED`、B00-3B/3C 许可或总表接受。

### 修复正确性与不回归审计

- 文本 `gitOutput()` 现在对 `status !== 0`、`result.error`、`result.signal` 或非 string `stdout` 全部返回 `undefined`，只有 status 0、无 error/signal 且 stdout 为 string 时才 `trim()`。Reviewer 以受控 runner 独立覆盖文本 nonzero/error/signal/Buffer/null，每一项均 fail closed 到当前 analyzed commit，无异常外泄或信任矛盾成功状态。
- raw `gitNameStatusOutput()` 仍只接受 status 0、无 error/signal 且 `Buffer` stdout；本修复未改 raw parser、16 MiB `maxBuffer`、`-z -M -C` 命令或单遍 cursor 实现。独立矩阵的 raw nonzero/error/signal/string/Uint8Array 也全部返回 analyzed commit，文本/raw fail-closed 对称且原有 NUL/Unicode/资源边界无回归。
- `defaultGitRunner` 仍是无 shell 的 `spawnSync('git', args, options)`；`createProductSourceShaResolver(runGit?)` 的注入方式、默认工厂行为、`resolveProductSourceSha` 导出及 Docker checker consumer 均未改。Tester 的 root command/default factory/真实 Git 病理路径/异常 runner 四角度完整保留。

### Red→Green 与独立门禁

- 从 `57e4bfc3` 创建仓库外隔离 archive，只重放 Tester 新增四角度：3 PASS / 1 FAIL / 40 skipped，exit `1`；唯一 Red 稳定显示 Buffer/null stdout 抛 `TypeError`，status 0 + error/signal 错误返回 frozen baseline。当前 `070b5773` 同四角度在全量 targeted 中全部 Green，证明用例真实锁定目标缺陷。隔离目录随后删除，未修改当前工作树。
- Node `v24.18.0`：`npx vitest run tests/deploy-product-source-boundaries.test.ts --reporter=verbose` → 1 file / **44 tests PASS**，exit `0`。
- `node --check scripts/check-supply-chain.mjs` → exit `0`；`node scripts/check-docker-build.mjs --static` → `PASSED`，exact five 为 web/api/worker/runtime-orchestrator/openclaw-runtime。
- `npx eslint --ext ts,tsx --report-unused-disable-directives --max-warnings 0 tests/deploy-product-source-boundaries.test.ts` → exit `0`；`git diff --check 57e4bfc3..070b5773` → exit `0`。本局部复审未运行官方 full `npm test`，不以定向 Green 替代后续 B00/P03 硬门。

### 局部交接

可将 `070b5773` 交原独立 Tester 在同范围重检。若 resolver、Git runner 工厂或 `tests/deploy-product-source-boundaries.test.ts` 再有任何修改，本局部许可立即过期；P03 任务级最终签收仍必须等待后续唯一线性硬门完成。

## Round 12 · 2026-07-12 15:43 +08:00 · `/root/p03_3b_spec_reviewer` / `47e0efdf`

- 独立规格审核范围：BASE `ece64fd4afcb39991655fcf38104d939e8ff7f3a` → Red `d7d5c40ccf4aa2011ca36bc49786a315e5ce464d` → Green `47e0efdf0cd973e78c3a8fb7b15a1f66870c1f28`。两个实施提交为直线父子关系，完整阅读根 `AGENTS.md`、V2 `07` B00-3B、P03 计划 §8.1/§8.3/§9.3、本轮开发记录、两个 commit diff 及正式 checker/schema/inventory。
- 结论：`SPEC_APPROVED_FOR_QUALITY_REVIEW_B00_3B`。未发现 B00-3B 规格缺口；这只是 **B00-3B 局部规格符合性交代码质量审核许可**，不是 P03 任务级 `APPROVED_FOR_TEST`、`PASSED`、Tester/协调者签收或总表接受。

### 规格逐项对账

- 真实 Red：在无 `.reports` 的独立 full-history clone 中运行 BASE 定向用例，得到 39 项中 37 FAIL / 2 PASS；36 项为复制主 checkout evidence 的 `ENOENT`，1 项为硬编码 `f93fdd59...` 与当前 product source `070b5773...` 不同。Red commit 在独立 archive 中因 `./helpers/supply-chain-evidence-fixture` 缺失稳定返回 `ERR_MODULE_NOT_FOUND`，没有用工具/网络失败伪充 Red。
- helper 契约：唯一对外工厂为 `createSupplyChainEvidenceFixture(options?)`，返回字段与 §9.3 完全一致。每次调用都 `mkdtemp` 创建独占 root，在其内 `git init --initial-branch=main` 并动态取 root-commit SHA；无 alternates、无主仓 `.git`/object 复用，`afterEach` 清理所有 fixture，定向与独立探针结束后 `/tmp/lobsterai-p03-evidence-*` 无残留。
- exact-five 与 inventory：基线生成恰好 5 个 image record、5 份 SPDX 2.3、5 份 Grype，动态绑定 source/tag/image digest/manifest digest/path，独立重算并对账 10 份 evidence SHA-256。最小资产树发现 11 项 inventory（synthetic kit + 与当前 `package.json` 逐字段相同的 10 个 OpenClaw plugin），与正式 checker 双向对账通过。
- Docker 隔离：fake Docker 只接受精确 `image inspect` 和 hardened OpenClaw plugin-state `run`，其他命令 exit `64`；`valid` / `unavailable` / `digest-drift` 三态均触达正式 checker。Docker state 在 report mutation 前快照生成，`writeReport`/证据 hash 协调不会更新该状态；独立将 POPO report status 协调为 `installed` 后 checker 仍非零，插件目录对账未被绕过。
- hermetic 边界：fixture 唯一 symlink 为当前 checkout `node_modules`，正式 checker 执行时 PATH 首位固定 fixture `bin`；不读主 `.reports`、本地 P03 镜像或主 Git object。禁用模式 `evidenceSourceSha|f93fdd59|objects/info/alternates|repositoryRoot.*\\.reports` 在 test/helper 中无匹配，两个实施提交未携带 `.reports`、镜像、Git object 或 secret/public credential。
- 产品 checker 与范围：`scripts/check-supply-chain.mjs` 仅将原 evidence path 字面量导出为唯一 `p03EvidenceRelativeDirectory`，`scripts/check-docker-build.mjs` 导入复用；该字面量在两 checker 中只有一处。无 `NODE_ENV=test`、fixture path 或 Docker/plugin 跳过分支。差异未修改 workflow、`package.json`、B00-3C、P02、计划、测试报告或总表。
- template 全部明示 synthetic，实际跟踪字节数 7,750 bytes，低于 500 KiB 硬上限。

### 独立验证证据

- Node `v24.18.0`：主 worktree 与无 `.reports` 的 `git archive 47e0efdf` 均运行 `npx vitest run tests/deploy-supply-chain-evidence-files.test.ts --reporter=verbose`，各得 1 file / **39 tests PASS**、exit `0`。
- `node --check scripts/check-supply-chain.mjs` 与 `node --check scripts/check-docker-build.mjs` 均 exit `0`；`node scripts/check-docker-build.mjs --static` 返回 `PASSED`，exact five 为 web/api/worker/runtime-orchestrator/openclaw-runtime。
- `npx eslint --ext ts,tsx --report-unused-disable-directives --max-warnings 0 tests/deploy-supply-chain-evidence-files.test.ts tests/helpers/supply-chain-evidence-fixture.ts` → exit `0`；`git diff --check ece64fd4..47e0efdf` → exit `0`。
- 本局部审核不运行、不宣称 full `npm test`；该 clean/build/contracts-check 启动链属后续 B00-3C，不能用 B00-3B 定向 Green 替代。

### 局部交接

B00-3B 可交独立代码质量 Reviewer 审核。若后续修改 helper、template、本定向 test 或两个 checker 的共享 evidence path，本局部结论立即过期；P03 任务级签收仍须等待 B00-3C、P02、live evidence、internal registry publish/signature 及后续唯一线性硬门。

## Round 13 · 2026-07-12 15:49 +08:00 · `/root/p03_3b_quality_reviewer` / `6163bcbf`

- 独立代码质量审核范围：BASE `ece64fd4afcb39991655fcf38104d939e8ff7f3a` → HEAD `6163bcbf9ff3b2cdc0ed96ed2b49545c39b18bf9`；完整阅读根 `AGENTS.md`、P03 计划 §8.3/§9.3、全部 diff、helper、synthetic template、39 项 test 及两个正式 checker 的相关路径。
- 结论：`QUALITY_CHANGES_REQUESTED_B00_3B`。P0/P1 未发现；存在下列 2 个已用独立 mutation 实证的 P2，因而本轮不得交 Tester。这只是 **B00-3B 局部代码质量结论**，不是 P03 任务级 `APPROVED_FOR_TEST` / `PASSED` 或总表接受。

### P2 发现

1. `tests/helpers/supply-chain-evidence-fixture.ts:212-215` 的 fake Docker 只用三个 `includes()` substring 判定 plugin-state `-e` 脚本“hardened”，未精确锁定完整 inspection script 语义。独立临时 mutation 删除 `stat.isFile()`、`!stat.isSymbolicLink()`、`real.startsWith(rootReal)` 和实际 filter，只把 `fs.realpathSync` / `fs.lstatSync` 留在注释，fake Docker 仍接受命令，正式 checker 返回 `PASSED/0`。因此弱化 plugin 路径 containment、regular-file 或 anti-symlink 保护的产品 mutation 可绕过 fixture，不满足 §9.3 “完整 hardened run，任何参数缺失 exit 64”的自证要求。
2. `tests/helpers/supply-chain-evidence-fixture.ts:315-332` 的公开 `mutateEvidenceDocument()` 直接信任可由公开 `writeReport()` 改写的 `image[kind].path`，在 `path.join()` 后无 containment、regular-file 或 symlink 检查。独立临时探针将 `web.sbom.path` 改为指向 fixture root 外 `/tmp` sentinel 的 `../../../../...json`，再调用该 helper，sentinel 被实际改写。这破坏“每个 mutation 独立临时 root”的 hermetic 边界，组合 mutation 或未来误用可覆盖工作树/其他临时文件。

### 已确认的非问题与门禁

- constructor 全段 `try/catch` 和幂等 `cleanup()` 路径成立：在 9-plugin 故障注入下工厂抛出预期错误，前后 `/tmp/lobsterai-p03-evidence-*` 都为空；两轮 39 项并发独立进程均 PASS，结束无 fixture 残留。
- 未改动 baseline 的动态 `sourceSha` 与正式 resolver 一致，临时 repo 只有 1 个自有 commit、无 alternates；10 份 evidence hash 全部可重算、无剩余 placeholder，report/evidence mode 均为 `0600`，唯一 symlink 为允许的当前 `node_modules`。Docker state 为创建时快照，report/plugin 协调篡改不会自动改写 state。
- template 实际 11,846 bytes，全部明示 synthetic；10-plugin 硬门对未来声明漂移 fail closed。P03 evidence path 字面量在两个 checker 中只有单一定义，Docker checker 单向导入 supply-chain checker，无循环 import。
- Node `v24.18.0` 并发启动两轮 `npx vitest run tests/deploy-supply-chain-evidence-files.test.ts --reporter=verbose` 各得 1 file / **39 tests PASS**，单轮约 9.60 s，最慢用例约 515 ms，FIFO 用例保持局部有界；未依赖全局 timeout、顺序或共享环境。
- `node --check scripts/check-supply-chain.mjs`、`node --check scripts/check-docker-build.mjs`、`node scripts/check-docker-build.mjs --static`、changed-test ESLint、禁用模式 `rg` 与 `git diff --check ece64fd4..6163bcbf` 均 exit `0`。POSIX shebang/mode/FIFO 假设与本 B00-3B 的 Linux 证据目标一致。

### 修复与局部交接

Developer 应让 fake Docker 以独立预期值精确校验完整 plugin-state argv/script，并增加删除/替换任一 path-hardening 子句即 exit `64` 的 mutation test；同时将 `mutateEvidenceDocument()` 固定到按 image/kind 生成的基线路径，或在读写前做 `resolve`/`relative` containment 与 non-symlink regular-file 检查，加入 traversal/symlink sentinel 未被改写的回归。修复后须重跑 39 项至少两轮、两个独立 mutation、Node check、Docker static、changed-TS ESLint 与 diff-check，再交新独立代码质量 Reviewer；本轮不进入 Tester。

## Round 14 · 2026-07-12 16:00 +08:00 · `/root/p03_3b_quality_reviewer` / `e166c35c`

- 复审范围：Round 13 `08c393b25759cb24e9640abe7a2add6365e8be6f` → test-only Red `42bbd87a` → Green `e166c35cff699394d38a106dc9f660fc715f58a3`；完整审阅两提交 diff，只验证 Round 13 的 fake Docker probe 语义绕过与 helper evidence path 越界两项 P2。
- 结论：`QUALITY_APPROVED_FOR_TEST_B00_3B`。Round 13 的 2 个 P2 已关闭，未发现新 P0/P1/P2/P3。这只是 **B00-3B 局部代码质量交 Tester 许可**，不是 P03 任务级 `APPROVED_FOR_TEST` / `PASSED`、B00-3C 许可或总表接受。

### 两项 P2 关闭证据

1. `p03HardenedPluginInspectionScript` 在正式 checker 中只定义一次，`validatePluginInstallations()` 真实用它构造 Docker `run`；helper 在临时 checker copy 被 mutation 前从 tracked 原版 module 取值并写入独立 Docker state，fake 对 `args[10]` 做全文等值而非 substring 匹配。独立直探针得到 `stateMatchesFormalSource=true`、原脚本 fake status `0`、regular-file 语义 mutation fake status `64`；改写 fixture checker copy 后 state 保持不变，checker status `1`。
2. helper 在 report 公开前固定 5 image × 2 kind 的 canonical relative/absolute path；`mutateEvidenceDocument()` 在读文件和调用 mutation callback 之前，同时校验 report path 不漂移、relative containment、regular-file、non-symlink 与 realpath 直属 evidence directory。relative traversal、absolute path 和 canonical-path symlink 三类用例全部抛错，root 外 sentinel bytes 保持不变；既有 canonical path 正向 mutation 继续通过全量用例。

### Red→Green 与门禁

- 在无 `.reports` 的 `git archive 42bbd87a` 中只跑新增 8 项：**7 FAIL / 1 PASS**。五类 plugin probe mutation（regular-file、anti-symlink、root containment、filter 表达式、comment-only 变化）全部被旧 substring fake 伪绿；relative traversal 和 canonical symlink 未抛错，absolute path 是旧实现唯一通过项。临时 archive 随后删除。
- 当前主 worktree 新增定向为 **8/8 PASS**；完整 `tests/deploy-supply-chain-evidence-files.test.ts` 为 **47/47 PASS**；无 `.reports` 的 `git archive e166c35c` 同文件也为 **47/47 PASS**。主 worktree 全量约 11.48 s，archive 约 12.30 s，未见 timeout/并发/清理回归；结束无 fixture、sentinel 或 archive 临时目录残留。
- Node `v24.18.0`；`node --check scripts/check-supply-chain.mjs`、`node --check scripts/check-docker-build.mjs`、`node scripts/check-docker-build.mjs --static`、changed-TS ESLint、禁用模式 `rg` 与 `git diff --check 08c393b2..e166c35c` 均 exit `0`。本局部复审不运行或宣称 full `npm test` / P03 任务级 Green。

### 局部交接

B00-3B 可交独立 Tester 按 `e166c35c` 做同范围复测。若后续修改 hardened probe 常量、fixture Docker state/matcher、canonical evidence path 防护或本定向 test，本局部批准立即过期；P03 任务级签收仍必须等待后续唯一线性硬门。

## Round 15 · 2026-07-12 16:34 +08:00 · `/root/p03_3c_spec_reviewer` / `93723abb`

- 独立规格审核范围：BASE `4791bce1d61b5712c7de78ae913863d489e6ccd4` → Red `e1a1261ad613f46455be7ff5cdaeaeffbbf3da26` → HEAD `93723abb3ba0a087edab6150bf5ee4f72eef9755`。完整阅读根 `AGENTS.md`、V2 `07` B00-3C、P03 计划 §8.3C/§9.4 的本步适用条款、本轮开发记录、五个线性提交、全部 diff、package/TS project references、主 CI、bootstrap/CI/helper 新测试及两个 Web build tests。
- 结论：`SPEC_APPROVED_FOR_QUALITY_REVIEW_B00_3C`。未发现 B00-3C 规格缺口；这只是 **B00-3C 局部规格符合性交代码质量审核许可**，不是 P03 任务级 `APPROVED_FOR_TEST` / `PASSED`，也不代表 B00-5A SaaS artifact DAG、P02、live evidence、registry/signature 或总表接受。

### 规格对账

- Red 精确锁定缺口：在仓库外 `git archive e1a1261a` 中重放两个新文件，11 项为 9 PASS / 2 FAIL，唯一失败分别是根 `pretest` 仅有 native rebuild、以及主 CI `test.needs=[build-main]`；无语法、工具、网络或预存输出伪 Red。Red 的父提交精确为 BASE，BASE→HEAD 无 merge commit。
- `pretest` 严格为 `npm rebuild better-sqlite3` → `tsc -b tsconfig.workspace.json --clean` → `tsc -b tsconfig.workspace.json --pretty false` → `node scripts/contracts/generate.mjs --check`，只用 `&&` 串行 fail fast。mutation 拒绝删除 clean/build/check、Vite、`build:saas`、`npm install`、`|| true`、`contracts:generate` 和 write-mode generator；没有 Vitest setup/alias、Docker、网络安装或 build artifact 交换。
- 从 9 个 workspace 输出根与 9 个 `tsconfig.tsbuildinfo` 全部不存在的状态走官方 `npm test -- ...`，pretest 重建全部 9+9；再删除 9 个输出根、保留并将 9 个 buildinfo 陈旧到 2000-01-01，同一链仍重建全部 9+9。contracts check 返回 PASS（158 routes / 32 channels / 184 inventory / 321 bridge properties），前后 tracked diff 均为空。
- Web fixture 动态遍历的精确闭包为 `apps/web`、`libs/shared/types`、`libs/client/bridge`、`libs/shared/contracts`；其他 app/server workspace 不存在于 temp root。复制时排除 `dist/dist-types/node_modules/*.tsbuildinfo`，唯一主 checkout 链接边界位于 temp `node_modules`，`@lobsterai/*` 则回绑 temp 闭包；两用例各自 `mkdtemp` 并在 `finally` cleanup，不删除或改写主 checkout Web 输出。运行后主 `apps/web/dist` 仍不存在，`/tmp/lobsterai-web-build-*` 无残留。
- 主 CI `test` 只 `needs: [lint]`，`lint` 与 `test` 均无 job-level `if`，`pull_request` 无 paths，checkout `fetch-depth: 0`，命令精确为 `npm ci` 后 `npm test`，test steps 无 `npm install`、`continue-on-error` 或 `|| true`。`tests/deploy-ci-evidence.test.ts` 用 `js-yaml` 真实解析结构，独立解析探针得到 `pullRequest=null`、`needs=[lint]`、两 job 无 `if`、`fetchDepth=0`、`runs=[npm ci,npm test]`。
- BASE→HEAD 实施差异只有主 CI、根 `package.json`、5 个 B00-3C TypeScript test/helper 与 P03 开发记录；没有修改 SaaS workflow、B00-3B helper/template/checker、P02、contract 源/生成物、计划、测试报告或总表。

### 独立验证与局部交接

- Node `v24.18.0`：无输出状态官方 targeted 覆盖 bootstrap、CI、两个 Web tests 及计划的六个原 clean-checkout 失败文件，得 **10 files / 67 tests PASS**；陈旧 buildinfo 状态的 bootstrap/CI 为 **2 files / 11 tests PASS**。
- 官方 full `npm test` 精确为 **210 files：208 PASS / 2 FAIL；2309 tests：2301 PASS / 2 FAIL / 6 SKIP**。唯一失败是 `tests/db/p03-merge-evidence.test.ts` 与 `tests/db/reviewer-round2-red.test.ts`，均报 `P02 evidence bootstrap: trusted file mismatch package.json`；这是后续 P02 provenance 重冻结事项，本轮未改旧断言伪造 full Green。
- 5 个 changed TypeScript 文件 ESLint、`git diff --check 4791bce1..93723abb`、contracts tracked-diff、YAML 解析、范围禁改对账均 exit `0`；本审核记录写入前 worktree clean，无 Web/Red archive temp 残留。

B00-3C 可交独立代码质量 Reviewer。若后续修改根 `pretest`、主 CI `test/lint` DAG、三个新 test/helper 或两个 Web build tests，本局部批准立即过期；P03 任务级签收仍等待后续唯一线性硬门。

## Round 16 · 2026-07-12 16:48 +08:00 · `/root/p03_3c_quality_reviewer` / `210c51e7`

- 独立代码质量审核范围：BASE `4791bce1d61b5712c7de78ae913863d489e6ccd4` → HEAD `210c51e7ebc538690415cacc07e80f66e3010ac6`。完整阅读根 `AGENTS.md`、P03 计划 §8.3C/§9.4、线性提交与完整 diff、workspace project references、pretest/codegen、Web fixture/两个消费者及主 CI/test 契约。
- 结论：`QUALITY_CHANGES_REQUESTED_B00_3C`。P0/P1 未发现；存在下列 2 个经隔离 mutation 实证的 P2，当前不得交 Tester。这只是 **B00-3C 局部代码质量结论**，不是 P03 任务级 `APPROVED_FOR_TEST` / `PASSED` 或总表接受。

### P2 发现

1. `tests/helpers/web-workspace-build-fixture.ts:28-59,98-116` 未拒绝仓库外 project reference。closure 把 `path.relative(repositoryRoot, absoluteReference)` 产生的 `../...` 继续加入集合，`copyProject()` 又把它直接拼到 `targetRoot`，因此 destination 可逃出 fixture root；constructor 的 catch 只删除原 root。独立 `git archive` 中给 Web 增加一个指向相邻临时 project 的 reference，并故意不提供该 project 的 `package.json`：用例按预期在第 86 行报 `ENOENT`，但复制出的 `/tmp/<probe>-external/tsconfig.json` 在 constructor cleanup 后仍存在。也就是说，异常 reference/缺 manifest 会在测试失败前写入并遗留 fixture 边界外的副本，重复或并发运行可能污染其他临时任务。应在读取/复制前以 resolved/relative 双重检查把 config 与 project directory 限制在 repository root，并把 destination 限制在 fixture root；先完整验证 closure/manifest 再复制，新增 cycle、out-of-root、missing-package 与构造失败零残留测试。
2. `tests/deploy-ci-evidence.test.ts:37-57` 只拒绝 `lint`/`test` 的 job-level `if`，没有检查实际 `npm test` step 的 `if`。独立 archive 给该 step 增加 `if: ${{ false }}` 后，契约测试仍为 **1/1 PASS**，但 GitHub Actions 会保留命令文本却不执行官方测试，因而“test 真正无条件”可被字符串伪造。应定位唯一的 `npm ci` 与 `npm test` step，要求两者无 step-level `if`、无任何形态的 `continue-on-error`，并锁定 checkout → setup-node → cache → npm ci → npm test 的实际 step 顺序；增加上述 skip mutation 必须 Red 的回归。

### 已确认的正确行为与门禁

- Node `v24.18.0`。先把 9 个 workspace 输出根和 9 个 buildinfo 清到不存在，再走官方 `npm test -- ...`：计划 targeted 为 **10 files / 67 tests PASS**；从已有输出状态原样重复第二次仍为 **10/67 PASS**，两轮总墙钟分别约 22.7s/22.9s。每轮 pretest 都按 native rebuild → clean → build → contracts `--check` 顺序执行并 fail-fast；重复成本来自计划要求的确定性全量重建，未见 stale buildinfo 复用。
- 在每个 outDir 放非构建 sentinel 后执行 `tsc -b tsconfig.workspace.json --clean`，9 个 sentinel 全部保留，编译文件/buildinfo 被删除；未发现 clean 误删非构建文件。`generate.mjs --check` 返回 PASS，14 个 managed output 的 SHA-256、mtime、ctime 前后逐项相同，且 tracked diff 为零，确认本轮 check-only 只读。
- 当前真实 Web closure 精确为 Web/shared-types/client-bridge/shared-contracts，copy filter 排除了 `dist`、`dist-types`、`node_modules` 和 `*.tsbuildinfo`。两个独立 Vitest 进程同时运行两个 Web build，各 **1/1 PASS**、约 6s；随后主 checkout `apps/web/dist` 仍不存在，`/tmp/lobsterai-web-build-*` 无残留。正常路径 cleanup 幂等，未发现共享输出竞态。
- 当前主 CI 实现本身为 `test.needs=[lint]`，lint/test 无 job-level `if`，pull_request 无 paths filter，实际 step 顺序为 checkout(full history) → setup-node → cache → `npm ci` → `npm test`；scripts/tests-only PR 不受 changed-files/build job skip 牵连。YAML parse、`node --check scripts/contracts/generate.mjs`、5 个 changed TS 的 CI 等价 ESLint与 `git diff --check 4791bce1..210c51e7` 均 exit `0`。
- 未增加全局 serial、`fileParallelism:false` 或全局 timeout；两个 Web 用例只保留各自现有的 30s 局部上限。本轮按协调要求未重复运行已知受 P02 provenance 阻断的 full `npm test`，不以 targeted Green 覆盖该已记录状态。

### 修复与局部交接

Developer 应先关闭上述 closure containment/失败零残留与 CI step-level skip 两项 P2，并用 test-only Red 证明旧实现分别会遗留边界外副本和接受被跳过的 `npm test`。修复后重跑无输出官方 targeted、已有输出重复、两个独立 Web 并发构建、contracts 只读、YAML/Node check/changed-TS ESLint/diff-check，再交新的独立代码质量 Reviewer；本轮不进入 Tester。
