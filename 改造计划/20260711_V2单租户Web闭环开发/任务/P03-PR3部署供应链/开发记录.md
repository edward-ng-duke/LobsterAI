# 开发记录

> 初始为空白；Developer 按 Round 追加 Red→Green→Refactor、commit（获授权后）、命令/退出码/证据与 Reviewer 交接。

## 元数据
- 任务 ID：P03-PR3部署供应链
- Developer Agent：`/root/p03_developer`
- 基线 SHA：`fa15faf1f9cc33c400c290729a5d967ec1e48062`

## 共享热点应用结果

P01 并行期间先记录并暂时移除共享热点草稿；P01 释放后，以其最终 HEAD 为基线重放并由 `169e5bbc` 提交。以下内容现均已应用：

- `package.json`：让 `lint:saas` 覆盖 `tests/deploy-*.test.ts`；新增 `supply-chain:validate`、`docker:build:validate`、`docker:static`、`helm:validate`、`helm:static`，保留三个公开 gate 命令经 stage runner 执行。
- `scripts/saas-stage-gates.json`：将 `supply-chain:check`、`docker:build:check`、`helm:lint` 改为 `PASS` 并分别执行真实 checker，fixtures 改为 P03 实际 schema/manifest/Dockerfile/chart/checker；其余 deferred gate 继续 exit 78。
- `scripts/run-saas-stage-gate.mjs`：命令失败前删除陈旧报告；ACTIVE 命令不得以 78 伪装 deferred；报告绑定 checkout source SHA，stdout+stderr hash，使用 mode 0600 原子写。
- `.github/workflows/saas-scaffold.yml`：路径过滤覆盖 P03 checker/deploy tests；以 commit SHA 固定 `Azure/setup-helm` v5.0.1 与 `anchore/sbom-action/download-syft` v0.24.0，工具版本固定 Helm v3.21.0 / Syft v1.44.0；CI 直接运行三个真实 gate，`expect-saas-stage-gate` 只保留 deferred gate。
- `scripts/check-saas-scaffold.mjs` 与 stage-gate 测试已同步覆盖 active/deferred 状态、fresh report/source SHA、fixture 删除和 checker 退出码；隔离副本使用同一 P03 checker 的 `--static` 模式，单元测试不会误触真实镜像构建。

## Round 1 · 2026-07-11 13:37 · `/root/p03_developer` / `cb37e1da`
### Red

- 先提交 `cb37e1da test(deploy): capture P03 red baseline`，只含三组 deploy 测试与五个恶意供应链 fixtures。
- 命令：`npx vitest run tests/deploy-*.test.ts`；exit `1`；结果：3 files failed、12 tests failed。
- 失败均来自目标行为缺失：五个 Dockerfile 不存在、debug 入口缺失、Helm schema/Deployment/NetworkPolicy/Quota/RBAC/SA token 基线缺失、inventory 仍为 `NOT_APPLICABLE` 且 checker 不存在。未把“本机缺 Docker/Helm”计作 Red。
- Red 覆盖 `P03-T01/T02/T03/T04/T07/T08/T10/T11/T12` 的初始缺失面；`T14` 在 stage runner 激活批次补 mutation。

### Green / Refactor

- `497f0ce1 feat(supply-chain): activate deterministic asset inventory`
  - ACTIVE schema/manifest 双向发现 57 项资产：bundled skills、第三方 OpenClaw plugins、本地 extensions、内置 Kit、MCP defaults。
  - 未锁定 MCP 默认模板如实标为 `blocked + productionEligible:false`；重复/大小写碰撞/symlink 越界/ghost/missing/integrity drift/critical finding/过期 waiver/digest drift/伪 trust identity 均有 mutation。
  - 内部 registry 与签名身份未冻结，manifest 明确 `signature.status=BLOCKED`，不伪造签名或发布。
- `9626ad54 feat(deploy): add hardened production image builds`
  - 新增 web/api/worker/runtime-orchestrator/openclaw-runtime 五个多阶段生产 Dockerfile，数字 UID/GID 非 root、health、exec entrypoint、runtime 无动态安装，生产与 `Dockerfile.debug` 分离。
  - OpenClaw build stage 使用 pinned 版本、Linux x64/arm64 映射和现有 ensure→patch→build→bundle→plugins/extensions/channel-deps→prune 链；最终镜像不复制宿主 vendor/mac 产物。
  - checker 提供静态策略、真实五镜像 build、只读根/断网/cap-drop smoke、graceful stop、history secret scan、Syft SPDX 与 digest/source SHA 证据。
- `876fa677 feat(helm): add secure production deployment baseline`
  - 严格 `values.schema.json`，四个常驻 Deployment、Web/API Service、ConfigMap、Secret name/key 引用、两个 ServiceAccount、最小只读 namespaced RBAC、default-deny + DNS/明确 service-plane NetworkPolicy、ResourceQuota、sandbox RuntimeClass/SA/digest fail-closed 占位。
  - 未实现真实 V1 Pod turn/PVC/egress，也未提前实现 V4 动态 Pod lifecycle。
- `169e5bbc ci(deploy): activate P03 supply chain gates`
  - 三门改为真实 `PASS` command；stage report 删除陈旧文件后原子写、绑定 source SHA，ACTIVE checker exit 78 被转为失败。
  - CI 固定 action commit 与 Helm v3.21.0 / Syft v1.44.0；active gates 直接运行，deferred gates 保持 verifier/exit 78。
- `8242bcf5 fix(deploy): resolve Docker SBOM scanning context`
  - 首次真实 build 发现 OrbStack 非默认 socket 导致 Syft 无法解析本地镜像；改为显式 `docker:<tag>` source 并从当前 Docker context 注入 `DOCKER_HOST`。
  - 同时固定 Dockerfile frontend digest、复用 npm install cache 层，并区分工具缺失 `BLOCKED` 与真实构建/策略 `FAILED`。
- `a64c0d9c style(deploy): sort Helm policy test imports`：修复 changed-file lint 的 import 顺序。

### 自测与证据

- 环境：macOS Darwin 25.5.0 arm64；Node `v24.15.0`；Docker client `29.4.0` / OrbStack；Helm `v3.21.0+ge0878d4`；Syft `v1.44.0`（commit `8cb78ce40ced...`）。Helm/Syft 均下载到 `/tmp`，按官方 checksum 校验后使用并删除，未进入生产镜像或仓库。
- `node scripts/check-supply-chain.mjs` / `npm run supply-chain:check`：exit `0`，57 assets，publish status `BLOCKED`（内部 trust 未冻结）；stage report 绑定 `169e5bbcb492f96872e3fc97d06632fbe5b3bf9f`。
- `node scripts/check-docker-build.mjs --static`：exit `0`，五镜像静态策略全过；`tests/deploy-docker-policy.test.ts` 7/7 pass。
- 完整 Docker 首次实跑：固定/校验 Syft 后，`lobster-web` 真实构建成功；image ID `sha256:cef75ec971b0236fbc435a135fd76bf7f2d32aa53c78fb48f9027a273a8b242e`，effective user `10001:10001`，health config 生效，断网/只读根 smoke 通过。随后 Syft 因未发现 OrbStack socket 失败，exit `2`；该原因由 `8242bcf5` 修复，但按协调者停止指令未重跑完整五镜像，因此 API/worker/orchestrator/OpenClaw 的真实 build/SBOM 证据仍缺失。
- Helm 完整实跑：首次发现 Go regex 不支持 schema negative lookahead；修正为 JSON Schema `not` 后，以校验 checksum 的 Helm v3.21.0 执行 `node scripts/check-helm.mjs` exit `0`，17 份 rendered documents，3 个负向 mutation 均被拒；report invocation `b2a5d025-a2d0-471a-8620-ede4697d1280`，report SHA-256 `c71f6eac5420e191ac2c55393ce44d2e9595a72829a1d01c1591d981881ba387`（`.reports` 未提交）。
- 本机不临时装工具时：`npm run docker:build:check` 与 `npm run helm:lint` 均 exit `2/BLOCKED`，而非 exit 0/78 假绿；CI 已提供固定版本工具安装。
- `npx vitest run tests/deploy-*.test.ts tests/scaffold-stage-gates.test.ts`：4 files、21 tests 全绿；stage 单测在隔离副本使用 static command，不依赖本机残留镜像。
- `npm run scaffold:check`：SCAF-1…SCAF-6 全绿。
- `npm run contracts:check`：exit `0`，确认 P01 `contracts:check` 保持 PASS；最近证据 source SHA `8242bcf58ec6dd75ae4468140e8403401c1f5f02`。
- `npm run typecheck`：exit `0`。
- changed-file ESLint：`npx eslint --report-unused-disable-directives --max-warnings 0 tests/deploy-*.test.ts tests/scaffold-stage-gates.test.ts` exit `0`；`npm run lint:saas` 首轮只发现 Helm test import 顺序，`a64c0d9c` 修复后 `npm run lint:saas` 全量复跑 exit `0`。
- `git diff --check`：exit `0`；交接前 worktree clean。
- 未运行 `npm test` 全量套件；本轮按协调要求停止扩展并完成 targeted/scaffold/contracts 回归。完整 Docker/SBOM 五镜像门也未在修复后重跑，不能把当前 Developer 证据表述为 P03 全验收完成。

### 交接 Reviewer

- 状态：`REVIEW_PENDING`，Developer 不自行批准、不写 Tester 报告、不勾总体表。
- Reviewer 请优先核对 `P03-AC01/AC05/AC13`：当前仅 web 有真实 build/smoke，Syft socket 修复后的五镜像/SBOM 全跑仍需批准构建环境或 CI 证据；缺证据应 `CHANGES_REQUESTED`，不得以静态门替代。
- `P03-AC14` 的 publish/signature 当前按计划显式 `BLOCKED`：内部 registry/trusted identity 未冻结；本地 inventory/build/Helm 可评审，但不得宣称可发布/部署。
- 重点复核：OpenClaw Linux 构建链和裸 `openclaw.mjs` 保留是否真实可构建；Helm rendered securityContext/RBAC/NetworkPolicy 是否符合 P03 占位边界；stage runner source SHA/fresh report/exit 78 mutation 是否不可绕过；CI 外部 actions 是否接受固定 commit 与版本。
- 若 Reviewer 退回，继续唤回 `/root/p03_developer` 修复；只有 Reviewer `APPROVED_FOR_TEST` 后才能启动独立 Tester。

## Round 2 · 2026-07-11 · `/root/p03_developer` / 待最终五镜像门禁

### Reviewer 返工落实

- `dd4ba726 test(deploy): capture P03 review regressions`：先把 Reviewer Round 1 指出的空/缺失/重复/额外/旧 SHA/SBOM digest 漂移、Critical/waiver 错配、Kit 输入漂移、宽 NetworkPolicy、假 readiness 等缺口固化为 Red。
- `2b86ec15`、`3ec43d3e`、`996f2175`、`ef0a40b5`：供应链门现在强制且只接受 web、api、worker、runtime-orchestrator、openclaw-runtime 五项当前提交证据；Docker 门要求 clean worktree、`--no-cache`、Linux 架构、镜像 digest、SBOM、Grype 漏洞报告和实际运行态结果，供应链门校验同一 source SHA、digest 与报告 hash，缺失/重复/幽灵/陈旧/错配一律失败。
- `3ec43d3e`、`4ce283e2`、`62bb1b25`：OpenClaw healthcheck 改为实际 WebSocket upgrade 探针，生产入口以空状态 `--allow-unconfigured` 启动到 gateway ready；入口仍强制 token，非法配置仍 fail closed，均有负向测试。
- `8801dd45`：Helm service-plane NetworkPolicy 收敛为实际 component→API TCP 端口规则；删除端口、扩大 selector、全内部互通的 mutation 均会失败。
- `2b86ec15`：Computer Use Kit integrity 同时绑定 Kit metadata/build fact、runtime 和共享常量输入，修改 bundle/skills/MCP 引用会造成 inventory drift。
- `b2598fa3`、`a987cdce`：普通服务使用固定 digest 的 Node 24.18.0 Alpine 3.24；OpenClaw 因 arm64 Matrix 原生模块不支持 musl，改用固定 digest 的 glibc Node build stage 与 Ubuntu 26.04 production stage，最终仍为数字 UID/GID `10001:10001`。
- `fa542f02`、`d370e0a2`：OpenClaw Docker 构建显式传入 source checkout，并在 build stage 启用项目固定的包管理器，修复真实无缓存构建暴露的 patch source 与 pnpm 缺失问题。
- `246b5f21`、`2a1cdc4d`、`f540f320`：真实 Grype 首次发现 DingTalk 插件 `form-data@4.0.0` 的 `GHSA-fjxv-7rqg-78g4` Critical；先提交真实 finding fixture，再增加构建期依赖加固，强制 direct dependency、override、runtime package、lock record 与固定 integrity 一致为 `4.0.4`，并通过 `npm dedupe` 去除 DingTalk 子树中的其他副本。未为当前漏洞添加 waiver。
- `.github/workflows/saas-scaffold.yml` 固定 Helm `v3.21.0`、Syft `v1.44.0`、Grype `v0.112.0` 及 action commit；scanner policy 记录版本与 checksum 策略，Critical 默认 fail closed，waiver 必须精确绑定 finding 和 image digest 且未过期。

### 当前自测与隔离动态证据

- Targeted：`npx vitest run tests/deploy-*.test.ts tests/scaffold-stage-gates.test.ts` 在 `f540f320` 为 5 files / 28 tests PASS；Docker static policy PASS；相关测试 ESLint PASS。
- 为避免与 P01/P04 并行写入互相污染，以下 OpenClaw 证据来自 detached clean worktree `/tmp/lobsterai-p03-openclaw-probe`，绑定 `f540f320`，仅作为 Round 2 中间证据，不替代最终统一 HEAD 的五镜像门。
- 无缓存构建完成，镜像 `lobsterai-p03-openclaw-probe:f540f320`，manifest-list digest `sha256:567eeaf39f0e9f791729c7b35d2afc24a0cc4d52d15bf348d2f3065fb6e31152`；build log 明确出现 `[openclaw-runtime-hardening] form-data@4.0.4 integrity verified`。
- 以 `--network=none`、read-only root、`cap-drop=ALL`、`no-new-privileges` 和只读/受限 tmpfs 运行生产入口：Docker health 到 `healthy`，日志出现 `[gateway] ready`；SIGTERM 后 exit `0`、`OOMKilled=false`。缺失 token 时 exit `2` 且报 `OPENCLAW_GATEWAY_TOKEN is required`；挂载非法 `/state/openclaw.json` 时 exit `1` 且给出 schema 错误，证明 `--allow-unconfigured` 不会吞掉坏配置。
- Syft `v1.44.0` 生成 SPDX JSON，SHA-256 为 `21fda1660f181e4fde1f146c2f3676b8a2e1f1166b713a69d7eea1d8a934d2e9`。Grype `v0.112.0` 对新镜像得到 203 个 vulnerability matches、Critical `0`。
- 从最终镜像递归枚举所有 `form-data` package/lock：共 5 个 runtime package；root、email、Lark、WeCom 为已修复的 `4.0.6`，DingTalk 为 `4.0.4`，`4.0.0` 数量为 `0`。DingTalk 子树仅有一个 runtime package `4.0.4`、一个 lock record `4.0.4`（integrity `sha512-KrGhL9Q4zjj0kiUt5OO4Mr/A/jlI2jDYs5eHBpYHPcBEVSiipAvn2Ko2HnPe20rmcuuvMHNdZFp+4IlGTMF0Ow==`）和一个 override `4.0.4`，三处严格一致。
- 构建期间 optional POPO 私有 registry 请求发生 `ECONNRESET`，既有 installer 选择 skip，随后输出的 “All 10 plugin(s) installed successfully” 不能作为 POPO 实际存在/可用证据。本轮不宣称 POPO 可用，也不把该行计入绿色供应链证据；生产离线环境仍不得依赖运行时下载。

### 当前状态与后续门禁

- 状态保持 `REVIEW_PENDING`，尚未把本轮交给 Reviewer/Tester。上述隔离镜像证明 OpenClaw 返工路径已可构建、运行且 Critical 归零，但不是 Planner 要求的返工后单一最终 HEAD 五镜像证据。
- 待 P01/P04 并行写入稳定并由协调者确认最终 clean HEAD 后，必须在该 SHA 运行完整五镜像 `docker:build:check`，再运行 `supply-chain:check`、固定 Helm 的 `helm:lint` 以及 Planner §6 全回归；五镜像报告须完整绑定同一 SHA，任一镜像缺失/扫描失败均不得交 Reviewer。
- 当前未创建发布、未推送镜像、未伪造签名/内部 registry 信任；`P03-AC14` 继续按计划保持 publish/signature `BLOCKED`。

## Round 3 · 2026-07-11 · `/root/p03_developer_final` / 最终五镜像门禁失败与返修

### 失败证据（fail closed）

- 在稳定集成基线 `a6066c31` 上的隔离 clean worktree 完成证据持久化、优雅停机、Runtime Orchestrator SIGTERM 和严格 schema 修复后，以 source SHA `34859331e0293a3bbf7940df5542c093344aeaee` 发起全新五镜像 no-cache invocation `a4d15587-3ba3-4ccc-8c7d-bc2f6aefc05e`。
- Web、API、Worker、Runtime Orchestrator 本轮构建与检查已通过；OpenClaw BuildKit record `1gzuzy46tdyrnx4gtiyvq0g8x` 显式记录 `NoCache=true`、VCS revision `34859331...`、status `failed`、21/24 steps。
- 失败根因为必需插件 `openclaw-netease-bee@0.1.3` 在安装依赖时超过通用 5 分钟边界，`spawnSync /usr/local/bin/node ETIMEDOUT`。checker 删除 RUNNING report 且 exit 1，未产生任何最终五镜像报告，也未复用上轮 OpenClaw SBOM/Grype 作为本轮证据。
- 同一日志中 POPO 仍为可选插件：私有 registry 请求 `ECONNRESET` 后诚实记录 warning 并 skip；Bee 没有被改为 optional，Bee 失败直接阻断整个构建。

### Red 与最小修复

- Red：`npm test -- tests/ensure-openclaw-plugins.test.ts` exit 1，15 tests 中新增 3 项失败；分别固化 Bee 必需且不得静默跳过、专用 10 分钟边界、非法/无界 timeout 拒绝。
- 最小修复：为 Bee 声明 `installTimeoutMs=600000`；安装器仅允许正整数且不超过 15 分钟的有界 override，其他插件保持 5 分钟默认；只有 `optional === true` 才允许跳过，Bee 任何安装失败仍必须阻断。
- 修复后必须先跑 targeted tests，然后以新 source SHA 重新发起完整五镜像 no-cache invocation；本节不将 `a4d15587...` 表述为绿色证据。

### 最终返工提交链

- `5eb9712c fix(deploy): persist hardened runtime evidence`：将 no-cache build、effective UID、断网/只读/cap-drop/no-new-privileges、health/gateway ready、stop、history scan 持久化到机器可验证报告和 schema。
- `15034560 fix(deploy): scope graceful stop evidence` 与 `bcc3ba60 fix(deploy): accept bounded SIGTERM exits`：仅 Worker、Runtime Orchestrator、OpenClaw 必须优雅停机；有界 exit 0/143 通过，137/OOM/timeout 仍失败。
- `fb416429 fix(runtime): stop orchestrator on SIGTERM`：修复实际发现的 Runtime Orchestrator SIGTERM 缺失，增加幂等 `server.close()` 与真实 signal 回归。
- `34859331 fix(supply-chain): reject unknown image evidence`：删除 schema 不允许的 `tag` 字段，并让手工 validator 和负向 mutation 拒绝任何未知证据字段。
- `e9940762 fix(openclaw): bound required plugin install time`：完成 Bee 有界 10 分钟安装返修，并把本 Round 失败 Red/Green 记录一并提交。

### 成功五镜像证据（单一 source SHA）

- 以 clean source SHA `e9940762c8ab4cc9b14495de23b43e99dc2a7e6c` 发起全新 invocation `489a1f9b-bbfe-457e-a630-8b2caca294e1`，最终原子 report status `PASSED`，生成时间 `2026-07-11T16:00:38.961Z`，report mode `0600`，SHA-256 `005a18ea66cd609e998dacb7378da68d617b4de50670f7b861675ef6692470c8`。
- BuildKit 最终 OpenClaw record `nnykfgo97txw7qhoyx4ti9ik4` 显式为 `completed`、`NoCache=true`、VCS revision `e9940762...`、25/25 steps；其他四镜像也由同一 checker 按 Web→API→Worker→Runtime Orchestrator→OpenClaw 顺序逐一强制 no-cache 重建，没有复用 `a4d15587...` 的失败证据。

| image | immutable digest | SBOM SHA-256 | Grype SHA-256 | Critical |
|---|---|---|---|---:|
| web | `sha256:5c90ade42fcc0ceeef496b855f7f1fd463fb7462e65072a2bee4952171564530` | `75bf34293478df5a592e145669ff67473b23099246802b51b814b8a9ab6f12f9` | `dea0b5faa045dfef260b4ff8f808de484839ffc54d8698086f510373ec544792` | 0 |
| api | `sha256:1dafd214e3471cca13b9fe018de5caae872b0f83da084eb325013f898c53d09f` | `3f01119f09931905351de27bf8eee363e2903c4f348e0d37237dc33978f669a4` | `cc30cc82338ef04a66bcdaf8b6eb19af70c72529cb19fc77fe34f0004bf23b79` | 0 |
| worker | `sha256:57c84b3e5dcf00562412d36aa38753164899cf69bb47081fb9213df707af1429` | `49d1f1fcf3ddd8424a65480862f27f444272032b1e15442bccf310f099e8aeaf` | `2a73e7a80d8cb935e64d98752edad090199cca80eb2d78560259a86cbe6f0c5f` | 0 |
| runtime-orchestrator | `sha256:63ebf6fb937f4c7870187e10543a8b3e38b953043d0c1c82123e4cefa9f0432b` | `d92af3df9b3a7ee8c55dafedf71656609de8de97290b7a8c7f7f446bd6aa1c10` | `daf4d6a58eb068c4d4ad749dfb0bdb6e580829a7033a4de02be2fdccd204dca2` | 0 |
| openclaw-runtime | `sha256:dcb4c1fbecd08449b4756b8c12864c2ca818fc1839b43253013a424a76be3e03` | `842a95a0f4b238b83bc38ba5c08b912d528fb0fda403a97d8d7d7fe7a88089ef` | `a576d1645d0924e29d5d6a40f8e929200f866b321ede1c617b0849c284c90154` | 0 |

- 五镜像实际 effective user 均为 `10001:10001`，platform 均为 `linux/arm64`，均在 `--network=none`、read-only root、`cap-drop=ALL`、`no-new-privileges`、受限 tmpfs 下到达 `healthy`；OpenClaw 日志额外包含 `[gateway] ready`。
- Worker stop 为 160ms / exit 143 / OOM false，Runtime Orchestrator 为 107ms / exit 0 / OOM false，OpenClaw 为 710ms / exit 0 / OOM false，均在规定边界内。API 不在 AC 必需优雅停机范围，其 10.171s / exit 137 / OOM false 被如实记录为 `required=false`，未伪称优雅退出。
- 独立 Ajv 2020 校验将最终 report 的五项合并到 manifest `imageEvidence`，结果 `ok=true`；每项只有 schema 允许的 10 个字段。五份镜像 digest 与 Docker inspect 一致，SBOM/Grype 文件哈希与 report 全部一致，history secret-like findings 全部为 0。
- OpenClaw 本轮日志显示 Bee 从 867.0s 开始、1108.6s 安装及缓存完成（约 242s），低于 10 分钟边界；随后出现 `[openclaw-runtime-hardening] form-data@4.0.4 integrity verified`。POPO 仍在私有 registry `ECONNRESET` 后以 optional warning 跳过，本轮不声称 POPO 可用，也不把误导性 `All 10 plugin(s) installed successfully` 作为 POPO 存在证据。
- OpenClaw 负向实跑：缺失 token exit 2 且明确报 `OPENCLAW_GATEWAY_TOKEN is required`；挂载非法 `gateway.mode` 配置 exit 1 且报 `Invalid config`，证明 `--allow-unconfigured` 不会吞掉坏配置。
- signature/publish 仍诚实为 `BLOCKED`：内部 trust identity 未冻结，publish forbidden；未签名、未推镜像、未部署。

### 最终 gates 与回归

- `npm run supply-chain:check`：PASS，invocation `913c2314-4792-4634-9107-ea8f47f3e4e7`，source `e9940762...`，stage report SHA-256 `80f3320f0aa397db7d301f4cb3a674467111a02e8be18417b2132dd495fcf471`。
- `npm run helm:lint`：PASS，invocation `3a856d64-13f7-4207-add6-0d6ad6331bed`，source `e9940762...`，17 份 rendered documents 与 3 个负向 mutation 通过；Helm 原始报告 SHA-256 `d5f0ca5fb91416c1e1c504674fd897c8dc52cc0ccd3e6f12578c99f58bc75dff`，stage report SHA-256 `75c0310e5f6003ed9da6df5f541864c7bb990a14dfee8f1f8cc54c55bfee8923`。
- Targeted：`npm test -- tests/ensure-openclaw-plugins.test.ts tests/deploy-docker-policy.test.ts tests/deploy-supply-chain-policy.test.ts tests/deploy-supply-chain-strictness.test.ts tests/deploy-helm-policy.test.ts tests/scaffold-stage-gates.test.ts`：5 files / 43 tests PASS。
- `npm run scaffold:check`：SCAF-1…SCAF-6 PASS；`npm run lint:saas`、`npm run typecheck`、`npm run contracts:check`均 PASS。
- `npm run build`：PASS（仅既有 Vite chunk/eval warnings）；`npm run compile:electron`：PASS。
- 官方全量 `npm test`：197 files PASS，2110 tests PASS，6 skipped，无失败。

### 交接状态

- Developer 状态：`REVIEW_PENDING`。五镜像动态证据锚定的产品 source SHA 为 `e9940762c8ab4cc9b14495de23b43e99dc2a7e6c`；后续若仅提交本开发记录，不得改写/伪造该证据 source SHA。
- 本轮未修改 Tester 报告、Reviewer 审核意见、总体表或 P02，也未写入共享主工作树。由协调者保留上述 SHA 链后唤起原 P03 Reviewer 独立复审。

## Round 4 · 2026-07-12 · `/root/p03_developer_final` / Reviewer Round 2 证据门禁返工

### Red→Green 提交链

- `306d9908 test(supply-chain): capture evidence verification gaps`：新增 23 项落盘证据测试；Red 为 21 fail / 2 pass，稳定复现 Reviewer 指出的 report/root/nested unknown、缺失/越界/symlink/非文件、协调哈希与坏 JSON、SPDX/Grype 主体/摘要漂移、raw findings 清空、全 `f` 假 digest、Critical 计数、product-source 与 plugin evidence/summary 缺口。
- `235d4b53 fix(supply-chain): verify persisted image evidence`：用 Ajv 2020 实际加载声明 schema；严格验证 report 与合并 manifest；对证据目录相对路径执行 regular/non-symlink/realpath containment、落盘 SHA-256/JSON 解析、SPDX subject/source/platform digest、Grype tag/repo digest/manifest digest/descriptor/raw matches、wrapper findings/Critical/local Docker image digest 交叉核验。OpenClaw 最终镜像新增 `pluginInstallations`；installer 改为 required/installed/skipped 诚实汇总。
- `b350c9de`、`3026eb03`：新证据隔离到 `.reports/supply-chain/20260712_PR3部署供应链证据`，Docker build 与 formal checker 都绑定最新 product-source commit，而不是 raw HEAD；`35a5f927`、`3fa5dec3` 仅更新对应测试证据目录/产品 SHA，product resolver 明确排除测试和 Developer/Reviewer/Tester 角色记录。

### 新 exact-five 动态证据

- clean HEAD `3fa5dec3` 的最新产品 source 为 `3026eb03a5fc012279165bf8af5e25ec1a890dd3`。新 invocation `0f5ce7fa-da9a-4525-ae6c-a24cbab58792` 按 `web → api → worker → runtime-orchestrator → openclaw-runtime` 全部 `--no-cache --pull=false`，最终 report `PASSED`；报告 mode `0600`，SHA-256 `612940682fa2481eafc608c78aebdcec65d143319d9830b9b65a56c5f06b5ab1`。

| image | immutable digest | SBOM SHA-256 | Grype SHA-256 | Critical |
|---|---|---|---|---:|
| web | `sha256:b58c71f8b37cb2c94514308a4b6a327cd09b8cf1faf9dc5501088bd380fdc5af` | `65ee0c110e8bc8671138081409f7057afcdbbd62225c86f1a39ab7437280a757` | `5104b32bd9bbfa496dbd7e0277da27a0701742bd815c79e9903ce6c9c043642c` | 0 |
| api | `sha256:160557cccd6f2a8ed92b6461aeec2a781dfe5a07416f229cbaa30278a08a0917` | `5243742ca92b03d1f05d470ee3fe423b8b3c67e304906b276d197835de671c47` | `73afa0179ff27f63b044b4380edaf8ba9897e773f4e2d04629e37b0c6c0d9d76` | 0 |
| worker | `sha256:16d559c8e7452e9e357d0138d3f4ad33cd070ae90451b75bd870776efeff043d` | `08069d9e8b2b9fe24805bfeb0dc1a2d351ef309ac60d3e79670395b3e6f656d4` | `d896af11a23617ed11df53b5db4e30586808828197d9c005d7094e0fb4a2f520` | 0 |
| runtime-orchestrator | `sha256:695e4da2caae8a752e58fa09eb8e72cbdd9ed992cf25a2bbeec2daebb3b01bf5` | `6f57f6708b0960e0d71f2f76ddae311abee2253a06f843ea8bc18a8157297478` | `2c6d11f7bfcbaab1c409408cb74f295d92b89b40feb1746ca8c77f2d28802be8` | 0 |
| openclaw-runtime | `sha256:ec9057acd2b3be8dacbdc76198093e173f7988e003b809b86a9990e1e7d196fc` | `82b2a20b1c7b63869626242610600b1ba9b25fecebce6e3d5e329c18ec0c5a55` | `8268f2772b6fab7120f3701cf4970fd89e69450277a3a6e2f88e45ff9f57eedd` | 0 |

- OpenClaw 最终镜像机器枚举 10 个声明插件：9 `installed`、1 `skipped`；唯一 skipped 为 optional `moltbot-popo`。required `openclaw-netease-bee` 为 installed，optional NIM 也为 installed；没有再输出“All 10 installed”的误导结论。
- `node scripts/check-supply-chain.mjs`：PASS，57 assets、5 image evidence、publish/signature 继续诚实 `BLOCKED`。23 项证据 mutation 全部拒绝，协调修改 wrapper、raw 文件与 hash 也不能绕过。

### 回归与交接

- Targeted：4 files / 57 tests PASS；包含新增证据套件 23/23、既有 supply-chain/Docker/plugin installer 回归。
- `node scripts/check-docker-build.mjs --static`：PASS，五镜像静态策略全过。
- 官方全量 `npm test`：198 files PASS，2133 tests PASS，6 skipped，无失败。
- changed TypeScript ESLint、三个改动脚本 `node --check`、`git diff --check` 均 PASS；提交开发记录前 worktree 除本记录外 clean。
- Developer 状态：`REVIEW_PENDING`。不得修改 Reviewer 意见、Tester 报告、总体表或 P02；由协调者唤回同一 P03 Reviewer 复审，不再启动 Tester。

### 当前产品源横向回归补证

- 以下均在 product-source `3026eb03a5fc012279165bf8af5e25ec1a890dd3`、角色记录 HEAD `c993d0c2` 上重新执行，未沿用 `e9940762` 的 Round 3 结果。
- `npm run contracts:validate`：exit 0，16 类检查 PASS（216 schemas、158 routes、184 inventory）；`npm run typecheck` 与 `npm run lint:saas` 均 exit 0。
- `npm run build:saas`：exit 0，invocation `0dcaae30-88e4-44c9-a9d2-1b9b15bfe014`，验证 9 workspaces / 18 fresh artifacts；build-result SHA-256 `fca24c1e6025cbf3fb0bc0126fbb7348a8712044d50b090c13959ae33f7aa7b5`。
- Renderer/Electron `npm run build`：exit 0（仅既有 Vite eval/chunk warnings）；`npm run compile:electron`：exit 0。
- Fresh `npm run supply-chain:check`：PASS，stage invocation `5effed87-91bc-4bc6-adf8-e0e4851fb380`，角色记录 source `c993d0c2910406ca9d710fa598c2ccf2be75f939`，report SHA-256 `aa0afbe11b421499e112da631ad63b44d0961e8932d2612d5e68c103c39610f2`；其内部 formal checker 仍绑定 product source `3026eb03...` 和 exact-five invocation `0f5ce7fa...`。
- Fresh `npm run helm:lint`：PASS，stage invocation `1338082b-57dc-46d9-8a29-3202b8955896`，report SHA-256 `c33c39cb3a23a4a943029503ecb2d2f441407bc2e4478a97e371ae370895755c`；Helm raw invocation `a784cf8f-81ef-453a-8eda-34f37c7499e9` 验证 20 rendered documents / 3 negative mutations，raw report SHA-256 `f9e46584c49f2fb49b388596c3de3fad46feda21a26200fd77e90b1d97f86129`。

## Round 5 · 2026-07-12 · `/root/p03_developer_final` / Tester identity 与镜像插件状态返工

### Red 与 Green

- 在 Tester 提交 `3a327fa1` / `fe35f595`、失败记录 `b134f47f` 上原样复跑：4 files / 56 tests = 53 PASS / 3 FAIL；确定性 Red 为重复 SPDX root package、同步重复 Grype raw/wrapper finding、POPO 目录 absent 但 report 改为 installed，正式 checker 均错误返回 exit 0。
- `44d1cf63 test(supply-chain): cover evidence identity symmetry`：额外增加“同一 Grype artifact identity 但 severity 不同”和“optional NIM 目录 present 但 report 标 skipped”两个对称 Red；证据套件当时 39 tests = 34 PASS / 5 FAIL。
- `992cb1ac fix(supply-chain): enforce unique evidence identities`：SPDX packages 的 `SPDXID` 必须全局唯一且 DESCRIBES 只能精确对应一个 root package；Grype wrapper 增加必需 `artifactId`，raw/wrapper 以 vulnerability + artifact identity 去重，并用双向 multiset 对账完整 finding record，避免 Set 或同步复制掩盖重复。
- `187da9a2 fix(supply-chain): verify final-image plugin state`：formal checker 对 bound OpenClaw tag 再执行 hardened Docker run，从 `/opt/openclaw/third-party-extensions/<id>/package.json` 独立枚举真实安装状态；report installed/skipped 必须与最终镜像逐项一致，required/optional 均不能只信 wrapper。`7ccc0a58` 仅更新测试 source 绑定。
- OpenClaw Dockerfile 明确 `COPY scripts ./scripts`，故未复用 `0f5`；按完整镜像输入变化重新执行 exact-five no-cache。

### 新 exact-five 与回归

- product source `187da9a20b742c318e947a96cf0dd66c7cf9eca9`，invocation `64595381-cec8-46b2-a8b8-4631e96dfb9b`，按 web→api→worker→runtime-orchestrator→openclaw-runtime 全部完成并原子写 `PASSED`；report mode `0600`，SHA-256 `613fe5f8478608d9723c5cf0296cb4de090dc488200c9a36f3a902cd366c67bc`。五镜像 Critical 均 0，所有 wrapper finding 都有 `artifactId`；OpenClaw 最终镜像实查为 9 installed / 1 skipped，仅 optional POPO skipped，required Bee installed。
- 五镜像 digest：web `sha256:67040e3975c3671600a6513c6fbdd2a60ff2342e7fe857617183b2d58883b21e`；api `sha256:36e5261c5660af60d4dc36924d56430fc891c36feaba407345570c930b917991`；worker `sha256:d7b6cb8f4f2eff334dc97a2a0f3f7a2f56b3a7eb73c6e2b8645097fa02838444`；runtime-orchestrator `sha256:01a874d08203cdd271c1d7df529226b10f7cdcc01a4cf66c3d8d773b3f740992`；openclaw-runtime `sha256:b3ed4a874b943c4aa9d942feffd6e49cd8f148486d88b16fbd8fa3da0b05a793`。
- Formal `node scripts/check-supply-chain.mjs` PASS；Tester targeted + Developer corners 为 4 files / 58 tests PASS。官方全量 `npm test`：199 files PASS，2154 tests PASS，6 skipped。
- 当前源 `contracts:validate` 16 checks、`typecheck`、`lint:saas`、Docker static、`build:saas`（invocation `818cc55b-a50a-400e-873c-447fe8baa97e`，9 workspaces / 18 fresh artifacts，report SHA `fd1737300309323a4b3d5a54c6a91606721437668d90df2a0b5578d20592ffa7`）、renderer build、compile:electron 均 exit 0。
- Fresh supply-chain stage PASS：invocation `4db84280-c97c-4470-b721-86f3b4e090c3`，report SHA `4248a68ac43b0dfce6c9e51e55cc64e139c719b06f3882b9b2045baa4da0d738`。Fresh Helm stage PASS：invocation `ea4ad9e9-228a-4d62-b0ef-dadabbb43baf`，report SHA `654fab94092ae7894ab17496f61f51afb0448786ed95ebb5a898362ad504082d`；raw Helm `5f2e6d7f-167d-4e5c-bc9b-ed335fbc1e4c` 为 20 documents / 3 negative mutations，SHA `3ad3cb09e9ca847da13588d354a2e40f2ac51bf4d8e5a461b0e52dcbbca58909`。
- 状态：`REVIEW_PENDING`。未修改 Tester 报告、Reviewer 意见、总体表、P02 或 shared main；先交同一 Reviewer 复审，通过后再交同一 Tester 复测。

## Round 6 · 2026-07-12 · `/root/p03_developer_final` / 共享集成 product-source 边界与 exact-five 补证

### Product-source Red→Green

- `e84514cf test(supply-chain): freeze image source boundaries`：新增 product-source 边界回归；Red 为 19 tests 中 18 PASS / 1 FAIL，证明旧 resolver 会被 `docs/**`、`改造计划/**`、`tests/**` 等不进入镜像的提交错误推进。
- `f93fdd59 fix(supply-chain): resolve source from image inputs`：resolver 改为按五镜像实际输入判定，而非任务名启发式。精确文件 `.dockerignore`、根 `package*.json`、`tsconfig.base.json`、`tsconfig.workspace.json`、`vite.config.ts`，以及 `apps/`、`docker/`、`libs/`、`openclaw-extensions/`、`resources/`、`scripts/`、`SKILLs/` 的 add/modify/delete/rename/merge 均推进；`.github/`、`charts/`、`docs/`、`tests/`、`改造计划/` 冻结；未知路径 fail closed。历史遍历固定 first-parent，merge 按第一父提交 diff，rename 同时检查 old/new path。
- `48beb622 test(supply-chain): bind integrated image source`：将 evidence fixture 绑定新产品 source。Green 为 product-source 19/19；后续 `d6b6f66c`、`304d3449` 两个 docs-only 提交后 resolver 仍稳定返回 `f93fdd59d98b029cb82da5cc4f5873520472bb7f`。

### 共享 exact-five 动态证据

- 前两次 clean 尝试 `8a088cfd-1761-4ed3-96bd-9e7e9eefcdae`、`51159f04-d7cd-4764-ae27-2f4aae1530ab` 分别在 OpenClaw/API build 遭遇 OrbStack RPC EOF；checker 均删除 RUNNING report，没有伪造或保留 final。误启动的 `ca2c4433-959e-48f9-aa57-cd6881ea415a` 被立即中止并删除仅含 `images=[]` 的 RUNNING report。清理仅限旧 P03 镜像、临时 worktree 与未使用 build cache，未停止用户容器、未删除 volumes 或用户镜像。
- 在共享 clean branch 以 source `f93fdd59d98b029cb82da5cc4f5873520472bb7f` 发起 invocation `5673b88b-2151-4f73-9f7c-b87a245a0d89`，按 web→api→worker→runtime-orchestrator→openclaw-runtime 全部 no-cache 完成；过程中逐镜检查宿主空间并保持高于 4 GiB 主动停止线。最终 report `PASSED`，mode `0600`，SHA-256 `3c1561b6e2b7fd787f7496bc950e7656cc4d2133ed0ec86bfd8367cab43ae0e6`，五镜像 Critical 均为 0。

| image | immutable digest | SBOM SHA-256 | Grype SHA-256 |
|---|---|---|---|
| web | `sha256:2531800b80c43b206fe463af0280837cff067cd527b9f2a08564d63fa596b358` | `0dc09010d01e6c174cbf715f01c3f722f50ddd0e299bb0b41bf7795f96d18464` | `3d22d1c8ae20fd0310de8422e8a0707f5f6335e59005feed34df32e03a3937f7` |
| api | `sha256:bd5101aac238d81bd895dcd1212704b3e6c39d216c08181b325c79dbf3ece309` | `283e6f9124b8fadb0d3ae9f10363525ea7ab882859cbc410eb70389aa4f805b5` | `66d2566279a3060dd0a9fa084847759a35e45ccedf8af322539cc531e2f80200` |
| worker | `sha256:514af31aa37d02a5f17737069fcd4bd2779db5dc48474c7bec109c60044b920c` | `9ec82d6354773da832e861e35184c9a3b9ed7d9a0525431061c13c45c0e20e98` | `d592559d7e571981f0d4295c09a9b6b4e93f49b447ec55f97ab49de4abc86cab` |
| runtime-orchestrator | `sha256:ca3e8102a129ba4b71dcb254708e428fcca21a9daa49cbcd6c76f114ba7fc594` | `11c7cc1d2643bc0cbc13d0a0e0f94f7a44222ed7a7d40338224da3475905e90a` | `650448936c691dce0ee782b2b5e871b88abe5345987c4a681b5b4ff55737ba14` |
| openclaw-runtime | `sha256:538975319b88934a851eea2e4d39bdea51994f406c71845308201ae57cb2adfd` | `a3e5bba866a4086a2c59bf481fec5776309d915ccd0767299666d342432f98ff` | `1fcb391b5cf0d007312a7fa2af78b607113331e76ac3bf65eebb62458b68fba7` |

- OpenClaw 最终镜像独立实查仍为 9 installed / 1 optional skipped；唯一 skipped 为 `moltbot-popo`，required Bee installed。Formal `node scripts/check-supply-chain.mjs` PASS：57 assets、5 image evidence、publish/signature 因内部 trust identity 未冻结继续诚实 `BLOCKED`。

### 当前源回归与 P02 handoff

- P03/部署定向 5 files / 76 tests：75 PASS；唯一失败为 `tests/db/p03-merge-evidence.test.ts`，其 P02 `codeEvidenceSha` / `stageEvidenceSha` 尚未对共享集成树重基线。其余四个 P03/部署文件 75/75 绿色。
- 官方 `npm test`：207 files 中 205 PASS / 2 FAIL；2253 tests 中 2250 PASS / 1 skipped / 2 FAIL。两个失败分别为 `p03-merge-evidence` 与 `reviewer-round2-red` 的 current-evidence 校验，均由同一 P02 evidence SHA 待重基线导致，无其他失败。
- `contracts:validate`、`typecheck`、`lint:saas`、Docker static、`build:saas`（9 workspaces / 18 fresh artifacts）及 Renderer/Electron `npm run build` 均 PASS。
- Fresh `npm run supply-chain:check` PASS，invocation `f2d62233-fd07-4974-9259-3a1bd3837a0e`；Fresh `npm run helm:lint` PASS，invocation `73a4bae9-9579-4538-8a24-cc7d1a55bff7`，20 rendered documents / 3 negative mutations。
- Developer 结论：P03 代码与供应链证据绿色，但整体暂记 `BLOCKED_BY_P02_REBASELINE`。本轮不修改 P02、Reviewer、Tester 或总体表；由协调者完成 P02 final rebaseline 后再运行官方全量，并唤回同一 Reviewer / Tester 做共享集成复核。

## Round 7 · 2026-07-12 11:53 +08:00 · `/root/p03_developer` / B00-3A NUL-safe parser

### 范围与环境

- 本轮只实施 B00-3A；未开始 B00-3B、B00-3C、P02、live evidence、registry/signature 或任务级签收。
- 工作树：`fix/v2-b00-baseline`，起始 HEAD `9e3c7569`；Linux `x86_64`；Node `v24.18.0`；Docker `28.3.3` / `linux/x86_64`；Syft `1.44.0`；Grype `0.112.0`；Helm `v3.21.0`；可用空间 `300G`。B00-0 检查 exit `0`，起始 tracked worktree clean。

### Red

- 基线诊断：`npx vitest run tests/deploy-product-source-boundaries.test.ts --reporter=verbose` → exit `1`，19 tests 中 17 PASS / 2 FAIL；两个失败均为中文 `改造计划/**` 角色/计划路径被现有按行、Tab parser 误判为 product source。
- 新增 direct Buffer parser 契约与真实临时 Git repo 覆盖后，同一命令 → exit `1`，33 tests 中 19 PASS / 14 FAIL。目标失败包括：尚未导出 `parseGitNameStatusZ`；`core.quotepath=true/false` 下含中文、NFC/NFD、空格、Tab、引号和换行的 frozen path 均错误推进 source；既有两项 quoted UTF-8 回归继续失败。真实 `-z -M -C` rename/copy fixture 本身通过，排除测试仓库构造错误。
- Red test-only commit：`e5f3513d test(supply-chain): reproduce NUL path parsing failures`。提交前 changed-test ESLint、`git diff --check` 均 exit `0`，提交范围只有 `tests/deploy-product-source-boundaries.test.ts`。

### Green / 最小实现

- `scripts/check-supply-chain.mjs` 新导出 `parseGitNameStatusZ(Buffer)`：空 Buffer 返回空路径；单路径 status 与 `R0..R100` / `C0..C100` 双路径按 NUL 字段消费；path 使用 fatal UTF-8 `TextDecoder` 且不归一化。
- name-status Git 调用与普通文本 `gitOutput()` 分离；`spawnSync` 不设 encoding，直接消费 raw Buffer，命令固定为 `git diff --name-status -z -M -C` / root commit 的 `git diff-tree --root --no-commit-id -r --name-status -z -M -C`。
- 无尾 NUL、截断 R/C、unknown status、无相似度/越界相似度、空/多余 path、非法 UTF-8，以及 Git 非零、spawn error/signal、输出超限全部返回解析失败；resolver 随即 fail closed 到当前 commit。

### Green 验证

- `node --check scripts/check-supply-chain.mjs` → exit `0`。
- `npx vitest run tests/deploy-product-source-boundaries.test.ts --reporter=verbose` → exit `0`，1 file / 33 tests PASS；覆盖 quotepath true/false、NFC/NFD、中文/空格/Tab/引号/换行、合法单路径、R/C 双路径、frozen↔product rename/copy 两端及 malformed Buffer。
- `node scripts/check-docker-build.mjs --static` → exit `0`，`PASSED`，五个生产镜像静态策略均通过。
- `npx eslint --ext ts,tsx --report-unused-disable-directives --max-warnings 0 tests/deploy-product-source-boundaries.test.ts` → exit `0`。
- `git diff --check` → exit `0`。

### 当前状态与剩余风险

- B00-3A 已达到本轮 Developer Green，待以 `fix(supply-chain): parse product paths as NUL records` 提交后交协调者进入 B00-3B；本轮不写 `APPROVED_FOR_TEST` / `PASSED`。
- 官方 `npm test` 仍受计划内 B00-3B hermetic evidence mutation 与 P02 provenance 返工影响，本轮未以这些已知失败冒充 B00-3A Red 或 Green。
- P03 最终接受仍受 BLK-05 内部 registry/robot/signing 输入和后续 B00-5B 硬门阻断；本轮未推送。

## B00-3A 返工 Round 2 · 2026-07-12 12:07 +08:00 · `/root/p03_developer` / Reviewer Round 6 P2

### 审核输入与 Red / mutation evidence

- 输入：`d8581543 docs(review): assess B00-3A specification compliance` 的 Reviewer Round 6；只关闭 direct parser grammar 与 resolver subprocess fail-closed 两项 P2，不实施 B00-3B/3C/P02。
- Reviewer 已提供明确缺口证据：原 33 项 targeted 对 name-status Git nonzero、`spawnSync.error`/ENOENT、signal、超过 16 MiB stdout 与 parser `undefined` 五条实现防御分支均无 resolver 集成用例；direct 正向只覆盖 `A/M/R100/C087`，删除或破坏对应未测分支不会触发 Red。此证据作为本轮 test-only TDD 的 mutation Red；未伪造产品实现失败。
- 新用例落下后，现有生产实现首次即 38/38 PASS，因此两项均为测试契约缺口，不需要修改 `scripts/check-supply-chain.mjs`，也不创建额外 fix commit。

### Test Green

- direct parser 正向现逐项接受 `A/D/M/T/U/X/B`，并直接输入 `R0`、`R100`、`C0`、精确 `C100`（保留中间值 `C087`）；既有无尾 NUL、截断 R/C、unknown `Z`、无相似度、`C101`、空/多余 path 与非法 UTF-8 拒绝断言全部保留。
- 五个 resolver 用例各自创建独立临时 Git repo；先提交 frozen `docs/**` 并证明正常 resolver 返回 baseline，再把 PATH 指向受控 fake Git。fake Git 把普通 `rev-list` 委托给真实 Git，只在 `diff/diff-tree --name-status` 阶段分别制造 nonzero、`spawnSync.error`/ENOENT、SIGTERM、17 MiB stdout overflow、无尾 NUL malformed output；每项均断言 `resolveProductSourceSha(root)` fail closed 返回当前 analyzed commit，而不是只测私有 helper。
- ENOENT 用例在第二次 `rev-list --parents` 委托成功后删除 fake executable，使紧随其后的 name-status `spawnSync('git')` 真实产生 `error`；未给生产代码添加 `NODE_ENV`、test mode、runner 注入或绕过分支。

### 验证与范围

- `node --check scripts/check-supply-chain.mjs` → exit `0`。
- `npx vitest run tests/deploy-product-source-boundaries.test.ts --reporter=verbose` → exit `0`，1 file / 38 tests PASS。
- `node scripts/check-docker-build.mjs --static` → exit `0`，status `PASSED`，exact five 静态策略通过。
- `npx eslint --ext ts,tsx --report-unused-disable-directives --max-warnings 0 tests/deploy-product-source-boundaries.test.ts` → exit `0`。
- `git diff --check` → exit `0`。
- 计划提交：`test(supply-chain): cover resolver failure boundaries`；提交范围只允许定向 test 与本开发记录。本轮不写任务级 `APPROVED_FOR_TEST` / `PASSED`，不运行或提前处理 B00-3B/3C/P02，不推送。

## B00-3A 返工 Round 3 · 2026-07-12 12:27 +08:00 · `/root/p03_developer_round3` / Reviewer Round 8

### 审核输入与根因

- 输入：`975166ba docs(review): assess B00-3A code quality` 中的 Round 8 `QUALITY_CHANGES_REQUESTED_B00_3A`；本轮只修复 parser 资源边界、Windows 可携 fake Git 与 leading BOM 回归，不实施 B00-3B/3C/P02。
- 根因确认：现实现在校验第一条 record 前先把所有 NUL 分隔字段物化为 `fields: Buffer[]`，因此 heap 随字段数而非仅随输入 bytes 增长。实测 100,000 条 `A\0pN\0` 仅 888,890 bytes，但旧实现在隔离 `--max-old-space-size=24` 子进程内稳定 OOM/`SIGABRT`；同输入的单遍 cursor 原型在 16 MiB heap 下仍能返回全部 100,000 paths。

### Test Red 与 mutation evidence

- 新增受限 heap 子进程回归：用预分配 `Buffer` 构造 888,890 bytes / 100,000 条合法且 path 唯一的 record，不用同进程 GC/RSS 差值；要求 parser 在 24 MiB old-space 内返回完整首尾与计数。`npx vitest run tests/deploy-product-source-boundaries.test.ts --reporter=verbose` 在旧实现上为 40 项中 39 PASS / 1 FAIL，子进程明确 `FATAL ERROR: Reached heap limit` 并以 `SIGABRT` 终止。
- leading BOM direct bytes 回归输入 `A\0<EF BB BF>docs/cafe<combining acute>.md\0`，断言返回 path 以 U+FEFF 开头，且 NFD 字节序列未被 NFC 归一化。当临时 mutation 将生产 decoder 的 `ignoreBOM: true` 改为 `false` 时，该定向用例稳定 Red：实际返回 `docs/cafe<combining acute>.md`，丢失 U+FEFF；随后已恢复生产文件。
- fake Git fixture 不再假定 `PATH/git`：依次搜索 `git` / `git.exe` / `git.cmd` / `git.bat` 并实际执行 `--version` 验证。POSIX 生成 shebang + `chmod(0755)` launcher；Windows 生成 `git.cmd` launcher，ENOENT 模式在成功代理 `rev-list --parents` 后自删 PATH 中的 launcher。用例不跳过 `win32`，且原 PATH 缺失时以 `delete process.env.PATH` 恢复。

### Test-only 提交边界

- 首个提交固定为 `test(supply-chain): cover parser resource boundaries`，只包含 `tests/deploy-product-source-boundaries.test.ts` 与本开发记录；保留资源回归 Red，不在 test commit 中修改 parser。

### Green / 单遍 parser

- `parseGitNameStatusZ()` 现以单一 `cursor` 逐个定位 NUL：status 字节立即校验 ASCII 并解码，随后按状态立即解码 1 或 2 个非空 UTF-8 path。实现不再创建全量 `fields` Buffer view 数组，只保留契约要求返回的 path strings；status grammar、fatal UTF-8、BOM 保留、NFC/NFD 不归一化与 malformed fail-closed 语义不变。
- Node `v24.18.0` 的归档 Red replay 在 test-only commit `e00ee093` 上只运行资源用例，仍以 `FATAL ERROR: Reached heap limit` / exit `1` 稳定失败；同一 Node 24 与同一 24 MiB 限制在新实现上 40/40 PASS。

### Green 验证与交付边界

- `PATH=$HOME/.nvm/versions/node/v24.18.0/bin:$PATH npx vitest run tests/deploy-product-source-boundaries.test.ts --reporter=verbose` → exit `0`，1 file / 40 tests PASS，包含受限 heap、BOM、fake Git 五类 resolver 失败边界与原 Unicode/rename/copy/merge 回归。
- Node `v24.18.0` 下 `node --check scripts/check-supply-chain.mjs` → exit `0`；`node scripts/check-docker-build.mjs --static` → exit `0`，`PASSED`，exact five 为 web/api/worker/runtime-orchestrator/openclaw-runtime。
- `npx eslint --ext ts,tsx --report-unused-disable-directives --max-warnings 0 tests/deploy-product-source-boundaries.test.ts` → exit `0`；`git diff --check` → exit `0`。
- 修复提交固定为 `fix(supply-chain): parse NUL records in one pass`，只包含 `scripts/check-supply-chain.mjs` 与本开发记录。本轮不写任务级 `APPROVED_FOR_TEST` / `PASSED`，不启动 B00-3B/3C/P02/live evidence/registry/signature，不推送。

## B00-3A 返工 Round 4 · 2026-07-12 12:39 +08:00 · `/root/p03_developer_round3` / Reviewer Round 9

### 审核输入与根因

- 输入：`ddf7f27d docs(review): recheck B00-3A resource safety` 中的 Round 9；Round 8 的 parser 资源边界和 BOM 项已关闭，本轮只关闭剩余 Windows fake Git P3，不实施 B00-3B/3C/P02。
- 根因确认：Windows fixture 只在 PATH 中生成 `git.cmd`，但生产调用是不启用 shell 的 `spawnSync('git', ...)`；`.cmd` 无法成为该调用的直接 executable，所以 Windows 会在首个 `rev-list` 前失败，永远不会进入五类 name-status 分支。

### Red 与 source mutation

- 先将五个 resolver 失败用例改为要求生产导出最小 `createProductSourceShaResolver(runGit?)` 工厂，删除 PATH mutation、Git 可执行搜索、POSIX shebang/chmod、Windows cmd launcher 和自删逻辑。在尚未实现工厂时，Node `v24.18.0` targeted 稳定 Red：40 项中 35 PASS / 5 FAIL，五项均明确拒绝缺失的 `createProductSourceShaResolver` 导出。
- Green 后临时将工厂 mutation 为忽略注入 runner、恒用 default runner；只重放五项时全部 Red，实际 resolver 返回 frozen baseline 而非 analyzed commit，证明测试真正依赖生产注入路径。mutation 随后已恢复。

### Green / 跨平台 Git runner

- 生产默认 runner 仍唯一执行 `spawnSync('git', args, options)`，不开 shell、不读 `NODE_ENV`。`createProductSourceShaResolver(runGit?)` 将同一 Git-specific runner 同时传给文本 `gitOutput` 与 raw Buffer name-status 路径；旧 `resolveProductSourceSha` 仍是以默认 runner 创建的原生产 API。
- 受控 runner 对普通 `rev-list` 继续委托当前平台真实 `spawnSync('git')`，仅对 `diff` / `diff-tree --name-status` 返回 nonzero、`ENOENT` error、`SIGTERM` signal、17 MiB + `ENOBUFS` overflow 或 malformed Buffer。每个用例断言普通 Git 委托 2 次、name-status 命中 1 次且 `maxBuffer === 16 MiB`，再断言 resolver fail closed 到 analyzed commit；没有 `win32` skip 或平台 launcher。

### 验证与边界

- Node `v24.18.0` 下 targeted 回归 → 1 file / 40 tests PASS；`node --check scripts/check-supply-chain.mjs`、Docker exact-five static、changed-test ESLint 与 `git diff --check` 均 exit `0`。
- 提交固定为 `refactor(supply-chain): inject Git runner for resolver tests`，只包含 `scripts/check-supply-chain.mjs`、`tests/deploy-product-source-boundaries.test.ts` 与本开发记录。本轮不写任务级 `APPROVED_FOR_TEST` / `PASSED`，不推送。

## B00-3A 返工 Round 5 · 2026-07-12 12:56 +08:00 · `/root/p03_developer_round4` / Tester P2

### Tester 输入、范围与 Red

- 输入：独立 Tester 的 `a9dd7a29 test(supply-chain): add independent parser corners` 与 `57e4bfc3 docs(test): record B00-3A failure`。本轮只关闭文本 Git runner 结果验证 P2；保留 Tester 四角度、runner factory/default、raw NUL parser 与既有 40 项，不实施 B00-3B/3C/P02/live evidence/registry/signature。
- 环境：Linux `6.8.0-124-generic` x86_64；Node `v24.18.0`。shell 默认 Node 为 `v22.18.0`，因此正式 Red/Green 与全部门禁均显式把 `$HOME/.nvm/versions/node/v24.18.0/bin` 放在 PATH 首位。
- 修改前正式 Red：`npx vitest run tests/deploy-product-source-boundaries.test.ts --reporter=verbose` → exit `1`，1 file / 44 tests，43 PASS / 1 FAIL。唯一失败为 `fails closed for abnormal raw and text runner result shapes`；Buffer/null stdout 分别抛 `TypeError`，`status:0 + error/signal` 则错误返回旧 frozen baseline。

### 根因与最小 Green

- 根因位于 `createProductSourceShaResolver()` 内的文本 `gitOutput()`：它为 `encoding: 'utf8'` 调用只检查 `status === 0`，随后无条件对 stdout 调用 `trim()`；与 raw `gitNameStatusOutput()` 已有的 fail-closed 验证不对称。
- 最小修复只为文本路径增加与 raw 路径一致的结果形状防御：任一 `status !== 0`、`error`、`signal` 或 `typeof stdout !== 'string'` 返回 `undefined`；只有无错误、无 signal、status 0 且 string stdout 才调用 `trim()`。raw 路径继续只接受 Buffer；默认 runner 仍为无 shell `spawnSync('git', ...)`，工厂注入与公开 resolver API 均未改变。
- Tester 新增四角度定向重放 → 4 PASS / 40 skipped；异常文本四种结果均 fail closed 到当前 analyzed commit，不再抛异常或信任矛盾的成功状态。

### Green 门禁与交接

- Node `v24.18.0` 全量 B00-3A targeted：`npx vitest run tests/deploy-product-source-boundaries.test.ts --reporter=verbose` → exit `0`，1 file / 44 tests PASS。
- `node --check scripts/check-supply-chain.mjs` → exit `0`。
- `node scripts/check-docker-build.mjs --static` → exit `0`，status `PASSED`，exact five 为 web/api/worker/runtime-orchestrator/openclaw-runtime。
- `npx eslint --ext ts,tsx --report-unused-disable-directives --max-warnings 0 tests/deploy-product-source-boundaries.test.ts` → exit `0`；`git diff --check` → exit `0`。
- 提交固定为 `fix(supply-chain): fail closed on invalid Git text output`，范围只包含 `scripts/check-supply-chain.mjs` 与本开发记录；Tester 测试不删改。本轮不写任务级 `APPROVED_FOR_TEST` / `PASSED`，不运行官方 full `npm test`，不推送。

## B00-3B · 2026-07-12 15:32 +08:00 · `/root/p03_developer_3b` / hermetic evidence mutation fixture

### 范围与基线 Red

- 本轮只实施 B00-3B；未修改 B00-3C workflow / `package.json` 测试启动链，未修改 P02，未生成 live evidence，未运行 Docker image build/publish/signature，也未写任务级 `APPROVED_FOR_TEST` / `PASSED`。
- 起始 HEAD `ece64fd4`，tracked worktree clean，主 checkout 不存在 `.reports`。Node `v24.18.0` 原样重放 `tests/deploy-supply-chain-evidence-files.test.ts` 得到 39 项中 37 FAIL / 2 PASS；36 项因复制不存在的主 checkout evidence 目录返回 `ENOENT`，1 项因硬编码 `f93fdd59...` 与当前动态 product source 不同失败。
- 先将原 39 项迁移到固定的 `createSupplyChainEvidenceFixture(options?)` 调用契约，删除主 checkout report 复制、历史 SHA、Git object 复用与宽松 shell Docker stub。定向命令稳定因 helper 模块缺失返回 `ERR_MODULE_NOT_FOUND`；Red commit 为 `d7d5c40c test(supply-chain): define hermetic evidence fixture`。

### Green 实现

- `tests/helpers/supply-chain-evidence-fixture.ts` 只对测试暴露 `createSupplyChainEvidenceFixture(options?)`，返回计划固定的 `root/reportPath/evidenceDirectory/sourceSha/runChecker/readReport/writeReport/mutateEvidenceDocument/cleanup`。每次创建独立临时 Git repo 和动态 root-commit SHA，不写 alternates；`afterEach` 逐 root 清理。
- fixture 只从 tracked source 复制正式 checker/schema，生成自包含最小资产树和双向 inventory；`package.json` 精确保留当前 10 个 OpenClaw plugin 声明，其余只有 synthetic Computer Use Kit 与空 MCP registry。可以 symlink 当前 `node_modules`，但不读主 checkout report、image 或 Git object。
- `exact-five-template` 明示声明 synthetic，生成恰好 5 个 image record、5 份 SPDX 2.3 和 5 份 Grype JSON；source/tag/image digest/manifest digest/path 全部动态绑定，10 份 evidence file hash 与 report 全部重算。目录大小 52 KiB，低于 500 KiB 上限。
- 受控 fake Docker 只接受 checker 的精确 `image inspect` 和 hardened OpenClaw plugin-state `run`；结果来自创建时 report 派生的不可变 Docker state，未知命令 exit `64`，并覆盖 `valid` / `unavailable` / `digest-drift`。正式 checker 没有 `NODE_ENV`/fixture/test bypass，Docker 对账与 plugin 目录对账未降级。
- `scripts/check-supply-chain.mjs` 导出唯一 `p03EvidenceRelativeDirectory`；`scripts/check-docker-build.mjs` 复用该常量，不再保留第二个 evidence path literal。

### 定向 Green 与交付边界

- Node `v24.18.0` 连续两次运行 `npx vitest run tests/deploy-supply-chain-evidence-files.test.ts --reporter=verbose` 均 exit `0`，1 file / 39 tests PASS；正向 exact-five、SPDX/Grype/hash/path/FIFO、schema、Docker unavailable/digest drift、plugin status 全部保持原 mutation 语义。
- `node --check scripts/check-supply-chain.mjs`、`node --check scripts/check-docker-build.mjs`、`node scripts/check-docker-build.mjs --static`、changed-TS ESLint、禁用模式 `rg`、template size 与 `git diff --check` 均 exit `0`。
- 本轮按协调要求不运行、不宣称 full `npm test` 整体绿色；B00-3C 与 P02 provenance 仍在后续线性步骤。本轮不推送。

## B00-3B 质量返工 · 2026-07-12 15:56 +08:00 · `/root/p03_developer_3b` / Reviewer Round 13 P2

### 审核输入与 Red

- 输入：`08c393b2 docs(review): assess B00-3B code quality`。本轮只关闭 fake Docker probe 精确绑定与 helper evidence path 逃逸两项 P2；未修改 B00-3C/workflow/package test bootstrap、P02 或 live evidence。
- test-only commit `42bbd87a test(supply-chain): reproduce fixture boundary escapes` 新增 8 项。旧实现定向重放为 7 FAIL / 1 PASS：删除 regular-file、anti-symlink、root containment，改动 filter 或仅加 comment 的 5 个临时 checker 均仍被 substring fake 接受并返回 `PASSED/0`；relative traversal 与 canonical symlink 实际未抛错，absolute path 已有边界因 `path.join` 行为对称通过。

### 最小 Green

- `scripts/check-supply-chain.mjs` 导出唯一完整事实源 `p03HardenedPluginInspectionScript`，正式 `validatePluginInstallations()` 直接用它构造 hardened Docker `run`。helper 从当前 tracked 原版 module 取该字符串，在临时 checker 可被 mutation 前写入独立 Docker state；fake 对 `args[10]` 与 state 全文做精确相等，不再用 `includes()`。
- helper 在 report 公开前为 5 image × 2 kind 捕获 canonical relative/absolute document path。`mutateEvidenceDocument()` 先要求 report 声明仍等于 canonical path，再校验 relative containment、regular-file、non-symlink 及 realpath 直属 evidence directory；任一失配在读/写/回调前抛错。
- 新增 8 项当前全绿；5 个 probe mutation 的 fake 精确返回 exit `64`，traversal/absolute/canonical-symlink 均抛错且 root 外 sentinel bytes 不变。

### 本地门禁与边界

- Node `v24.18.0` 完整定向：1 file / 47 tests PASS（原 39 + 新增 8）。
- `node --check scripts/check-supply-chain.mjs`、`node --check scripts/check-docker-build.mjs`、`node scripts/check-docker-build.mjs --static`、changed-TS ESLint、禁用模式 `rg` 与 `git diff --check` 均 exit `0`。
- 本轮不运行/宣称 full `npm test`，不生成 live evidence，不推送，不写 P03 任务级 `APPROVED_FOR_TEST` / `PASSED`。

## B00-3C · 2026-07-12 16:25 +08:00 · `/root/p03_developer_3c` / clean-checkout 默认测试启动链

### 范围与 TDD Red

- 本轮只实施 B00-3C；未修改 P02 断言/provenance、B00-3B、contract schema/registry/generated 内容、SaaS workflow/artifact、live evidence、registry/signature 或总体表，也未运行 Vite 作为 `pretest` 前置。
- Node `v24.18.0` 下先新增 `tests/scaffold-default-test-bootstrap.test.ts` 与 YAML 解析的 `tests/deploy-ci-evidence.test.ts`。test-only 重放为 2 files / 11 tests：9 PASS / 2 FAIL；失败精确为根 `pretest` 缺 clean/build/check 三步，以及主 CI `test.needs=[build-main]`，不是语法、依赖或网络错误。
- Red commit 为 `e1a1261ad613f46455be7ff5cdaeaeffbbf3da26 test(scaffold): reproduce clean-checkout test bootstrap`。随后把两个 Web build 用例切到尚不存在的 helper API，2 个 suite 均以 `ERR_MODULE_NOT_FOUND` 证明 fixture 缺失；实现 helper 后才转 Green。

### 最小 Green 与提交边界

- 根 `pretest` 现严格串行执行 `npm rebuild better-sqlite3` → `tsc -b tsconfig.workspace.json --clean` → `tsc -b tsconfig.workspace.json --pretty false` → `node scripts/contracts/generate.mjs --check`。四步仅用 `&&` fail fast；mutation 契约拒绝缺任一步、Vite、`build:saas`、`npm install`、`|| true`、`contracts:generate` 与 write-mode `generate.mjs`。
- `tests/helpers/web-workspace-build-fixture.ts` 从 `apps/web/tsconfig.json` 动态遍历 TS project-reference 闭包，只复制 Web、shared types、client bridge、shared contracts 及必要根配置；排除 `dist`、`dist-types`、`node_modules` 与所有 `*.tsbuildinfo`。外部依赖通过临时 `node_modules` symlink，`@lobsterai/*` 精确重绑到临时闭包；构造失败和用例 `finally` 都递归 cleanup 独占 root。
- 两个 Web build 测试只在各自 temp root 运行 `npm run build --workspace @lobsterai/web`，不再 `rmSync` 或读取主 checkout 的 `apps/web/dist`。full suite 后主 checkout `apps/web/dist` 仍不存在，且 `/tmp` 无 `lobsterai-web-build-*` 残留；没有设置 `fileParallelism:false`、全局 timeout、suite 顺序或 src alias。
- 主 CI `test` 现只依赖无 job-level `if` 的 `lint`，自身无 job-level `if`；checkout `fetch-depth: 0`，保留 npm cache，顺序为 `npm ci` → `npm test`。YAML 契约另锁定 `pull_request` 无 paths、无 `npm install`、`continue-on-error` 或 `|| true`。
- Green commits：`15da0b8b5d9283800e0ddb7453c3e745352fcfef fix(test): prepare workspace outputs before Vitest`、`96672ad5142c390d4d5a0ccf0ade88e9b6841cee ci(test): run official tests with full history`、`9701a916a22919675049dff10a5ef813b17be90c test(ci): require an unconditional test dependency`。

### Node 24 clean-checkout 与回归证据

- 5 个直接受影响文件（bootstrap、CI、两个 Web、scaffold apps）实际为 **5 files / 30 tests PASS**，不是预估的 40；记录真实收集数。3 个 bootstrap/Web 文件单独为 3 files / 16 tests PASS，CI 契约单独为 1/1 PASS。
- 先运行 `scripts/clean-saas-build.mjs` 并跳过 `node_modules` 核对 workspace 中无 `dist/dist-types/*.tsbuildinfo`；随后通过官方 `npm test -- ...` 重放计划中的 6 个原 clean-checkout 失败文件，pretest 自建输出后 **6 files / 50 tests PASS**。
- 第二种状态只删除 `dist/dist-types`，保留并把 9 个 `*.tsbuildinfo` mtime 设为 2000-01-01；同一官方入口先 `--clean` 再 build/check，仍为 **6 files / 50 tests PASS**，证明不信任 stale incremental state。
- 在仓库外执行 `git clone --no-local --no-hardlinks . <tmp>/repo`；确认 shared contracts/shared types/server auth 均无 `dist`，Node 24 `npm ci` exit 0，再由官方 pretest 重放同 6 个文件为 **6 files / 50 tests PASS**。clone 无 object alternates，trap 已清理临时目录。
- 官方 full `npm test` 的准确结果为 exit `1`：**210 files：208 PASS / 2 FAIL；2309 tests：2301 PASS / 2 FAIL / 6 SKIP**。仅失败 `tests/db/p03-merge-evidence.test.ts` 与 `tests/db/reviewer-round2-red.test.ts`，两者都明确报 `P02 evidence bootstrap: trusted file mismatch package.json`；这是计划内 P02 provenance 重冻结事项，本轮未修改相关断言制造全绿。
- 在 tracked worktree clean 时直接运行 `node scripts/contracts/generate.mjs --check` 返回 `PASS`（158 routes / 32 channels / 184 inventory / 321 bridge properties）；前后 `git diff --exit-code` 均为 0，证明 check-only 未写 tracked generated 内容。
- changed TS ESLint、`js-yaml` 解析 `.github/workflows/ci.yml` 与 `git diff --check` 均为 exit 0。本轮不写任务级 `APPROVED_FOR_TEST` / `PASSED`，不推送；P03 仍等待后续 P02/live/registry/signature 唯一线性硬门。

## B00-3C Round 16 返工 · 2026-07-12 16:59 +08:00 · `/root/p03_3c_rework_developer`

### 范围、根因与 TDD Red

- 输入为 `ee6c0cbc4688d1cb9cd620d96b9451f899b2411e docs(review): assess B00-3C code quality`；本轮只关闭 Round 16 的 project-reference closure 越界/失败残留与 CI command step 可跳过两项 P2，未修改根 `pretest`、正式 workflow、P02、contract 源或 generated、计划总表、live evidence、registry/signature。
- 根因一是旧 fixture 把仓库外 config 转成 `../...` 后先复制再读 manifest，导致 constructor 抛错时只有 fixture root 被删除，越界 destination 留在外部；根因二是旧 CI 契约把 workflow step 扁平化为 command string，只检查命令存在和 `continue-on-error === true`，无法发现 step-level `if`、`continue-on-error: false`、关键 action 重排或重复命令。
- test-only commit `496959c6 test(ci): reproduce fixture and workflow boundaries` 的直接定向为 **2 files / 10 tests：3 PASS / 7 FAIL**。6 个 CI mutation（`npm ci`/`npm test` 的 step-level `if`、两条命令各自的 `continue-on-error: false`、setup-node/checkout 重排、重复 `npm test`）均被旧验证器错误接受；仓库外 reference 用例实际发现 escaped destination 仍存在。相同探针中的 missing-package constructor 清理与正常双节点 cycle 分别 PASS，排除了测试 harness 普遍失败。

### 最小 Green 与边界

- Green commit `7925998e fix(test): harden fixture and CI boundaries` 在 `mkdtemp` 前遍历并完整验证 closure：每个 config、project directory 和 manifest 同时通过 resolved-relative 与 canonical repository containment，visited set 在读取引用前登记以保持 cycle 安全；missing manifest、非法引用或仓库外 reference 在任何复制前 fail closed。
- 复制、依赖 symlink 与 `@lobsterai/*` link 的每个 destination 再次限制在各自 fixture root/scope；package link 名拒绝路径分隔符。创建 root 之后的任一复制或链接异常仍由 constructor catch 递归删除唯一 root，因此本任务不再创建无法纳入 cleanup 的外部路径。
- CI test helper 结构化定位唯一 checkout、setup-node、cache、`npm ci`、`npm test`，要求严格按该顺序出现；`npm ci`/`npm test` 不得有 step-level `if`，任一 test-job step 只要声明任何形态的 `continue-on-error` 即拒绝，重复关键命令同样 fail closed。正式 `.github/workflows/ci.yml` 无需修改，因为当前实际结构已满足强化契约。
- 边界测试为每个动态 fixture module 分配独立 `TMPDIR`，既能精确证明构造失败零残留，又不把同时运行的合法 Web fixture 误判为泄漏；结束时恢复原环境并删除 source、escaped sentinel 与 fixture root。

### Green 门禁与残留检查

- Node `v24.18.0`。直接受影响的 CI/boundary/两个 Web consumer 为 **4 files / 16 tests PASS**；changed-TS ESLint 与 `git diff --check` 均 exit `0`。
- 无输出状态先由 `scripts/clean-saas-build.mjs` 删除 9 个 workspace 的 `dist/dist-types/*.tsbuildinfo`，再经官方 `npm test -- ...` 运行原 Round 16 targeted 加本轮边界文件，得到 **11 files / 76 tests PASS**；保留已有输出原样重复同一官方入口仍为 **11/76 PASS**。两轮 pretest 的 contracts check 都为 158 routes / 32 channels / 184 inventory / 321 bridge properties。
- 两个独立 Vitest 进程并发运行 `tests/scaffold-web-build.test.ts` 与 `tests/scaffold-tester-corners.test.ts`，分别 **1/1 PASS** 与 **5/5 PASS**。结束后主 checkout `apps/web/dist` 不存在，`/tmp` 无 `lobsterai-web-build-*`、`lobsterai-web-fixture-source-*` 或 escaped probe 残留。
- `node scripts/contracts/generate.mjs --check` 返回 `PASS`；14 个 managed output 的 SHA-256、size、mtime、ctime 前后逐项相同。`node --check scripts/contracts/generate.mjs`、`js-yaml` 解析主 CI（test steps = 5）、4 个本轮 changed TS 的 CI 等价 ESLint、`git diff --check ee6c0cbc..7925998e` 与 tracked `git diff --exit-code` 均 exit `0`。
- 本轮不重复官方 full `npm test`：最近完整证据仍只有两个已知 P02 provenance mismatch，Round 16 明确允许本返工不重复该耗时门。当前结论仅为 Developer 返工完成并交新的独立 Quality Reviewer；不是 B00-3C `QUALITY_APPROVED_FOR_TEST`、P03 任务级 `PASSED` 或总表接受。

### 剩余风险

- fixture 仍按 B00-3C 既定 Linux CI 证据使用 symlink 连接当前 `node_modules`；Windows 分支继续使用 junction，但本轮没有新增 Windows runner 实证。containment 同时使用平台原生 `path.relative`/`path.isAbsolute` 和 canonical realpath，可在 Windows 词法规则下 fail closed，仍应由后续跨平台总体门复验。
- CI 契约锁定主 test job 的当前五个关键 step 及其先后关系，但允许在相邻关键 step 之间增加无条件诊断 step；若未来新增该类 step，Reviewer 仍需确认它不改变 frozen install 或测试失败传播语义。

## B00-3C Round 17 返工 · 2026-07-12 17:15 +08:00 · `/root/p03_3c_rework_developer`

### 范围、根因与 TDD Red

- 输入为 `121edefe docs(review): recheck B00-3C quality boundaries`。本轮只关闭仓库内 symlink project source 链回 source checkout 与 repo-root/空 destination 一项 P2；未修改正式 CI、根 `pretest`、P02、contract 源/generated、计划、测试报告、总表、live evidence 或 registry/signature。
- 根因是 Round 16 的 `assertRepositorySource()` 只用 realpath 验证 canonical target 仍在 repository，却返回 lexical symlink path继续复制。`cpSync()` 因此把 project directory symlink 原样带到 fixture；fixture 下写入会真实落到 source project。closure identity 同样使用 lexical config，未显式冻结 canonical alias cycle；严格 containment 又允许 `candidate === root`，所以空 relative destination 未 fail closed。
- test-only commit `248c5fb3 test(scaffold): reproduce symlink source escapes` 在旧 helper 上得到 **1 file / 7 tests：3 PASS / 4 FAIL**。普通 out-of-repo、missing manifest、正常非 alias cycle 保持 PASS；单 alias 与 canonical alias cycle 两项都在写 fixture sentinel 后发现临时 source sentinel 真实存在，repo-root destination 与 project 内嵌 symlink 也被旧实现接受。探针 source 全在隔离临时仓库，失败后 fixture/source/TMPDIR 均无残留，未写 tracked checkout。

### 最小 Green 与隔离语义

- Green commit `dfdf7638 fix(test): isolate canonical project sources` 将 closure identity 改为 canonical config。每个 canonical project 只解析一次，并为所有合法 lexical alias 维护去重 destination 集合；cycle 访问已在递归前登记，因此 canonical alias cycle 有界。复制源统一为验证后的 canonical real directory，而每个 alias destination 都接收独立的真实目录副本，保留原 tsconfig 相对引用且不创建回源 symlink。
- project lexical destination 与 canonical source directory 都必须是各自 repository root 的非空严格子路径；repo-root config、空 relative destination、词法/规范化越界均在 `mkdtemp` 前失败。每个 copy destination 与 package link source 再在 fixture root 内执行严格子路径检查。
- 在创建 fixture 前递归扫描 canonical project source；除明确排除的 `dist`、`dist-types`、`node_modules` 与 `*.tsbuildinfo` 外，任何 symlink 都 fail closed。`cpSync` filter 对实际复制输入再次 `lstat`，防止 source symlink 被带入 fixture；允许的外部 symlink 仍只由 helper 在 temp `node_modules` 内显式创建。
- 多 alias project 只生成一个 `@lobsterai/*` package link，指向排序后的首个 fixture 实体副本；任何 alias 下的写入都留在独占 fixture。正常无 alias closure、双节点 cycle、两个真实 Web build consumer 均保持原行为。

### Green 门禁与残留检查

- Node `v24.18.0`。CI/boundary/两个 Web consumer 直接定向为 **4 files / 20 tests PASS**；新增 alias、canonical alias cycle、repo-root 与 nested symlink 四项全部 Green。
- 无输出状态先删除 9 个 workspace 的 `dist/dist-types/*.tsbuildinfo`，再经官方 `npm test -- ...` 运行 B00-3C 11 个 targeted 文件，得到 **11 files / 80 tests PASS**；保留已有输出原样重复仍为 **11/80 PASS**。两轮 pretest 均执行 native rebuild → clean → build → contracts `--check`，统计保持 158 routes / 32 channels / 184 inventory / 321 bridge properties。
- 两个独立 Vitest 进程并发运行两个 Web consumer，分别 **1/1 PASS** 与 **5/5 PASS**。结束后主 checkout `apps/web/dist` 不存在，`/tmp` 无 `lobsterai-web-build-*`、`lobsterai-web-fixture-source-*` 或 escaped external probe 残留。
- `node scripts/contracts/generate.mjs --check` 返回 `PASS`，14 个 managed output 的 SHA-256、size、mtime、ctime 前后逐项相同；generator Node check、主 CI YAML parse（test steps = 5）、7 个 B00-3C TS 文件 CI 等价 ESLint、`git diff --check 121edefe..dfdf7638` 与 tracked `git diff --exit-code` 全部 exit `0`。
- 按 Round 17 交接不重复已知受 P02 provenance 阻断的 full `npm test`。当前只完成 Developer 返工并交新的独立 Quality Reviewer；不是 B00-3C `QUALITY_APPROVED_FOR_TEST`、P03 任务级 `PASSED` 或总表接受。

### 剩余风险

- 任何 project source 在非排除路径使用 symlink 现在都会明确失败，而不是自动 dereference；这是避免跨 checkout 写入与外部逃逸的有意 fail-closed 策略。若未来产品 workspace 必须跟踪 symlink 资产，需要先设计可审计的复制白名单并新增独立边界测试，不能直接放宽 filter。
- canonical project 经多个 lexical alias 引用时会在 fixture 内复制多份实体内容；closure 与 destination set 均有界，但极端 alias 数会增加临时磁盘/时间。本轮没有添加人为上限，因为当前真实 closure 无 alias，后续若引入 alias 应同时评估资源门限。
- Windows 使用 junction 创建明确的 temp `node_modules` 链接，但本轮没有新增 Windows runner 实证；project source symlink 拒绝与 strict-subpath 使用平台原生 path/realpath 语义，仍应由后续跨平台总体门复验。

## B00-3C Tester 失败返工 · 2026-07-12 17:36 +08:00 · `/root/p03_3c_rework_developer`

### 输入、有效 Red 与范围

- 输入为 Tester test-only commit `dcb2c93b test(ci): expose B00-3C gate bypasses` 与失败报告 `2b6dab6a docs(test): verify B00-3C clean checkout gate`。本轮保持 `FAILED_B00_3C_LOCAL_GATE` 原报告和两份 Tester 测试原样不动，只修复 `tests/helpers/ci-workflow-evidence.ts`；未修改正式 CI、根 `pretest`、Web fixture、审核记录、P02、计划、测试报告、总表或后续 live/registry/signature。
- Node `v24.18.0` 直接重放两份 Tester 测试得到报告中的精确 Red：**2 files / 41 tests：35 PASS / 6 FAIL**。失败为 checkout/setup-node/cache 三个 action step 的 `if` 被接受、test job 的 `continue-on-error` 被接受、`pull_request.paths-ignore` 被接受、setup-node `22.x` 被接受；其余 35 项包括所有 bootstrap fail-fast、command-step `if`、任意 step `continue-on-error`、唯一/顺序/needs、Web parser/alias/cleanup/并发均 PASS。
- 根因是 validator 只对 `npm ci`/`npm test` 检查 step-level `if`，只对 `steps[]` 检查 `continue-on-error`，PR filter 仅识别 `paths`，且唯一 setup-node step 未校验 `with.node-version`。这些均为正式 validator 的真实缺口，不是工具、网络或 mutation harness 失败。

### 最小 Green

- Green commit `3a1168be fix(ci): close official test gate bypasses` 只扩展 workflow 类型与 validator。checkout/setup-node/cache/`npm ci`/`npm test` 五个关键 step 任一声明 step-level `if` 都拒绝；test job 只要自有任意值的 `continue-on-error` 属性即拒绝，既有所有 step 任意值 `continue-on-error` 严格约束保持不变。
- `pull_request` object 同时拒绝 `paths` 与 `paths-ignore`；唯一 setup-node 必须精确为冻结契约 `node-version: 24.x`，不会接受 Node 22。五步唯一性、checkout full history、checkout → setup-node → cache → npm ci → npm test 顺序、test 仅依赖 lint、命令 fail-fast 与禁用 `npm install` 的既有约束均未放宽。
- 修复后两份 Tester 测试直接为 **2 files / 41 tests PASS**；Developer/Reviewer 既有 bootstrap、CI、fixture boundary 与两个 Web consumer 为 **5 files / 30 tests PASS**。Tester 的测试和失败报告未改，当前只能交新的独立 Reviewer，不能由 Developer 自行把失败结论改写为 PASS。

### 官方门禁与跨文件竞态披露

- 无输出状态删除 9 个 workspace 的 `dist/dist-types/*.tsbuildinfo` 后，原 B00-3C 11 文件经官方 `npm test -- ...` 得 **11 files / 80 tests PASS**；保留已有输出原样重复仍为 **11/80 PASS**。两轮 pretest 都执行 native rebuild → clean → build → contracts `--check`，统计为 158 routes / 32 channels / 184 inventory / 321 bridge properties。
- 两个独立 Vitest 进程并发运行既有 Web consumer，分别 **1/1 PASS** 与 **5/5 PASS**；Tester Web 文件自己的双 fixture 并发与二次构建也在 41 项定向中 PASS。结束后主 checkout `apps/web/dist` 不存在，所有 B00-3C bootstrap/fixture/source/external 临时前缀均无残留。
- 额外把 Tester 两文件与原 11 文件合并到同一官方 Vitest 进程时得到 **13 files / 121 tests：120 PASS / 1 FAIL**。唯一失败位于 Tester `supports concurrent builds...` 的全局 `/tmp/lobsterai-web-build-*` 集合等值断言：该文件记录 baseline 时看到了另一个并行测试文件正在使用的合法 root `lobsterai-web-build-cPi330`，结束时对方已按约清理，因此期望集合含该 root、实际为空。相同 Tester 文件单独执行 11/11、两 Tester 文件联合 41/41、既有两个独立进程与原 11 文件两轮均 Green，最终 `/tmp` 也为空；本轮不修改 Tester 断言、不启用全局串行，由新 Reviewer 判断这是 Tester harness 的跨文件并发 P2 还是需要进一步处理的实现问题。
- Contracts `--check` PASS，14 个 managed output 的 SHA-256、size、mtime、ctime 前后逐项相同；generator Node check、主 CI YAML parse（5 steps / Node `24.x`）、本轮 helper + 两份 Tester 与全部 B00-3C 相关 TS 的 CI 等价 ESLint、`git diff --check 2b6dab6a..3a1168be` 与 tracked `git diff --exit-code` 全部 exit `0`。
- 按本局部返工范围不运行已知仍受 P02 provenance 阻断的 full `npm test`，不推送。当前状态仅为六个有效 validator Red 已由 Developer 转 Green并等待独立 Reviewer；不是 Tester `PASSED`、P03 任务级 `PASSED` 或总表接受。

### 剩余风险

- Node 版本契约当前精确冻结为 workflow 的 `24.x`，与根 `engines.node >=24.15.0 <25` 一致；未来升级 Node 主版本时必须同步调整 engines、workflow 与本验证契约，不能只改其中一处。
- validator 只禁止五个关键 step 的 `if`，其他新增诊断 step 仍可使用条件；若未来添加非关键 step，Reviewer 必须确认其不改变 frozen install、测试启动或失败传播语义。
- 13 文件合并运行的跨文件 `/tmp` baseline 竞态尚未由独立 Reviewer定性，因此 B00-3C 仍不交 Tester 复测，也不宣称局部门禁完成。

## B00-5A live evidence 与 CI artifact Developer Round 18 · 2026-07-12 · `/root/p03_b00_5a_developer`

### 状态、边界与旧 source 诊断

- 本轮只执行冻结计划 §8.4/§9.4/§9.5；结论为 `LOCAL_GREEN / REVIEW_PENDING_INTEGRATION`，不是 P03 任务级 `APPROVED_FOR_TEST`/`PASSED`。新 source 尚未推送，因此 required `CI`/`SaaS Scaffold` 新 run、SHA-bound artifact 名/下载 manifest 与 workflow run URL/ID 均保持待验，不用旧 run 代替。
- 旧 source run `29196686567` 的 `scaffold` job `86660817935` 真实失败：五镜像各出现 `Grype: source/digest/scanner structure does not match image evidence` 与 `local Docker image digest does not match evidence`。本机首轮 checkout `d49b52a8d27934b4914acc61e208428b2d004235`、product source `e0e984dcef235fc601a7156d137383e8bef5b6fb` 的 no-cache exact-five 也先以 stage invocation `ee91cb15-f2db-4aeb-8f57-94b977fb016c` 生成 report `ba81000f-635c-4eb7-bd7e-3d7991f6afd9`，随后 formal checker 稳定复现同样 10 条错误。
- Phase 1 四方采样确认根因是概念混淆：Docker `metadata.Id` 是 config/local image ID；Syft root checksum 与 Grype `manifestDigest` 是 OCI manifest digest；未 push 的本地 tag 其 Docker/Grype `RepoDigests` 合法为 `[]`。旧 generator 把 config ID 写成 publish digest，旧 validator 又同时强制本地 `RepoDigests` 存在，实际数据不可能同时满足；本轮未伪造空 `RepoDigests`。
- 旧 source run `29196686540` 的 main `test` job `86660929282` 另真实暴露 PR head checkout 与 GitHub merge `GITHUB_SHA` 冲突。修复保留 runner 的 HEAD 对账，只让 official `npm test` 显式传入与 checkout `ref` 相同的 `SAAS_SOURCE_SHA=${{ github.event.pull_request.head.sha || github.sha }}`；P02 phase env 语义未改。

### TDD Red 、最小 Green 与 commits

- `8ffc94ec test(ci): require exact-five evidence upload`：真实 `js-yaml` 解析下 21 项为 20 PASS / 1 FAIL，唯一 Red 精确列出 push/PR helper+template path、mode 0600 步骤与 SHA-bound upload 缺失；`d49b52a8 ci(supply-chain): upload exact-five evidence` 最小 Green 后 21/21 PASS。
- `3a414479 test(ci): bind official tests to checkout SHA`：26 项为 25 PASS / 1 FAIL，唯一 Red 为当前 official test 缺 `SAAS_SOURCE_SHA`；merge-only、PR-only 与错误 step override mutation 均拒绝。`c0a738c2 ci(test): bind gates to checked-out source` 后 26/26 PASS。
- `dd2e1edf test(supply-chain): separate local and manifest digests`：将 hermetic fixture 改为真实未发布结构：`localImageId != digest`、Grype `imageID=localImageId`、`manifestDigest=digest`、`repoDigests=[]`，全部文件 hash 重算。52 项为 49 PASS / 3 个目标 FAIL，失败来自旧 schema/validator 拒绝真实语义，不是 fixture hash 漂移。
- `a9504672 test(supply-chain): bind frozen evidence ancestry` 增加 report checkout 后仅追加 `tests/**` frozen commit 的正例，以及 checkout/product/legacy source 漂移负例；旧实现 56 项为 52 PASS / 4 目标 FAIL。`d06cdfd8 test(supply-chain): require blocked publish evidence` 另固定 `signature/publish=BLOCKED`。
- `af25bd55 fix(supply-chain): distinguish OCI and local image IDs` 是产品 Green：schema/report 增 `localImageId`、`checkoutSha`、`productSourceSha`、`publish`；generator 以 Grype imageID+manifestDigest 生成可发布 digest；formal checker 用 SPDX/Grype/manifest 对账 digest，单独对账 local config ID，未 push 时允许空 RepoDigests，非空时只接受精确 manifest digest。四文件 B00 集合 131/131 PASS；全 deploy+stage 默认并发最终 9 files / 165 tests PASS，未设全局串行或提高 timeout。

### 最终本机 live candidate 与工具

- 环境：Linux `6.8.0-124-generic` / `linux/amd64`；Node `v24.18.0`；npm `11.16.0`；Docker client/server `28.3.3`；Syft `1.44.0`；Grype `0.112.0`；Helm `v3.21.0+ge0878d4`。开始前 tracked/untracked clean，可用空间 278 GiB，远高于 40 GiB 入口与 4 GiB 停止线。
- 最终 checkout 与 product source 都为 `af25bd55d1c3b5ce334da3a324fd6fdebd9e61a1`。`npm run docker:build:check` 使用 `--no-cache --pull=false` 重建恰好五镜像，exit 0；stage invocation `334a613c-2c25-451c-bdd0-262d0a8941e0`，raw report invocation `a24f7b9e-d564-4e1b-9481-75f8c69d3585`，完成于 `2026-07-12T15:43:17.059Z` / `2026-07-12 23:43:17.059 +08:00`。
- raw report mode `0600`，SHA-256 `a198ddd2c49f5f9f27a3096c9fba22be31647d9adbfbe744a7b75a286ba94895`。NUL-safe 排序文件 hash manifest 位于 ignored `.reports/supply-chain/20260712_PR3部署供应链证据.sha256`，`sha256sum -c` 对 report + 5 SPDX + 5 Grype 共 11 个文件全部 `OK`；manifest 在 evidence 目录之外，不改变 exact-five artifact 文件集。

| image | OCI manifest digest | local config/image ID | SPDX SHA-256 | Grype SHA-256 |
|---|---|---|---|---|
| web | `sha256:276c2913d9e0494217d1f4bf13515c19e9df7a7a8f2f248da0b356de58cad0e6` | `sha256:7b60d85805c50fd47285fef98a2ce4722dccf91eae737e2175ba63b60667b4f0` | `sha256:60df16ded26b7d563b771d65346b30cc9ca0a7fe6e87e21f7bd8cf4dd6eb24ea` | `sha256:1c8fa188b8788d53f1728ce5e136f7e13d5408d1541b141ad85a0575d6109b38` |
| api | `sha256:6a160c048a4dc93882ae23cc40db3a1f979c413be5eed7ca3119bcdc74c6a7a9` | `sha256:2c16301ce8d4abc64ed64526a6ee14ce4f8d2244102c9d097754f72ce06ac6c6` | `sha256:5d344a3a13a13217640636b61cdb53a04505de6d6056300a941cd774217d4881` | `sha256:28dbfaf84d0b8d034eb0c147186a261552ba73451626dd741bda9bcb70a96efe` |
| worker | `sha256:5d1e2f4197401b88fa34442491f03ff50e3813bbbe07be896a80799cfc953471` | `sha256:9ac12a6fc52d02bf822f0e9abb7d0d943f5e3f9a38717eff3ebda4fa2fdcb714` | `sha256:22a97bf7e86b4336168f3be47f76780b6b070b595db4842109edcacf3651839c` | `sha256:7976b834f10b15783ee141df53fe73cbc3ad28e9f145e8eed7dc5d029db4e602` |
| runtime-orchestrator | `sha256:378500d72385b903c9c6d9f2661d52ef12cd7a6f1b6b53ce06667ae896e09119` | `sha256:eff41eebb6204b83dd9e2e02eaf5379613901d5994cb1ed7b022f1a6f77c4d66` | `sha256:316cad23e38be994e0db5bcf692299c6af14089da12af7075688f2a3f1ab1a4d` | `sha256:32872304434bf14b372fd5cd15970aa67ba2568f34e848079c8a8b540368bfb7` |
| openclaw-runtime | `sha256:f76984cc5ef6e67b6dd0c6dbef3ed4dc87198c482fdf32ce3c189602930676ca` | `sha256:f487e0b3d40852f55fbc120e0b914b6878403dbba8ff70f791eb40ade62baa5e` | `sha256:4b66c5f10e547ebc7f1d658de240d318f67a88e42c6373b58e271144b8c7883a` | `sha256:6d5f1773e3d51ba178885551d7e78d49090d25f9f3bebba14bcfc8a723eae985` |

- 五镜像均为 `linux/amd64`，有效 user 均为 `10001:10001`，Critical 计数均为 0；SPDX root checksum、Grype `manifestDigest`、report/wrapper digest 与 local inspect config ID 四方已重算。OpenClaw plugin 实查为 9 个 installed；唯一 optional `moltbot-popo=skipped`，`openclaw-nim-channel` 与 `openclaw-netease-bee` 均为 installed regular file。hardened probe 使用 `--rm --network=none --read-only --cap-drop=ALL --security-opt=no-new-privileges --user=10001:10001`，最终 P03 容器残留为 0。
- 有效 secret scan 使用 `rg --` 防止 private-key pattern 被误解为选项，结果 0 matches。一次无 `--` 的旧命令因 `rg: unrecognized flag` 证据无效，本记录不引用它。report 中 `signature=BLOCKED`、`publish=BLOCKED`，原因仍为 BLK-05 的内部 registry/trust 输入未冻结；未发布、未签名、未写伪造 remote digest。

### formal gates、测试与交接

- `npm run supply-chain:check` exit 0，stage invocation `ff0abc8a-cad5-4b7b-9d0a-11fb4b01f573`，source `af25bd55...`；`npm run helm:lint` exit 0，stage invocation `4d899446-5f79-4f5f-8138-5f85d907de66`，source 相同。
- B00 四文件 targeted：`tests/deploy-ci-evidence.test.ts`、`tests/deploy-product-source-boundaries.test.ts`、`tests/deploy-supply-chain-evidence-files.test.ts`、`tests/deploy-supply-chain-evidence-fixture-tester-corners.test.ts` → 4 files / 131 tests PASS。无 `.git`/`.reports` 的 `git archive HEAD` 重放 evidence 套件→ 1 file / 56 tests PASS，临时目录已删除。
- `P02_EVIDENCE_PHASE=pre-freeze SAAS_SOURCE_SHA=af25bd55... npm test` 官方默认入口 exit 0：221 files PASS，2503 tests PASS / 6 skipped / 0 failed。pretest 真实执行 native rebuild → workspace clean/build → contracts `--check`。
- fresh `node scripts/contracts/generate.mjs --check` exit 0：158 routes / 32 channels / 184 inventory / 321 bridge properties；前后 tracked diff hash 均为 empty-diff `e3b0c442...`，check-only 未写 tracked 内容。从 `8ea766b8` 起的 5 个 changed TS 以 CI 等价 ESLint 全绿；`npm run lint:saas`、两 checker Node syntax、schema JSON parse、Docker static、`git diff --check` 均 exit 0。
- 新 source 推送后，协调者必须另行验证 required main `CI` 的 `lint=success/test=success`，`SaaS Scaffold` 的 `scaffold=success/db-platform-arm64=success`，并下载 `p03-supply-chain-${{ github.sha }}`（retention 14 days）。artifact 必须以 NUL-safe 排序 hash manifest 与本机 11 文件交叉对账；未取得这些事实前不得交 P02 B00-6，也不得表述 B00-5A 已完成。

## B00-5A live evidence Developer Round 19 重建 · 2026-07-13 · `/root/p03_b00_5a_developer`

### 作废边界、candidate 与环境

- 本轮继续只履行 P03 Developer 的 B00-5A 本机证据职责，不承担 Reviewer、Tester、协调验收、B00-5B、P02 freeze 或总表更新。状态严格为 `LOCAL_GREEN_WAITING_NEW_CI_ARTIFACT`，不是 B00-5A/P03 `PASSED`。
- P02 后续修复已推进 image-input source。Round 18 的 `af25bd55...` 本机报告、run `29199066496`、artifact `8261987638` 以及 `84ff` evidence chain 全部作废，只保留历史诊断；本轮在目标 evidence 目录删除并确认不存在后才开始构建，没有复用旧 raw 文件或手改 JSON。
- 当前构建 checkout 为 `6512015af40d4e693c154e5df3ef0d08e59317a5`，tracked worktree 启动时 clean；`resolveProductSourceSha()` 独立解析为 `2963f9745e90a798a66ebaabc60da8afaf811bf7`，且该提交位于 checkout 的 first-parent 链。开始前可用空间 267 GiB。
- 工具为 Linux `6.8.0-124-generic` / `linux/amd64`、Node `v24.18.0`、npm `11.16.0`、Docker client/server `28.3.3`、Syft `1.44.0`、Grype `0.112.0`、Helm `v3.21.0+ge0878d4`。

### 当前 candidate 的 fresh exact-five

- `npm run docker:build:check` exit `0`；stage invocation `1317803f-ebfa-47e5-b62b-2fc57a89dccb`，stage source/checkout `6512015af40d4e693c154e5df3ef0d08e59317a5`。新 raw report invocation `477417d2-b0cc-4816-a40d-42474337a702`，生成时间 `2026-07-12T18:02:53.958Z`（UTC）/ `2026-07-13 02:02:53.958 +08:00`，status `PASSED`，checkout=`6512015a...`，product/source=`2963f974...`。
- evidence 目录严格只有 11 个 regular files：`docker-build-check.json` + 5 SPDX + 5 Grype；无其它目录、symlink 或额外文件。report mode `0600`，SHA-256 `b2e645fd2b83b9fbc96538e1700557ab710fd3f3ae8d6490c247f9e41acc799b`。
- 新 NUL-safe 排序 manifest 位于 ignored `.reports/supply-chain/20260712_PR3部署供应链证据.sha256`，`sha256sum -c` 对 11/11 文件全部 `OK`；manifest 留在 exact-five 目录外，不改变 CI artifact 文件集。

| image | OCI manifest digest | local config/image ID | SPDX SHA-256 | Grype SHA-256 |
|---|---|---|---|---|
| web | `sha256:c1e2f683cbcc506b4894e5dc41d85318c35bd6b852d7cd658ac8f744b5abda53` | `sha256:49a6fc55a865dda2bf86d57c4ccf85c032c2a8717eb0419daf2e5a6119512651` | `sha256:6fdcdd5889318253957f32add7d406f1996b09ea676789f5c136746cf29871df` | `sha256:c42b46d16d06818965ea023af153ffcd51b7c6144bbed82610f3d03f139a7423` |
| api | `sha256:7d9fce97328c3a2c79862830130eaa7b5493eca1d135d48bc97c6652a2dbcb1b` | `sha256:e38794422c1b8dd2e0e60573739acf9825e1025603abc2ed6b23e72028373215` | `sha256:0e7106abb14b83c9950d939d7d9a4ace48b0271067205e43a5aad9467afe3f1e` | `sha256:918a34dd435140ef183b4a80587ae89ca166f2f337b93f0fa29e84ef4b11b2b8` |
| worker | `sha256:c0622ecf5c43c025a6f73ca1fc3b521d31b93fecda94f437b9aff3faa8eb7001` | `sha256:f5075c2fa2b368a9f53cc26aea9cee235471c9fa1d866da09806afe15604bef9` | `sha256:d227bcb140f86c3d2ac3633ef4b980a876e8eda29efe11c8ebad3d337e432511` | `sha256:0a690705804b202d22d2218c5688da514067e2bc0d178b5841aa29b5d188b9e1` |
| runtime-orchestrator | `sha256:79e6c8322de11ca2b5f06fcb2c6a48d168d5763188e068bc0df5a216bab458d4` | `sha256:aba32964db19a7821784d046e8c1cddaf6bdea33a73b3c2692005c497f1660c1` | `sha256:123230b695bbb5aeb83dfab3829d95d39893611c7d7c0a5be2ec37d71d0f0be2` | `sha256:fdd8fc177f2785e0487da5fb7060858bf98f0b08706479ae2b079e098e4a9f77` |
| openclaw-runtime | `sha256:cc5edcafb3b713040e867dd929d2676e592fa4f83884c1fb01f366aa0999160d` | `sha256:839c59cecd3d8739d3c325b84bcac44fb254356b680e05ecab28358516296ca3` | `sha256:f829f0e1551bddd7b4ee161f5faf1bf997c666b0f19c6c69adb17aa072621e14` | `sha256:fcb377e6bca6c36c11a69dffc6b5aa8291050df25ba42968082bd71888acb9eb` |

- 五镜像 report 与独立 `docker image inspect` 均为 `linux/amd64`、effective user `10001:10001`，local config ID 与 report 相同。5 份 raw Grype 的 matches 分别为 web/api/worker/runtime-orchestrator `8/8/8/8`、openclaw-runtime `204`，Critical 全部为 `0`；10 份 SPDX/Grype 实际文件 hash、subject digest 与 source 已重新对账。
- OpenClaw 最终插件证据共 10 项：8 个 required 全部 installed；optional `openclaw-nim-channel=installed`，optional `moltbot-popo=skipped`。required 中 `openclaw-netease-bee=installed`。formal hardened probe 已通过。
- 构建前后 running-container 集合逐字节相同；按五个新 tag 查询 container residue 均为 `0`。精确 secret-signature scan 使用 `rg --` 返回 0 matches。未删除或修改用户既有 Docker 容器。
- report 诚实记录 `signature=BLOCKED`（内部 trust identity 未冻结）与 `publish=BLOCKED`（内部 registry target 未冻结）；本轮未发布、未签名、未伪造 remote/CI digest，也未执行 B00-5B。

### formal、Helm、targeted 与后续硬门

- `npm run supply-chain:check` exit `0`，stage invocation `a3878014-a4d5-438a-92ce-6e4d95dc019e`、source `6512015a...`；`npm run helm:lint` exit `0`，stage invocation `3514f567-dc52-4046-b240-1689dee033a0`、source 相同。
- 计划指定四文件 `tests/deploy-supply-chain-evidence-files.test.ts`、`tests/deploy-product-source-boundaries.test.ts`、`tests/deploy-docker-policy.test.ts`、`tests/deploy-helm-policy.test.ts` 在 Node 24 下为 **4 files / 116 tests PASS**；没有 skip、全局 timeout 或串行规避。
- 当前本地 evidence 仅证明 `6512015a...` checkout / `2963f974...` product source 的 linux/amd64 candidate。协调者必须推送包含本记录的 frozen descendant，等待新的 required `CI` 与 `SaaS Scaffold` 同 product source Green，下载新的 `p03-supply-chain-${{ github.sha }}` artifact 后重算其 11 文件 manifest，并逐项比较 local-vs-CI 五 digest、SPDX/Grype hashes、invocation 与 report SHA。任何不一致均使本 candidate 作废并从 B00-5A 重来。
- 在新的 CI artifact 下载并完成上述比较前，不得进入 P02 B00-6，不得复用 run `29199066496` / artifact `8261987638`，不得把当前状态升级为 `REVIEW_PENDING_INTEGRATION`、Reviewer/Tester 许可或总体完成。

## B00-5A live evidence Developer Round 20 重建 · 2026-07-13 · `/root/p03_b00_5a_developer`

### 作废边界与新 source

- P02 validator TDD 改动推进 executable image-input source 后，Round 19 local invocation `477417d2-b0cc-4816-a40d-42474337a702`、CI artifact `8263155247` 与 arm64 artifact `8263029696` 全部作废；本轮在删除旧 raw 目录与旧外部 manifest并确认目标不存在后才运行，不复用旧 evidence、不手改 JSON。
- 本轮仍只承担 P03 B00-5A Developer 本地动态证据，不修改 P02、Reviewer、Tester、总体表或 B00-5B，不 push。状态为 `LOCAL_GREEN_WAITING_NEW_CI_ARTIFACT`，不是 B00-5A/P03 `PASSED`。
- clean 构建 checkout 为 `837d8d8a4b4e4c6c6bc59925444ff4a993f5e13b`；`resolveProductSourceSha()` 独立解析为 `59121bf9f42d980a82efeb1354672e135d4f03cd`，且位于 checkout first-parent 链。开始前可用空间 251 GiB。
- 工具为 Linux `6.8.0-124-generic` / `linux/amd64`、Node `v24.18.0`、npm `11.16.0`、Docker client/server `28.3.3`、Syft `1.44.0`、Grype `0.112.0`、Helm `v3.21.0+ge0878d4`。

### fresh no-cache exact-five 与 hashes

- `npm run docker:build:check` exit `0`；stage invocation `a2331b6e-9f88-4f2c-8532-b40a716880b5`、source/checkout `837d8d8a...`。raw invocation `26848464-394e-4bd5-9263-25ad1dc612e3`，生成于 `2026-07-12T19:04:25.345Z` / `2026-07-13 03:04:25.345 +08:00`，status `PASSED`，checkout=`837d8d8a...`，product/source=`59121bf9...`。
- exact-five 目录严格为 11 个 regular files：report + 5 SPDX + 5 Grype，无目录、symlink 或额外文件。report mode `0600`、size 64897 bytes、SHA-256 `4273027749058a173bcf5c84d969180214a5a46e64b5d93c9589c5df7f8d1af4`；新 ignored NUL-safe 排序 manifest 对 11/11 文件校验 `OK`。

| image | OCI manifest digest | local config/image ID | SPDX SHA-256 | Grype SHA-256 |
|---|---|---|---|---|
| web | `sha256:3cf4c4be8ab6a49363fe3d3fe915128f6bc007c7ef1f83bb6a59b14f2e0e93e3` | `sha256:31fce6153555d15d1b0ca81efec7add9ba359eb35e372b1d8d667f20a10ab329` | `sha256:0760ad4290995471efd53c60f97fe10f18460024aa5c724a4819f9cf9163e5ce` | `sha256:65a0aed756ecdc2640a87f7f93f1a67ab5b5500164320286edd80f816999e490` |
| api | `sha256:a86813872aa86c78e0faf9bba1f35ffb35a24d53bcf0e40b12596386056e9b1d` | `sha256:1e9cce20bd74a63f8cc5420ae3258e5f2b3ce5312dfef87e05aa719d5dea84eb` | `sha256:77efa5be3117387090378df5a03096a2f242d4abee9d16b45cb384f3a8c340e3` | `sha256:e12daa3015e1aca5a2467aa8dce0fcf8a55eb73653272f32825efe8f68bbeffe` |
| worker | `sha256:e3b5fbd532b503c05dc1db782cfa289a55294b809b3cf340057f5a17e254e07e` | `sha256:51673be32eefe38c45ae9ad77add9b1c678f011e9fd3b1227838a795572c8338` | `sha256:8cc31913513175943cec105d3a2d671fe524f60c545b1fbc559f4bdc8bcbfefe` | `sha256:160570c176589fed6f698ebdb2b058243ea272ce45e9e1ae931a9aed631081bc` |
| runtime-orchestrator | `sha256:c1615633a6aeea61b62a515fdf54bd7a07c99df6fce06e3fc4c965a57925f926` | `sha256:03068f3902c0696824d7cd7ca29dc319d2b5083bec7fbb3bd9621f8e8d2a9cfe` | `sha256:2d186aaa192397b8e3d68ceb85f1b4b33ef463281b3d2525d96ccc74d88982c7` | `sha256:0a343e1a04acada5db14bae90ec74d23be64a360cbdcf43135b1ad1433df3007` |
| openclaw-runtime | `sha256:1271dfb6ae8d5f85aa77de886db62d0cf4f4a14c67feef5423c94eb7d6ddd8d3` | `sha256:638ea6753c77b960506d277c865e204ec6ca00e36012e7c420f643b379912826` | `sha256:ec448717f752be894a78071c02c62ced0ad162721c65d1afd427f51f1a2a731a` | `sha256:4708f32ea1aadd447112483bbe45c2ad737396fcaf6a27caef327f7d180fc168` |

- 五镜像 report 与独立 live `docker image inspect` 均为 `linux/amd64`、effective user `10001:10001`；local image ID、OCI digest、SPDX/Grype subject/source/hash 全部重算一致。raw Grype matches 为 `8/8/8/8/204`，五镜像 Critical 均为 `0`。
- OpenClaw plugin 证据为 10 项：8 个 required 全 installed；optional `openclaw-nim-channel=installed`、optional `moltbot-popo=skipped`，required `openclaw-netease-bee=installed`；formal runtime hardened probe PASS。
- 五个新 tag 的 container residue 均为 `0`，构建前后 running-container 集合逐字节相同；secret-signature scan 使用 `rg --` 得 0 matches。report 诚实保持 `signature=BLOCKED`（trust identity 未冻结）和 `publish=BLOCKED`（registry target 未冻结）。

### formal、Helm、targeted/corners 与交接

- `npm run supply-chain:check` exit `0`，stage invocation `965a719c-9046-4eda-999e-b85d8c84c5ab`、source `837d8d8a...`；`npm run helm:lint` exit `0`，stage invocation `e76cd868-1c8b-4441-95e0-94fd87a0cf5a`、source相同。
- 七个 P03 相关文件 `deploy-{ci-evidence,docker-policy,helm-policy,product-source-boundaries,supply-chain-evidence-files,supply-chain-evidence-fixture-tester-corners,supply-chain-policy}.test.ts` 在 Node 24 默认并发下为 **7 files / 157 tests PASS**；没有 skip、全局 timeout 或串行规避。
- 协调者必须推送本 docs-only frozen descendant，取得新的 required CI/SaaS exact-five artifact 与同 source arm64 evidence；下载后重算 11 文件 manifest，并比较 local-vs-CI 五 OCI digest、十 evidence hash、report/invocation/source。任何漂移均使本 candidate 作废并从 B00-5A 重来；在此之前不得进入 P02 freeze 或复用 `8263155247`/`8263029696`。

## B00-5A live evidence Developer Round 21 刷新 · 2026-07-13 · `/root/p03_b00_5a_developer`

### 作废边界、新 candidate 与工具

- P02 phase/stale-layout trusted pin 修复再次推进 executable product source；Round 20 raw invocation `26848464-394e-4bd5-9263-25ad1dc612e3` 与 CI artifact `8263697094` 全部作废。本轮先删除旧 evidence 目录和外部 manifest并确认不存在，不复用旧文件、不手改 JSON。
- clean build checkout=`425e6018cdb32c29c96f058b204248040e0b9cc9`；resolver 独立返回 product/source=`cb2f861fc537db6a3847e3974edb2165b5a74358`，且位于 checkout first-parent 链。开始前 234 GiB 可用。
- 环境为 Linux `6.8.0-124-generic` / `linux/amd64`、Node `v24.18.0`、npm `11.16.0`、Docker client/server `28.3.3`、Syft `1.44.0`、Grype `0.112.0`、Helm `v3.21.0+ge0878d4`。
- 本轮仍只承担 P03 B00-5A Developer 本地动态证据；不修改 P02、Reviewer、Tester、总体表或 B00-5B，不 push。状态为 `LOCAL_GREEN_WAITING_NEW_CI_ARTIFACT`，不是 B00-5A/P03 `PASSED`。

### exact-five 与完整 hashes

- `npm run docker:build:check` exit `0`；stage invocation `c65025b1-8c42-4d87-9a68-5e3f8cf7b54d`、source/checkout=`425e6018...`。raw invocation `1a34d542-5494-4ffe-a7fc-8a36141924e9`，generatedAt=`2026-07-12T19:59:33.632Z` / `2026-07-13 03:59:33.632 +08:00`，status `PASSED`，checkout=`425e6018...`，product/source=`cb2f861f...`。
- 目录严格为 11 个 regular files：report + 5 SPDX + 5 Grype；无目录、symlink 或额外文件。report mode `0600`、size 64898 bytes、SHA-256 `c182dc9bae7c84df81082810d724f16d1cd142a57b06a1322286b4df20fa6ad5`；新 NUL-safe 排序 manifest 的 11/11 文件校验均 `OK`。

| image | OCI manifest digest | local config/image ID | SPDX SHA-256 | Grype SHA-256 |
|---|---|---|---|---|
| web | `sha256:519eb61cd2b9794898af04f3bedc54eb917cc006350446342945917fe4c72f5d` | `sha256:9243ea630b7a20622de15b90ca4af973b6d017e74ff4011a02262c72076ecee6` | `sha256:48cf3448b8c776b0193003c9fa7e8579f7f7c05c05b84f049265a541dbf6d8cc` | `sha256:24f0f7bacb122fd15c456172dfd179d7c32bf170448d7123cc68a5846f40dff3` |
| api | `sha256:288ae1ac91c6063ff236ff53564accb85f921947ad3a220815fe01a392aa08f4` | `sha256:f4632b13abfc5823ae673e25b0ec4b77a026569d58b2549cda1ba6c2b6c9ebbc` | `sha256:541e88531855484a3e33ae52449254bd69fcb9395b01d52a257eff9806f88a65` | `sha256:347263c5732fa78dc491973b51c421e2e1535b57eea2dcf8afb0af1132cda5a1` |
| worker | `sha256:3a1ce9bc32ea0546b8a0198c4772e1a0dd13f7289f73261005992ec1020f5bd9` | `sha256:fdfe462b310d794c485a23efe6961f288042eeebe32585f2fdfb24c707bfaec0` | `sha256:024d2b0a47251efa5d78db3aa52f17915b9adc4ec15269f6e64d13ec179b1ae5` | `sha256:14b7b6ee52d2182d5cf65aed8f9d02f33a12dab3657f6319ff22fdedc725fc5f` |
| runtime-orchestrator | `sha256:8ff30a6dff7b31c22de2b33060c95c794b191f7ec7fc403f1b10d98ddb287fb9` | `sha256:4b782c39f682dc8735ccd2f78fc54f0d8195f6fe8ffc2c328b43eb03f1df5bca` | `sha256:714a2ae6cda7100c228bfeb58e6d5ce3d7e74f43992c1aea4ef5275ea408b134` | `sha256:9994e37558120895519565ae69705100891e6ccae288ec3032a407d1c476669f` |
| openclaw-runtime | `sha256:f966d9cf0afa76a47dd75f97fdf6451f5481679ef389219fe8103bb92fdafc95` | `sha256:a2b9239f154c0afd181e208935b98b31a142af927aa4556a33b7216a625978f1` | `sha256:48719cd139766c228b10b797558b169768a26d2316929b09b4ca13a46e501557` | `sha256:9e27325efe38a21d73a840b8312d066c888a0f1433aeb8ca8b51bb3b7dbcb032` |

- 五镜像的 report 与独立 `docker image inspect` 均为 `linux/amd64`、effective user `10001:10001`；local ID、OCI digest、SPDX/Grype subject/source/hash 重算一致。raw Grype matches 为 `8/8/8/8/204`，Critical 全为 `0`。
- OpenClaw 最终 image 共 10 个 plugin：8 required 全 installed；optional `openclaw-nim-channel=installed`、optional `moltbot-popo=skipped`，required `openclaw-netease-bee=installed`；formal runtime hardened probe PASS。
- 五个 tag container residue 均为 `0`，构建前后 running-container 集合不变；secret-signature scan 0 matches。report 保持 `signature=BLOCKED` 与 `publish=BLOCKED`，未发布或签名。

### formal、Helm、tests 与等待项

- `npm run supply-chain:check` exit `0`，stage invocation `63c4048d-e4f6-444a-8c62-0ac7d423464c`；`npm run helm:lint` exit `0`，stage invocation `448465ff-3e10-4a24-b18a-cc15156bad30`；两者 source 均为 `425e6018...`。
- 七个 P03 文件 `deploy-{ci-evidence,docker-policy,helm-policy,product-source-boundaries,supply-chain-evidence-files,supply-chain-evidence-fixture-tester-corners,supply-chain-policy}.test.ts` 在默认并发下为 **7 files / 157 tests PASS**，无 skip、全局 timeout 或串行规避。
- 协调者需推送本 docs-only frozen descendant并取得新的同 product source required CI/SaaS artifact；下载后重算 11 文件 manifest，比较 local-vs-CI 五 digest、十 hashes、report/source。新 artifact 完整对账前不得复用 `8263697094`、进入 P02 freeze 或升级 B00-5A 状态。

## B00-5A live evidence Developer Round 22 最终未推送刷新 · 2026-07-13 · `/root/p03_b00_5a_developer`

### resolver 预期更正与作废边界

- P02 suite-observability/trust-boundary TDD 推进 candidate 后，Round 21 本机/CI 证据全部作废。本轮先删除旧 evidence 目录和外部 manifest并确认不存在，再执行完整构建；没有复用旧 raw、CI artifact 或手改 JSON。
- 协调输入最初预期 product source=`993115fd4085e3dcb877bbbe4b73400bccfd2dba`，但当前 clean HEAD `fab7d33f873b6b58433ca11b93e827b54285de91` 上 resolver 连续三次均返回 `376610ebc095b21930cd295c2f1139c779eac3bc`。name-status 证明 `993115fd` 只修改 `tests/db/tester-evidence-boundary.test.ts`，属于 frozen tests；`376610eb` 修改 `package.json`、`scripts/check-saas-scaffold.mjs`、`scripts/db/**` 与 stage manifest等 image inputs。协调者确认以 resolver 为准，不改 resolver、不伪写 source。
- build checkout=`fab7d33f873b6b58433ca11b93e827b54285de91`，product/source=`376610ebc095b21930cd295c2f1139c779eac3bc`，两者位于同一 first-parent 链。开始前可用空间 219 GiB。
- 工具为 Linux `6.8.0-124-generic` / `linux/amd64`、Node `v24.18.0`、npm `11.16.0`、Docker client/server `28.3.3`、Syft `1.44.0`、Grype `0.112.0`、Helm `v3.21.0+ge0878d4`。

### fresh exact-five 与 hashes

- `npm run docker:build:check` exit `0`；stage invocation `8e2ab765-48ee-426b-8617-88410d142a92`、source/checkout=`fab7d33f...`。raw invocation `d949784a-9620-42e5-b900-64a7a6a71def`，generatedAt=`2026-07-12T21:16:24.488Z` / `2026-07-13 05:16:24.488 +08:00`，status `PASSED`，checkout=`fab7d33f...`，product/source=`376610eb...`。
- 目录严格为 11 个 regular files：report + 5 SPDX + 5 Grype；无目录、symlink 或额外文件。report mode `0600`、size 64897 bytes、SHA-256 `8bc7f52342bceb323f9d4dea7b52145cfeacc525707da877634048f5f8b7546d`；新 NUL-safe 排序 manifest 对 11/11 文件校验 `OK`。

| image | OCI manifest digest | local config/image ID | SPDX SHA-256 | Grype SHA-256 |
|---|---|---|---|---|
| web | `sha256:2ba213fa69c02e6e6345da318194a744dab933183b9c68662cbb29b07b5f3494` | `sha256:40f2c32c0ccc28e5d20c9ee411f1854d9d74e8005ccb131f54dd616e9e65a2fe` | `sha256:c62f15b22d9894b299bc64f800634c8eb00b826a7ec7fd86ffaac3772155a245` | `sha256:7a335cb588de4abdb3ce3367045678e966f94e51b5cca45e9a8746d2735798ef` |
| api | `sha256:8bea4de56940ee323f2256b452c9dc72980ec82a3d2fa572ef4b345da67849d2` | `sha256:6668f6859d7e0e2e23e0d6350896590d0911051985b66aa22d99dd77732bc75a` | `sha256:fe8bd1a0ac6531050c5743dcc0542d8e88238a91ad734d0d8a25c7c2ad4d23e1` | `sha256:e39c6effc1530e49e932bc1e6c5d859f2ea443458334ca17324598876b7090b4` |
| worker | `sha256:9d04a62604cc60101872f549f9ed69d5ad434f8b6931b7747df0e794fd0e50a8` | `sha256:a40c002d776c9b2b185ca310de22a59057727c39fe2eaf72b8efc0a2c090bf35` | `sha256:d1855737fbc91b0911b00f36dc6e3f9a182d99f6cd4b86d3788902a11a71ae2d` | `sha256:a10f37d879df1f9c2410a051ebc37837a0f9a47c43634b6df23bc9e8b4b5b9d8` |
| runtime-orchestrator | `sha256:74533fb9fcc9f5b3df0f8d63d7aa227c35ded53d13077d589956da9f955c96f2` | `sha256:7fa618c7ce4cb339f4861b3965ea2c51ee0a5b612a924b8b8d930e32bb4c133c` | `sha256:a23241d9e52384f2f98af43c91dd633d4e543b8f3a306dca5f6e193c43b42360` | `sha256:9de63c716d801cfab970645b479ac7862e72cad2db68102de1b26a0119744b7b` |
| openclaw-runtime | `sha256:ceab57e399e15984813495a6a30e85dfe842027dce4fe651e579894bc59b7b5f` | `sha256:5a0d3bdca48883d8e1365150296b757dd0aedba0eda11a5ae465327e5321bdfd` | `sha256:a6c802e621eb6e48c75c226d8c09fa720bb58602dba73e831abb590045689842` | `sha256:2736def830d6bd229812d9e38d3a948d046e5c67ca59676a6591a80ad57c171f` |

- 五镜像 report/live inspect 均为 `linux/amd64`、effective user `10001:10001`；local ID、OCI digest、SPDX/Grype subject/source/hash 重算一致。raw Grype matches=`8/8/8/8/204`，Critical 全为 `0`。
- OpenClaw final image 的 10 个 plugin：8 required 全 installed；optional `openclaw-nim-channel=installed`、optional `moltbot-popo=skipped`，required `openclaw-netease-bee=installed`；formal hardened probe PASS。
- 五个 tag container residue 均为 `0`，构建前后 running-container 集合不变；secret-signature scan 0 matches。`signature/publish` 继续诚实 `BLOCKED`。

### formal、Helm、tests 与一次性 push 交接

- `npm run supply-chain:check` exit `0`，stage invocation `ff893470-ec01-4c88-9e44-12c1645e2b88`；`npm run helm:lint` exit `0`，stage invocation `bc8fb783-4dea-4f60-9b2c-c0cbb9920be1`；两者 source 均为 `fab7d33f...`。
- 当前七个 P03 文件 `deploy-{ci-evidence,docker-policy,helm-policy,product-source-boundaries,supply-chain-evidence-files,supply-chain-evidence-fixture-tester-corners,supply-chain-policy}.test.ts` 在默认并发下为 **7 files / 157 tests PASS**，无 skip、全局 timeout 或串行规避。
- 本 Developer 不 push；状态为 `LOCAL_GREEN_WAITING_NEW_CI_ARTIFACT`。协调者应在本记录 docs-only commit 后只推一次最终 HEAD，使 arm artifact 与 SaaS exact-five artifact 精确绑定该 frozen descendant；下载后再比较 local-vs-CI 五 digest、十 hashes、report/source，未对账前不得升级 B00-5A 或进入 P02 freeze。
