# 开发记录

> 初始为空白；Developer 按 Round 追加 Red→Green→Refactor、commit（获授权后）、命令/退出码/证据与 Reviewer 交接。

## 元数据
- 任务 ID：P03-PR3部署供应链
- Developer Agent：`/root/p03_developer`
- 基线 SHA：`fa15faf1f9cc33c400c290729a5d967ec1e48062`

## 共享热点应用结果

P01 并行期间先记录并暂时移除共享热点草稿；P01 释放后，以其最终 HEAD 为基线重放并由 `169e5bbc` 提交。以下内容现均已应用：

- `package.json`：让 `lint:saas` 覆盖 `tests/deploy-*.test.ts`；新增 `supply-chain:validate`、`docker:build:validate`、`docker:static`、`helm:validate`、`helm:static`，保留三个公开 gate 命令经 stage runner 执行。
- `scripts/saas-stage-gates.json`：将 `supply-chain:check`、`docker:build:check`、`helm:lint` 改为 `PASS` 并分别执行真实 checker，fixtures 改为 P03 实际 schema/manifest/Dockerfile/chart/checker；其余 deferred gate 继续 exit 78。
- `scripts/run-saas-stage-gate.mjs`：命令失败前删除陈旧报告；ACTIVE 命令不得以 78 伪装 deferred；报告绑定 checkout source SHA，stdout+stderr hash，使用 mode 0600 原子写。
- `.github/workflows/saas-scaffold.yml`：路径过滤覆盖 P03 checker/deploy tests；以 commit SHA 固定 `Azure/setup-helm` v5.0.1 与 `anchore/sbom-action/download-syft` v0.24.0，工具版本固定 Helm v3.21.0 / Syft v1.44.0；CI 直接运行三个真实 gate，`expect-saas-stage-gate` 只保留 deferred gate。
- `scripts/check-saas-scaffold.mjs` 与 stage-gate 测试已同步覆盖 active/deferred 状态、fresh report/source SHA、fixture 删除和 checker 退出码；隔离副本使用同一 P03 checker 的 `--static` 模式，单元测试不会误触真实镜像构建。

## Round 1 · 2026-07-11 13:37 · `/root/p03_developer` / `cb37e1da`
### Red

- 先提交 `cb37e1da test(deploy): capture P03 red baseline`，只含三组 deploy 测试与五个恶意供应链 fixtures。
- 命令：`npx vitest run tests/deploy-*.test.ts`；exit `1`；结果：3 files failed、12 tests failed。
- 失败均来自目标行为缺失：五个 Dockerfile 不存在、debug 入口缺失、Helm schema/Deployment/NetworkPolicy/Quota/RBAC/SA token 基线缺失、inventory 仍为 `NOT_APPLICABLE` 且 checker 不存在。未把“本机缺 Docker/Helm”计作 Red。
- Red 覆盖 `P03-T01/T02/T03/T04/T07/T08/T10/T11/T12` 的初始缺失面；`T14` 在 stage runner 激活批次补 mutation。

### Green / Refactor

- `497f0ce1 feat(supply-chain): activate deterministic asset inventory`
  - ACTIVE schema/manifest 双向发现 57 项资产：bundled skills、第三方 OpenClaw plugins、本地 extensions、内置 Kit、MCP defaults。
  - 未锁定 MCP 默认模板如实标为 `blocked + productionEligible:false`；重复/大小写碰撞/symlink 越界/ghost/missing/integrity drift/critical finding/过期 waiver/digest drift/伪 trust identity 均有 mutation。
  - 内部 registry 与签名身份未冻结，manifest 明确 `signature.status=BLOCKED`，不伪造签名或发布。
- `9626ad54 feat(deploy): add hardened production image builds`
  - 新增 web/api/worker/runtime-orchestrator/openclaw-runtime 五个多阶段生产 Dockerfile，数字 UID/GID 非 root、health、exec entrypoint、runtime 无动态安装，生产与 `Dockerfile.debug` 分离。
  - OpenClaw build stage 使用 pinned 版本、Linux x64/arm64 映射和现有 ensure→patch→build→bundle→plugins/extensions/channel-deps→prune 链；最终镜像不复制宿主 vendor/mac 产物。
  - checker 提供静态策略、真实五镜像 build、只读根/断网/cap-drop smoke、graceful stop、history secret scan、Syft SPDX 与 digest/source SHA 证据。
- `876fa677 feat(helm): add secure production deployment baseline`
  - 严格 `values.schema.json`，四个常驻 Deployment、Web/API Service、ConfigMap、Secret name/key 引用、两个 ServiceAccount、最小只读 namespaced RBAC、default-deny + DNS/明确 service-plane NetworkPolicy、ResourceQuota、sandbox RuntimeClass/SA/digest fail-closed 占位。
  - 未实现真实 V1 Pod turn/PVC/egress，也未提前实现 V4 动态 Pod lifecycle。
- `169e5bbc ci(deploy): activate P03 supply chain gates`
  - 三门改为真实 `PASS` command；stage report 删除陈旧文件后原子写、绑定 source SHA，ACTIVE checker exit 78 被转为失败。
  - CI 固定 action commit 与 Helm v3.21.0 / Syft v1.44.0；active gates 直接运行，deferred gates 保持 verifier/exit 78。
- `8242bcf5 fix(deploy): resolve Docker SBOM scanning context`
  - 首次真实 build 发现 OrbStack 非默认 socket 导致 Syft 无法解析本地镜像；改为显式 `docker:<tag>` source 并从当前 Docker context 注入 `DOCKER_HOST`。
  - 同时固定 Dockerfile frontend digest、复用 npm install cache 层，并区分工具缺失 `BLOCKED` 与真实构建/策略 `FAILED`。
- `a64c0d9c style(deploy): sort Helm policy test imports`：修复 changed-file lint 的 import 顺序。

### 自测与证据

- 环境：macOS Darwin 25.5.0 arm64；Node `v24.15.0`；Docker client `29.4.0` / OrbStack；Helm `v3.21.0+ge0878d4`；Syft `v1.44.0`（commit `8cb78ce40ced...`）。Helm/Syft 均下载到 `/tmp`，按官方 checksum 校验后使用并删除，未进入生产镜像或仓库。
- `node scripts/check-supply-chain.mjs` / `npm run supply-chain:check`：exit `0`，57 assets，publish status `BLOCKED`（内部 trust 未冻结）；stage report 绑定 `169e5bbcb492f96872e3fc97d06632fbe5b3bf9f`。
- `node scripts/check-docker-build.mjs --static`：exit `0`，五镜像静态策略全过；`tests/deploy-docker-policy.test.ts` 7/7 pass。
- 完整 Docker 首次实跑：固定/校验 Syft 后，`lobster-web` 真实构建成功；image ID `sha256:cef75ec971b0236fbc435a135fd76bf7f2d32aa53c78fb48f9027a273a8b242e`，effective user `10001:10001`，health config 生效，断网/只读根 smoke 通过。随后 Syft 因未发现 OrbStack socket 失败，exit `2`；该原因由 `8242bcf5` 修复，但按协调者停止指令未重跑完整五镜像，因此 API/worker/orchestrator/OpenClaw 的真实 build/SBOM 证据仍缺失。
- Helm 完整实跑：首次发现 Go regex 不支持 schema negative lookahead；修正为 JSON Schema `not` 后，以校验 checksum 的 Helm v3.21.0 执行 `node scripts/check-helm.mjs` exit `0`，17 份 rendered documents，3 个负向 mutation 均被拒；report invocation `b2a5d025-a2d0-471a-8620-ede4697d1280`，report SHA-256 `c71f6eac5420e191ac2c55393ce44d2e9595a72829a1d01c1591d981881ba387`（`.reports` 未提交）。
- 本机不临时装工具时：`npm run docker:build:check` 与 `npm run helm:lint` 均 exit `2/BLOCKED`，而非 exit 0/78 假绿；CI 已提供固定版本工具安装。
- `npx vitest run tests/deploy-*.test.ts tests/scaffold-stage-gates.test.ts`：4 files、21 tests 全绿；stage 单测在隔离副本使用 static command，不依赖本机残留镜像。
- `npm run scaffold:check`：SCAF-1…SCAF-6 全绿。
- `npm run contracts:check`：exit `0`，确认 P01 `contracts:check` 保持 PASS；最近证据 source SHA `8242bcf58ec6dd75ae4468140e8403401c1f5f02`。
- `npm run typecheck`：exit `0`。
- changed-file ESLint：`npx eslint --report-unused-disable-directives --max-warnings 0 tests/deploy-*.test.ts tests/scaffold-stage-gates.test.ts` exit `0`；`npm run lint:saas` 首轮只发现 Helm test import 顺序，`a64c0d9c` 修复后 `npm run lint:saas` 全量复跑 exit `0`。
- `git diff --check`：exit `0`；交接前 worktree clean。
- 未运行 `npm test` 全量套件；本轮按协调要求停止扩展并完成 targeted/scaffold/contracts 回归。完整 Docker/SBOM 五镜像门也未在修复后重跑，不能把当前 Developer 证据表述为 P03 全验收完成。

### 交接 Reviewer

- 状态：`REVIEW_PENDING`，Developer 不自行批准、不写 Tester 报告、不勾总体表。
- Reviewer 请优先核对 `P03-AC01/AC05/AC13`：当前仅 web 有真实 build/smoke，Syft socket 修复后的五镜像/SBOM 全跑仍需批准构建环境或 CI 证据；缺证据应 `CHANGES_REQUESTED`，不得以静态门替代。
- `P03-AC14` 的 publish/signature 当前按计划显式 `BLOCKED`：内部 registry/trusted identity 未冻结；本地 inventory/build/Helm 可评审，但不得宣称可发布/部署。
- 重点复核：OpenClaw Linux 构建链和裸 `openclaw.mjs` 保留是否真实可构建；Helm rendered securityContext/RBAC/NetworkPolicy 是否符合 P03 占位边界；stage runner source SHA/fresh report/exit 78 mutation 是否不可绕过；CI 外部 actions 是否接受固定 commit 与版本。
- 若 Reviewer 退回，继续唤回 `/root/p03_developer` 修复；只有 Reviewer `APPROVED_FOR_TEST` 后才能启动独立 Tester。
