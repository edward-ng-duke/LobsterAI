# P04 · PR-4 V1 PoC harness

- 状态：TODO
- 依赖：P00、P03；Day-0 缺口可阻断但不能伪造。
- 权威输入：根 AGENTS、19、17 对应 V1/V2、16、附录 C；冲突以根 AGENTS 的生产断公网和附录 C 契约决策为准。

## 源码边界

apps/runtime-orchestrator/poc、docker/openclaw-runtime、sandbox-pod.yaml、docs/poc/YYYYMMDD_V1_OpenClaw沙箱PoC证据包。

## TDD：先写的失败测试

先写缺 env/RuntimeClass/manifest/脚本/证据目录或出现公网 URL 时失败的 poc:v1 检查。 Red 必须因目标行为缺失而失败；环境缺失另记 BLOCKED，不算 Red。

## Corner cases

未替换占位符、命令成功却无 artifact、runId 冲突、清理失败、debug 配置误进 prod。

## 实施与验收

Developer 按最小垂直切片逐个 Green，避免无关重构。验收命令：npm run poc:v1:check；npm run helm:lint；npm run docker:build:check。

## 回滚

harness 独立回滚，不影响桌面端；清理测试 namespace/volume。

## 完成证据

可复现入口、环境清单、sandbox 样例和证据 schema；不得含假通过日志。 证据须绑定 SHA/runId/环境；所有触碰 TS 文件 changed-file ESLint 0 warning，相关 Vitest 全绿。

## Developer → Reviewer → Tester 循环

Developer 记录每轮 Red/Green 与 diff → 独立 Reviewer 对照本文件和源码审核；若 CHANGES_REQUESTED，退同一 Developer 修复并进入下一轮 → Reviewer APPROVED_FOR_TEST 后独立 Tester 新增 corner-case/回归测试；Tester FAILED 必须退 Developer 且修复后重新 Reviewer → Tester PASSED 后由协调者核验证据并勾总体表。任何角色不得自行勾 Todo。

