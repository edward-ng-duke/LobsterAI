# P00 · PR-0 SaaS workspace scaffold

- 状态：ACCEPTED（2026-07-11，总体协调者复核）
- 依赖：当前桌面仓库；Day-0 未决项允许进入 blocked 报告，但不得写假值。
- 权威输入：根 AGENTS、19、17 对应 V1/V2、16、附录 C；冲突以根 AGENTS 的生产断公网和附录 C 契约决策为准。

## 源码边界

根 package.json/package-lock、tsconfig*.json、apps/* 空壳、libs/* 空壳、.github/workflows；不得实现业务。

## TDD：先写的失败测试

先写 workspace 目录/依赖方向/命令非 no-op/旧桌面命令兼容检查，证明当前仓库失败。 Red 必须因目标行为缺失而失败；环境缺失另记 BLOCKED，不算 Red。

## Corner cases

空 workspace、循环 project reference、libs/shared 反向引用 React/Nest/Electron、命令 exit 0 无检查、lockfile 漂移。

## 实施与验收

Developer 按最小垂直切片逐个 Green，避免无关重构。验收命令：npm ci；npm run lint；npm run typecheck；npm test；所有新门入口；npm run build；npm run compile:electron。

## 回滚

整体撤销 scaffold commit；不得留下部分 workspace/lockfile 状态。

## 完成证据

SCAF-1～6 报告、CI run、旧桌面构建结果、codegen 后 clean diff。 证据须绑定 SHA/runId/环境；所有触碰 TS 文件 changed-file ESLint 0 warning，相关 Vitest 全绿。

## Developer → Reviewer → Tester 循环

Developer 记录每轮 Red/Green 与 diff → 独立 Reviewer 对照本文件和源码审核；若 CHANGES_REQUESTED，退同一 Developer 修复并进入下一轮 → Reviewer APPROVED_FOR_TEST 后独立 Tester 新增 corner-case/回归测试；Tester FAILED 必须退 Developer 且修复后重新 Reviewer → Tester PASSED 后由协调者核验证据并勾总体表。任何角色不得自行勾 Todo。
