# 测试报告

## Round 1 · 2026-07-11 12:59 CST · `/root/p00_tester` / `361f9f54`

- Tester Agent：`/root/p00_tester`（全新独立 Tester，未复用 Developer/Reviewer Agent）
- 基线 SHA：`361f9f541c6dc41162cf4e928cef139401990e83`
- Tester 测试提交：`9c93940c`
- 环境：macOS arm64（Darwin 25.5.0），Node `v24.15.0`，npm `11.12.1`，Vitest `v4.1.10`
- namespace/digest：P00 本地 scaffold 测试不涉及集群或镜像，故不适用
- 结论：`FAILED`

Reviewer Round 4 已明确为 `APPROVED_FOR_TEST`后才开始本轮。新增 `tests/scaffold-tester-corners.test.ts` 从 Reviewer mutation 之外验证 artifact 路径/时间边界、registry 原始 JSON 歧义、stage report nonce 与 Web bundle 引用一致性。5 项中 2 项通过、3 项失败；失败均是当前实现接受了应拒绝的输入，不是环境或语法失败。依角色协议，Tester 未修改实现，现退回 Developer；修复后必须重新经过 Reviewer `APPROVED_FOR_TEST` 才能再测。

| 用例 ID | 独立角度 | 测试文件/脚本 | 结果 | 证据 |
|---|---|---|---|---|
| P00-TST-01 | artifact symlink 越界 | `tests/scaffold-tester-corners.test.ts` / `check-saas-build-artifacts.mjs` | FAILED | 将已登记的 `apps/api/dist/index.js` 替换为指向仓库外临时文件的 symlink，validator 仍退出 `0`，输出 `verified 18 fresh artifacts for 9 workspaces`。 |
| P00-TST-02 | artifact 未来时间 / clock skew | `tests/scaffold-tester-corners.test.ts` / `check-saas-build-artifacts.mjs` | FAILED | 全部 18 个产物 mtime 设为当前时间 +24h，validator 仍退出 `0`，输出 `verified 18 fresh artifacts for 9 workspaces`。 |
| P00-TST-03 | registry 重复原始 key | `tests/scaffold-tester-corners.test.ts` / `check-saas-scaffold.mjs` | FAILED | 在合法 JSON 中重复声明 `apps/api` key，checker 静默采用后一值并退出 `0`，SCAF-1～6 全部报 `passed`。 |
| P00-TST-04 | stage report 重放 nonce /原子写残留 | `tests/scaffold-tester-corners.test.ts` / `run-saas-stage-gate.mjs` | PASSED | 连续两次调用均退出 `78`，`invocationId` 不同，最终 report 目录无 `.tmp` 残留。 |
| P00-TST-05 | Web bundle 引用与实体一致 | `tests/scaffold-tester-corners.test.ts` / Web workspace build | PASSED | Vite production build 退出 `0`；生成 HTML 不引用 `/src/`、公网或 data URL，所有本地 `src`/`href` 都存在于 `dist`且可读取计算 SHA-256。 |

## 失败复现

```bash
npx vitest run tests/scaffold-tester-corners.test.ts
```

结果：退出码 `1`，1 file failed，5 tests 中 3 failed / 2 passed。三个失败断言均期望检查器非零退出，实际为 `0`。

Developer 修复完成证据应至少包含：

1. artifact validator 使用 `lstat`/`realpath` 或等价方式拒绝 symlink、仓库外真实路径及非普通文件；
2. 产物 mtime 除不能早于本次 build 外，也必须有明确的未来时间容差，超过容差非零退出；
3. registry 在 JSON parse 前或通过支持 duplicate-key 检测的 parser 拒绝重复 workspace key；
4. 三个新增负向测试全绿，并保持既有 58 项 scaffold suite 全绿。

## 回归、非功能与交接

- `npm run test:scaffold`：退出 `0`，6 files / 58 tests 全过，证明本轮失败是新增覆盖所暴露，非既有 scaffold 回归。
- `npx eslint --ext ts,tsx --report-unused-disable-directives --max-warnings 0 tests/scaffold-tester-corners.test.ts`：首次发现仅为 import sort，运行 ESLint autofix 后重跑退出 `0`，0 warning。
- `git diff --check`：退出 `0`。
- 未运行 `npm test`、`npm run typecheck`、`npm run build:saas`、旧 renderer build 和 Electron compile：独立 corner-case 已明确 FAILED，按状态机应立即退 Developer，不以宽窄回归结果覆盖三个失败。
- 未覆盖风险：Windows symlink 权限/路径 separator，同名 stage gate 真并发 verifier，API 重复 listen/close，worker 多次安装 signal handler 的交叉清理，远端 Linux GitHub Actions runId。这些不改变本轮 `FAILED` 结论，修复后 Tester 应继续扩充。
- 交接：退回原 P00 Developer 处理三个失败；修复记录追加新 Round，由同一 P00 Reviewer 重新审核。本 Tester 不修改实现、不改 Reviewer 历史、不勾总体完成表。

## Round 2 · 2026-07-11 13:13 CST · `/root/p00_tester` / `5b0196b6`

- Tester Agent：`/root/p00_tester`（同一 P00 独立 Tester）
- Reviewer 重新批准：Round 5 `APPROVED_FOR_TEST`，commit `5b0196b6`
- 受测 SHA：`5b0196b67c220648afbc9155d1e01b45d35b5d62`
- 环境：macOS arm64（Darwin 25.5.0），Node `v24.15.0`，npm `11.12.1`，Vitest `v4.1.10`
- namespace/digest：P00 本地 scaffold 测试不涉及集群或镜像，故不适用
- 结论：`PASSED`

Round 1 的 3 项失败已全部关闭：仓库外 artifact symlink 被拒绝，未来 +24h mtime 被拒绝，重复 raw workspace key 被拒绝。Round 1 原 5 项与 Developer 新增 duplicate-key/非普通文件边界共同复测全绿；无新的 P0/P1 失败。

| 用例 ID | 复测角度 | 结果 | 证据 |
|---|---|---|---|
| P00-TST-R2-01 | Round 1 原 5 项 | PASSED | `tests/scaffold-tester-corners.test.ts` 5/5；symlink、future mtime、raw duplicate 三项由红转绿，nonce/Web 两项保持绿。 |
| P00-TST-R2-02 | duplicate-safe JSON parser | PASSED | escaped 等价 key、nested/array duplicate、普通字符串值不误报及 syntax 边界通过。 |
| P00-TST-R2-03 | artifact 普通文件与路径边界 | PASSED | artifact 目录伪装、symlink/realpath 越界、stale/future 时间窗口及 18 个 fresh output 验证通过。 |
| P00-TST-R2-04 | scaffold 完整回归 | PASSED | `npm run test:scaffold`：8 files / 68 tests 全过。 |
| P00-TST-R2-05 | 旧桌面回归 | PASSED | 全量 Vitest、renderer production build 和 Electron main/preload compile 全部退出 `0`。 |

### Round 2 命令证据

- `npx vitest run tests/scaffold-tester-corners.test.ts tests/scaffold-json-duplicate-keys.test.ts tests/scaffold-build-artifacts.test.ts`：退出 `0`，3 files / 17 tests 全过。
- `npm run scaffold:check`：退出 `0`，SCAF-1～SCAF-6 全部 `passed`。
- `npm run test:scaffold`：退出 `0`，8 files / 68 tests 全过。
- `npm run typecheck`：退出 `0`，9 个 workspace project references 通过。
- `npm run build:saas`：退出 `0`；clean invocation `a888b860-d3fb-4e51-9e20-bd67be90c155`，9 个 workspace 真实构建，Web Vite 转换 32 modules，验证 18 个 fresh regular artifacts。
- `npx eslint --ext ts,tsx --report-unused-disable-directives --max-warnings 0 tests/scaffold-tester-corners.test.ts tests/scaffold-json-duplicate-keys.test.ts tests/scaffold-build-artifacts.test.ts`：退出 `0`，0 warning。
- `npm test`：退出 `0`，183 files，1987 passed / 1 skipped / 0 failed。
- `npm run build`：退出 `0`；旧 renderer 与 Electron Vite bundle 完成。仅有已知 CJS API deprecation、`lottie-web` eval、dynamic/static import 及 bundle-size warning，无构建失败。
- `npm run compile:electron`：退出 `0`，main/preload TypeScript 编译完成。
- `git diff --exit-code` 与 `git status --short`：写入本报告前退出 `0`/无输出，未产生 lockfile、report 或受跟踪构建产物漂移。

### 残余风险与交接

- Windows 实机的 symlink 权限与 separator 边界未在本地 macOS 执行；负向 symlink 测试会在 Linux CI 执行，Windows 上因系统权限明确 skip。
- 同名 stage verifier 真并发、API 重复 listen/close 和 worker 多组 signal handler 交叉清理未继续扩展；当前 P00 验收和已暴露风险均有直接绿色证据，这些作为后续可增强项，不阻断本轮。
- 远端 GitHub Actions runId/Linux runner 完整证据仍由总体协调者在合并流程核对；本地 Tester 不伪造远端证据。
- Tester 结论：P00 在受测 SHA `5b0196b6` 上 `PASSED`。现交总体协调者核对 Planner/Developer/Reviewer/Tester 证据与依赖后决定是否勾选总表；本 Tester 未修改实现、未修改 Reviewer 历史、未勾总体表。
