# 审核意见

## Round 1 · 2026-07-11 12:02 CST · `/root/p00_reviewer` / `b9453520`

- Reviewer Agent：`/root/p00_reviewer`
- 审核基线：`4f5d9695`
- 审核提交：`80d62271`、`2ec15ce1`、`0d732b57`、`fc499ce4`、`b9453520`
- 结论：`CHANGES_REQUESTED`
- 阻断摘要：当前 9 个 npm workspace、lockfile、TS references 和四个 app 壳本身能够编译/启动，heartbeat 测试修订也与既有生产行为一致；但权威文档 `19` 的 PR-0 必须产物和 §3.6 命令大面积缺失，现有检查器/CI 却把缺失状态报告为 SCAF-1～6 全通过，并可被 no-op、反向依赖和额外 workspace 绕过。按角色协议，任一 P1、AC 缺失或 Red 证据缺失均不得进入 Tester。

| 严重级 | 用例/需求 | 文件:行 | 实际/期望 | 复现 | 要求证据 |
|---|---|---|---|---|---|
| P1 | PR0-AC / SCAF-1、SCAF-2、SCAF-4、SCAF-5 | `package.json:77-94`；`.github/workflows/saas-scaffold.yml:38-49`；`改造计划/19-开工前补件与工程脚手架冻结.md:215-256` | 实际只建立 9 个 workspace 和 5 个新增根命令；`prisma/`、`docker/`、`charts/lobsterai/`、`docs/supply-chain/`、`docs/poc/`、供应链 schema/manifest、PoC harness 均不存在，`test:e2e`、`contracts:check`、`prisma:validate`、`supply-chain:check`、`docker:build:check`、`helm:lint`、`poc:v1:check` 均未定义，CI 也未调用。期望按权威 `19` §4.2/§4.4 交付 PR-0 全部目录、命令、CI 阻断入口和生成规则；若 Planner 确实要将这些拆到 P01～P04，必须先按角色协议把与 `19` 的冲突写入风险决策并更新权威 AC，Reviewer 不能自行接受缩小范围。 | `for c in test:e2e contracts:check prisma:validate supply-chain:check docker:build:check helm:lint poc:v1:check; do npm run "$c"; done` 均退出 1（`Missing script`）；`test -e prisma -a -e docker -a -e charts/lobsterai -a -e docs/supply-chain -a -e docs/poc` 失败。 | 权威计划冲突已正式消解；当前 PR-0 AC 对应目录/fixture/README 存在；每个命令明确输出 passed 或 blocked/not-applicable，不能是 missing/no-op；CI 中同名入口实际执行并按阶段语义阻断；codegen/clean diff 有可复现证据。 |
| P1 | SCAF-1～5 结果真实性 | `scripts/check-saas-scaffold.mjs:8-26, 196-230` | 检查器把预期对象硬编码为 9 个 workspace 和 5 个命令，最后无条件以这些较窄条件打印 SCAF-1～6 passed；它完全没有检查上条缺失的 PR-0 目录、§3.6 命令、codegen 头/生成一致性、供应链与 PoC 入口。因此当前 `npm run scaffold:check` 的六门通过结论与权威门定义不一致，属于伪绿。 | 在当前 HEAD 执行 `npm run scaffold:check` 得到六门 passed；紧接着执行上条缺失目录/命令检查全部失败。 | `collectScaffoldErrors` 与 SCAF-1～6 逐项建立权威 AC 映射；为每个门提供至少一个会因删除/篡改目标产物而失败的测试；输出不得把 TS reference graph 冒充 SCAF-5 codegen clean gate。 |
| P1 | SCAF-2 / SCAF-3 / 依赖方向 corner cases | `scripts/check-saas-scaffold.mjs:101-107, 134-193` | no-op 识别只匹配少数 shell 文本；声明依赖只检查“workspace dependency 有 reference”，不禁止 shared 包声明 React/Nest/Electron/server/app 依赖，也不禁止 shared project reference 指向 app；额外 `apps/*` workspace 不在固定清单中，能绕过 build/typecheck/lint 要求。隔离副本实测：把 contracts 的 build/typecheck 改成 `node -e "process.exit(0)"`、增加 React 依赖和指向 worker 的 TS reference仍通过；再增加无 build/typecheck 的 `apps/gateway` 仍通过。期望门禁拒绝这些 corner cases，且 `--if-present` 不能让 workspace 静默逃逸。 | 在 `/tmp` 的 `git archive HEAD` 副本作上述 mutation 后运行 `node scripts/check-saas-scaffold.mjs`，两轮均输出六门 passed（记录标记：`MUTATED_FORBIDDEN_DEP_NOOP_REVERSE_REF_ACCEPTED`、`ROGUE_WORKSPACE_WITHOUT_GATES_ACCEPTED`）。 | 为 no-op 包装、shared manifest 反向依赖、shared TS reference 反向依赖、额外 workspace 缺门和 lockfile/workspace 漂移新增失败测试；修复后相同 mutation 均非零退出，同时正常树通过。 |
| P1 | 可运行 Web 壳 / CI 真实构建 | `package.json:86`；`apps/web/package.json:7-13`；`.github/workflows/saas-scaffold.yml:43` | `build:saas` 调用各 workspace 的 `build`，而 Web 的 `build` 只有 `tsc -b`；SaaS CI 只运行 `build:saas`，不会执行 `vite build`。删除 `apps/web/dist` 后运行该门仍退出 0，却没有 `apps/web/dist/index.html`，因此损坏的 `index.html`、Vite 配置或浏览器 bundle 可让 CI 伪绿。Developer 手工运行的 `build:web` 不是自动门。期望 SaaS build/CI 真实产出并校验 Vite bundle。 | `rm -rf apps/web/dist && npm run build:saas; test -f apps/web/dist/index.html`：前者退出 0，后者失败并输出 `WEB_VITE_OUTPUT_MISSING_AFTER_BUILD_SAAS`；独立 `npm run build:web --workspace @lobsterai/web` 可成功构建 32 modules，说明壳可构建、问题在门禁未覆盖。 | `build:saas` 或 CI 必须实际运行 Web Vite production build，并断言 `apps/web/dist/index.html`/静态资源存在；加入破坏 `index.html` 或入口时失败的自动测试/CI 证据。 |
| P1 | TDD / app 壳与 checker 行为覆盖 | `tests/scaffold.test.ts:23-68`；`开发记录.md:13-17, 47` | 提交顺序上 `80d62271` 确实早于实现，Red 的 11 failed/1 passed 与当时缺失 workspace 相符；但测试仅检查文件/manifest/少量脚本文本和旧命令，没有测试 checker 的依赖/循环/no-op/额外 workspace corner cases，也没有导入或启动 API、runtime、worker、Web。app probe 只记录为一次手工命令且未入库，关键行为没有 Red→Green 自动证据。角色协议明确“缺失 Red 证据必须退回”。 | `rg -n "createApiShell|createRuntimeOrchestratorShell|createWorkerShell|healthz|readyz|build:web" tests apps/**/{*.test.ts,*.test.tsx}` 无对应测试；`npm run test:scaffold` 只有 1 file / 12 tests。 | 先为本轮已暴露的门禁缺陷建立可复现失败测试，再修复至绿；把 app health/readiness/404、非法端口、worker start/stop/SIGTERM、Web production build/入口至少纳入自动化；开发记录追加真实 Red/Green 命令、退出码和 commit。 |
| P2 | 任务源码边界 / heartbeat 基线修订 | `src/main/libs/openclawConfigSync.runtime.test.ts:241-276`；`开发任务.md:7` | `fc499ce4` 只给两条既有断言补 `skipWhenBusy: true`，与 `0766168e` 已存在的 `openclawConfigSync.ts` 生产配置一致，独立 targeted test 41/41 通过，未发现掩盖回归；但该文件不在 P00 列出的源码边界。期望将此作为明确的基线债务/独立修复授权，而不是默认为 scaffold 内容。 | `git show 0766168e -- src/main/libs/openclawConfigSync.ts` 可见两个 cadence 都已写入 `skipWhenBusy: true`；`npm test -- openclawConfigSync.runtime` 退出 0，41 passed。 | Planner/协调者明确该独立测试修复的归属或更新 P00 边界；保持生产实现不变，并在最终 diff/PR 描述中与 scaffold 分开说明。 |

### 已验证通过但不足以批准

- `npm ci --dry-run --ignore-scripts`：退出 0；根 lockfile v3 含 9 个 workspace link，`.gitignore` 只追踪根 lockfile，未发现嵌套运行时 lockfile误纳入。
- `npm run typecheck`：退出 0；当前 9 个 TypeScript project references 可构建且无环。
- changed-file ESLint：退出 0、0 warning。
- `npm run test:scaffold`：1 file、12 tests 全过；证明当前窄测试集是绿，不证明权威 PR-0 AC 完成。
- 独立 app probe：API `/healthz`、`/readyz`、404；runtime `/healthz`、404；非法端口；worker 重复 start/stop 均通过。未发现越权会话/租户/计费/MCP/Artifact/K8s 调度业务，也未发现 Web 硬编码可见字符串或新增生产公网调用。
- `npm run build:web --workspace @lobsterai/web`：退出 0，Vite 32 modules；Web 壳可手工构建。
- `npm test -- openclawConfigSync.runtime`：41/41 通过；heartbeat 断言修订与既有实现一致。
- `git diff --check 4f5d9695..b9453520`、`git diff --exit-code`：通过；受跟踪工作树干净。远端 GitHub Actions runId 未提供，Linux runner 完整复现尚无证据。

### Developer 下一轮回复格式

请保留本轮历史，在 `开发记录.md` 追加 Round 2，对每条意见逐项回复 `fixed / rejected with evidence / blocked`。任何对 PR-0 权威范围的缩减必须先由 Planner 更新计划/风险决策；Reviewer 不接受仅把缺失项口头标为“后续 P01～P04”作为本轮 AC 完成证据。

### 交接 Tester

未交接。当前为 `CHANGES_REQUESTED`；P1 与 AC/Red 缺口全部关闭并重新审核为 `APPROVED_FOR_TEST` 后，Tester 才可开始。

## Round 2 · 2026-07-11 12:28 CST · `/root/p00_reviewer` / `0bc9923d`

- Reviewer Agent：`/root/p00_reviewer`（同一 P00 Reviewer）
- 复审提交：`92aedb43`、`34578d95`、`4c796501`、`5125d3aa`、`0bc9923d`
- 结论：`CHANGES_REQUESTED`
- Round 1 关闭情况：缺目录/命令、原始 no-op+React+reverse-reference mutation、lock drift、Web Vite bundle、app 自动测试与 heartbeat 归属均已有有效修复或明确决策；但 SCAF-2/3 和 deferred gate verifier 仍各存在一个可复现伪绿路径，不能进入 Tester。

| 严重级 | 用例/需求 | 文件:行 | 实际/期望 | 复现 | 要求证据 |
|---|---|---|---|---|---|
| P1 | SCAF-1/2/3 · 冻结物理 deployable 与非 no-op 命令 | `scripts/check-saas-scaffold.mjs:7-17, 106-127, 173-198, 220-238, 300-363` | checker 虽定义 9 个预期 workspace，但只用该表检查必需项；对动态发现的额外 workspace 只要求存在 build/typecheck/reference/lock metadata，不拒绝未授权 deployable。`isNoOpCommand` 又把任何含 `readFile` 的 inline Node 命令视为非 no-op。因此新增计划明确禁止的 `apps/gateway`，并让 build/typecheck 只读取 `package.json`，补齐空 tsconfig/solution reference/lock entry 后，SCAF-1～6 全部 passed。期望 P00 冻结的四个 apps 是 allowlist；新增物理服务必须先有 RFC/计划变更，且 build/typecheck 必须真实产生/校验编译结果。 | 隔离 `git archive HEAD` 副本新增 `apps/gateway`；scripts 均设为 `node -e "require('node:fs').readFileSync('package.json')"`，补 solution reference 与 lock entry；执行 `node scripts/check-saas-scaffold.mjs` 退出 `0`，标记 `ROGUE_FAKE_GATES_EXIT=0`。 | 新增 mutation test：完整伪装 package/README/src/tsconfig/lock/reference 的 `apps/gateway` 必须得到带 SCAF tag 的非零结果；P00 checker 比较 discovered apps 与冻结 allowlist（未来 RFC 同提交更新 allowlist），或采用等价不可绕过的 deployable registry；workspace build/typecheck 不再依赖“出现 readFile 等关键词”判断真实性，应校验允许的真实构建命令/产物。 |
| P1 | SCAF-2 · deferred exit 78 / verifier 防伪语义 | `scripts/run-saas-stage-gate.mjs:17-52`；`scripts/expect-saas-stage-gate.mjs:23-48`；`tests/scaffold.test.ts:83-97`；`.github/workflows/saas-scaffold.yml:58-66` | 正常 runner 会检查非空 fixture、写 report 并 exit 78；但 verifier 只信任子进程 stdout 的 `gate/stage/status` 和 exit code，不比较 `activationTask/reason/fixturesChecked`，也不验证 report 文件。46 项测试没有 runner/verifier 的正常、缺 fixture、空 fixture、未知 gate 或伪 runner 负向测试。隔离副本把 runner 改成三行“打印 `{gate,stage:'P00',status:'NOT_APPLICABLE'}` + exit 78”，既不读 manifest、不检查 fixture、也不写 report；`scaffold:check` 与 `expect-saas-stage-gate.mjs contracts:check` 仍都退出 0，故 CI 会把真正的空壳 gate 转为绿灯。期望 verifier/测试证明 exit 78 来自实际 fixture 检查后的声明状态，而非任意进程伪造的三字段。 | 在隔离副本覆盖 `run-saas-stage-gate.mjs` 为上述三行；`node scripts/check-saas-scaffold.mjs` → `FAKE_RUNNER_SCAFFOLD_EXIT=0`；`node scripts/expect-saas-stage-gate.mjs contracts:check` → `verified NOT_APPLICABLE (exit 78)`、`FAKE_RUNNER_VERIFIER_EXIT=0`。 | 新增并接入 `test:scaffold` 的 stage-gate 测试：7 个正常 gate 均直接 exit 78 且 report 全字段/落盘一致；删除和置空每类声明 fixture 时 runner/verifier 均非零；未知 gate 非零；测试必须在 runner 被上述空壳替换时失败。verifier 至少比对 activationTask、reason、fixturesChecked 与 manifest，并验证本次新写 report；checker/测试不得用同一份可任意修改的 stdout 声明作为唯一事实。 |

### Round 2 已独立验证通过

- `npm run scaffold:check`：退出 0；正常树按新映射输出 SCAF-1～6 passed。
- `npm run test:scaffold`：4 files、46 tests 全过；覆盖 API/runtime health/404/非法端口、worker 幂等+SIGTERM、Web production bundle、Round 1 原始 checker mutations。
- `npm run typecheck`：退出 0。
- `npm run build:saas`：退出 0；9 个 workspace 均运行，Web 执行 `tsc -b && vite build`，`apps/web/dist/index.html` 和 JS asset 存在。
- 7 个 deferred 根命令逐个直接执行均输出完整 `NOT_APPLICABLE` JSON 并 exit 78；逐个 verifier 均 exit 0。正常路径没有把 gate 本身报告为 passed。
- Round 1 原始复合 mutation（contracts 的 `process.exit(0)`/`node -p true`、React manifest dependency、指向 worker 的 shared TS reference）现退出 1，并分别产生 SCAF-2/SCAF-4 错误；缺 fixture、缺命令与 lock drift 已由 targeted mutation tests覆盖。
- `DEC-09` 已明确 `fc499ce4` 是 P00 前 heartbeat 基线债务；生产实现未在 Round 2 修改。
- 新增目录均为 P00 placeholder/职责边界；未发现真实会话、租户、计费、MCP、Artifact 或 K8s 调度业务，也未发现新增生产公网依赖或硬编码 Web 可见文案。

### Developer Round 3 要求

保留历史，在 `开发记录.md` 追加 Round 3，逐项回复上述两个 P1。优先以失败 mutation 测试固定问题，再做最小 checker/verifier 修复；不得把 verifier 对 exit 78 的包装成功当作后续 PR-1～PR-4 功能门完成。

### Round 2 交接 Tester

未交接。当前仍为 `CHANGES_REQUESTED`；两个 P1 关闭并由同一 Reviewer 复审后才可改为 `APPROVED_FOR_TEST`。

## Round 3 · 2026-07-11 12:41 CST · `/root/p00_reviewer` / `7c85ebd4`

- Reviewer Agent：`/root/p00_reviewer`（同一 P00 Reviewer）
- 复审提交：`8b4484dd`、`39930dce`、`7c85ebd4`
- 结论：`CHANGES_REQUESTED`
- Round 2 关闭情况：未登记但完整伪装的 `apps/gateway` 现被 registry allowlist 拒绝；三行 fake runner 因 SHA 不一致被 checker/verifier 拒绝；51 项测试已覆盖 7 gate 正常 exit 78、完整 fresh report、stale report 被替换、missing/empty fixture、unknown gate 和 stdout-only runner。仍有一个新增 artifact registry 的 P1 伪绿路径。

| 严重级 | 用例/需求 | 文件:行 | 实际/期望 | 复现 | 要求证据 |
|---|---|---|---|---|---|
| P1 | SCAF-2 · workspace registry / build artifact 真实性 | `scripts/saas-workspace-registry.json:1-70`；`scripts/check-saas-scaffold.mjs:221-243, 274-290`；`scripts/check-saas-build-artifacts.mjs:11-29` | registry 的 `schemaVersion`、`stage`、`kind`、`planReference` 目标和 artifact 路径语义未被校验；checker 只要求 `planReference` 非空、artifact 数组非空，artifact validator 又只检查 registry 指向的路径是否已经存在。隔离副本把 9 个 workspace 的 artifacts 全部改为预先存在的 `package.json`，不生成任何 `dist`，`scaffold:check` 与 artifact validator 仍同时退出 0，并输出 “verified artifacts for 9 workspaces”。因此 registry 可在一个配置改动中把新加的产物门完全降级为源码存在性检查，51 项测试也没有该 mutation。期望 artifact 声明只能指向真实构建输出，并由测试证明缺少所有构建输出时 validator 必须失败。 | `git archive HEAD` 隔离副本中将每个 registry entry 的 `artifacts` 改为 `["package.json"]`，副本不含任何 ignored `dist`；`node scripts/check-saas-scaffold.mjs` → `REGISTRY_SOURCE_ARTIFACT_SCAFFOLD_EXIT=0`；`node scripts/check-saas-build-artifacts.mjs` → `[SaaS Build] verified artifacts for 9 workspaces`、`REGISTRY_SOURCE_ARTIFACT_VALIDATOR_EXIT=0`。 | 先新增失败 mutation test，再约束 registry schema：stage/schema/kind 合法，planReference 指向实际权威文档/允许锚点，artifact 必须是 workspace 内受允许的构建输出（至少位于 `dist/` 或 Web 的明确 bundle 路径，禁止 `package.json`、`src/`、README、tsconfig 等预存输入）；删除所有登记输出时 validator 非零。测试必须在“9 项 artifacts 全改 package.json”时失败，并接入 `test:scaffold`/CI。 |

### Round 3 已独立验证通过

- `npm run scaffold:check`：正常树退出 0，SCAF-1～6 passed。
- `npm run test:scaffold`：5 files、51 tests 全过；Round 2 两个原始 P1 mutation 已转为有效负向覆盖。
- 完整伪装但未登记的 `apps/gateway` 现在同时产生 SCAF-1 “unapproved physical deployable”和 SCAF-2 registry command/artifact 错误。
- 三行 stdout-only fake runner 现在使 checker/verifier 非零；正常 runner 写 UUID、ISO 时间、runner SHA、全 manifest 字段并原子替换 report。
- 每个 gate 的 missing/empty fixture、unknown gate 均使 runner/verifier 非零；正常 7 gate 仍直接 exit 78，不把后续 PR-1～PR-4 功能标为完成。
- `npm run typecheck`：退出 0。
- 从 fresh 状态同时删除 9 个 workspace 的 `dist`/`dist-types` 和 `.tsbuildinfo` 后，`npm run build:saas` 退出 0；Web 构建 32 modules，artifact validator 验证当前 registry 的 9 个真实输出。
- 未发现新增业务逻辑、生产公网依赖、硬编码 Web 可见字符串或旧 Electron 行为改动。

### Developer Round 4 要求

保留历史，在 `开发记录.md` 追加 Round 4。只需围绕上述 artifact registry mutation 先 Red 后 Green，并提供正常 fresh build 与删除输出后的 validator 负向证据；不得扩大为新的构建系统重构。

### Round 3 交接 Tester

未交接。当前仍为 `CHANGES_REQUESTED`；artifact registry P1 关闭并由同一 Reviewer 复审后才可进入 Tester。

## Round 4 · 2026-07-11 12:51 CST · `/root/p00_reviewer` / `70e7af53`

- Reviewer Agent：`/root/p00_reviewer`（同一 P00 Reviewer）
- 复审提交：`f49ef9d1`、`a99b5440`、`70e7af53`
- 结论：`APPROVED_FOR_TEST`
- 总结：Round 3 唯一剩余的 artifact registry P1 已关闭。固定 policy 独立约束 workspace keys、schema/stage/kind、权威计划引用、build/typecheck 与精确输出；clean preflight 同时清除输出和增量状态；validator 将本次 invocation、registry SHA、时间和 18 个输出的路径/大小/mtime/SHA-256 绑定。未发现剩余 P0/P1、P00 AC 缺口或越权业务实现。

### Round 4 逐项复审

| 复审项 | 结论 | 证据 |
|---|---|---|
| registry 全改 `package.json`、无任何 `dist` | 通过：checker 与 validator 均必须非零 | `tests/scaffold-build-artifacts.test.ts` 在隔离副本把 9 项 artifacts 全降级并移除输出，断言 SCAF-2 与 validator failure；targeted 7/7、总计 58/58 通过。 |
| 非法 schema/stage/kind/planReference | 通过：逐类拒绝 | `saas-workspace-policy.mjs` 固定 schemaVersion 1、P00、apps=deployable、libs=library、两个权威 `19` 锚点；四类 mutation 全部非零。 |
| missing/stale artifacts | 通过：均拒绝 | validator 要求 current `build-start` 合同和 registry SHA，固定输出缺失时报错，mtime 早于本次 build时报 stale；负向测试通过。 |
| `.tsbuildinfo` / 残留输出假绿 | 通过：clean 后真实重建 | `clean-saas-build.mjs` 在 build 前删除每个冻结 workspace 的 `dist`、`dist-types`、`tsconfig.tsbuildinfo` 并删除旧 result；本轮 `build:saas` 从 clean 状态重新产生 18 个 fresh artifacts。 |
| clean 删除范围安全 | 通过 | 删除循环只遍历 `expectedWorkspaceArtifacts` 的固定 9 个 key，且只拼接三个固定生成路径；不接受 registry 注入的 workspace/path，不删除源码、仓库外路径、用户目录或其他 workspace。隔离测试逐 workspace 验证三个生成目标被清除。 |
| 18 个产物策略 | 通过 | 8 个 Node/TS workspace 各要求 `dist/index.js` + `dist/index.d.ts`（16），Web 要求 `dist/index.html` + 至少一个 `dist/assets/*.js`（2）；本轮真实输出 `[SaaS Build] verified 18 fresh artifacts for 9 workspaces`。 |
| Round 2 gate/report 防伪回归 | 通过 | 58 项集合继续包含完整 gateway、三行 fake runner、fresh/stale/missing report、missing/empty fixture、unknown gate 负向矩阵。 |

### Reviewer 独立命令

- `npm run scaffold:check`：退出 0，SCAF-1～6 passed。
- `npm run test:scaffold`：6 files、58 tests 全过。
- `npm run typecheck`：退出 0。
- `npm run build:saas`：退出 0；clean preflight 后 9 workspace 真实构建，Web Vite 32 modules，验证 18 个 fresh artifacts。
- `git diff --exit-code`、`git diff --check c78696a3..70e7af53`：退出 0；无受跟踪生成物、report 或无关工作树改动。

### 非阻断说明

- 7 个 contracts/Prisma/supply-chain/Docker/Helm/PoC/E2E gate 在 P00 仍诚实处于 `NOT_APPLICABLE`/exit 78；本批准只证明 P00 入口、fixture、报告和防伪门成立，不代表 P01～P04 功能完成。
- 远端 GitHub Actions runId 仍待合并流程/总体协调者核对；不以本地批准替代远端 CI 或后续独立 Tester。

### Round 4 交接 Tester

`APPROVED_FOR_TEST`。现交接全新独立 Tester；Tester 应在不复用 Developer/Reviewer 上下文的前提下，补充其独立 corner-case、回归与安全测试。只有 Tester 写出 `PASSED` 且总体协调者核对所有证据后，P00 才可在总体完成表勾选完成。
