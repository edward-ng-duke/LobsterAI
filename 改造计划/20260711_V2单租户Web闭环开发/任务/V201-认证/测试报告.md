# 测试报告

> Planner 仅预置独立 Tester 的测试协议；当前没有测试结论。只有 Reviewer 在当前修复 SHA 明确 APPROVED_FOR_TEST 后，独立 Tester 才能开始并追加 Round。

## Tester 启动门

- Tester Agent 不得复用Planner、Developer、Reviewer。
- Reviewer APPROVED_FOR_TEST commit与被测SHA一致；任何Developer后续实现变更都使批准失效，必须先重审。
- V107 go、P01/P02 accepted、Day-0和真实内网IdP可用；缺外部输入写BLOCKED/2，不写PASSED。
- Tester保留Reviewer/Developer原测试，并从新角度新增交付测试；不能只重跑已有命令。

## Round N · YYYY-MM-DD HH:mm · Tester/commit

- Tester Agent：
- Reviewer批准commit：
- 被测实现SHA：
- Tester新增测试commit：
- 环境/namespace/image digest：
- IdP metadata hash / keyset kids（无秘密）：
- 结论：FAILED | PASSED | BLOCKED

## 独立必测矩阵

| 用例ID | 独立角度 | 预期硬断言 | 测试文件/脚本 | 结果/证据 |
|---|---|---|---|---|
| V201-TST-01 | 多tab/刷新/后退 | state、nonce、verifier不串tab；callback单用；刷新/后退不重放code |  |  |
| V201-TST-02 | redirect/parser fuzz | encoded traversal、Unicode host、userinfo、fragment、scheme-relative、端口/尾斜杠差异全拒绝；合法精确URI通过 |  |  |
| V201-TST-03 | mix-up/claim property | 随机组合iss/aud/azp/sub/tenant/iat/nbf/exp只接受唯一合法集合 |  |  |
| V201-TST-04 | JWKS轮换/故障 | 旧→双→新key、重复kid、同kid换材料、未知kid single-flight、IdP/JWKS超时均按策略fail closed |  |  |
| V201-TST-05 | code并发/时间边界 | 过期前后1秒、skew边界、并发消费只一胜者 |  |  |
| V201-TST-06 | refresh多副本竞态 | 至少20并发、响应乱序、grace内同successor、超窗reuse整family撤销 |  |  |
| V201-TST-07 | logout竞态 | logout/refresh/ticket签发消费交错；本地撤销不依赖IdP；旧access/refresh/ticket均失败 |  |  |
| V201-TST-08 | back-channel条件分支 | 支持时验证logout_token及jti重放；不支持时能力检测为明确N/A且本地logout完整 |  |  |
| V201-TST-09 | CSRF/CORS/Cookie浏览器 | 跨站form/fetch、恶意Origin、缺header、同名Cookie、Secure/HttpOnly/SameSite/Path/Domain均断言 |  |  |
| V201-TST-10 | header/cookie尺寸 | 阈值-1/阈值/阈值+1、重复Authorization、代理合并场景稳定400/413/431且不回显secret |  |  |
| V201-TST-11 | API/WS身份一致 | 同一token REST/WS context逐字段相等；ticket复用/过期/撤销/越scope/客户端伪tid失败 |  |  |
| V201-TST-12 | 身份/DB/RLS | disabled、错tenant、缺membership、email碰撞、iss+sub唯一、raw app过滤关闭后RLS仍隔离 |  |  |
| V201-TST-13 | secret/PII canary | Git、镜像、PG明文、Redis长期值、URL、日志、trace、错误信封均无secret/token/code/cookie |  |  |
| V201-TST-14 | 内网与断公网 | 批准IdP/DNS/NTP可达；公网、metadata、未批准RFC1918和动态jku/x5u不可达 |  |  |
| V201-TST-15 | 真实IdP | active登录/刷新/logout、disabled/错tenant拒绝、真实key rotation或SEC批准演练均通过 |  |  |
| V201-TST-16 | 故障恢复/回滚 | IdP/PG/Redis短故障不伪绿；关闭flag、本地撤销、key回退与forward migration恢复可复现 |  |  |

Tester 还须在Chromium必跑，并在WebKit/Firefox至少抽样callback、Cookie/SameSite、silent refresh、logout和header限制；浏览器差异不能用只测HTTP client替代。

## 命令与三态证据

| 命令 | 预期 | 实际退出码/状态 | runId/报告 |
|---|---|---|---|
| npm run test:auth:unit | PASS/0 |  |  |
| npm run test:auth:integration | PASS/0 |  |  |
| npm run test:auth:fixture-idp | PASS/0 |  |  |
| npm run test:auth:real-idp | PASS/0 |  |  |
| npm run auth:validate | PASS/0 |  |  |
| npm run test:db:integration | PASS/0 |  |  |
| npm run test:e2e -- --project=auth | PASS/0 |  |  |
| npm run typecheck + changed-file ESLint | PASS/0 |  |  |
| npm test + build:saas + build + compile:electron | PASS/0 |  |  |

## 失败、阻断与资源清理

| 严重级 | 用例 | 期望/实际 | 复现 | 退回证据要求 |
|---|---|---|---|---|

- FAILED：产品/安全/回归断言失败，退Developer；修复后必须重新Reviewer，不允许Tester直接复测跳过审核。
- BLOCKED：缺真实IdP/账号/rotation窗口/集群网络等外部输入；说明owner和解除条件，任务保持TODO。
- 清理：撤销全部测试family/ticket/code，删除fixture数据库/Redis key/container/network，真实IdP测试会话登出；残留资源必须列出且不能PASSED。

## 最终交接

- 16项独立用例结果：
- 新发现并固化的corner：
- fixture与真实IdP证据位置：
- secret/network/cleanup结论：
- 回滚演练结论：
- 最终稳定SHA：
- Tester结论：
- 若PASSED，交总体协调者核对；Tester不修改总体表。
