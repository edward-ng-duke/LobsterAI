# V201 · OIDC/PKCE 单租户认证

- 状态：TODO
- 依赖：V107=go、P01/P02、批准内网 OIDC。
- 权威输入：根 AGENTS、19、17 对应 V1/V2、16、附录 C；冲突以根 AGENTS 的生产断公网和附录 C 契约决策为准。

## 源码边界

apps/api auth、libs/server/auth、apps/web login；不接公网 IdP。

## TDD：先写的失败测试

先写 PKCE/state/nonce、callback、JWT、refresh rotation/family replay、gateway middleware 失败测试。 Red 必须因目标行为缺失而失败；环境缺失另记 BLOCKED，不算 Red。

## Corner cases

code 重放、redirect 绕过、并发 refresh、时钟偏差、禁用用户、issuer/key rotation、IdP 不可用。

## 实施与验收

Developer 按最小垂直切片逐个 Green，避免无关重构。验收命令：auth unit/integration；Playwright login；changed-file eslint；typecheck。

## 回滚

feature flag 关闭 Web auth/Ingress；撤销测试 token family，保留桌面认证。

## 完成证据

内网 issuer 配置、登录 trace、攻击用例、日志无 token。 证据须绑定 SHA/runId/环境；所有触碰 TS 文件 changed-file ESLint 0 warning，相关 Vitest 全绿。

## Developer → Reviewer → Tester 循环

Developer 记录每轮 Red/Green 与 diff → 独立 Reviewer 对照本文件和源码审核；若 CHANGES_REQUESTED，退同一 Developer 修复并进入下一轮 → Reviewer APPROVED_FOR_TEST 后独立 Tester 新增 corner-case/回归测试；Tester FAILED 必须退 Developer 且修复后重新 Reviewer → Tester PASSED 后由协调者核验证据并勾总体表。任何角色不得自行勾 Todo。

