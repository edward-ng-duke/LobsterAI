# 开发记录

> 初始为空白；Developer 按 Round 追加 Red→Green→Refactor、commit（获授权后）、命令/退出码/证据与 Reviewer 交接。

## 元数据
- 任务 ID：P01-PR1契约
- Developer Agent：待协调者创建独立 Agent（Planner 不兼任）
- 基线 SHA：`cde584c0`

## Planner handoff · 2026-07-11 · `/root/p01_planner`

- 计划版本：`P01-PLAN-v2`；范围与 18 个 AC 已冻结在 `开发任务.md`。
- Planner 仅更新计划与本 handoff 元数据，未写实现/测试代码、未运行 Developer Red/Green、未作 Reviewer/Tester 结论、未修改总体表。
- Developer 开始前须以实际 HEAD 更新本文件的开发基线 SHA，并按 AC01～18 追加 Round 1 Red→Green→Refactor 证据；不得覆盖本 handoff。

## Round 1
### Red

- 基线：`3c4be174`（P01 Planner v2 后）；Red commit：`54fff820 test(contracts): capture P01 red baseline`。
- `npx vitest run tests/contracts/p01-red.test.ts`：exit 1，5/5 失败；分别证明 P00 gate 仍为 exit 78、registry/spec/generated 缺失、Cowork 10（含 goal）缺失、旧路径 checker 缺失、`test:contract` 缺失。
- 失败原因均是计划要求的行为尚未存在；没有用语法错误、缺依赖或既有全仓失败充当 Red。

### Green / Refactor

- `fa15faf1`：唯一 Zod/schema 与 route/channel/error/inventory/bridge metadata 源；附录 A GA/部分 GA 账本。
- `328be1df`：确定性 OpenAPI/AsyncAPI、consumer、bridge、manifest 生成。
- `1a198266`：P01 gate/CI/baseline 激活。
- `be74f0cf`：§3-B DTO、10×valid/invalid fixture、完整 ElectronBridge + preload static `satisfies`。
- `cd993575`：Appendix denominator、mutation 与 breaking gates。
- `862673f7`：P01 changed-file lint 收敛。
- `a0c21a8d`：route metadata 反链 legacy inventory；unsupported node 注入 501 错误。
- `f4088c59`：实际使用 zod-to-openapi 3.1 与 openapi-typescript 生成消费者类型。
- `35e98172` / `191170a9`：活跃阶段测试副本与并行时限稳定性。
- 未实现 controller/service/provider/计费/ASR 上游，未增加网络调用或假 handler。

### 自测与证据

- 证据：`docs/contracts/20260711_P01契约与代码生成证据/README.md`。
- 最终环境：Node `v24.15.0`，npm `11.12.1`；UTC `2026-07-11T06:11:20Z`。
- counts：schema 103；route 161；channel 32；Appendix expected/inventory 184/184；bridge 321；Cowork 10。
- hashes：source `23cd3a93acf62f509d5dc397eedcfde45743c61366e30018da35c93b98d2bb9a`；output `938632af5ec833ef6d7240018076196ccd73004f6d3548f045b06b7a13c06969`；inventory `6625d36440f3787aecaea3ecebbf221b768a912d2bcbf052aa7316dabfde6413`。
- 总门：`npm ci`、generate/check/test:contract/typecheck、P01 lint、`npm test`、build:saas/build/compile:electron、clean diff 均 exit 0；全量 Vitest 188 files / 2016 passed / 1 skipped。
- 本地 gate invocationId：`5150c6f0-b7cf-4892-935f-ea0e19eb4b5a`。本地无远端 CI runId，明确待 Reviewer/CI 补证，不伪绿。
- 已记录既有 Vite chunk / third-party eval 警告与 install scripts allowlist 提示；它们不改变本次契约行为，Reviewer 可独立评估。

### 交接 Reviewer

- Developer 状态：`REVIEW_PENDING`；18 AC 本地证据齐全，但 Developer 不作审核通过结论、不修改 `审核意见.md`/`测试报告.md`、不勾总体表。
- Reviewer 请独立运行 AC01～18，并至少执行删 goal、手改生成物、冒号路径、附录 A 漏行、breaking field 五类 mutation；任一集合缺口或远端门失败均退回本 Developer。

## Round 2

### Red

- Reviewer Round 1 结论为 `CHANGES_REQUESTED`；五项 P1 均先固化到 `tests/contracts/reviewer-round1-red.test.ts`。
- Red commit：`abf478e5 test(contracts): capture reviewer round one gaps`。
- `npx vitest run tests/contracts/reviewer-round1-red.test.ts` 初始 exit 1，9/9 失败；覆盖通用/虚构路由与 security/status、inventory↔bridge target、命名类型被 `unknown` 擦除、Cowork null/string 真实 payload、三类递归 breaking mutation、删除后 rename 窗口和额外 stale output。

### Green / Refactor

- `98ebee70` / `cd407d66`：删除三条虚构 REST，修正 184 行 canonical target，并生成逐 operation security、success status 与 error policy。
- `8f0afcdf`：321 unique bridge leaves 与 inventory/route/channel target 双向绑定；未映射桌面叶子降级；合并重复 `auth` 声明。
- `46690e6e` / `25c708a7`：恢复可解析 OpenAPI，修正 Cowork `complete` nullable 与 `error` string，并把 IPC/AsyncAPI/bridge 三元 metadata 写入消息扩展。
- `e8b0c296`：breaking diff 递归解析 `$ref`，覆盖 nested/object/array/union、request/response/status/security 与 AsyncAPI payload。
- `05e4bac2`：全部输出先同目录 staging+fsync，故障注入后旧产物不变，直接 atomic rename，check/generate 均处理受管目录 stale 文件。
- `3719da83`：用声明 bundling 内联精确 Electron 类型；不再生成 `type X = unknown`；32 个 stream consumer 的 `data` 从 channel schema 派生。
- `7aeea729` / `1d33fc05`：ErrorRegistry↔RouteRegistry↔OpenAPI、inventory↔bridge↔route/channel、Cowork 10 四方结构化 equality 入门；移除 GenericRequest/GenericResponse 事实源，并把关键 GA 路由绑定到字段级 DTO。
- `5dc93387` / `200c3694`：隔离并发 mutation 临时文件；恢复 additive AsyncAPI 兼容；auth bridge 使用真实判别联合并通过完整 renderer/preload 编译。

### Round 2 自测与证据

- Reviewer mutation：`tests/contracts/reviewer-round1-red.test.ts` 10/10 PASS；原 9 项缺口剩余失败数 `0`，另增 staging 故障注入用例。
- targeted：`tests/contracts/contracts.test.ts`、Reviewer Red、`tests/scaffold-tester-corners.test.ts` 合计 3 files / 22 tests PASS。
- contracts：103 schemas、158 REST routes、32 channels、184/184 inventory、321 unique bridge leaves、Cowork 10；source hash `2d771c6e7552895d65d036854afbd676ce25660cf2a79844a68419175cddf394`，output hash `8e2e6f43c14cbf5c9173bcb2f7202fcf622d589fec3956aed43f0abe2226f24d`。
- `npm ci`、`contracts:generate`、`contracts:check`、`test:contract`、workspace typecheck、changed-file ESLint 均 exit 0。
- 全量 Vitest：189 files PASS，2031 passed / 1 skipped；`build:saas` 验证 9 workspaces / 18 fresh artifacts；`npm run build` 与 `npm run compile:electron` exit 0。
- 构建仍仅有既有 Vite chunk、第三方 `lottie-web` eval 与 install-scripts 提示；未扩大 P01 运行时范围。本地无远端 CI runId，不伪造远端结果。

### Round 2 交接 Reviewer

- Developer 状态：`REVIEW_PENDING`；稳定实现基线为 `200c3694` 加本记录提交。
- 请原独立 Reviewer 复审 Round 1 五项 P1、10 个 mutation 和 AC01～18；Developer 不修改 `审核意见.md`、`测试报告.md` 或总体完成表。

## Round 3

### Red

- 基线：Reviewer Round 2 `CHANGES_REQUESTED` commit `fc9c3888`；Red commit：`e19e31a3 test(contracts): capture reviewer round two gaps`。
- `npx vitest run tests/contracts/reviewer-round2-red.test.ts`：exit 1，6/6 failed；分别证明 55/118 route 仍共享 request/response 占位、canonical htmlShare/BYOK/列表 payload 被拒、refresh auth 与 model status/error 漂移、ticket 重复 scope 被接受、D12 四桶与 account 兼容视图缺失、ledger delta 同号/求和未冻结。
- Red 均由 Reviewer 指出的语义缺口触发，不以缺依赖、编译错误或部署文件失败代替。

### Green / Refactor

- `4cc3ea8f`：ticket schema 拒绝重复 session 与完全相同 resource scope，保留同 workspace 不同 path。
- `90ff85fb`：D12 balance 改为 `daily/monthly/granted/topup`；独立 signed delta；account 派生兼容字段；ledger 同号与总和校验。
- `d68fe41e`：删除 path-based route policy heuristic；158 条 route 逐项声明 auth/status/errors；增加 refresh-cookie security；修正 login、model proxy/config check/stream 与 session read。
- `dcfdb79a`：删除 `OperationRequest/OperationResponse`，新增字段级 GA domain schemas；htmlShare、BYOK、model、skill/plugin、scheduled task、runtime、session、workspace、列表等 route 绑定真实 DTO。
- `d8f7d5da`：checker/test 同时拒绝旧占位名和改名后的 `{input,idempotencyKey}` / `{success,data}` 同构占位，并增加 Reviewer 点名域的 valid/invalid matrix。
- `926006e8`：凭据 login schema 进入 core DTO 守门；PKCE 与 email/password 两种入口均为显式联合。
- 未实现 controller/provider/ledger/runtime 行为；未触碰 Docker、Charts、部署探针、Reviewer/Tester 报告或总体表。

### Round 3 自测与证据

- Reviewer Round 2 Red：11/11 PASS（原 6 项加 5 组 domain fixture matrix）；Round 1 Red 与 contract/scaffold 合计 4 files / 33 tests PASS。
- contracts：209 Zod schemas、158 REST routes、32 channels、184/184 inventory、321 unique bridge leaves、Cowork 10；source hash `f3505cbb20ad6fa6a5445621e340efed7ab216c6b84a88f1e31fde9dfb67a2cf`，output hash `c84d95d7c924dcb3b81b3b905860c61bb262d4f67603ef8d9f7874b67885985a`。
- `contracts:check`、15/15 `test:contract`、workspace typecheck、changed-file ESLint 均 exit 0。
- 全量 Vitest：190 files PASS，2042 passed / 1 skipped；`build:saas` 验证 9 workspaces / 18 fresh artifacts；`npm run build` 与 `npm run compile:electron` exit 0。
- Round 3 未改依赖/lock；使用 Round 2 同一 `npm ci` 锁定安装。构建仅保留既有 Vite chunk、第三方 `lottie-web` eval 与 install-scripts 提示。

### Round 3 交接 Reviewer

- Developer 状态：`REVIEW_PENDING`；本记录提交后的 HEAD 为 Round 3 稳定复审基线。
- 请同一 `/root/p01_reviewer` 独立复审 Round 2 四项 P1、Round 3 mutation 与 AC01～18；Developer 不作审核/测试结论。

## Round 4

### Red

- 基线：Reviewer Round 3 `CHANGES_REQUESTED` commit `0f07228a`；Red commit：`94d4cdef test(contracts): capture reviewer round three gaps`。
- `npx vitest run tests/contracts/reviewer-round3-red.test.ts`：初始 exit 1，20/20 failed；分别证明 query/path/body metadata 未分离且 OpenAPI 丢 query、高风险 operation error set 无独立权威期望、password+PKCE/refresh-cookie/OAuth grant 边界错误、HTML/plugin/schedule 非 canonical shape、ticket uniqueness 未进 OpenAPI，以及 D12 breakdown 不可唯一派生。
- Red 在任何契约修复前独立提交；fixture 以附录 A/C、D11/D12 与真实 bridge shape 为输入，不从 RouteRegistry 反推期望。

### Green / Refactor

- `66434c7c`：password login 同时绑定 credentials+PKCE/CSRF；OAuth token 改为 authorization-code/refresh-token 判别 grant；refresh request 严格空体；HTML source 回归 `html|artifact`，plugin config 支持 `unknown` 值，task schedule 改为 `at/every/cron`；D12 结构化 breakdown 与 ticket uniqueness metadata 入源 schema。
- `e597f188`：RouteRegistry 分离 `pathSchema/querySchema/bodySchema`，生成器将 path/query 参数和 body 分别输出到 OpenAPI；`requestId` path/body equality 以稳定扩展可审计。新增独立 `route-policy-expectations.ts`，精确锁定 auth/session/file/billing/task 高风险错误集。
- `2a9a5431`：`/auth/refresh` 仅 refresh-cookie security 且无 request body；OAuth grant/redirect binding 进 OpenAPI 扩展。
- `83fe6b4f`：D12 `creditsRemaining/creditsLimit/creditsUsed` 与 structured breakdown 的派生公式进 schema metadata，Zod 同时校验三项派生关系。
- `304e9755` / `aae6cc82`：将 stale-output 与 staged-failure 破坏性 mutation 全部迁入临时仓库副本，消除 Vitest 并行扫描真实 generated 目录的竞态，不降低 mutation 断言。
- 未实现 controller/provider/ledger/runtime 行为；未修改 Reviewer/Tester 报告、总体表或 P02/P03 任务文档。

### Round 4 自测与证据

- Reviewer Round 3 Red：20/20 PASS；Round 1/2/3 + contracts/scaffold targeted：5 files / 53 tests PASS。
- contracts：216 Zod schemas、158 REST routes、32 channels、184/184 inventory、321 bridge leaves、Cowork 10；source hash `3e9c1cac73c37407c43512e2c0e292aa250f524aebe0571820a8a788039a3fe9`，output hash `d75f6768a566d6735ee9501c0de70f769fdba9f6924e7112143ac48a4e07c1ed`。
- 最终 `contracts:check` exit 0，invocationId `c3dc0efb-240e-4f3a-82e3-ce7900f9e72b`；15/15 `test:contract`、workspace typecheck、Round 4 changed-file ESLint（zero warnings）均 exit 0。
- 全量 Vitest：192 files PASS，2064 passed / 1 skipped；`build:saas` 验证 9 workspaces / 18 fresh artifacts；`npm run build` 与 `npm run compile:electron` exit 0。
- 首次全量 Vitest 暴露的两个 generated temp-file 并行竞态已通过上述两个隔离提交修复；最终重跑全绿。构建仅保留既有 Vite chunk、第三方 `lottie-web` eval 与 install-scripts 提示。
- 验证实现基线：`aae6cc8223eba44fe46ef30c16c656a1ef022ec4`；UTC `2026-07-11T08:25:38Z` / CST `2026-07-11 16:25:38 +0800`；本地无远程 CI runId，不伪造。

### Round 4 交接 Reviewer

- Developer 状态：`REVIEW_PENDING`；本记录提交后的 HEAD 为 Round 4 稳定复审基线。
- 请同一 `/root/p01_reviewer` 独立复审 Round 3 六项 P1、20 个新 mutation 及 AC01～18；Developer 不作审核/测试结论。

## Round 5

### Red

- 基线：Reviewer Round 4 `CHANGES_REQUESTED` commit `473dfdc2`；Red commit：`3716b670 test(contracts): capture reviewer round four gaps`。
- `npx vitest run tests/contracts/reviewer-round4-red.test.ts`：初始 12 tests 中 11 failed / 1 passed；精确失败于两条 runs 路由五字段 query、真实 every/cron bridge payload、自创 schedule 字段拒绝、158-route 独立 policy 全集、19 条 GET/DELETE 假错误和 task stop 三个专用错误。
- mutation 遍历全部 RouteRegistry，要求独立 expectation operationId 分母与 registry 双向相等，并对每个 operation 的 error set 增/删都不得等于权威集。

### Green / Refactor

- `118ce498`：两条 scheduled runs 路由共享 `TaskRunQuerySchema`，冻结 `limit/cursor/status/startDate/endDate`；TaskSchedule 直接对齐真实桥 `at`、`everyMs/anchorMs`、`expr/tz/staggerMs`，严格拒绝 `intervalMs` 和 `expression/timezone`。
- `187612f0`：独立文件显式冻结 158 个 operationId 分母与 18 类 exact auth/status/error policy；checker 先校验分母双向 equality，再逐 operation 比对。清除全部 19 条 GET/DELETE 假配额/写入错误，同时去除 model config/check、media cancel、stream abort 的消费型假 `QUOTA_EXCEEDED`；`NO_RUNNING_TASK_RUN/CANCEL_NOT_OWNED/CANCEL_FAILED` 进 ErrorRegistry 与 task stop exact set。
- `e09d6d74`：Round 2 回归 fixture 改用真实 cron shape，不降低旧 mutation 断言。

### Round 5 自测与交接

- Reviewer Round 4 Red + Round 1～3 + contracts/scaffold targeted：6 files / 65 tests PASS；全量 Vitest：193 files PASS，2076 passed / 1 skipped。
- contracts：216 schemas、158 routes、32 channels、184/184 inventory、321 bridge leaves、Cowork 10；source `a7672b976c10ea78a8b18518ddb4ab98f02c417d62e7182937acacbb6f47fe39`，output `99a3b2a1ad3927447330925bab2ee87887952678f290d757ee5a933298a6e9bb`。
- `contracts:check` PASS，invocationId `7f64369b-88f4-41aa-a24a-2dadb6849fe0`；15/15 `test:contract`、typecheck、Round 5 changed-file ESLint zero warnings 均 exit 0。
- `build:saas` 9 workspaces / 18 fresh artifacts，`npm run build`、`npm run compile:electron` 均 exit 0；仅既有 Vite chunk、`lottie-web` eval 与 install-script allowlist 提示。
- Developer 状态：`REVIEW_PENDING`；请同一 `/root/p01_reviewer` 复审 Round 4 三项 P1、12 个新 mutation 及 AC01～18。Developer 未修改审核意见、测试报告、总体表或 P03。
