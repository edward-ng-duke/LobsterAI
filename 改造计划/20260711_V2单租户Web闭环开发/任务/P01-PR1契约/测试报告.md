# 测试报告

> 初始为空白；仅在 Reviewer APPROVED_FOR_TEST 后由独立 Tester 填写。

- Tester Agent：`/root/p03_developer/p01_tester`（独立 Tester，未复用 Planner/Developer/Reviewer）
- 前置：Reviewer Round 6 commit `1c9329d8` 已明确 `APPROVED_FOR_TEST`。
- 被测实现 SHA：`1c9329d8df5b1dd4ca6882d81f49af163ea27eaa`
- 环境：macOS，Node `v24.15.0`，npm `11.12.1`；UTC `2026-07-11T09:37:01Z` / CST `2026-07-11 17:37:01 +0800`
- namespace/digest：纯契约/隔离临时副本测试，不涉及集群 namespace 或镜像 digest。
- 结论：`FAILED`

本轮新增 `tests/contracts/p01-tester-corners.test.ts`，从 Reviewer 未覆盖的 property 边界、恶意 formal path、TZ/locale 确定性、bootstrap base 状态和 inventory/bridge 反向边执行 10 项独立测试。结果 6 项通过、4 项失败；失败为当前实现接受本应拒绝的路径，以及首次 bootstrap 基线漂移，不是缺依赖、语法错误或环境波动。Tester 未修改任何实现。

| 用例 ID | 独立角度 | 测试文件/脚本 | 结果 | 证据 |
|---|---|---|---|---|
| P01-TST-01 | property：随机容量步进的 ticket 唯一性与 100/101 上限 | `tests/contracts/p01-tester-corners.test.ts` | PASSED | 0、7、14……98 个 unique session 均接受；每组追加重复项均拒绝；101 项拒绝。 |
| P01-TST-02 | schema boundary：NaN/Infinity/非整数及 limit off-by-one | 同上 | PASSED | `everyMs` 拒绝 `NaN/Infinity/0/-1/1.5`；`limit=1/100` 接受，`0/101` 拒绝；ping 非整数拒绝。 |
| P01-TST-03 | 恶意路径：明文 dot-segment | 同上，隔离副本 mutation | FAILED | 将 `/api/v1/runtime/restart` 改为 `/api/v1/runtime/../auth/login` 后，`node scripts/contracts/check.mjs --only routes` 仍 exit `0` 并输出 `routes: PASS`。 |
| P01-TST-04 | 恶意路径：编码 dot-segment | 同上，隔离副本 mutation | FAILED | 改为 `/api/v1/runtime/%2e%2e/auth/login` 后仍 exit `0` / `routes: PASS`。 |
| P01-TST-05 | 恶意路径：重复斜杠 | 同上，隔离副本 mutation | FAILED | 改为 `/api/v1//runtime/restart` 后仍 exit `0` / `routes: PASS`。 |
| P01-TST-06 | determinism：跨 TZ/locale 字节稳定 | 同上 + `scripts/contracts/generate.mjs --check` | PASSED | `TZ=UTC, LC_ALL=C` 与 `TZ=Asia/Shanghai, LC_ALL=tr_TR.UTF-8` 均 exit `0`。 |
| P01-TST-07 | baseline 缺失必须 BLOCKED | 同上 + `scripts/contracts/breaking-diff.mjs` | PASSED | 删除 `CONTRACT_BASE_REF` 后 exit `2`，stderr 含 `BLOCKED`。 |
| P01-TST-08 | 首次 bootstrap：有效 pre-contract SHA | 同上 + `CONTRACT_BASE_REF=cde584c0` | FAILED | 期望 bootstrap PASS（开发证据也明示记录 `cde584c0 bootstrap PASS`）；实际 exit `1` / `status=BREAKING`，包含虚构旧 route、`Generic*` 及大量旧 schema/policy 差异。 |
| P01-TST-09 | 双向集合：inventory route/channel target 存在性与重复边 | 同上 | PASSED | 184 行 `routeTargets/channelTargets` 均无重复且 target 存在。 |
| P01-TST-10 | 双向集合：bridge target 与 inventory 反向 union 精确相等 | 同上 | PASSED | 321 bridge leaves 的 target 无重复，并与引用该 property path 的 inventory `routeTargets + channelTargets` 精确相等。 |

## 回归、非功能与交接

### 命令证据

- `npx vitest run tests/contracts/p01-tester-corners.test.ts`：exit `1`；1 file，10 tests，`6 passed / 4 failed`。
- `npx eslint --ext ts,tsx --report-unused-disable-directives --max-warnings 0 tests/contracts/p01-tester-corners.test.ts`：exit `0`，zero warnings。
- `npm run test:contract`：exit `0`，15/15 现有 focused checks PASS；216 schemas / 158 routes / 32 channels / 184 inventory / 321 bridge / Cowork 10。这证明新测试找到的是现有门未覆盖的伪绿角度。
- `npm run contracts:check`：exit `0`，stage `P03`，status `PASS`，invocationId `931e02ba-81fe-4a00-afe5-959cff2aedd9`。该对照结果与 Tester mutation 失败一起证明当前 gate 有盲区，不能作为 Tester PASS 依据。

### 缺陷与退回

1. `P1 · P01-AC-05`：formal route canonical validator 仅检查前缀和冒号 action，未拒绝明文/百分号编码 dot-segment 及重复斜杠。这些形状在 proxy/router 规范化后可指向不同资源，必须在 registry/checker 层 fail closed。
2. `P1 · P01-AC-16`：`baselines/bootstrap/{openapi,asyncapi}.yaml` 仍是早期 Generic/fake-route 版本，导致权威 pre-contract base `cde584c0` 无法首次 bootstrap PASS，与本任务完成定义和已提交开发证据矛盾。

按独立 Tester 职责，本轮结论固定为 `FAILED`，未运行 `typecheck`、全量 `npm test`、`build:saas`、Renderer build 或 Electron compile，因为 targeted 已确定性触发 P1 缺陷，继续执行总门不会改变退回结论。退回原 P01 Developer 先写 Red 并修复；修复后必须重新经 Reviewer `APPROVED_FOR_TEST`，再由独立 Tester 重跑本文件与全量验收门。

## Round 2 · 2026-07-11 19:17 +0800 · `/root/p01_tester_round2`

- Tester Agent：`/root/p01_tester_round2`（全新独立 Tester；原 Tester 线程已回收，本轮仅通过仓库文件接手，未复用 Planner/Developer/Reviewer 上下文）。
- 前置：Reviewer Round 8 commit `5d574e98c3de5a6f838f2429ef0ea4abc53ebcc2` 对 stable `dad753474ac1d7e62c1ab248a0b40c9943649e5a` 明确 `APPROVED_FOR_TEST`。
- 被测 HEAD：`5d574e98c3de5a6f838f2429ef0ea4abc53ebcc2`；Tester 新增测试 commit：`cea3edbfac058c1e92dad6afbbc63141ea697fbb`。
- 环境：macOS，Node `v24.15.0`，npm `11.12.1`；UTC `2026-07-11T11:17:27Z` / CST `2026-07-11 19:17:27 +0800`。
- namespace/digest：纯契约、git baseline 与隔离临时副本测试，不涉及集群 namespace 或镜像 digest。
- 结论：`PASSED`。保留上述 Round 1 `FAILED` 历史；当前修复已使原 4 个失败点全部转绿，本轮未发现新 P0/P1。

### 独立覆盖与结果

| 用例 ID | 独立角度 | 结果 | 证据 |
|---|---|---|---|
| P01-TST-R2-01 | 原 Tester 10 项逐项回归 | PASSED | `p01-tester-corners.test.ts` 10/10；原 TST-03/04/05 恶意路径与 TST-08 bootstrap 由失败转为通过。 |
| P01-TST-R2-02 | Round 8 formal grammar 恶意 segment 扩展 | PASSED | 独立拒绝非首段 `.well-known`、NBSP、`.`/`_` 标点和 `{1id}`/`{}`/`{id_}`/`{ id}` 参数。8/8 mutation 均 exit 1 且精确报 `non-canonical formal route`。 |
| P01-TST-R2-03 | formal grammar 合法路径不误拒 | PASSED | 合法 `/api/v1/foo-1/bar-2`、`/.well-known/test-1` 隔离 mutation 与当前 158 条 registry 全部 exit 0。 |
| P01-TST-R2-04 | pre-contract allowlist 和 frozen artifact | PASSED | allowlist 精确为 `['cde584c0']`；受控 OpenAPI/AsyncAPI 为空 canonical 面，SHA-256 分别为 `ca80c435e945146f50bc7a3cf5cd47644be9360cd14ecf4a94e26176cb782a68` / `aa0cb2259de6bb53050f093d52c6a48b154a9f9c88016b32a5cf7b24a122957b`。 |
| P01-TST-R2-05 | allowlisted pre-contract commit | PASSED | `CONTRACT_BASE_REF=cde584c0` exit 0，OpenAPI/AsyncAPI 均为 `bootstrap` mode。 |
| P01-TST-R2-06 | 坏 ref 必须 BLOCKED | PASSED | 有效但未 allowlist 且无契约的 `cde584c0^` 与不存在 ref `p01-tester-missing-ref` 均 exit 2 / `BLOCKED`，未伪绿为 bootstrap/PASS。 |
| P01-TST-R2-07 | 真实 breaking 不得漏报 | PASSED | `CONTRACT_BASE_REF=53bb7553` exit 1 / `BREAKING`，报告含真实 schedule schema 不兼容项。 |
| P01-TST-R2-08 | 真实兼容 git baseline | PASSED | `CONTRACT_BASE_REF=7f4a1520` exit 0，OpenAPI/AsyncAPI 均为 `git` mode，证明 allowlist 未泄漏到后续契约 commit。 |

### 命令证据

- `npx vitest run tests/contracts/p01-tester-corners.test.ts`：exit 0，1 file / 10 tests PASS。
- `npx vitest run tests/contracts/p01-tester-corners.test.ts tests/contracts/reviewer-round7-red.test.ts`：exit 0，2 files / 12 tests PASS。
- `npx vitest run tests/contracts/p01-tester-round2-corners.test.ts tests/contracts/p01-tester-corners.test.ts tests/contracts/reviewer-round7-red.test.ts`：exit 0，3 files / 20 tests PASS；新增 Round 2 独立用例 8/8。
- `npm run contracts:generate`：exit 0；216 schemas / 158 routes / 32 channels / 184 inventory / 321 bridge；source `897bd39077d409e52a88e9efe89383380564812f494efb88fe09e2af6a1ed2c8`，output `583f7b819dd84131362d6d8190ce3ea34ae68b82cc8524ab15e1c9a04937e748`。
- `npm run contracts:check`：exit 0，stage `P03`，status `PASS`，invocationId `181c34a2-2793-48e3-984d-25b58eced34e`。
- `npm run test:contract`：exit 0，15/15 checks PASS；counts 与上述生成证据一致，Cowork stream 10。
- `npm run typecheck`：exit 0。
- `npx eslint --ext ts,tsx --report-unused-disable-directives --max-warnings 0 tests/contracts/p01-tester-round2-corners.test.ts tests/contracts/p01-tester-corners.test.ts`：exit 0，zero warnings。
- `npm test`：exit 0，197 files / 2111 passed / 1 skipped（2112 total）。
- `npm run build:saas`：exit 0，9 workspaces / 18 fresh artifacts。
- `npm run build`、`npm run compile:electron`：均 exit 0。
- 生成与全门后未出现生成物漂移；Tester 只新增测试与追加本报告，未修改实现、开发记录、审核意见、总体表或 P03。

### 非功能、风险与交接

- 构建仅出现已知 install-script allowlist、Vite chunk/dynamic-import 和 `lottie-web` eval 提示，不是本轮新增回归或 P01 行为失败。
- 本轮为纯契约/codegen 测试，无 UI/E2E、集群、网络或镜像运行时行为；未发现 controller/provider/ledger/runtime 越界实现或生产公网依赖。
- Tester Round 2 结论为 `PASSED`，交总体协调者核对依赖、Reviewer/Tester 证据和 commit；Tester 不修改 `00-总体完成表.md`。
