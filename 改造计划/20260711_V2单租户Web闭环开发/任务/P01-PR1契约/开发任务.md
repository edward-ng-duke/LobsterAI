# P01 · PR-1 contracts/codegen

- 状态：TODO
- 依赖：P00 通过；附录A/附录C D1 和现有 IElectronAPI/stream 注册点。
- 权威输入：根 AGENTS、19、17 对应 V1/V2、16、附录 C；冲突以根 AGENTS 的生产断公网和附录 C 契约决策为准。

## 源码边界

libs/shared/contracts、libs/client/bridge 生成类型、contract check 脚本；不实现 provider/计费/ASR 上游。

## TDD：先写的失败测试

先写缺 route/channel、旧路径、手改生成物、遗漏 goal/10 个 onStream 的失败断言。 Red 必须因目标行为缺失而失败；环境缺失另记 BLOCKED，不算 Red。

## Corner cases

OpenAPI {id} 与文档 :id 归一、冒号 action 拒绝、相似 substring 不误伤、未知事件和错误信封。

## 实施与验收

Developer 按最小垂直切片逐个 Green，避免无关重构。验收命令：npm run contracts:check；npm test -- contract；npm run typecheck；git diff --exit-code。

## 回滚

契约源与生成物同提交回滚；禁止只回滚生成物。

## 完成证据

OpenAPI/AsyncAPI、全量 registry、10 事件双向断言、生成可复现报告。 证据须绑定 SHA/runId/环境；所有触碰 TS 文件 changed-file ESLint 0 warning，相关 Vitest 全绿。

## Developer → Reviewer → Tester 循环

Developer 记录每轮 Red/Green 与 diff → 独立 Reviewer 对照本文件和源码审核；若 CHANGES_REQUESTED，退同一 Developer 修复并进入下一轮 → Reviewer APPROVED_FOR_TEST 后独立 Tester 新增 corner-case/回归测试；Tester FAILED 必须退 Developer 且修复后重新 Reviewer → Tester PASSED 后由协调者核验证据并勾总体表。任何角色不得自行勾 Todo。

