# 审核意见

> 初始为空白；Reviewer 独立审核并按 Round 追加。

## Round 1 · 2026-07-11 14:22 +0800 · `/root/p01_reviewer` / `2d742d68`
- Reviewer Agent：`/root/p01_reviewer`（独立 Reviewer；未复用 Planner/Developer）
- 审核范围：P01 commits `54fff820..6bc7bbf7`，以及 P03 已激活后的当前生成物、stage gate 和 preload 静态约束。
- 基线/commit：Developer evidence `6bc7bbf7`；独立复核 HEAD `2d742d688f3d033485cc674d4889c3199d6345d6`。
- 结论：`CHANGES_REQUESTED`
- 原因：下列 5 项均为 P1/验收标准缺口；按角色协议，任一 P1 或 AC 缺口均不得交 Tester。

| 严重级 | 用例/需求 | 文件:行 | 实际/期望 | 复现 | 要求证据 |
|---|---|---|---|---|---|
| P1 | P01-AC-02/03/04/06/07/14：GA 字段级 schema、canonical route/inventory、auth/error/status | `libs/shared/contracts/src/domains/core.schema.ts:3-5`; `libs/shared/contracts/src/registry/routes.ts:17-35,123-149,177-184`; `libs/shared/contracts/src/registry/ipc-ga-inventory.ts:78-80,154`; `scripts/contracts/generate.mjs:111-158` | 161 routes 中 73 个 request 使用可接受任意字段的 `GenericRequest`，149 个 response 仅为 `data?: unknown`，并非附录 C §3 要求的每个 GA request/response/error 字段级契约。账本还为 `generate-session-title` 虚构 `POST /api/v1/sessions/{id}`（文档要求结果经已有 PATCH 落标题）、为 `auth:exchange` 虚构 `POST /auth/callback`（附录 A 明确不得当 token exchange），并把 ASR WS stream 虚构成 REST POST、同时错误反链到 model stream。生成器忽略 `route.auth`，OpenAPI 0 个 operation 带 security；所有成功响应固定 200，unsupported node 仍暴露 200，未冻结预期 202/501；错误检查也只验证“出现的 code 合法”，未验证 ErrorRegistry 与响应双向相等。期望：逐 GA 路由使用明确 Zod DTO、正确 method/path/status/auth/errors，账本只映射真实 canonical route/channel/bridge。 | `node --input-type=module` 枚举得到 `genericRequests=73, genericResponses=149, securedOperations=0`；列出 fake routes 得到 `POST /api/v1/asr/sessions/{asrSessionId}/stream`、`POST /api/v1/sessions/{id}`、`POST /auth/callback`。与此同时 `npm run test:contract` 仍 PASS，证明现门未覆盖该缺口。 | 删除虚构映射；为 184 行建立真实 route/channel/bridge target；补齐全部 GA DTO 和 route success status/security/error 双向断言；mutation 将任一真实 DTO 换成 catch-all/unknown、改变 auth/status/错误集合时必须非零。 |
| P1 | P01-AC-04/10/17：三注册表双向与完整 ElectronBridge 精确类型 | `libs/shared/contracts/src/registry/bridge.ts:18-40`; `scripts/contracts/check.mjs:205-244`; `scripts/contracts/generate.mjs:226-268`; `libs/client/bridge/src/electronBridge.ts:1-41,2009-2036` | 321 个 BridgeRegistry row 的 `target` 全部为空，coverage 只比较 property path/optional，未与 inventory/route/channel 做第三方双向映射。生成器把全部 import type alias 改成 `unknown`，stream consumer 也统一生成 `data: unknown`；`satisfies ElectronBridge` 因此不能证明返回形状。源 `IElectronAPI` 实际 330 leaves/321 unique，9 个重复 auth leaves 被 Map/生成器静默折叠；并且 `skills.getRoot`、系统日历权限、`auth.getAccessToken/getPendingCallback` 等附录 A 明确降级项仍标 `ga/rest`。期望：321 unique leaves 均保留真实可选性/签名，重复声明显式报错或归一，GA/deferred/browser/unsupported 与附录一致，所有 GA bridge row 有准确 target。 | TypeScript AST 枚举输出 `leaves=330, unique=321`，重复项为 9 个 `auth.*`；运行时统计输出 `noBridgeTargets=321`；`rg '^type .* = unknown' libs/client/bridge/src/electronBridge.ts` 可见类型擦除。当前 `npm run typecheck && npm run compile:electron` 仍 exit 0，说明静态门被弱类型掩盖。 | 不以 `unknown/any` 替代已命名 import 类型；建立可编译共享类型边界；checker 比较结构化签名/返回值且拒绝重复 property path；补 inventory↔bridge↔route/channel 双向 equality 和 disposition mutation。 |
| P1 | P01-AC-09/12：Cowork 10 四方 payload 精确一致 | `libs/shared/contracts/src/registry/cowork-stream.ts:34-41,51-53`; `src/renderer/types/electron.d.ts:957-965`; `scripts/contracts/check.mjs:211-230` | 数量和名称为 10，但 payload 不精确：旧桥 `complete.claudeSessionId` 为 `string|null`，Zod 只接受 optional string；旧桥 `error` 为 string，Zod 强制 `{code,message}`。现 valid fixture 省略 `claudeSessionId` 且给 error object，绕开真实 bridge 形状。checker 只确认 bridge property 存在，不比较 payload/signature，也不验证 `ipcTopic`/`asyncChannel` 的全值映射。期望：IPC topic、bridge callback、wire envelope data、AsyncAPI payload 四方结构一致；若 wire 有意升级错误结构，必须生成明确的 bridge adapter 契约而不是宣称相等。 | `CoworkStreamRegistry.complete.schema.safeParse({sessionId:'s',claudeSessionId:null})` 与 `error.schema.safeParse({sessionId:'s',error:'oops'})` 均返回 `success:false`；但 `npm run test:contract -- cowork-stream` PASS。 | 增加从 IElectronAPI/共享 payload type 到 Zod/AsyncAPI 的结构比较；补 null/string 真实 valid fixture；对 10 项逐项执行 schema round-trip mutation。 |
| P1 | P01-AC-16：breaking diff 对嵌套字段、类型和 operation response 无效 | `scripts/contracts/breaking-diff.mjs:31-74` | checker 只比较 component schema 顶层 properties/required/enum，以及 operation 是否存在；不会递归 `$ref`/object/array/union，也不比较 operation request/response/status/auth。独立 mutation 删除嵌套必填字段、把对象改为 string、把 200 response `$ref` 改成 ErrorEnvelope，三次均返回空 breaking 数组。期望：任何删除字段/类型收窄/new required/错误/事件/operation schema 破坏均报警，base 不能伪绿。 | 直接调用 `analyzeBreakingDiff` 的三组 mutation 输出：`nestedRemoved []`、`typeNarrowed []`、`responseChanged []`；`CONTRACT_BASE_REF=HEAD npm run test:contract -- breaking` 仍 PASS。 | 使用权威 OpenAPI breaking diff 或实现递归、`$ref` 感知比较；覆盖 request/response/status/security、nested/array/union/discriminator；把上述三个 mutation 固化为非零测试。 |
| P1 | P01-AC-15：原子生成与 stale output 清理 | `scripts/contracts/generate.mjs:311-325` | 写入前先 `rmSync(absolutePath)`，再写临时文件并 rename；写入/磁盘/进程失败窗口会使旧有效生成物消失，不是原子替换。生成器只删除固定文件，不清理受管目录中的额外 stale 文件，`--check` 也不会拒绝未知生成物。期望：同目录 temp 完整写成功（必要时 fsync）后直接 atomic rename 覆盖；受管输出先在 staging 生成/校验，再原子发布，并拒绝 stale 文件。 | 源码顺序 `rmSync → writeFileSync(tmp) → renameSync` 可直接复现失败窗口；现 mutation 仅覆盖 hand-edit 已知文件，未覆盖部分写失败或额外 stale 文件。 | 增加故障注入（write/rename failure）证明旧输出保持完整；增加受管目录额外文件 mutation；两次生成、失败恢复和 clean diff 均为绿。 |

### 独立命令证据

- `npm run contracts:generate`：exit 0；161 routes / 32 channels / 184 inventory / 321 bridge leaves。
- `npm run contracts:check`：exit 0；P03 active stage report 为 `stage=P03,status=PASS`，P01 gate 未因 P03 回归。
- `npm run test:contract`：exit 0；15 focused checks PASS，但上表 mutation 证明其存在语义盲区。
- `npx vitest run tests/contracts/contracts.test.ts tests/contracts/p01-red.test.ts tests/scaffold-stage-gates.test.ts`：exit 0；3 files / 16 tests。
- `npm run typecheck`、`npm run compile:electron`：exit 0。
- 权威解析器由 `test:contract` 实际执行：Swagger Parser 接受 OpenAPI 3.1、AsyncAPI Parser 接受 AsyncAPI 2.6；此结果仅证明语法/引用可解析，不覆盖上表 canonical 语义缺口。

### 交接 Tester

未交接。状态回到原 P01 Developer：修复以上 P1 后更新 `开发记录.md` 为新一轮 `REVIEW_PENDING`，再由独立 Reviewer 复审。Tester 在 `APPROVED_FOR_TEST` 前不得开始。

## Round 2 · 2026-07-11 15:35 +0800 · `/root/p01_reviewer` / stable `22c9b085`

- Reviewer Agent：`/root/p01_reviewer`（沿用同一 P01 Reviewer，仅复审原任务；未读取 Developer 结论作为通过依据）。
- 审核范围：Round 1 修复链 `abf478e5..200c3694`、Developer 记录 `22c9b085`；执行时 HEAD `a987cdce` 仅多一个 P03 deploy commit，`git diff --quiet 22c9b085..HEAD -- <P01 paths>` exit 0。
- 结论：`CHANGES_REQUESTED`
- Round 1 回归：桥 target/命名类型（原第 2 项）、Cowork complete/error（原第 3 项）、三组 breaking mutation（原第 4 项）、stale/故障注入原子生成（原第 5 项）均通过；原第 1 项仅修复了虚构 route、security/status/error metadata，字段级 request/response 仍未完成，因此不得交 Tester。

| 严重级 | 用例/需求 | 文件:行 | 实际/期望 | 独立复现 | 要求证据 |
|---|---|---|---|---|---|
| P1 | P01-AC-02/03/04/07/13/14/17：全部 GA route 必须是字段级唯一 Zod 契约 | `libs/shared/contracts/src/domains/core.schema.ts:3-17`; `libs/shared/contracts/src/registry/routes.ts:37-76,85-145`; `scripts/contracts/check.mjs:70-81`; `scripts/contracts/generate.mjs:98-104` | Round 2 只是把 `GenericRequest/GenericResponse` 改名并按 operationId 复制成不同 component 名：55 条 route 的 request 仍引用同一个 `OperationRequestSchema {input?,idempotencyKey?}`，118 条 response 仍引用同一个 `OperationResponseSchema {success,data?}`。这不是附录 C §3 要求的逐 GA request/response 字段级定义，checker 仅拒绝名字以 `Generic` 开头，无法识别同一占位 schema。实际 canonical payload 会被拒绝，例如 htmlShare `{source,clientSourceKey}`、BYOK `{provider,secret}`；显式绑定也有错误形状：`GET /agents` 返回单个 `AgentDto`、`GET /sessions` 返回单个 `SessionSummary`、memory entries 复用 `MessageListResponse`。期望：158 条 route 的 body/query/path/response 均绑定真实域 DTO，合并路由可共享同一“真实”schema，但不得用万能 input/data 包壳或动态改名伪装唯一 schema。 | 运行时枚举：`operationRequest=55, operationResponse=118`；`OperationRequest.safeParse({source:'html',clientSourceKey:'x'})` 与 BYOK canonical payload 均 `false`，而 `{input:{anything:'value'}}` 为 `true`。`npm run test:contract` 仍 PASS。 | 删除 `OperationRequest/OperationResponse` 占位事实源；为 Appendix A 184 行/158 routes 建立可审计的 domain DTO，包含 query/path/body 与旧桥响应适配；checker 以 schema identity/结构和必需字段验证，不以名称验证。增加 htmlShare、BYOK、model config、skills/plugins、scheduled tasks、runtime、列表响应等 valid/invalid fixtures与任一占位替换 mutation。 |
| P1 | P01-AC-03/06/07/11：认证和逐 operation error/status 不能由 path heuristic 代替契约 metadata | `libs/shared/contracts/src/registry/routes.ts:17-34,37-75,231-237`; `scripts/contracts/generate.mjs:136-165` | `routeErrors()` 按 substring 批量塞错误：所有 session GET 都得到 `SESSION_BUSY/IN_PROGRESS`，所有受保护路由都得到 `NOT_FOUND`；公开 login 反而没有可表达错误凭据的 `UNAUTHENTICATED/AuthError`。`/auth/refresh` 被标为 `access-token`，与 D11 的 HttpOnly refresh-cookie rotation 主流程冲突（access token 过期时无法刷新）；`LoginRequest` 也只有 PKCE 字段，没有 email/password 登录字段。success status 同样按路径批量推断，例如所有 `/api/v1/model/*` POST 固定 202，而非逐 operation 冻结。期望：每个 route 显式声明 auth scheme（access/refresh-cookie/public）、success status 和准确 error set，OpenAPI 生成/检查全值一致。 | 枚举得到 `/auth/refresh -> access-token`；源码显示 `auth`、`accepted` 和 `routeErrors` 均由路径条件生成。现 parser/check 只证明生成物与这些 heuristic 自洽，不能证明与 D11/附录 A/C 一致。 | 扩展 route metadata 的 auth scheme/status/error 明细并逐 operation 填写；补 refresh-cookie security scheme、错误凭据、模型非流式/配置检查等 fixture；改变任一 operation auth/status/error 必须被权威期望集 mutation 拒绝。 |
| P1 | P01-AC-11：ticket 重复订阅 corner case 未实现 | `libs/shared/contracts/src/envelope.schema.ts:36-39`; `scripts/contracts/check.mjs:313-332` | 计划明确把“重复 session”列为 corner case/Red，但 `sessions` 与 `resourceSubscriptions` 只有长度限制，无唯一性 refine；`{sessions:['s1','s1']}` 被接受，现 invalid matrix 未覆盖。重复资源订阅同样可进入。期望：同一 ticket scope 内 session/resource subscription 唯一，或在契约中明确稳定去重语义并生成一致类型/测试。 | `StreamTicketRequestSchema.safeParse({sessions:['s1','s1']}).success === true`；`npm run test:contract -- stream-ticket` 仍 PASS。 | schema 拒绝重复或冻结标准化结果；新增重复 session、完全相同 resource、同 workspace 不同 path 的边界测试，并同步 OpenAPI fixture。 |
| P1 | P01-AC-14：billing 四桶 DTO 与 D12 权威字段冲突 | `libs/shared/contracts/src/domains/deferred.schema.ts:81-100`; `libs/shared/contracts/src/registry/routes.ts:91-95` | `QuotaBuckets` 仍是 `subscription/purchased/promotional/overdraft`，但附录 C D12 权威四桶为 `daily/monthly/granted/topup`；`GET /billing/account` 直接返回该错误结构，也缺 D12 要求的 `creditsRemaining/creditsLimit/creditsUsed` 兼容视图与 breakdown。`BillingLedgerEntry.bucketDeltas` 因此同步漂移。期望：PR-1 虽不实现 ledger/quota 行为，但必须冻结 D12 的最小正确字段与旧桥兼容响应。 | `rg 'subscription|purchased|promotional|overdraft' libs/shared/contracts/src/domains/deferred.schema.ts` 命中四桶；`daily/monthly/granted` 均缺失。当前 deferred-contract check 只检查少数 schema 名，未覆盖 billing 字段。 | 按 D12 改为四个权威 balance/delta 字段，定义 billing account breakdown + 三个派生兼容字段；增加四桶同号/求和字段级 fixture（只冻结 DTO，不实现账务行为）。 |

### Round 2 独立命令证据

- `npm run contracts:generate`：exit 0；158 routes / 32 channels / 184 inventory / 321 bridge leaves，hash 与 Developer 记录一致。
- `npm run contracts:check`：exit 0；P03 active stage 下 `status=PASS`。
- `npm run test:contract`：exit 0；15 focused checks PASS。
- `npx vitest run tests/contracts/reviewer-round1-red.test.ts tests/contracts/contracts.test.ts tests/scaffold-tester-corners.test.ts`：exit 0；3 files / 22 tests，含 Reviewer Round 1 mutation 10/10。
- `npm run typecheck`、`npm run compile:electron`：exit 0。
- 上述绿灯证明 Round 1 四类修复及解析/编译稳定；本轮四个 P1 复现证明 AC02/03/07/11/14 的语义覆盖仍不足。未发现 controller/provider/ledger/公网运行依赖等 V5 提前实现。

### Round 2 交接 Tester

未交接。回原 P01 Developer 修复本轮 P1，追加新一轮 Red→Green 与字段级 coverage 证据后再 `REVIEW_PENDING`；Tester 仍不得介入。

## Round 3 · 2026-07-11 16:03 +0800 · `/root/p01_reviewer` / stable `eae7da66`

- Reviewer Agent：`/root/p01_reviewer`（同一 P01 Reviewer 独立复审）。
- 审核范围：Round 3 `e19e31a3..eae7da66`、209-schema export graph、158 routes、184 inventory、OpenAPI/AsyncAPI 生成物及 Round 1/2 mutation。
- 结论：`CHANGES_REQUESTED`
- Round 2 回归摘要：万能 `Operation*` schema 已删除，route policy 已改成逐行 metadata，refresh-cookie security scheme、Zod ticket 去重及四桶字段名均已落地；但下列 6 个 P1 表明字段级 REST/认证/D12 契约仍未闭环，因此不得交 Tester。

| 严重级 | 用例/需求 | 文件:行 | 实际/期望 | 独立复现 | 要求证据 |
|---|---|---|---|---|---|
| P1 | P01-AC-03/07/13/17：GET/DELETE query/path schema 必须生成到 OpenAPI | `libs/shared/contracts/src/registry/routes.ts:144-155,187-197`; `libs/shared/contracts/src/domains/deferred.schema.ts:134-145`; `scripts/contracts/generate.mjs:128-160` | RouteRegistry 仍把 method/path/query/body 混成一个 `request`：41 条含 `{id}/{wid}/...` 的路由绑定 `EmptyRequest`；生成器只从 path 文本生成统一 `string minLength=1` path parameter，并对 GET/DELETE 完全丢弃 request schema。整个 OpenAPI 的 query parameter 数为 0，因此 sessions `limit/cursor/pinned`、messages `limit/cursor/direction`、htmlShare `source/clientSourceKey`、scheduled runs filters、files `path/as/depth/sort/cursor` 均不存在；即便 tree 绑定了 `WorkspaceTreeRequest`，它也未被 operation 引用。期望：route metadata 分离 path/query/body schema，并把每项生成到对应 OpenAPI parameter/requestBody；path/body `requestId` 等跨位置约束需有机器可审计 invariant。 | OpenAPI 枚举：`queryParams=0,pathParams=81`；`GET /sessions` 无 parameters，`GET .../files/tree` 只有 `wid`，无 `path/cursor/depth`；`RouteRegistry.filter(path含{} && requestName==='EmptyRequest').length===41`。所有现 contract tests 仍 PASS。 | 为全部 158 routes 明确 `path/query/body` 三类 schema；按 19 §3.3/3.4 与附录 A 逐项生成；新增删除任一 query/path 参数、把 non-empty query 换 EmptyRequest、path/body mismatch 的 mutation。 |
| P1 | P01-AC-03/06/07：显式 error metadata 仍是批量假集合，不是逐 operation 权威语义 | `libs/shared/contracts/src/registry/routes.ts:46-205`; `scripts/contracts/check.mjs:193-230` | 虽然 policy 现在写在每行，但大量 error set 仍机械复制且与操作语义冲突：只读 `GET /billing/account` 带 `QUOTA_EXCEEDED`，文件 stat/download GET 带 `PAYLOAD_TOO_LARGE/STORAGE_QUOTA_EXCEEDED`，scheduled-task list 带 `TASK_LIMIT_EXCEEDED`，而授权码交换 `/oauth/token` 缺无效/已消费 code 的 `UNAUTHENTICATED`。现 equality 只证明 RouteRegistry 与它自己生成的 OpenAPI 相等，没有独立权威期望集。期望：逐 operation 只声明真实可发生且已冻结的 error codes，关键 auth/model/file/task 路由由独立 fixture/expected registry 校验。 | 直接枚举上述四个 operation 得到这些错误集合；`npm run test:contract -- errors` 仍 PASS。 | 修正每条 route error policy；建立独立于 RouteRegistry 的高风险期望/fixture，mutation 增删错误 code 均失败；至少覆盖 login/token/refresh、session read/write、file read/write、billing read/write、task list/create。 |
| P1 | P01-AC-02/03/11/13：password login 未绑定 PKCE/CSRF，refresh-cookie 端点仍接受 body token | `libs/shared/contracts/src/domains/core.schema.ts:3-25`; `libs/shared/contracts/src/registry/routes.ts:198-204` | `LoginRequest` 被实现为 PKCE 分支或 email/password 分支二选一；password 分支无需 `codeChallenge/redirectUri/state` 即可换 code，而同时携带凭据+PKCE 反被 strict union 拒绝。这破坏 D11 的“登录 code 再以 PKCE、redirect_uri、CSRF state 绑定换 token”。`/auth/refresh` 虽标 refresh-cookie，`RefreshRequest` 仍接受 body `refreshToken`，与 D11 规定浏览器 cookie 刷新、非浏览器 body 只走 `/oauth/token` 冲突。期望：password 登录请求同时含凭据与 PKCE/CSRF 上下文；`/auth/refresh` body 为空，移动端 refresh grant 明确进入 OAuthTokenRequest 判别联合。 | `{email,password}` 对 LoginRequest 为 `true`；`{email,password,codeChallenge,redirectUri,state}` 为 `false`；`RefreshRequest.safeParse({refreshToken:'raw-mobile-token'})` 为 `true`。 | 修正 login/refresh/token 判别联合与 OpenAPI security/body；新增缺任一 PKCE/CSRF 字段、body refresh token、redirect mismatch 的 negative fixture。 |
| P1 | P01-AC-02/03/04/13/14：随机域 DTO 仍与附录 A/旧桥 canonical shape 冲突 | `libs/shared/contracts/src/domains/ga.schema.ts:35-40,62-70`; `libs/shared/contracts/src/domains/deferred.schema.ts:166-175`; `libs/shared/contracts/src/registry/routes.ts:68-75,108-143` | Developer 的 valid matrix 使用了自定义值而非权威值：htmlShare schema 只接受 `source='html-file'|'artifact-file'`，但附录 A 明确为 `html|artifact`；plugin config 限制为 `Record<string,string>`，旧 `IElectronAPI` 为 `Record<string,unknown>`，布尔/数字/嵌套配置被拒绝；scheduled task 的 `schedule` 仅为 string，附录 A 明确必须支持 `at/every/cron` 三种 schedule。期望：字段和值域以附录 C 优先、附录 A GA 兼容面和真实 bridge shape 为输入，不用测试自行创造的新枚举规避迁移语义。 | `source:'html'`、`source:'artifact'` 均被 htmlShare 拒绝；`config:{enabled:true,nested:{x:1}}` 被 plugin config 拒绝；`schedule:{kind:'at',...}` 与 `{kind:'every',...}` 均被 scheduled task 拒绝。 | 修正三域 schema 与 route bindings；抽查清单继续覆盖 model config、skills、runtime、list responses，并为每个 Appendix A 分支至少给一个真实旧桥 valid fixture 和 unknown-field invalid fixture。 |
| P1 | P01-AC-11/17：ticket 去重只存在于 Zod refine，OpenAPI/codegen 未冻结 | `libs/shared/contracts/src/envelope.schema.ts:36-55`; `scripts/contracts/generate.mjs:92-108` | Zod `superRefine` 已拒绝重复 scope，但该约束不会进入 JSON Schema；生成的 `StreamTicketRequest.sessions/resourceSubscriptions` 均没有 `uniqueItems` 或等价扩展。因此后端 validator 与 OpenAPI/前端生成类型语义漂移，违反 AC11“fixtures 与 OpenAPI 同源”。期望：至少 session 和完全相同 resource object 的 uniqueness 在 OpenAPI 中可见；复合规范化语义用稳定扩展/描述并由客户端与服务端共享 helper。 | 查看 `openapi.yaml#/components/schemas/StreamTicketRequest`：两个 array 仅有 `maxItems/items`，无 `uniqueItems`；Zod duplicate test PASS 不能证明生成契约。 | 让生成器输出 `uniqueItems:true`/稳定复合唯一约束，并在 checker 中比较 Zod/JSON Schema；直接对生成 OpenAPI schema 做 duplicate valid/invalid conformance。 |
| P1 | P01-AC-14：D12 account breakdown 仍不能派生 `creditsLimit` | `libs/shared/contracts/src/domains/deferred.schema.ts:81-106`; `libs/shared/contracts/src/registry/routes.ts:58-62` | 四桶名称与余额求和已修复，但 `breakdown` 仍只是四个 balance 数字，没有 D12 规定的 `daily.limit/monthly.limit/granted.total/topup.balance`，所以 `creditsLimit` 只能作为任意独立数字输入，不能由 breakdown 派生。当前 refine 仅检查 `limit-remaining=used`，例如 breakdown 合计 10、`creditsLimit=1,000,000`、`creditsUsed=999,990` 仍通过。期望：breakdown 冻结每桶 balance 与适用 limit/total，三项兼容字段均可从 breakdown 唯一推导。 | 上述极端对象 `BillingAccountResponse.safeParse(...).success === true`；OpenAPI breakdown 也只有 daily/monthly/granted/topup 四个 number。 | 按 D12 定义结构化 bucket breakdown；Zod 校验三项派生关系，并为 OpenAPI 输出可审计说明/扩展；补错误 limit/used、四桶 balance/limit/total 边界 fixture。 |

### Round 3 独立命令证据

- `npm run contracts:generate`：exit 0；209 schemas / 158 routes / 32 channels / 184 inventory / 321 bridge，hash 与 Developer 记录一致。
- `npm run contracts:check`、`npm run test:contract`：exit 0；OpenAPI/AsyncAPI 权威 parser 均可解析。
- `npx vitest run tests/contracts/reviewer-round2-red.test.ts tests/contracts/reviewer-round1-red.test.ts tests/contracts/contracts.test.ts tests/scaffold-tester-corners.test.ts`：exit 0；4 files / 33 tests。
- `npm run typecheck`、`npm run build:saas`（9 workspaces / 18 fresh artifacts）、`npm run compile:electron`：exit 0。
- 未发现 provider/controller/ledger/runtime 行为或生产公网依赖提前进入 P01；本轮 P1 均为生成契约与权威输入不一致，现有绿门尚未覆盖。

### Round 3 交接 Tester

未交接。回原 P01 Developer 逐项修复并以真实附录/bridge fixture 新增 Round 4 Red；再次 `REVIEW_PENDING` 后复审。Tester 仍不得介入。

## Round 4 · 2026-07-11 16:45 +0800 · `/root/p01_reviewer` / stable `53bb7553`

- Reviewer Agent：`/root/p01_reviewer`（同一 P01 Reviewer 独立复审）。
- 审核范围：Round 4 `94d4cdef..53bb7553`、Round 3 六项 P1、20 个新回归断言、`304e9755` / `aae6cc82` mutation 隔离修复及 P01-AC-01～18；执行时 HEAD `8432fca6` 只多无关文档/依赖提交，`git diff --quiet 53bb7553..HEAD -- <P01 paths>` exit 0。
- 结论：`CHANGES_REQUESTED`。
- 已独立确认的修复：path/query/body 分离与参数生成机制、D11 login/refresh/OAuth grant 基础分流、HTML source、plugin unknown config、ticket OpenAPI uniqueness、D12 结构化 breakdown 及三项派生校验均已落地；20/20 Round 3 回归断言与两个临时副本隔离用例均通过。但下列 3 项 P1 仍使 AC03/06/07/13/14/17 未闭环，不得交 Tester。

| 严重级 | 用例/需求 | 文件:行 | 实际/期望 | 独立复现 | 要求证据 |
|---|---|---|---|---|---|
| P1 | P01-AC-03/07/13/17：scheduled runs 的 query 字段仍未按权威路由冻结 | `libs/shared/contracts/src/domains/ga.schema.ts:88`; `libs/shared/contracts/src/registry/routes.ts:154,160`; `tests/contracts/reviewer-round3-red.test.ts:33-40`; `改造计划/11-定时任务调度.md:526-528` | 单任务 `GET /scheduled-tasks/{id}/runs` 没有 `querySchema`，OpenAPI 为 0 个 query parameter；全局 runs 只有 `limit/cursor/status`，两条路由均缺权威文档明确的 `startDate/endDate`。Round 3 mutation 只断言全局路由的三个字段，把原 Reviewer 点名的日期过滤从期望集删掉，因此 20/20 是伪完整。期望两条 runs 路由共享含 `limit/cursor/status/startDate/endDate` 的字段级 query schema 并全部进入 OpenAPI。 | OpenAPI 枚举：`get_api_v1_scheduled_tasks_id_runs -> []`；`get_api_v1_scheduled_tasks_runs -> [cursor,limit,status]`；但 53 项 targeted tests 和 `test:contract` 均 PASS。 | 补齐共享 query DTO/两条 route binding；用权威文档的五字段期望集替换现 mutation，删任一字段均必须失败。 |
| P1 | P01-AC-02/04/13/14：scheduled task schedule 仍不是真实旧桥 canonical shape | `libs/shared/contracts/src/domains/deferred.schema.ts:133-141`; `tests/contracts/reviewer-round3-red.test.ts:114-123`; `src/scheduledTask/types.ts:3-21`; `改造计划/11-定时任务调度.md:80-88` | `at` 分支对齐，但真实 `ScheduleEvery` 是 `{kind:'every',everyMs,anchorMs?}`，真实 `ScheduleCron` 是 `{kind:'cron',expr,tz?,staggerMs?}`；新 schema/测试却自创 `intervalMs` 与 `expression/timezone`，并拒绝真实桥 payload。这正是 Round 3 要求“真实旧桥 valid fixture”所禁止的测试自证。 | `TaskCreateRequest.safeParse`：`at` 真实形状 `true`；`{kind:'every',everyMs:60000,anchorMs:0}` 为 `false`；`{kind:'cron',expr:'0 9 * * *',tz:'Asia/Shanghai',staggerMs:1000}` 为 `false`；自创两形状均为 `true`。 | 直接以 `src/scheduledTask/types.ts` / 11 章三分支生成 valid/invalid fixture，修正 Zod/OpenAPI/consumer types；不得用历史 migrate 字段代替当前桥契约。 |
| P1 | P01-AC-03/06/07：error policy 只修原四个样例，大量逐 operation 假集合仍存在 | `libs/shared/contracts/src/registry/route-policy-expectations.ts:27-58`; `libs/shared/contracts/src/registry/routes.ts:78-80,114-135,151-161,207-211`; `改造计划/11-定时任务调度.md:540-543` | 独立 expectations 只覆盖 13/158 operations，Round 3 error mutation 只查 4 条。枚举仍有 19 条 GET/DELETE 机械携带不可发生的配额/写入错误：例如读 billing plan/usage 和删 BYOK 带 `QUOTA_EXCEEDED`，读 model catalog/config 带 `QUOTA_EXCEEDED`，删/读 scheduled task/runs/channels 带 `TASK_LIMIT_EXCEEDED`，删文件和读 tree/thumbnail 带 `PAYLOAD_TOO_LARGE/STORAGE_QUOTA_EXCEEDED`。更明确地，`POST .../{id}/stop` 未冻结 11 章指定的 `NO_RUNNING_TASK_RUN/CANCEL_NOT_OWNED/CANCEL_FAILED`，却带 `TASK_LIMIT_EXCEEDED`。期望全 158 operations 的语义准确集合，而非只给原四个反例打补丁。 | 独立枚举 `GET|DELETE` 与四类上限错误的交集得 19 条；`contracts:check` / `test:contract` 仍 PASS，证明 checker 的独立权威集仍不完整。 | 按 auth/session/file/billing/task/model/media 权威语义审计所有 route errors；扩展独立 expectation/fixture 使任一 operation 增删 code 都失败；至少把上述 19 条和 task stop 专用错误固化。 |

### Round 4 独立命令证据

- `npm run contracts:generate`、`npm run contracts:check`、`npm run test:contract`：exit 0；216 schemas / 158 routes / 32 channels / 184 inventory / 321 bridge，source/output hash 与 Developer 记录一致。
- `npx vitest run tests/contracts/reviewer-round3-red.test.ts tests/contracts/reviewer-round2-red.test.ts tests/contracts/reviewer-round1-red.test.ts tests/contracts/contracts.test.ts tests/scaffold-tester-corners.test.ts`：5 files / 53 tests PASS；含 Round 3 20/20 和 stale/staged 临时副本隔离。
- `npm run typecheck`、Round 4 diff changed-file ESLint：exit 0；`npm test`：192 files / 2064 passed / 1 skipped。
- `npm run build:saas`：9 workspaces / 18 fresh artifacts；`npm run build`、`npm run compile:electron`：exit 0。仅有已知 Vite chunk/dynamic-import、`lottie-web` eval 与 install-script allowlist 提示。
- `npm run contracts:generate` 后 `git status --short` 为空；未发现 P01 越界实现 controller/provider/ledger/runtime 行为或生产公网依赖。

### Round 4 交接 Tester

未交接。状态回原 P01 Developer：修复上述 3 个 P1，以权威文档/真实桥 payload 替换当前伪完整 mutation，再次 `REVIEW_PENDING` 后由同一 Reviewer 复审。Tester 在 `APPROVED_FOR_TEST` 前仍不得开始。
