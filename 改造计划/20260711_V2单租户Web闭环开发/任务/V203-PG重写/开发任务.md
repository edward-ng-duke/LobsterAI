# V203 · coworkStore → Prisma/PG 重写

- 状态：TODO
- 依赖：V202；现有 coworkStore 行为快照。
- 权威输入：根 AGENTS、19、17 对应 V1/V2、16、附录 C；冲突以根 AGENTS 的生产断公网和附录 C 契约决策为准。

## 源码边界

libs/server/db repositories、apps/api data service；旧 SQLite 路径保留桌面使用。

## TDD：先写的失败测试

先迁移 CRUD/分页/replace/sequence/事务行为快照到双实现测试，确认 PG 实现缺失而红。 Red 必须因目标行为缺失而失败；环境缺失另记 BLOCKED，不算 Red。

## Corner cases

并发 sequence、游标同值、空页、重复请求、事务中断、连接池、large payload、logical/internal ID 混用。

## 实施与验收

Developer 按最小垂直切片逐个 Green，避免无关重构。验收命令：repository unit+Testcontainers；并发压力；npm test；changed-file eslint。

## 回滚

按 repository feature flag 切回旧路径仅限桌面；SaaS 数据不反灌 SQLite。

## 完成证据

约68处语义覆盖清单、快照等价、事务/并发/性能报告。 证据须绑定 SHA/runId/环境；所有触碰 TS 文件 changed-file ESLint 0 warning，相关 Vitest 全绿。

## Developer → Reviewer → Tester 循环

Developer 记录每轮 Red/Green 与 diff → 独立 Reviewer 对照本文件和源码审核；若 CHANGES_REQUESTED，退同一 Developer 修复并进入下一轮 → Reviewer APPROVED_FOR_TEST 后独立 Tester 新增 corner-case/回归测试；Tester FAILED 必须退 Developer 且修复后重新 Reviewer → Tester PASSED 后由协调者核验证据并勾总体表。任何角色不得自行勾 Todo。

