# 开发记录

> Developer 只能在 V202 被协调者 ACCEPTED 后开始，并按 Round 追加 Red→Green→Refactor、commit、mutation、命令/退出码/证据。不得覆盖 Planner handoff，不得写 Reviewer/Tester 结论或勾总表。

## Planner handoff · 2026-07-12 · `/root/v203_planner`

- 任务 ID：V203
- 计划版本：`V203-PLAN-v1`
- 只读规划基线：`420c06a890c885e35a1c6b3efc9b82265e536f37`
- 计划提交：用 `git log -1 --format=%H -- 改造计划/20260711_V2单租户Web闭环开发/任务/V203-PG重写/开发任务.md` 解析本次独立 Planner commit。
- 唯一目录：`改造计划/20260711_V2单租户Web闭环开发/任务/V203-PG重写/`；Planner 未创建重复目录。
- 当前状态：`PLANNED_BLOCKED_BY_V202`。规划时总表 P02/V107/V202 均未协调者 ACCEPTED，V202 四文件仍为空白骨架。P02 已有多轮实现/证据不等于 V202 已通过，不能越级开工。
- 已核对：根 AGENTS；V2 `01`～`04`；P02/V202 计划与状态；`06/16/17/19/附录C`；`src/main/coworkStore.ts` 3044 行、`sqliteStore.ts` ad-hoc migrations、核心测试/调用方；当前 P02 `libs/server/db` facade、tenant context/scope、Prisma schema/migration/contracts。
- 核心边界：只做 `libs/server/db` 四域 repository/facade 与测试/门禁；`src/main/coworkStore.ts` 和 SQLite 桌面线只读；不触碰 V204 服务/OpenClaw adapter/controller/WS；不实现 V5 桌面导入。
- 关键防伪：V202 非 ACCEPTED、mock/SQLite/admin role 冒充 PG17、`sequence` 作 cursor、生产 SQLite dual-write、无锁 `MAX+1`、skip、未解释 golden diff、stale evidence、cleanup 失败，均不能 PASS。

## Developer 解锁前置元数据

- Developer Agent：
- Developer 基线 SHA：
- V202 协调者 accepted SHA：
- V202 Developer / Reviewer / Tester SHA：
- V202 migration names + SHA-256：
- V202 RLS inventory/catalog report SHA-256：
- V107 `go` SHA：
- P02 accepted implementation/stage SHA：
- contracts version/source hash：
- PostgreSQL image ref/digest/server version/platform：
- V203 evidence directory：

若任一值缺失或重放不一致，记录：

```text
status=BLOCKED
exitCode=2
missing=[...]
```

随后停止，不得修改实现。

## 行为分母与差异清单

| 域 | legacy public 方法数/SQL 证据 | golden fixture/hash | preserve | approved difference | deferred effect | 未分类 |
|---|---:|---|---:|---:|---:|---:|
| sessions |  |  |  |  |  |  |
| messages |  |  |  |  |  |  |
| capsules |  |  |  |  |  |  |
| agents |  |  |  |  |  |  |

未分类必须为 0；每个 approved difference 填精确 policy ID，不能写 wildcard。

## AC / TDD 追踪

| AC | Red commit/失败原因 | Green commit | mutation/corner | 状态 | 证据 |
|---|---|---|---|---|---|
| AC-01～04 硬门/facade/RLS |  |  |  |  |  |
| AC-05～09 sessions/fork |  |  |  |  |  |
| AC-10～15 messages/JSON |  |  |  |  |  |
| AC-16～18 capsules/agents |  |  |  |  |  |
| AC-19～21 tx/retry/errors |  |  |  |  |  |
| AC-22～24 shadow/cutover/SQLite |  |  |  |  |  |
| AC-25～28 perf/PG17/evidence/scope |  |  |  |  |  |

## Round 1 · YYYY-MM-DD HH:mm · Agent/commit

### Red

- AC/T：
- 测试文件：
- 命令/exit code：
- 失败为何是目标行为缺失而非环境/语法：
- 原始输出路径/hash：

### Green / Refactor

- commit：
- 最小实现：
- transaction/lock/idempotency/retry 影响：
- schema/migration 是否只读消费：
- 与 V204/SQLite 的边界：

### Mutation / corner

- mutation：
- 预期/实际：
- 临时 worktree/schema/container：
- 恢复与 cleanup：

### 自测与证据

- focused unit/golden：
- PG17 integration/RLS/app role：
- concurrency/deadlock/rollback：
- reconcile/cutover rehearsal：
- query-count/EXPLAIN/performance：
- contracts/prisma/typecheck/changed-file ESLint：
- full tests/build：
- report sourceSha/runId/status：
- worktree/diff allowlist：

### 逐条回复上一轮意见

| Reviewer/Tester finding | `fixed / rejected with evidence / blocked` | commit | 复现/证据 |
|---|---|---|---|

### 交接 Reviewer

- 状态只能写 `REVIEW_PENDING` 或 `BLOCKED`，不得写 approved/passed。
- 实现 SHA：
- commits：
- 28 AC 证据索引：
- 已知 approved differences/deferred effects：
- 残余风险/cleanup：

