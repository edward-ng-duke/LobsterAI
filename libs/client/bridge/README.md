# Client bridge boundary

Browser transport wrappers belong here. The bridge consumes generated contract
types rather than copying DTO fields. P00 exposes only an explicit scaffold
availability state and performs no network calls.
