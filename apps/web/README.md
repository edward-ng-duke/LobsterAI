# Web application boundary

This workspace is the browser SPA host and consumes the client bridge. P00
contains a renderable React shell with no user-visible copy, routes, or migrated
Cowork business state. User-visible strings added later must use the shared
Chinese/English i18n workflow.
