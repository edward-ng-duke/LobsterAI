# PoC evidence index

Evidence packages use `YYYYMMDD_主题` directory names and must bind commands,
exit codes, namespace, RuntimeClass, image digest, manifests, logs, and metrics.
P00 creates the V1 template; it contains no fabricated execution result.
