# Supply-chain evidence boundary

P00 freezes the manifest and schema locations. The manifest explicitly reports
`NOT_APPLICABLE` until PR-3 inventories bundled skills, OpenClaw plugins, and
Kit/MCP defaults, then activates SBOM/signature consistency checks.
